services:
  dev:
    platform: linux/amd64
    build: .
    volumes:
      - .:/workspace
      - /workspace/node_modules
    working_dir: /workspace
    command: bash
    stdin_open: true
    tty: true
