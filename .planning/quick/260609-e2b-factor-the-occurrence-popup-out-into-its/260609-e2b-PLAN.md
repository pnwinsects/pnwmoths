---
phase: quick-260609-e2b
plan: 01
type: execute
status: complete
wave: 1
depends_on: []
files_modified:
  - src/components/pnwm-occurrence-popup.js
  - src/components/pnwm-occurrence-map.js
  - src/components/main.js
autonomous: true
requirements:
  - QUICK-260609-e2b

must_haves:
  truths:
    - "Clicking an occurrence marker on a species map shows a popup with the same fields, in the same order, as before the refactor"
    - "Fields with null/empty values are still omitted from the popup body"
    - "Popup field values containing HTML-special characters (e.g. <, >, &) render as text and are not interpreted as markup"
    - "The map component no longer constructs popup DOM imperatively — it instantiates a <pnwm-occurrence-popup> element and binds it to the marker"
  artifacts:
    - path: "src/components/pnwm-occurrence-popup.js"
      provides: "Lit web component <pnwm-occurrence-popup> with a .record property that renders the occurrence fields"
      contains: "customElements.define('pnwm-occurrence-popup'"
    - path: "src/components/pnwm-occurrence-map.js"
      provides: "Refactored marker loop that creates a pnwm-occurrence-popup element per record"
      contains: "pnwm-occurrence-popup"
    - path: "src/components/main.js"
      provides: "Side-effect import that registers pnwm-occurrence-popup at page init"
      contains: "pnwm-occurrence-popup"
  key_links:
    - from: "src/components/pnwm-occurrence-map.js"
      to: "src/components/pnwm-occurrence-popup.js"
      via: "document.createElement('pnwm-occurrence-popup') + el.record = r + marker.bindPopup(el)"
      pattern: "pnwm-occurrence-popup"
    - from: "src/components/main.js"
      to: "src/components/pnwm-occurrence-popup.js"
      via: "side-effect import"
      pattern: "import.*pnwm-occurrence-popup"
---

<objective>
Extract the Leaflet popup body currently built imperatively inside `pnwm-occurrence-map.js` into a dedicated Lit web component `<pnwm-occurrence-popup>`. The map binds an instance of this component to each marker via `marker.bindPopup(el)` after setting its `.record` property.

Purpose: Decouple popup rendering from the map component so popups can grow richer formatting (links, images, styled fields) without bloating the map component, while preserving the existing XSS-safe rendering guarantee.

Output: A new `pnwm-occurrence-popup.js` component, a refactored `pnwm-occurrence-map.js` that uses it, and a registration import in `main.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@src/components/pnwm-occurrence-map.js
@src/components/pnwm-plate-viewer.js
@src/components/pnwm-image-slideshow.js
@src/components/main.js
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create pnwm-occurrence-popup Lit component</name>
  <files>src/components/pnwm-occurrence-popup.js</files>
  <behavior>
    The new component MUST preserve current popup behavior exactly:
    - Field order: Locality, State, County, Year, Month, Collector, Type
    - Each field rendered as `Label: value` inside its own `<p>` element
    - Fields with null/undefined/empty-string values are omitted (matches existing `.filter(Boolean)` behavior on the truthy-check pattern `r.locality && ...`)
    - The `record_type` field on the input record maps to the `Type:` label (matches existing line 114)
    - Values are interpolated via Lit's `html` tagged template (auto-escaped) — XSS is prevented without manual textContent calls
    - No use of `unsafeHTML`, `unsafeStatic`, or any directive that bypasses escaping
  </behavior>
  <action>
Create `src/components/pnwm-occurrence-popup.js` defining a Lit element `<pnwm-occurrence-popup>` with a single reactive property `record` declared as `{ attribute: false }` (record is a JS object, set via property assignment from JS — never as an attribute).

Use shadow DOM (the Lit default — do NOT override `createRenderRoot`). Rationale: Leaflet's `.leaflet-popup-content` container provides its own padding/typography; encapsulating the popup body in shadow DOM prevents Pico CSS from styling `<p>` with global margins/colors that fight Leaflet's popup chrome. Per the task_context guidance, prefer shadow DOM with minimal styles.

Define minimal `static styles` using `css`` `` `:
- `:host { display: block; }`
- `p { margin: 0 0 0.25rem 0; line-height: 1.3; }` so consecutive lines are tightly stacked inside the Leaflet popup
- `p:last-child { margin-bottom: 0; }`

The `render()` method:
- If `this.record` is null/undefined, return an empty template (`html``  ``)
- Otherwise build the same field list as the existing imperative code (`pnwm-occurrence-map.js` lines 107–115):
  - `Locality: ${r.locality}` when truthy
  - `State: ${r.state}` when truthy
  - `County: ${r.county}` when truthy
  - `Year: ${r.year}` when truthy
  - `Month: ${r.month}` when truthy
  - `Collector: ${r.collector}` when truthy
  - `Type: ${r.record_type}` when truthy (note: input key is `record_type`, label is `Type`)
- Render via `html` so escaping is automatic. Suggested shape: build an array of `{label, value}` pairs filtered by truthiness, then map to `html`<p>${label}: ${value}</p>`` inside the top-level template.

Register with `customElements.define('pnwm-occurrence-popup', PnwmOccurrencePopup)` at the bottom of the file (mirrors `pnwm-occurrence-map.js` line 156 and `pnwm-plate-viewer.js` line 62).

Do NOT use `unsafeHTML` or any escape-bypassing directive — this preserves the T-03-01 XSS mitigation noted in the existing comment at `pnwm-occurrence-map.js:106`.
  </action>
  <verify>
    <automated>node -e "import('./src/components/pnwm-occurrence-popup.js').then(() => { const el = document.createElement('pnwm-occurrence-popup'); console.log(el.tagName); }).catch(e => { console.error(e); process.exit(1); })" 2>&1 || echo "node check is best-effort; rely on build + manual"; npx vite build 2>&1 | tail -20</automated>
  </verify>
  <done>
    File `src/components/pnwm-occurrence-popup.js` exists, exports/registers `<pnwm-occurrence-popup>`, has a reactive `record` property, renders the seven fields in order with `Label: value` formatting via Lit's escaped `html` template, omits empty values, and uses shadow DOM with minimal styles. `vite build` completes without errors involving this file.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire popup component into the map and register at page init</name>
  <files>src/components/pnwm-occurrence-map.js, src/components/main.js</files>
  <action>
In `src/components/pnwm-occurrence-map.js`, replace the imperative popup-construction block (currently lines ~105–123 inside the `for (const r of visible)` loop):

Remove these blocks:
- The comment `// Use Lit html tag is not available here; build popup content manually` (line 105)
- The `// All values are rendered via Leaflet's popup (text only) — XSS safe via textContent approach` comment (line 106)
- The `const parts = [...]` array (lines 107–115)
- The `// Create popup element using DOM text nodes to prevent XSS (T-03-01 mitigation)` comment (line 116)
- The `const popupEl = document.createElement('div'); for (...) { ... popupEl.appendChild(p); }` block (lines 117–122)

Replace with:
- `const popupEl = document.createElement('pnwm-occurrence-popup');`
- `popupEl.record = r;`
- `marker.bindPopup(popupEl);` (unchanged behavior — Leaflet accepts an HTMLElement)

Add a short comment above the new block explaining the delegation, e.g.:
`// Delegate popup rendering to <pnwm-occurrence-popup> — Lit's html template handles XSS escaping (T-03-01 mitigation preserved)`

Do NOT add an `import './pnwm-occurrence-popup.js'` at the top of `pnwm-occurrence-map.js`. Custom elements are registered globally via side-effect imports in `main.js`, matching the existing convention for every other `pnwm-*` component (see `main.js` lines 1–7).

In `src/components/main.js`, add a side-effect import for the new component. Place it next to the other component imports — recommended position is immediately after `import './pnwm-occurrence-map.js';` so the popup sits visually adjacent to its consumer:

```
import './pnwm-occurrence-map.js';
import './pnwm-occurrence-popup.js';
```

(Do not place fenced code in the executor's action — the snippet above is for the planner's intent; the executor should add the bare `import './pnwm-occurrence-popup.js';` line in `main.js` per the convention shown in lines 1–7.)

No other files change. The map component continues to use light DOM (`createRenderRoot` returning `this`); only the popup body lives in shadow DOM.
  </action>
  <verify>
    <automated>grep -n "pnwm-occurrence-popup" src/components/main.js src/components/pnwm-occurrence-map.js && ! grep -n "document.createElement('p')" src/components/pnwm-occurrence-map.js && ! grep -n "parts.push\|parts =" src/components/pnwm-occurrence-map.js && npx vite build 2>&1 | tail -20</automated>
    <human-check>Run `npx vite dev`, visit a species detail page that has occurrence markers (e.g. a common species), click a marker. Confirm: (a) popup appears with the same fields in the same order as before, (b) empty fields are still omitted, (c) no console errors mentioning `pnwm-occurrence-popup`, (d) styling looks reasonable inside the Leaflet popup chrome.</human-check>
  </verify>
  <done>
    `pnwm-occurrence-map.js` no longer constructs popup DOM via `document.createElement('p')` / `textContent`; instead it creates a `<pnwm-occurrence-popup>` element, sets `.record`, and binds it. `main.js` imports `./pnwm-occurrence-popup.js`. `vite build` succeeds. Manual click on a marker shows the popup with identical content to before the refactor.
  </done>
</task>

</tasks>

<verification>
- Grep verifies the new component is registered in `main.js` and consumed in `pnwm-occurrence-map.js`.
- Grep verifies the imperative `document.createElement('p')` popup-building code has been removed from `pnwm-occurrence-map.js`.
- `vite build` succeeds end-to-end (catches syntax errors, bad imports, Lit decorator/property misuse).
- Manual click-through on a real species page confirms behavior parity (fields, order, omission of empty values).
- No use of `unsafeHTML` anywhere in the new component (`grep -r unsafeHTML src/components/pnwm-occurrence-popup.js` returns nothing).
</verification>

<success_criteria>
- `<pnwm-occurrence-popup>` is a registered custom element with a `.record` property.
- Popup content rendered on a species map matches the pre-refactor output exactly: same seven fields, same order, same `Label: value` format, same omission rule for empty values.
- XSS safety is preserved via Lit's auto-escaping `html` template (T-03-01 mitigation carried forward; no `unsafeHTML`).
- The map component is smaller and no longer responsible for popup DOM construction.
- Build (`vite build`) passes; species detail page loads without console errors.
</success_criteria>

<output>
Create `.planning/quick/260609-e2b-factor-the-occurrence-popup-out-into-its/260609-e2b-SUMMARY.md` when done, recording: files changed, the new component's public surface (the `record` property and tag name), and confirmation that XSS escaping is now delegated to Lit's `html` template.
</output>
