---
phase: quick
plan: 260412-u07
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/build-data.js
  - scripts/build-data.test.js
  - _instructions/ADDING_RECORDS.md
  - _instructions/ADDING_PHOTO.md
  - data/images.csv
  - src/_data/images.js
autonomous: true
must_haves:
  truths:
    - "sight_field_notes record_type passes validation without manual mapping"
    - "BC records up to ~55N latitude pass validation"
    - "ADDING_PHOTO.md documents copyright license convention and view/specimen metadata"
    - "images.csv schema includes view and specimen columns"
  artifacts:
    - path: "scripts/build-data.js"
      provides: "Updated validator accepting sight_field_notes and lat up to 55"
    - path: "_instructions/ADDING_RECORDS.md"
      provides: "Docs matching validator bounds and accepted types"
    - path: "_instructions/ADDING_PHOTO.md"
      provides: "Photo license convention and view/specimen field docs"
    - path: "data/images.csv"
      provides: "Schema with view and specimen columns"
  key_links:
    - from: "scripts/build-data.js"
      to: "data/records.csv"
      via: "record_type IN list and lat bounds check"
      pattern: "sight_field_notes|55\\.0"
---

<objective>
Address four contributor-facing deficiencies identified through Copilot feedback:
validator gaps (record type alias, latitude bound), missing photo license convention,
and missing image metadata fields.

Purpose: Remove friction for contributors migrating data from the source site.
Output: Updated validator, schema, docs, and tests.
</objective>

<context>
@scripts/build-data.js
@scripts/build-data.test.js
@_instructions/ADDING_RECORDS.md
@_instructions/ADDING_PHOTO.md
@data/images.csv
@src/_data/images.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix validator — accept sight_field_notes and expand latitude to 55N</name>
  <files>scripts/build-data.js, scripts/build-data.test.js, _instructions/ADDING_RECORDS.md</files>
  <action>
In scripts/build-data.js, make two changes to the validationChecks array:

1. **record_type validation (line ~152):** Add 'sight_field_notes' to the NOT IN list
   so it becomes:
   `WHERE record_type NOT IN ('specimen', 'photograph', 'literature', 'field notes', 'sight_field_notes')`

2. **Latitude bound (line ~166):** Change `latitude > 52.0` to `latitude > 55.0`
   and update the description comment from "lat 42.0-52.0" to "lat 42.0-55.0".

In scripts/build-data.test.js, add two unit tests after the existing state validation test:

- Test that 'sight_field_notes' passes the record_type validation query (create in-memory
  DuckDB table with record_type='sight_field_notes', run the validation query, assert 0 rows).
- Test that latitude 54.5 (valid BC record) passes the coordinate bounds check (create table
  with lat=54.5, lon=-122.0, run the bounds query, assert 0 rows).

In _instructions/ADDING_RECORDS.md:
- Update the record_type field description from "specimen, photograph, literature, or field notes"
  to "specimen, photograph, literature, field notes, or sight_field_notes"
- Update the latitude example text from "~41-54 lat" to "~42-55 lat" to match the validator
- Update the longitude bounds description if it says -125 to also say ~-130 to -110 to match
  the docs (check: the validator uses -125.0 to -110.0; keep the validator as-is but make the
  docs say "~-125 to -110" for accuracy)
  </action>
  <verify>
    <automated>node --test scripts/build-data.test.js 2>&1 | tail -5</automated>
  </verify>
  <done>
  - sight_field_notes accepted by validator without errors
  - Latitude 54.5 passes bounds check
  - ADDING_RECORDS.md matches actual validator rules
  - All existing and new tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Document photo license convention and add view/specimen metadata fields</name>
  <files>_instructions/ADDING_PHOTO.md, data/images.csv, src/_data/images.js, scripts/build-data.js</files>
  <action>
**Photo license convention in ADDING_PHOTO.md:**

In the Schema table, update the `license` field description to add guidance for proprietary photos.
After the table, add a "License conventions" subsection:

```
### License conventions

| Situation | license value | Example |
|-----------|--------------|---------|
| Creative Commons | The CC license identifier | `CC BY 4.0` |
| Copyrighted (used with permission) | `(c) Photographer Name` | `(c) Merrill Peterson` |
| Public domain | `public domain` | `public domain` |
```

**Add view and specimen columns to images.csv schema:**

1. In data/images.csv, add two new columns to the header and backfill existing rows with
   empty values. The header becomes:
   `species_id,filename,photographer,weight,license,view,specimen`
   Each existing data row gets two trailing commas (empty values for both new fields).

2. In _instructions/ADDING_PHOTO.md schema table, add two rows:
   - `view` | string | no | `dorsal` | View angle: dorsal, ventral, lateral, or head
   - `specimen` | string | no | `A` | Specimen identifier letter (A, B, C, D) when multiple specimens shown

   Update the CSV example row to show the new fields:
   ```csv
   1,02.jpg,Jane Doe,2,CC BY 4.0,dorsal,A
   ```

   Add a note: "The `view` and `specimen` fields are optional. When importing from the source
   site, these can be extracted from the original filename (e.g., `B-V-acronicta-americana-01.jpg`
   where B = specimen, V = ventral)."

3. In src/_data/images.js, add the two new columns to the DuckDB schema:
   Add `'view': 'VARCHAR'` and `'specimen': 'VARCHAR'` to the columns object.
   Add `view` and `specimen` to the SELECT list.

4. In scripts/build-data.js, update the validateCsv call for images.csv (line ~74) to include
   the new required columns list: add 'view' and 'specimen' to the array. Since these fields
   are optional in the CSV data but must exist as columns, they should be in the requiredColumns
   list (validateCsv checks column presence, not row values).
  </action>
  <verify>
    <automated>node --test scripts/build-data.test.js 2>&1 | tail -5</automated>
  </verify>
  <done>
  - ADDING_PHOTO.md documents license conventions for CC, copyright, and public domain
  - images.csv has view and specimen columns in header
  - Existing rows have empty values for new columns (no data loss)
  - src/_data/images.js reads and passes through view and specimen fields
  - build-data.js validates that view and specimen columns exist in images.csv
  - All tests pass
  </done>
</task>

</tasks>

<verification>
```bash
# Full test suite passes
node --test scripts/build-data.test.js

# Full build succeeds with updated CSV schema
npm run build:data
```
</verification>

<success_criteria>
- npm run build:data completes without validation errors
- All unit and integration tests pass
- sight_field_notes and lat up to 55N accepted by validator
- ADDING_RECORDS.md bounds match the validator
- ADDING_PHOTO.md has license convention table and view/specimen field docs
- images.csv has view and specimen columns; existing data intact
</success_criteria>
