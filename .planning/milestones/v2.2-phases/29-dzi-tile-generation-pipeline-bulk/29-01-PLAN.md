---
phase: 29-dzi-tile-generation-pipeline-bulk
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/tile-config.json
  - scripts/lib/dropbox-download.js
  - scripts/lib/dropbox-download.test.js
  - scripts/lib/manifest.js
  - scripts/lib/manifest.test.js
autonomous: true
requirements:
  - TILE-03
must_haves:
  truths:
    - "Tile parameters live in one committed JSON file that the tile script reads at runtime"
    - "Two runs of the tile script with the same scripts/tile-config.json produce byte-identical .dzi descriptors for the same source TIFF"
    - "A reusable Dropbox file-download helper exists that streams a single shared-link file to a local path with retry-safe semantics (no token leakage on error)"
    - "manifest.js exposes a status-transition helper that advances rows downloaded→tiled without touching unrelated columns"
  artifacts:
    - path: "scripts/tile-config.json"
      provides: "Committed tile parameters consumed by scripts/tile-photos.js (Plan 02)"
      contains: "tileSize, overlap, suffix, layout, tileOutputDir, tiffCacheDir, dropboxShareUrl"
    - path: "scripts/lib/dropbox-download.js"
      provides: "downloadSharedFile({ shareUrl, dropboxPath, token, destPath }) — POST /2/sharing/get_shared_link_file; streams response to destPath; throws redacted Error on non-2xx"
      exports: ["downloadSharedFile"]
    - path: "scripts/lib/dropbox-download.test.js"
      provides: "Unit test for downloadSharedFile error handling — token-redaction in error messages"
    - path: "scripts/lib/manifest.js"
      provides: "advanceStatus(row, nextStatus, extra) helper added; existing exports unchanged"
      exports: ["COLUMNS", "readManifest", "writeManifest", "sortForInvestigation", "advanceStatus"]
    - path: "scripts/lib/manifest.test.js"
      provides: "Tests for advanceStatus — sets row.status, clears row.last_error on success, preserves other columns"
  key_links:
    - from: "scripts/lib/dropbox-download.js"
      to: "Dropbox /2/sharing/get_shared_link_file endpoint"
      via: "fetch POST with Dropbox-API-Arg header"
      pattern: "content\\.dropboxapi\\.com|Dropbox-API-Arg"
    - from: "scripts/tile-config.json"
      to: "scripts/tile-photos.js (Plan 02 consumer)"
      via: "JSON.parse at script startup"
      pattern: "tileSize.*256|suffix.*webp"
---

<objective>
Create the committed tile-generation configuration file, the Dropbox file-download library, and the manifest status-transition helper that Plan 02's `scripts/tile-photos.js` will compose.

Purpose: TILE-03 requires tile parameters to live in a single committed pipeline-config file that makes reruns deterministic. Plan 02 needs (a) a way to pull source TIFFs from the Dropbox shared folder one file at a time (Phase 26's ingest is metadata-only — TIFFs are not on disk after Phase 26) and (b) a tested helper that advances a manifest row's `status` field without disturbing the other thirteen columns.

Output: `scripts/tile-config.json` with pilot-confirmed parameters; `scripts/lib/dropbox-download.js` + tests; `advanceStatus` exported from `scripts/lib/manifest.js` + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/28-end-to-end-vertical-slice-pilot-one-species/PILOT-LESSONS.md
@.planning/phases/28-end-to-end-vertical-slice-pilot-one-species/TILE-RECIPE.md
@scripts/lib/manifest.js
@scripts/lib/dropbox-list.js
@scripts/ingest-photos.js

<interfaces>
<!-- Interfaces extracted from the codebase that Plan 02 + Plan 03 will consume. -->

From scripts/lib/manifest.js (existing — DO NOT change column order or existing exports):
```
export const COLUMNS = [
  'content_hash', 'dropbox_path', 'size_bytes', 'server_modified',
  'filename_raw', 'binomial_raw', 'specimen_id', 'view',
  'binomial_resolved', 'species_slug', 'match_bucket', 'status', 'last_error',
];
export async function readManifest(path)
export async function writeManifest(path, rows)
export function sortForInvestigation(rows)
```

From scripts/lib/dropbox-list.js (existing — pattern reference for new dropbox-download.js):
```
export async function dbxCall(endpoint, body, token)
export async function* listSharedFolder({ shareUrl, token })
```
`dbxCall` uses `https://api.dropboxapi.com${endpoint}` POST with `Authorization: Bearer ${token}` and JSON body. The new `downloadSharedFile` MUST use the content-download host `https://content.dropboxapi.com/2/sharing/get_shared_link_file` instead — that endpoint puts the JSON args in the `Dropbox-API-Arg` HTTP header and returns the file body, not JSON.

Pilot-confirmed tile parameters (from PILOT-LESSONS.md §"Tile Parameters That Survived Contact"):
- `--tile-size 256`
- `--overlap 1`
- `--suffix .webp[Q=80]` (NOT .jpg — WebP is ~30% smaller, OSD handles it; the dzi descriptor's Format attribute correctly becomes "webp")
- `--layout dz`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit scripts/tile-config.json with pilot-confirmed parameters</name>
  <files>scripts/tile-config.json</files>
  <read_first>
    - .planning/phases/28-end-to-end-vertical-slice-pilot-one-species/PILOT-LESSONS.md (§"Tile Parameters That Survived Contact" — exact flag values)
    - .planning/phases/28-end-to-end-vertical-slice-pilot-one-species/TILE-RECIPE.md (operator runbook — same flag values, used as a cross-check)
    - scripts/ingest-photos.js (lines 41-45 — env-vars-at-top pattern; the dropbox share URL value)
  </read_first>
  <action>
    Create `scripts/tile-config.json` as a plain JSON object containing exactly these top-level keys with these values:

    - `tileSize`: integer `256`
    - `overlap`: integer `1`
    - `suffix`: string `".webp[Q=80]"` (verbatim — square brackets are part of the vips dzsave argument, not JSON metadata)
    - `layout`: string `"dz"`
    - `tileOutputDir`: string `"/var/lib/pnwmoths/tiles"` (default datacenter path; operator overrides via TILE_OUTPUT_DIR env var in Plan 02)
    - `tiffCacheDir`: string `"/var/lib/pnwmoths/tiffs"` (default datacenter path; operator overrides via TIFF_CACHE_DIR env var in Plan 02)
    - `dropboxShareUrl`: string `"https://www.dropbox.com/scl/fo/uf3sg1efxau1fug4f6ibe/AARZETfHfpzlvILrd6KLWlc?rlkey=7m1pm3z0rnasb9i01a5ht0ppf&st=emehj9n2&dl=0"` (verbatim from scripts/ingest-photos.js DROPBOX_SHARE_URL default — same shared folder; Plan 02 reuses it for the download step)

    File MUST be valid JSON parseable by `JSON.parse` with no trailing comma and no JS comments. Use 2-space indentation. No additional keys — TILE-03 requires the config surface to be small and deterministic.

    Do NOT inline these values into `scripts/tile-photos.js` later; Plan 02 will read this file at startup so the operator can change parameters and rerun deterministically.
  </action>
  <verify>
    <automated>node -e "const c = require('./scripts/tile-config.json'); if (c.tileSize !== 256 || c.overlap !== 1 || c.suffix !== '.webp[Q=80]' || c.layout !== 'dz' || !c.tileOutputDir || !c.tiffCacheDir || !c.dropboxShareUrl) { console.error('config invariants failed', c); process.exit(1); } console.log('ok');"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/tile-config.json` exists and is valid JSON (`node -e "JSON.parse(require('node:fs').readFileSync('scripts/tile-config.json'))"` exits 0)
    - `scripts/tile-config.json` contains the literal key/value pair `"tileSize": 256`
    - `scripts/tile-config.json` contains the literal key/value pair `"overlap": 1`
    - `scripts/tile-config.json` contains the literal key/value pair `"suffix": ".webp[Q=80]"`
    - `scripts/tile-config.json` contains the literal key/value pair `"layout": "dz"`
    - `scripts/tile-config.json` contains a non-empty string under `"tileOutputDir"`, `"tiffCacheDir"`, and `"dropboxShareUrl"` (verify with `node -e "const c = require('./scripts/tile-config.json'); for (const k of ['tileOutputDir','tiffCacheDir','dropboxShareUrl']) { if (typeof c[k] !== 'string' || c[k].length === 0) process.exit(1); }"`)
    - `scripts/tile-config.json` contains exactly 7 top-level keys (no `format`, no `quality`, no extras — those values are encoded inside `suffix`)
  </acceptance_criteria>
  <done>scripts/tile-config.json committed with the seven keys above; parameters match the pilot-confirmed values from PILOT-LESSONS.md exactly.</done>
</task>

<task type="auto">
  <name>Task 2: Implement scripts/lib/dropbox-download.js with shared-link file download</name>
  <files>scripts/lib/dropbox-download.js, scripts/lib/dropbox-download.test.js</files>
  <read_first>
    - scripts/lib/dropbox-list.js (pattern reference — `dbxCall` shape, `Authorization: Bearer ${token}` header convention, no-SDK-just-fetch decision in the file header comment)
    - scripts/ingest-photos.js (lines 71-103 — `redact()` and `withRetry()` patterns; the new download library should NOT include withRetry itself but MUST emit redacted error messages, mirroring dropbox-list.js's "no retry in the library" stance)
  </read_first>
  <action>
    Create `scripts/lib/dropbox-download.js` exporting one function:

    `export async function downloadSharedFile({ shareUrl, dropboxPath, token, destPath })`

    Behavior:
    1. Validate inputs — throw a non-retriable `Error` (message: `'downloadSharedFile: missing required param: <name>'`) if any of `shareUrl`, `dropboxPath`, `token`, `destPath` is falsy. Do NOT include the token value in the message.
    2. POST to `https://content.dropboxapi.com/2/sharing/get_shared_link_file` with headers:
       - `Authorization: Bearer ${token}`
       - `Dropbox-API-Arg: ${JSON.stringify({ url: shareUrl, path: dropboxPath })}` — this endpoint takes its JSON args in a header, not the body
       - Body: empty (no `Content-Type` header)
    3. On non-2xx response, read `await res.text()` and throw `new Error(\`/2/sharing/get_shared_link_file → ${res.status}: ${text}\`)`. Same shape as `dbxCall` in `scripts/lib/dropbox-list.js`. Do NOT echo the `Authorization` header into the message.
    4. On 2xx response, stream `res.body` (a Node `ReadableStream`) to `destPath` using `node:stream/promises` `pipeline` and `node:fs.createWriteStream`. Create the parent directory of `destPath` first with `mkdir({ recursive: true })`.
    5. Return `undefined` on success.

    No retry policy lives in this library. Callers (Plan 02's `scripts/tile-photos.js`) wrap with `withRetry`.

    Also create `scripts/lib/dropbox-download.test.js` with two tests using `node --test`:

    1. Throws when `token` is missing — assert error message contains `'missing required param: token'` and does NOT contain any token-like substring.
    2. Throws a shaped error when fetch returns non-2xx — mock `globalThis.fetch` to return `{ ok: false, status: 401, text: async () => 'invalid_access_token' }`, call `downloadSharedFile({ shareUrl: 'x', dropboxPath: '/a', token: 'sl.SECRET', destPath: '/tmp/x' })`, assert error message starts with `/2/sharing/get_shared_link_file → 401:` and does NOT include the substring `sl.SECRET`. Restore the original `fetch` in a try/finally.

    Match the JSDoc and header-comment style of `scripts/lib/dropbox-list.js` — top-of-file block comment naming the phase, the API endpoint touched, and the "no retry here" design note.
  </action>
  <verify>
    <automated>node --test scripts/lib/dropbox-download.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/lib/dropbox-download.js` exists and exports `downloadSharedFile` (`node -e "import('./scripts/lib/dropbox-download.js').then(m => { if (typeof m.downloadSharedFile !== 'function') process.exit(1); })"`)
    - `scripts/lib/dropbox-download.js` contains the literal string `content.dropboxapi.com/2/sharing/get_shared_link_file` (grep gate: `grep -v '^[[:space:]]*\*\|^[[:space:]]*//' scripts/lib/dropbox-download.js | grep -c 'content\.dropboxapi\.com/2/sharing/get_shared_link_file'` returns at least 1)
    - `scripts/lib/dropbox-download.js` contains the literal string `Dropbox-API-Arg`
    - `scripts/lib/dropbox-download.js` contains `pipeline` from `node:stream/promises` (grep `grep -c "from 'node:stream/promises'" scripts/lib/dropbox-download.js` returns 1)
    - `node --test scripts/lib/dropbox-download.test.js` exits 0 and reports 2 passing tests
    - The non-2xx error test asserts the thrown message does NOT contain `'sl.SECRET'` (token-leakage gate)
  </acceptance_criteria>
  <done>downloadSharedFile is implemented and tested; tests cover missing-param validation and shaped non-2xx errors with no token leakage.</done>
</task>

<task type="auto">
  <name>Task 3: Add advanceStatus helper to scripts/lib/manifest.js</name>
  <files>scripts/lib/manifest.js, scripts/lib/manifest.test.js</files>
  <read_first>
    - scripts/lib/manifest.js (lines 31-51 — COLUMNS contract; lines 73-96 — existing export style; ensure new export does not change COLUMNS ordering)
    - scripts/ingest-photos.js (lines 344-350 — the existing in-place row.match_bucket / row.status mutation pattern from the RESORT_ONLY path; advanceStatus should follow this same in-place mutation convention)
  </read_first>
  <action>
    Add a new export to `scripts/lib/manifest.js`:

    `export function advanceStatus(row, nextStatus, extra = {})`

    Behavior (in-place mutation of the row object passed in — same convention as the RESORT_ONLY path in scripts/ingest-photos.js):

    1. If `row` is null/undefined, throw `new TypeError('advanceStatus: row required')`.
    2. If `nextStatus` is not a non-empty string, throw `new TypeError('advanceStatus: nextStatus must be a non-empty string')`.
    3. Set `row.status = nextStatus`.
    4. If `nextStatus === 'failed'`:
       - Set `row.last_error = String(extra.last_error ?? '')`.
    5. Otherwise (any non-failed status, including `'downloaded'`, `'tiled'`, `'uploaded'`):
       - Set `row.last_error = ''` (clear any prior failure reason now that the row has progressed).
    6. Do NOT touch any other column in the row (`content_hash`, `dropbox_path`, `size_bytes`, `server_modified`, `filename_raw`, `binomial_raw`, `specimen_id`, `view`, `binomial_resolved`, `species_slug`, `match_bucket` MUST remain byte-identical).
    7. Return the same `row` reference (for chaining if a caller wants it).

    Preserve every existing export and every line of existing logic in `scripts/lib/manifest.js`. The `COLUMNS` array MUST remain in the same order; do not reorder.

    Add three tests to `scripts/lib/manifest.test.js` (create the file if it does not exist; if it does, append):

    1. Setting `downloaded` clears `last_error` and preserves all other columns. Construct a row with every COLUMN populated (including `last_error: 'previous failure'` and `match_bucket: 'clean-match'`), call `advanceStatus(row, 'downloaded')`, assert `row.status === 'downloaded'`, `row.last_error === ''`, and every other column equals its prior value via a per-column equality loop.
    2. Setting `failed` records `last_error` from `extra.last_error` and preserves all other columns. Construct a row, call `advanceStatus(row, 'failed', { last_error: 'vips: cannot open input' })`, assert `row.status === 'failed'`, `row.last_error === 'vips: cannot open input'`, every other column unchanged.
    3. Throws on missing nextStatus. Call `advanceStatus({}, '')` and assert it throws a `TypeError`.

    Use `import { describe, it } from 'node:test'` and `import assert from 'node:assert/strict'` matching the style of `scripts/ingest-photos.test.js`.
  </action>
  <verify>
    <automated>node --test scripts/lib/manifest.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/lib/manifest.js` exports `advanceStatus` (`node -e "import('./scripts/lib/manifest.js').then(m => { if (typeof m.advanceStatus !== 'function') process.exit(1); })"`)
    - `scripts/lib/manifest.js` still exports `COLUMNS`, `readManifest`, `writeManifest`, `sortForInvestigation` (run the same import probe for each)
    - The `COLUMNS` array in `scripts/lib/manifest.js` still has 13 entries in the original order (`grep -A 14 "export const COLUMNS" scripts/lib/manifest.js | grep -c "'"` returns 13)
    - `node --test scripts/lib/manifest.test.js` exits 0 and reports 3 new advanceStatus tests passing
    - The "preserves all other columns" tests iterate every column except `status` and `last_error` (the test body MUST contain references to `content_hash`, `dropbox_path`, `binomial_raw`, `species_slug`, `match_bucket` — grep gate: `grep -c "row\.content_hash\|row\.dropbox_path\|row\.binomial_raw\|row\.species_slug\|row\.match_bucket" scripts/lib/manifest.test.js` returns at least 5)
  </acceptance_criteria>
  <done>advanceStatus exported from scripts/lib/manifest.js with three tests proving status transition, last_error semantics, and per-column preservation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator-shell → script | Operator runs `node scripts/tile-photos.js` (Plan 02 consumer) with `DROPBOX_TOKEN` and (optional) `TILE_OUTPUT_DIR` env vars |
| script → Dropbox API | `downloadSharedFile` POSTs the bearer token to `content.dropboxapi.com`; the response is an untrusted file body that vips will subsequently read |
| script → local disk | Tile script writes TIFFs into `tiffCacheDir` and DZI output into `tileOutputDir`; both paths come from operator-controlled config |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29.01-01 | Information disclosure | dropbox-download.js error handling | mitigate | Error from non-2xx fetch echoes only `res.status` + response body text; Dropbox does not echo `Authorization` in error bodies (same property used in scripts/lib/dropbox-list.js dbxCall). Test 2 asserts the thrown error does not contain the token substring. |
| T-29.01-02 | Tampering | scripts/tile-config.json | accept | Config file is committed to the repo; tampering requires repo write access, which is already trusted. No runtime integrity check needed. |
| T-29.01-03 | Information disclosure | downloadSharedFile destPath argument | accept | destPath is operator-controlled (sourced from `tiffCacheDir` in tile-config.json); operator already has filesystem write rights on the datacenter server. No path-traversal mitigation needed because there is no untrusted caller. |
</threat_model>

<verification>
- `scripts/tile-config.json` parses as JSON and contains the seven keys with pilot-confirmed values.
- `node --test scripts/lib/dropbox-download.test.js` exits 0 with 2 passing tests.
- `node --test scripts/lib/manifest.test.js` exits 0 with at least 3 advanceStatus tests passing.
- `npm test` exits 0 (regression gate — existing Phase 26/27 ingest-photos and manifest tests still pass).
</verification>

<success_criteria>
1. `scripts/tile-config.json` is committed with the seven pilot-confirmed keys.
2. `scripts/lib/dropbox-download.js` exposes `downloadSharedFile` with token-redacted error semantics, tested.
3. `scripts/lib/manifest.js` exposes `advanceStatus` with per-column preservation, tested.
4. `npm test` exits 0 with the three new test files contributing passing assertions.
</success_criteria>

<output>
Create `.planning/phases/29-dzi-tile-generation-pipeline-bulk/29-01-SUMMARY.md` when done. Include: confirmation of tile-config.json contents (paste verbatim); confirmation of new exports and their signatures; list of test counts added.
</output>
