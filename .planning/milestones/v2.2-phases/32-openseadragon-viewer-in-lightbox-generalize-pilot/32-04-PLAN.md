---
phase: 32-openseadragon-viewer-in-lightbox-generalize-pilot
plan: 04
type: execute
wave: 3
depends_on:
  - 32-01
  - 32-02
  - 32-03
files_modified: []
autonomous: false
requirements:
  - VIEWER-01
  - VIEWER-02
  - VIEWER-03
  - VIEWER-04
tags:
  - verification
  - human-verify
  - regression
must_haves:
  truths:
    - "On abagrotis-apposita (high-res, 2 specimens) the carousel shows two thumbnails (level-0 tiles)"
    - "Clicking the main slide on abagrotis-apposita opens OSD lightbox; pan/zoom/home-reset all work"
    - "Prev and next buttons are visible inside the lightbox; clicking them swaps DZI tile sources without close/reopen and updates the caption"
    - "Close button (×) reliably closes the lightbox on a high-res species page even with OSD active"
    - "On a non-high-res species (any species where speciesPhotos[sp.slug] is undefined or high_res_available is false), the existing Phase 23 carousel + static lightbox works unchanged — no OSD, no prev/next, no regression"
    - "On any high-res species with a single specimen, the lightbox shows OSD but NO prev/next buttons"
    - "Build emits 1,364 species pages (Phase 31 baseline preserved)"
  artifacts:
    - path: "_site/species/abagrotis-apposita/index.html"
      provides: "rendered high-res figures inside pnwm-image-slideshow"
      contains: "_files/0/0_0.webp"
    - path: ".planning/phases/32-openseadragon-viewer-in-lightbox-generalize-pilot/32-03-SUMMARY.md"
      provides: "human-verify checkpoint outcome and any defects observed"
  key_links:
    - from: "browser carousel click"
      to: "OSD lightbox + prev/next"
      via: "Lit component _openLightbox + _prevSpecimen/_nextSpecimen"
      pattern: "high-res species page visual flow"
---

<objective>
End-to-end verification of Phase 32's generalized OSD lightbox wiring. After Plans 01 and 02 ship, this plan runs a full build, inspects generated HTML for both high-res and low-res code paths, and pauses for a human-verify checkpoint that exercises the live browser flow on the pilot species (abagrotis-apposita, 2 specimens, multi-specimen path) and at least one non-high-res control species (Phase 23 regression).

Purpose: Phase 31 + this phase together change what gets rendered on every species page. Unit tests cover the new component logic, but the end-to-end browser flow (OSD initialization, DZI tile loading from CDN, prev/next viewer.open swap, close-button click while OSD is active, no-regression on Phase 23 carousel) requires visual confirmation per VIEWER-01/02/03/04. Build-level regression checks also catch any template surprises (template syntax errors, page-count drift).

Output: Build artifacts confirmed on disk; human-verify checkpoint approved with notes; PILOT-LESSONS-style 32-03-SUMMARY.md recording any defects or follow-ups.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-openseadragon-viewer-in-lightbox-generalize-pilot/32-CONTEXT.md
@.planning/phases/32-openseadragon-viewer-in-lightbox-generalize-pilot/32-UI-SPEC.md
@.planning/phases/32-openseadragon-viewer-in-lightbox-generalize-pilot/32-01-PLAN.md
@.planning/phases/32-openseadragon-viewer-in-lightbox-generalize-pilot/32-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full build + automated regression checks</name>
  <files>_site/species/abagrotis-apposita/index.html, src/components/pnwm-image-slideshow.test.js, data/species-photos.json, data/images.csv</files>
  <read_first>
    - .planning/phases/32-openseadragon-viewer-in-lightbox-generalize-pilot/32-01-PLAN.md (acceptance criteria recap — what _site/species/abagrotis-apposita/index.html must contain)
    - .planning/phases/32-openseadragon-viewer-in-lightbox-generalize-pilot/32-02-PLAN.md (acceptance criteria recap — what component changes must be present)
    - data/species-photos.json (identify the pilot species `abagrotis-apposita` entry and one single-specimen high-res species if present)
  </read_first>
  <action>
    Run the full test suite and a clean build. Then perform four automated source/HTML assertions:

    1. `npm test` — exits 0; entire suite passes including the 7 new tests from Plan 02.

    2. `npm run build` — exits 0; final log line shows `Wrote 1364 files` (or whatever the exact Phase-31-baseline page count is — read it from the last roadmap update; the requirement is "no change from Phase 31 baseline").

    3. Assert the pilot species HTML emitted by Plan 01:
       - `_site/species/abagrotis-apposita/index.html` exists
       - Contains exactly two `<figure>` elements inside `<pnwm-image-slideshow>` whose `<img src>` ends with `_files/0/0_0.webp`
       - Contains `<figcaption>Specimen A · Dorsal</figcaption>` AND `<figcaption>Specimen A · Ventral</figcaption>` in that DOM order (D before V, matching the `data/species-photos.json` specimens array order)
       - Does NOT contain any `<img src="https://pnwmoths.b-cdn.net/abagrotis-apposita/...">` low-res figure (DATA-03 guard preserved)

    4. Assert the non-high-res regression path:
       - Identify one species where `speciesPhotos[slug]?.high_res_available` is false (or `speciesPhotos[slug]` is undefined) and `images[slug]` has ≥ 1 entry. Read `data/species-photos.json` and `data/images.csv` (or `src/_data/images.js`) to pick one; if none can be confirmed by inspection, default to any non-`abagrotis-apposita` slug from `data/species.csv` that has at least one image in `data/images.csv`.
       - Confirm its `_site/species/{slug}/index.html` contains the existing low-res `<img src="{{ cdnBaseUrl }}/{slug}/...">` figures (unchanged from Phase 23/31) and does NOT contain any `_files/0/0_0.webp` references.

    Record the chosen non-high-res species slug in the summary so the human-verify checkpoint can use the same one.

    Do NOT modify any source files in this task. If any assertion fails, surface the failure clearly and stop — do not attempt to patch in this plan; instead surface a defect for re-planning against Plan 01 or Plan 02.
  </action>
  <verify>
    <automated>npm test 2>&amp;1 | tail -10 &amp;&amp; npm run build 2>&amp;1 | tail -10 &amp;&amp; test -f _site/species/abagrotis-apposita/index.html &amp;&amp; grep -c '_files/0/0_0.webp' _site/species/abagrotis-apposita/index.html &amp;&amp; grep -c 'Specimen A · Dorsal' _site/species/abagrotis-apposita/index.html &amp;&amp; grep -c 'Specimen A · Ventral' _site/species/abagrotis-apposita/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0 with all tests passing (including the 7 added in Plan 02)
    - `npm run build` exits 0 and produces 1,364 species pages (no drift from Phase 31 baseline)
    - `_site/species/abagrotis-apposita/index.html` exists
    - `_site/species/abagrotis-apposita/index.html` `grep -c '_files/0/0_0.webp'` returns ≥ 2 (one per specimen figure)
    - `_site/species/abagrotis-apposita/index.html` `grep -c 'Specimen A · Dorsal'` returns ≥ 1
    - `_site/species/abagrotis-apposita/index.html` `grep -c 'Specimen A · Ventral'` returns ≥ 1
    - Dorsal figcaption appears BEFORE Ventral figcaption in DOM order (`awk '/Dorsal/{print NR; exit}' _site/species/abagrotis-apposita/index.html` < `awk '/Ventral/{print NR; exit}' _site/species/abagrotis-apposita/index.html`)
    - The chosen non-high-res control species page does NOT contain `_files/0/0_0.webp` AND still contains at least one low-res `<img src="https://pnwmoths.b-cdn.net/{slug}/` reference
    - Control species slug recorded in `32-03-SUMMARY.md` for downstream human-verify
  </acceptance_criteria>
  <done>Test suite green; build produces unchanged page count; pilot species HTML contains two correctly-captioned, correctly-ordered high-res figures; one control species confirms low-res path unchanged; control species slug captured for the human checkpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify lightbox flows on pilot and control species</name>
  <what-built>
    Phase 32 deliverable: (a) `species.njk` `{% elif highRes %}` branch renders one `<figure>` per high-res specimen as the carousel/no-JS source; (b) `pnwm-image-slideshow.js` gains `_prevSpecimen`/`_nextSpecimen` methods, in-lightbox prev/next buttons (rendered only when `useOsd && _highResSpecimens.length > 1`), and a `.lightbox-close` z-index fix; (c) seven new unit tests cover wrap-around navigation, low-res regression (`useOsd` false path), and view→label mapping.

    The Phase 28 OSD wiring is now generalized — any species marked `high_res_available: true` in `data/species-photos.json` will render an OSD lightbox in production.
  </what-built>
  <how-to-verify>
    Start the dev server: `npm run start` (or `npm run dev` if that is the convention — read `package.json` `scripts` to confirm). Open the served site in a browser (Firefox or Chromium recommended for OSD WebGL parity).

    **A. Pilot species — high-res, multi-specimen (`abagrotis-apposita`):**

    1. Navigate to `http://localhost:8080/species/abagrotis-apposita/` (or whatever port your dev server uses).
    2. Confirm the carousel shows two thumbnails. Each thumbnail should be a level-0 tile (a single small WebP image — coarsest resolution of the DZI pyramid).
    3. Confirm the main slide above the thumbnail strip shows the level-0 tile of the currently-selected specimen (clicking each thumbnail should swap the main slide).
    4. Click the main slide → OSD lightbox should open. Verify:
       - The OSD viewer fills the lightbox (90vw × 70vh).
       - Pan (click-drag) works.
       - Zoom (scroll wheel or +/− toolbar buttons) works.
       - "Home" (the house icon in OSD's toolbar) resets to fit-all.
       - Caption below reads `Specimen A · Dorsal` (or whichever specimen you started on).
    5. Confirm prev (`❮`) and next (`❯`) buttons are visible — left- and right-edge of the lightbox, vertically centered, semi-transparent dark, 44×44px.
    6. Click `next` → the OSD viewer should swap to the other specimen's DZI tiles WITHOUT close/reopen flicker. Caption should update to the other specimen's view label.
    7. Click `next` again → wrap around to the first specimen.
    8. Click `prev` → cycle backward; confirm wrap.
    9. Click the `×` close button in the top-right → lightbox dismisses. Confirm focus returns to the page. (This is the folded Phase 23 close-button todo — if it does NOT close, record the failure mode precisely: does the click register? Is it captured by OSD? Is the button visually obscured?)
    10. Reopen the lightbox and press Escape → dismisses (existing behavior, regression check).

    **B. Control species — non-high-res (Phase 23 regression):**

    1. Navigate to `http://localhost:8080/species/{control-slug}/` (use the slug recorded by Task 1's automated step).
    2. Confirm carousel + thumbnails render exactly as they did in v2.1 (Phase 23 behavior).
    3. Click the main slide → static `<img>` lightbox opens (NOT OSD).
    4. Confirm there are NO prev/next arrow buttons inside the lightbox.
    5. Click the `×` close button → dismisses. (Regression check — if it worked in v2.1, it should still work.)
    6. Press Escape → dismisses.

    **C. Single-specimen high-res species (if available):**

    If `data/species-photos.json` contains any species with `high_res_available: true` and exactly one specimen, navigate to that species page:
    1. Open the lightbox.
    2. Confirm OSD initializes and works.
    3. Confirm NO prev/next buttons are rendered (single-specimen guard).
    4. Confirm close button works.

    If no single-specimen high-res species exists yet, skip section C and note "n/a — all high-res species in the current dataset have multiple specimens" in the resume signal.

    Record any defects with exact reproduction steps. Acceptable outcomes:
    - All pass → reply "approved" with optional notes
    - Cosmetic-only issues (button hover too subtle, arrow glyph weight, etc.) → reply "approved with follow-ups: <list>" — these become a deferred-items entry, not a Phase 32 blocker
    - Functional defects (OSD doesn't load, prev/next doesn't swap, close button broken) → reply "blocked: <description>" — Phase 32 must re-plan to fix before merging
  </how-to-verify>
  <resume-signal>Reply with `approved`, `approved with follow-ups: <list>`, or `blocked: <description>`.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local dev server → human reviewer | Eleventy dev server serves `_site/` content; reviewer interacts via browser. No new trust boundary introduced by this verification plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-03-01 | Repudiation | Human checkpoint approval without recorded notes | mitigate | Resume signal requires `approved`, `approved with follow-ups: ...`, or `blocked: ...`; outcome recorded in `32-03-SUMMARY.md` with timestamp. |
| T-32-03-02 | Tampering | Stale `_site/` from a previous build masking current code state | mitigate | Task 1 runs `npm run build` fresh before assertions; reviewer instructed to use the dev server which rebuilds on demand. |
</threat_model>

<verification>
- Task 1 automated assertions all pass
- Task 2 human checkpoint resolved with `approved` or `approved with follow-ups: ...`
- `32-03-SUMMARY.md` records: chosen control species slug, browser used, OSD behavior observed, prev/next swap behavior, close-button outcome (still broken? z-index fix sufficient?), any follow-ups
</verification>

<success_criteria>
- All four phase requirements (VIEWER-01..04) confirmed live in the browser
- No regression to Phase 23 carousel/lightbox behavior on non-high-res species
- Close-button issue resolved or precisely documented for follow-up
- Phase 32 ready for `/gsd:verify-work` gate and merge to milestone v2.2
</success_criteria>

<output>
Create `.planning/phases/32-openseadragon-viewer-in-lightbox-generalize-pilot/32-03-SUMMARY.md` when done, recording:
- Test suite + build outputs (final lines)
- Chosen control species slug
- Pilot species HTML inspection results (figure count, ordering, caption text)
- Human-verify outcome verbatim (resume signal)
- Browser used (Firefox/Chromium/Safari)
- Single-specimen high-res species: tested? n/a? observed behavior?
- Any defects, follow-ups, or deferred items surfaced during verification
</output>
