---
phase: 22-phenology-chart-improvements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/pnwm-phenology-chart.js
autonomous: false
requirements:
  - CHART-01
  - CHART-02
tags:
  - chart.js
  - lit
  - phenology

must_haves:
  truths:
    - "X-axis of the phenology chart displays the label 'Month'"
    - "Y-axis of the phenology chart displays the label '# Records'"
    - "Y-axis begins at 0 regardless of dataset"
    - "Y-axis maximum auto-scales to the highest monthly record count (no fixed cap)"
    - "A species with all records in one month renders one tall bar plus eleven zero bars, with the Y-axis scaled to that bar"
  artifacts:
    - path: "src/components/pnwm-phenology-chart.js"
      provides: "Chart.js bar chart configuration including scales.x.title, scales.y.title, and scales.y.beginAtZero"
      contains: "scales:"
  key_links:
    - from: "src/components/pnwm-phenology-chart.js"
      to: "Chart.js v4 axis title API"
      via: "options.scales.{x,y}.title.{display,text}"
      pattern: "scales:\\s*\\{"
    - from: "src/components/pnwm-phenology-chart.js"
      to: "Chart.js v4 LinearScale beginAtZero"
      via: "options.scales.y.beginAtZero"
      pattern: "beginAtZero:\\s*true"
---

<objective>
Add axis titles ("Month" on X-axis, "# Records" on Y-axis) and pin the Y-axis floor to 0 with auto-scaled max on the phenology chart, satisfying CHART-01 and CHART-02.

Purpose: Users currently see an unlabeled bar chart; phase 22 closes a UX gap relative to the reference pnwinsects-app by labeling the axes and ensuring the Y-scale always starts at 0 (so single-month species do not appear with a misleading baseline).

Output: Updated `src/components/pnwm-phenology-chart.js` with a `scales` block inside the Chart.js `options` object.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-phenology-chart-improvements/22-CONTEXT.md
@.planning/phases/22-phenology-chart-improvements/22-PATTERNS.md
@src/components/pnwm-phenology-chart.js

<interfaces>
<!-- Key contracts the executor needs. Extracted from src/components/pnwm-phenology-chart.js. -->

Already imported and registered in pnwm-phenology-chart.js (no new imports needed):
- Chart, BarController, BarElement, CategoryScale, LinearScale, Tooltip from 'chart.js'
- Chart.register(BarController, BarElement, CategoryScale, LinearScale, Tooltip)

Chart instantiation site (lines 108-127):
- new Chart(canvas, { type: 'bar', data: {...}, options: {...} })
- options currently has keys: responsive, plugins (legend.display=false)
- Target: add a sibling `scales` key with `x.title.{display,text}` and `y.{beginAtZero, title.{display,text}}`

Update path (lines 104-106) is unchanged - Chart.js retains scale options across `chart.update()`:
- this._chart.data.datasets[0].data = counts
- this._chart.update()

Per D-04: do NOT import or register the standalone `Title` plugin from chart.js. Axis titles are built into CategoryScale and LinearScale in Chart.js v4.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add scales config (axis titles + beginAtZero) to phenology chart options</name>
  <files>src/components/pnwm-phenology-chart.js</files>
  <read_first>
    - src/components/pnwm-phenology-chart.js (full file - the change lives inside _renderChart, lines 100-128; verify current options object has only `responsive` and `plugins` keys before editing)
    - .planning/phases/22-phenology-chart-improvements/22-CONTEXT.md (D-01 through D-04 lock styling, label text, Y-axis floor, and forbid the Title plugin import)
    - .planning/phases/22-phenology-chart-improvements/22-PATTERNS.md (target snippet under "Target: add `scales` key to `options`")
  </read_first>
  <behavior>
    - X-axis renders a title "Month" below the month labels
    - Y-axis renders a title "# Records" rotated along the Y-axis
    - Y-axis baseline is 0 for every species, including species with all records in one month
    - Y-axis maximum is computed by Chart.js (no explicit max), so a species peaking at 3 records and a species peaking at 300 both scale appropriately
    - Filter updates (via _chart.update()) preserve the new scales config - axis titles and beginAtZero remain after data changes
  </behavior>
  <action>
    Inside the `new Chart(canvas, { ... })` call in `_renderChart()` (currently at lines 108-127), add a `scales` key to the `options` object as a sibling of the existing `responsive` and `plugins` keys per D-02, D-03, and D-04.

    Concrete target state for the `scales` key:
    - `scales.x.title.display` = `true`
    - `scales.x.title.text` = `'Month'` (per D-02)
    - `scales.y.beginAtZero` = `true` (per D-03 - preferred over `min: 0`)
    - `scales.y.title.display` = `true`
    - `scales.y.title.text` = `'# Records'` (per D-02)

    Do NOT modify:
    - The existing `responsive: true` or `plugins.legend.display: false` keys
    - The import list at the top of the file (no new chart.js imports - D-04 forbids importing the `Title` plugin)
    - The `Chart.register(...)` call (CategoryScale and LinearScale already cover the axis title API in Chart.js v4)
    - The data path (lines 104-106) that handles `_chart.update()` on filter change
    - The hardcoded bar `backgroundColor: '#0172ad'` (out of scope per CSS-custom-props-in-Canvas decision)
    - Any axis label font, color, or family - D-01 accepts Chart.js defaults

    Use 2-space indentation matching the surrounding file. Keep the `scales` block inline (not extracted to a named const) - per Claude's Discretion in CONTEXT.md, inline is preferred at this scope.
  </action>
  <verify>
    <automated>node --test src/components/phenology.test.js 2>&amp;1 | tail -5 &amp;&amp; grep -q "scales:" src/components/pnwm-phenology-chart.js &amp;&amp; grep -q "text: '# Records'" src/components/pnwm-phenology-chart.js &amp;&amp; grep -q "text: 'Month'" src/components/pnwm-phenology-chart.js &amp;&amp; grep -q "beginAtZero: true" src/components/pnwm-phenology-chart.js &amp;&amp; ! grep -E "^import .*\bTitle\b" src/components/pnwm-phenology-chart.js</automated>
  </verify>
  <acceptance_criteria>
    - src/components/pnwm-phenology-chart.js contains the substring `scales:` exactly once (new block inside `_renderChart`)
    - src/components/pnwm-phenology-chart.js contains the substring `text: 'Month'`
    - src/components/pnwm-phenology-chart.js contains the substring `text: '# Records'`
    - src/components/pnwm-phenology-chart.js contains the substring `beginAtZero: true`
    - `grep -n "Chart.register" src/components/pnwm-phenology-chart.js` still shows the original line registering `BarController, BarElement, CategoryScale, LinearScale, Tooltip` and does NOT include `Title`
    - `grep -E "^import " src/components/pnwm-phenology-chart.js` does NOT mention `Title` (the standalone chart-level Title plugin must not be imported)
    - Existing `responsive: true` line is still present
    - Existing `legend: { display: false }` block is still present
    - Existing `backgroundColor: '#0172ad'` line is still present
    - `node --test src/components/phenology.test.js` exits 0 (existing aggregateByMonth tests unaffected by the options edit)
  </acceptance_criteria>
  <done>
    The phenology chart file declares a `scales` block configuring X/Y axis titles and `beginAtZero: true` on Y, with no new imports and no changes to the data/update path. Existing tests pass.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Full test suite + production build verification</name>
  <files>(none - read-only verification)</files>
  <read_first>
    - src/components/pnwm-phenology-chart.js (confirm Task 1 edits landed)
    - package.json (the `test` and `build` scripts: full pipeline is build:data -> build:eleventy -> build:copy-parquet -> build:copy-images -> build:species-states -> build:pagefind -> build:validate-links -> build:check-weight)
  </read_first>
  <behavior>
    - `npm test` exits 0 (all suites in eleventy.config.test.js, scripts/*.test.js, src/components/*.test.js, src/_lib/*.test.js)
    - `npm run build` exits 0 (full pipeline produces _site/)
    - The built JS bundle(s) for the phenology chart contain the axis title strings (proves the edit survived the Vite build)
  </behavior>
  <action>
    Run the full test suite, then run the full build pipeline, then grep the built JS bundle(s) under `_site/` for the axis title strings to prove they survived Vite bundling.

    Do NOT modify any source files in this task. If `npm test` or `npm run build` fail, surface the failure and stop - Task 1 needs revision.

    The bundle grep covers Vite's chunked output: the phenology chart code may land in a hashed chunk, so search recursively under `_site/` for the literal string `# Records` inside any `.js` file. Post-Vite, single quotes may become double quotes - the grep below accepts either via a character class.

    If the grep for `# Records` returns no matches, that is a real failure: investigate whether the `scales` block was tree-shaken or omitted from the production bundle.
  </action>
  <verify>
    <automated>npm test 2>&amp;1 | tail -10 &amp;&amp; npm run build 2>&amp;1 | tail -10 &amp;&amp; grep -rE "['\"]# Records['\"]" _site/ --include="*.js" | head -3</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0
    - `npm run build` exits 0
    - `grep -rE "['\"]# Records['\"]" _site/ --include="*.js"` returns at least one match (proves the Y-axis title string survived Vite bundling)
    - No new files appear under `src/` (this task is read-only)
    - No errors or warnings about missing Chart.js plugins (would indicate accidental `Title` import)
  </acceptance_criteria>
  <done>
    Full test suite and production build both succeed; the bundled JS contains the new axis title strings. Phase 22 is functionally complete and ready for human verification of the rendered chart on a species fact sheet page.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verifies rendered phenology chart on a species page</name>
  <files>(none - human visual verification)</files>
  <read_first>
    - .planning/phases/22-phenology-chart-improvements/22-CONTEXT.md (success criteria CHART-01 and CHART-02)
  </read_first>
  <what-built>
    Phenology chart on every species fact sheet now displays:
    - "Month" label below the X-axis
    - "# Records" label rotated along the Y-axis
    - Y-axis starting at 0 with max scaled to the species' tallest monthly bar
    No layout changes, no styling changes, no new dependencies.
  </what-built>
  <action>
    Pause and wait for human verification of the rendered chart. The human follows the steps under `<how-to-verify>` below and responds with "approved" or a description of any issues.
  </action>
  <how-to-verify>
    1. Run `npm run dev` (or open the latest `_site/` build via a static server) and visit any species fact sheet page that has occurrence records, e.g. `/species/{genus}-{species}/`.
    2. Confirm the phenology chart is visible and shows 12 bars labeled Jan-Dec along the X-axis.
    3. Confirm the text "Month" is rendered below the X-axis tick labels.
    4. Confirm the text "# Records" is rendered rotated 90 degrees along the left edge of the chart (Y-axis title).
    5. Confirm the bottom of the Y-axis is exactly 0 (the lowest gridline aligns with the X-axis).
    6. Confirm the top of the Y-axis approximately matches the tallest visible bar (no large empty headroom above the bars, no truncated bars).
    7. Apply a state filter or year filter that narrows records, and confirm the chart re-renders with new bar heights while keeping the axis titles and Y=0 floor intact.
    8. Visit a second species page with a different record distribution (ideally one where records are concentrated in one or two months) and confirm the Y-axis still starts at 0 and auto-scales to that species' peak.

    Expected outcome: All eight checks pass. If the Y-axis title appears upside-down, mis-rotated, or cut off on small screens, note it - that is acceptable per D-01 (Chart.js defaults) but worth recording for a future polish phase.
  </how-to-verify>
  <verify>
    <human-check>Human reports "approved" via resume-signal after completing the eight steps in `<how-to-verify>` on at least two distinct species fact sheet pages.</human-check>
  </verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues observed.</resume-signal>
  <acceptance_criteria>
    - Human reports both axis titles render on at least two distinct species pages
    - Human reports Y-axis baseline is 0 on at least two species pages (one multi-month, one single-month if available)
    - Human reports filter changes do not remove the axis titles or shift the Y baseline off 0
  </acceptance_criteria>
  <done>
    Human has signaled "approved", confirming the chart renders correctly in a real browser on real species data. CHART-01 and CHART-02 are observationally satisfied.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time -> browser | Chart.js options are bundled into static JS; no user input crosses this boundary in this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Tampering | Chart.js options object | accept | Static string literals (`'Month'`, `'# Records'`) compiled into the bundle; no runtime injection path |
| T-22-02 | Information Disclosure | rendered chart | accept | Axis labels are public/static text; no PII or non-public data exposed |
| T-22-03 | Denial of Service | Chart.js rendering | accept | No new computation added; axis titles are O(1) render cost |
| T-22-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this phase - chart.js v4.5.1 already in dependencies; legitimacy audit is N/A |
</threat_model>

<verification>
- `npm test` exits 0
- `npm run build` exits 0
- `grep -q "scales:" src/components/pnwm-phenology-chart.js`
- `grep -q "text: 'Month'" src/components/pnwm-phenology-chart.js`
- `grep -q "text: '# Records'" src/components/pnwm-phenology-chart.js`
- `grep -q "beginAtZero: true" src/components/pnwm-phenology-chart.js`
- `! grep -E "^import .*\bTitle\b" src/components/pnwm-phenology-chart.js` (no Title plugin import)
- `grep -rE "['\"]# Records['\"]" _site/ --include="*.js"` returns >=1 match
- Human checkpoint (Task 3) confirms rendered output on actual species pages
</verification>

<success_criteria>
- CHART-01 satisfied: X-axis labeled "Month", Y-axis labeled "# Records" on every species fact sheet
- CHART-02 satisfied: Y-axis begins at 0 and scales to the species' highest monthly count
- No regressions in `npm test`
- No new imports, no new dependencies, no styling changes beyond Chart.js defaults
- Filter-driven `_chart.update()` calls preserve axis titles and Y=0 floor
</success_criteria>

<output>
Create `.planning/phases/22-phenology-chart-improvements/22-01-SUMMARY.md` when done.
</output>
</content>
</invoke>
