---
phase: 36-eleventy-data-files-config-migration
plan: 02
type: execute
wave: 2
depends_on: ["36-01"]
files_modified:
  - src/_data/species.ts
  - src/_data/species.js
  - src/_data/glossary.ts
  - src/_data/glossary.js
  - src/_data/taxon.ts
  - src/_data/taxon.js
  - src/_data/taxon.d.ts
autonomous: true
requirements: [MIG-03]

must_haves:
  truths:
    - "species.ts, glossary.ts, taxon.ts exist; their .js predecessors are deleted"
    - "Each DuckDB-boundary data file narrows getRowObjectsJS() output via a local interface + type guard (D-03), never an unguarded as unknown as T"
    - "taxon.d.ts is deleted so TypeScript resolves taxon types from taxon.ts directly (D-04)"
    - "All three files load via the addDataExtension(\"ts\") mechanism from Plan 01 and produce the same template data as their .js versions"
    - "npm run build remains byte-identical to baseline after these three conversions"
  artifacts:
    - path: "src/_data/species.ts"
      provides: "Species global data loader: DuckDB read_csv → reshape (id→string, similar_slugs, slug) returning SpeciesRow[]"
      contains: "getRowObjectsJS"
    - path: "src/_data/glossary.ts"
      provides: "Glossary global data loader: DuckDB query → grouped Record<string, GlossaryEntry[]> by first letter"
      contains: "getRowObjectsJS"
    - path: "src/_data/taxon.ts"
      provides: "Taxon tree loader: two DuckDB queries → four-level TaxonFamily[] tree"
      contains: "getRowObjectsJS"
  key_links:
    - from: "src/_data/species.ts"
      to: "@duckdb/node-api"
      via: "DuckDBInstance read_csv on data/species.csv"
      pattern: "DuckDBInstance"
    - from: "src/_data/taxon.ts"
      to: "src/types/index.ts"
      via: "TaxonFamily / NavImage derived types (D-03 clean-fit path)"
      pattern: "TaxonFamily"
---

<objective>
Convert the three DuckDB-boundary `src/_data` files — `species.js`, `glossary.js`, `taxon.js` — to TypeScript with the locked Phase 34/35 D-03 minimal-interface-plus-guard idiom, and delete the redundant `taxon.d.ts` stopgap (D-04). These are the files that call `getRowObjectsJS()` (typed `unknown`) and reshape the rows before returning, so they need the boundary guard. Plan 01 already registered `addDataExtension("ts")`, so these `.ts` files become discoverable the moment they are renamed.

Purpose: MIG-03 (SC-1) — no `.js` source remains in `src/_data/`; zero `tsc --noEmit` errors; no `@ts-ignore`/`allowJs`/double-casts; `_site/` byte-identical (SC-4).

Output: `src/_data/species.ts`, `src/_data/glossary.ts`, `src/_data/taxon.ts`; `taxon.d.ts` and the three `.js` predecessors deleted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-eleventy-data-files-config-migration/36-CONTEXT.md
@.planning/phases/36-eleventy-data-files-config-migration/36-RESEARCH.md
@.planning/phases/36-eleventy-data-files-config-migration/36-PATTERNS.md
@src/types/schemas.ts
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 02 portion)

New symbols (exclude from drift verification):

- **`src/_data/species.ts`** — `export default async function (): Promise<SpeciesRow[]>`. New local `interface SpeciesDbRow` (post-query DuckDB shape with `id: number`), guard `isSpeciesDbRow(obj): obj is SpeciesDbRow`, exported emitted interface `SpeciesRow` (id widened to `string`).
- **`src/_data/glossary.ts`** — `export default async function (): Promise<Record<string, GlossaryEntry[]>>`. New local `interface GlossaryEntry` (extends `GlossaryWord` with `letter`, `slug`) and guard `isGlossaryEntry`.
- **`src/_data/taxon.ts`** — `export default async function (): Promise<TaxonFamily[]>`. New local `interface TaxonSpeciesDbRow`, `interface NavImageDbRow` (narrow query projections), reusing `TaxonFamily`/`TaxonGenus`/`TaxonSubfamily`/`TaxonSpecies`/`NavImage` derived types from `src/types/`.
- **Deleted:** `src/_data/taxon.d.ts` (Phase-35 stopgap `declare function taxon(): Promise<unknown[]>`).
</artifacts_produced>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Convert species.js → species.ts (D-03 interface + guard)</name>
  <files>src/_data/species.ts, src/_data/species.js (delete)</files>
  <read_first>
    - src/_data/species.js (the file being converted — full read; note the DuckDB read_csv column map, the SELECT projection with derived similar_slugs + slug, the post-query `row.id = String(row.id)` mutation)
    - .planning/phases/36-eleventy-data-files-config-migration/36-PATTERNS.md §"src/_data/species.ts" (the full guard pattern + SpeciesDbRow/SpeciesRow interfaces + noUncheckedIndexedAccess for...of note)
    - scripts/lib/manifest.ts lines 84–88 (the canonical Phase 34 guard idiom: `obj is T` with typeof checks over Record<string, unknown>)
    - scripts/build-data.ts line ~239 (the acceptable inline single-cast alternative for narrow projections)
    - src/types/schemas.ts §"Species" (the SpeciesSchema fields, if deriving the emitted interface via Omit/extension — optional D-03 path)
  </read_first>
  <action>
    Create `src/_data/species.ts` from `species.js`. Keep the DuckDB setup, the `read_csv` column map, and the `SELECT` projection (id, genus, species, common_name, noc_id, authority, family, subfamily, derived `similar_slugs`, derived `slug`) byte-for-byte unchanged — this is a type-only conversion.

    Apply the D-03 idiom (PATTERNS §species.ts is the literal template):
    - Define a local `interface SpeciesDbRow` covering the post-`getRowObjectsJS()` shape (id: number; genus/species: string; common_name/noc_id/authority/family/subfamily: string | null; similar_slugs: string[]; slug: string).
    - Define a guard `function isSpeciesDbRow(obj: unknown): obj is SpeciesDbRow` checking `typeof obj === 'object' && obj !== null`, then the load-bearing fields (id is number, genus/species/slug are string, similar_slugs is an array). Filter the rows through it.
    - Define and export the emitted `interface SpeciesRow extends Omit<SpeciesDbRow, 'id'> { id: string }` (id is stringified post-query because Eleventy template context distinguishes string vs number — the existing `.js` does `row.id = String(row.id)`).
    - Annotate the default export as `Promise<SpeciesRow[]>`.

    Under `noUncheckedIndexedAccess` use `for...of` (never `rows[i]`). The id-string mutation must produce the same emitted shape as the `.js` version (id as string). A single `as` cast is permitted to bridge the mutation; `as unknown as T` is forbidden — prefer building the typed array explicitly if a single cast is not clean.

    Then DELETE `src/_data/species.js`. Do NOT introduce Zod runtime validation here (D-11 reservation; build-side consumes derived types only). No `@ts-ignore`, no `allowJs`.
  </action>
  <verify>
    <automated>test -f src/_data/species.ts && test ! -f src/_data/species.js && grep -q 'isSpeciesDbRow' src/_data/species.ts && npm run typecheck && node --test eleventy.config.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/_data/species.ts` exists; `src/_data/species.js` does NOT.
    - `grep -q 'getRowObjectsJS' src/_data/species.ts` and `grep -q 'isSpeciesDbRow' src/_data/species.ts` succeed (guard present).
    - `grep -RnE '@ts-ignore|as unknown as|allowJs' src/_data/species.ts` returns empty.
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>`species.ts` reshapes DuckDB output through a guarded local interface, emits `SpeciesRow[]` (id as string), and typechecks clean with no `.js` remaining.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Convert glossary.js → glossary.ts (D-03 interface + guard, grouped return)</name>
  <files>src/_data/glossary.ts, src/_data/glossary.js (delete)</files>
  <read_first>
    - src/_data/glossary.js (the file being converted — full read; note it closes BOTH `conn.closeSync()` and `db.closeSync()`, and groups rows by `letter` into `{ A: [...], F: [...] }`)
    - .planning/phases/36-eleventy-data-files-config-migration/36-PATTERNS.md §"src/_data/glossary.ts" (GlossaryEntry extends GlossaryWord + isGlossaryEntry guard + grouped Record return; the "retain whichever close pattern is in the source — do not normalize" note)
    - scripts/lib/manifest.ts lines 84–88 (guard idiom)
    - src/types/schemas.ts §"GlossaryWord" (term, definition, image_filename: string|null, photographer: string|null)
  </read_first>
  <action>
    Create `src/_data/glossary.ts` from `glossary.js`. Keep the DuckDB read_csv column map and the `SELECT` projection (term, definition, image_filename, photographer, derived `letter` = `upper(left(term,1))`, derived `slug`) byte-for-byte unchanged. RETAIN the existing `conn.closeSync(); db.closeSync();` ordering exactly — do NOT normalize to match `species.ts` (PATTERNS note: close pattern stays as-in-source to preserve byte-identical behavior).

    Apply D-03 (PATTERNS §glossary.ts is the literal template):
    - Define `interface GlossaryEntry extends GlossaryWord { letter: string; slug: string }` (import `GlossaryWord` as a type from `../types/index.ts`). `GlossaryWord` already has `term`, `definition`, `image_filename` (string|null), `photographer` (string|null); the query adds `letter` and `slug`.
    - Define a guard `isGlossaryEntry(obj): obj is GlossaryEntry` checking term/definition/letter/slug are strings. Filter rows through it.
    - Build the grouped accumulator `const grouped: Record<string, GlossaryEntry[]> = {}` with a `for...of` loop, pushing into `grouped[row.letter]` (initialize the array if absent). Under `noUncheckedIndexedAccess`, guard the array initialization (`if (!grouped[row.letter]) grouped[row.letter] = []`) — same shape as the `.js`.
    - Annotate the default export as `Promise<Record<string, GlossaryEntry[]>>`.

    Use `import type { GlossaryWord }` (verbatimModuleSyntax). Then DELETE `src/_data/glossary.js`. No Zod runtime, no `@ts-ignore`, no double-casts.
  </action>
  <verify>
    <automated>test -f src/_data/glossary.ts && test ! -f src/_data/glossary.js && grep -q 'isGlossaryEntry' src/_data/glossary.ts && grep -q 'db.closeSync' src/_data/glossary.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/_data/glossary.ts` exists; `src/_data/glossary.js` does NOT.
    - `grep -q 'isGlossaryEntry' src/_data/glossary.ts` succeeds (guard present).
    - `grep -q 'db.closeSync' src/_data/glossary.ts` succeeds (both close calls retained, not normalized).
    - `grep -RnE '@ts-ignore|as unknown as|allowJs' src/_data/glossary.ts` returns empty.
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>`glossary.ts` returns the same grouped-by-letter map through a guarded `GlossaryEntry` interface, retains the source close ordering, and typechecks clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Convert taxon.js → taxon.ts (schema-derived types, two queries); delete taxon.d.ts (D-04); byte-identical build gate</name>
  <files>src/_data/taxon.ts, src/_data/taxon.js (delete), src/_data/taxon.d.ts (delete)</files>
  <read_first>
    - src/_data/taxon.js (the file being converted — full read; note the TWO DuckDB queries (species + images), the `pickNavImages` helper, the four-level tree build with `delete subfam.genusMap` / `delete fam.subfamilyMap`)
    - src/_data/taxon.d.ts (the stopgap to DELETE — `declare function taxon(): Promise<unknown[]>`)
    - .planning/phases/36-eleventy-data-files-config-migration/36-PATTERNS.md §"src/_data/taxon.ts" (TaxonSpeciesDbRow / NavImageDbRow narrow-projection interfaces + schema-derived TaxonFamily return + the delete-d.ts action)
    - src/types/schemas.ts §"TaxonNode" (NavImage, TaxonSpecies, TaxonGenus, TaxonSubfamily, TaxonFamily derived types — the clean-fit return shapes)
    - scripts/build-data.ts line ~239 (acceptable single-cast for narrow SELECT projections)
    - .planning/phases/36-eleventy-data-files-config-migration/36-RESEARCH.md §"Byte-Identical Gate" (the diff -r gate command + Vite-hashed-asset caveat)
  </read_first>
  <action>
    Create `src/_data/taxon.ts` from `taxon.js`. Keep BOTH `read_csv` column maps, both `SELECT` projections (species query: family, subfamily, genus, species, common_name, derived `slug`, derived `genus_slug`; images query: species_slug, filename, photographer, `TRY_CAST(weight AS INTEGER) AS weight`, navigational), the `pickNavImages` helper logic, and the tree-build algorithm byte-for-byte unchanged.

    Apply D-03 with the schema-derived-types clean-fit path (PATTERNS §taxon.ts is the literal template):
    - Define narrow local `interface TaxonSpeciesDbRow` (family: string; subfamily: string|null; genus/species: string; common_name: string|null; slug/genus_slug: string) and `interface NavImageDbRow` (species_slug/filename: string; photographer: string|null; weight: number|null; navigational: string). Cast each `getRowObjectsJS()` result with a single `as TaxonSpeciesDbRow[]` / `as NavImageDbRow[]` (acceptable per RESEARCH §Pattern 1 — narrow explicit projections; NOT `as unknown as`).
    - Import the tree types `TaxonFamily, TaxonGenus, TaxonSubfamily, TaxonSpecies, NavImage` as types from `../types/index.ts` and use them to type the tree-build intermediate structures and the `Promise<TaxonFamily[]>` return. Type the `familyMap`/`subfamilyMap`/`genusMap`/`bySpeciesSlug` accumulators with index-signature record types (`Record<string, ...>`). Helper `pickNavImages` takes `string[]` slugs + the `bySpeciesSlug` record and returns `NavImage[]` (sliced to 4).
    - Where the existing code uses `delete subfam.genusMap` / `delete fam.subfamilyMap`, model the intermediate map objects as types that permit the optional map key (e.g. an intermediate build type with the map field optional) so the deletes typecheck — do NOT cast to `any`. Under `noUncheckedIndexedAccess`, use `for...of` and guard array initialization.

    DELETE `src/_data/taxon.d.ts` in this same task (D-04) — once `taxon.ts` provides real types, the `declare function taxon(): Promise<unknown[]>` stopgap is wrong and would shadow the real types (Pitfall 4). Then DELETE `src/_data/taxon.js`.

    Finally, run the byte-identical build gate to prove all THREE Wave-2 DuckDB conversions (species + glossary + taxon) together produce identical output: `npm run build:data && npm run build:eleventy && diff -r _site/ _site_baseline/`. Expect only Vite content-hashed asset-name diffs (per Phase 34/35); any HTML/Parquet difference is a FAIL.

    No Zod runtime, no `@ts-ignore`, no `allowJs`, no double-casts.
  </action>
  <verify>
    <automated>test -f src/_data/taxon.ts && test ! -f src/_data/taxon.js && test ! -f src/_data/taxon.d.ts && find src/_data -name '*.js' | grep -qv . ; rc=$? ; npm run typecheck && node --test eleventy.config.test.ts && npm run build:data && npm run build:eleventy && find _site -path '*species*index.html' | wc -l | grep -qx 1433 && test $rc -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `src/_data/taxon.ts` exists; `src/_data/taxon.js` and `src/_data/taxon.d.ts` do NOT exist.
    - `find src/_data -name '*.js' | head -1` returns empty (no `.js` source remains in `src/_data/` after this plan — `speciesSlugs.json` stays as JSON).
    - `grep -q 'getRowObjectsJS' src/_data/taxon.ts` succeeds; `grep -RnE '@ts-ignore|as unknown as|allowJs' src/_data/taxon.ts` returns empty.
    - `npm run typecheck` exits 0; `node --test eleventy.config.test.ts` exits 0.
    - `npm run build:eleventy` succeeds; `find _site -path '*species*index.html' | wc -l` equals `1433`.
    - `diff -r _site/ _site_baseline/` reports only Vite content-hashed asset-name differences — zero HTML-prose and zero Parquet differences.
  </acceptance_criteria>
  <done>`taxon.ts` builds the four-level tree using schema-derived types, `taxon.d.ts` is gone, no `.js` source remains in `src/_data/`, and the full build is byte-identical to baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build → DuckDB | `species.ts` / `glossary.ts` / `taxon.ts` read committed CSVs (`data/species.csv`, `data/glossary.csv`, `data/images.csv`) via in-memory DuckDB `read_csv`. No network, no user input. |
| DuckDB → app code | `getRowObjectsJS()` returns `unknown`; narrowed via D-03 type guards before reshape. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-04 | Tampering | DuckDB `read_csv` over committed CSV data | accept | Inputs are committed repository CSVs (not user-supplied). DuckDB typed `read_csv` already coerces/fails on bad values (SCHEMA-06, prior phases). Type guards add defense-in-depth at the unknown boundary; no high-severity surface. |
| T-36-05 | Tampering | `getRowObjectsJS()` unknown→typed narrowing | mitigate | D-03 minimal-interface + runtime type guard (`isSpeciesDbRow`/`isGlossaryEntry`) narrows the `unknown` return rather than an unguarded `as unknown as T` double-cast. Build-side only; no CDN-facing boundary (Zod reserved per D-11). |
| T-36-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed this phase (RESEARCH §Package Legitimacy Audit). Gate N/A. |

Build-side only; nothing ships to the browser bundle. No ASVS L1 category applies (RESEARCH §Security Domain). No high-severity threats.
</threat_model>

<verification>
- `npm run typecheck` — zero errors after all three conversions.
- `node --test eleventy.config.test.ts` + `npm test` — full suite green.
- `find src/_data -name '*.js' | head -1` — empty (no `.js` source remains; `speciesSlugs.json` excepted).
- `test ! -f src/_data/taxon.d.ts` — stopgap deleted.
- `npm run build:eleventy && diff -r _site/ _site_baseline/` — byte-identical (HTML + Parquet).
</verification>

<success_criteria>
- `species.ts`, `glossary.ts`, `taxon.ts` exist; all three `.js` predecessors and `taxon.d.ts` deleted (SC-1, D-04).
- Each DuckDB-boundary file narrows `getRowObjectsJS()` via a guard/local interface — no `as unknown as T` (SC-1, D-03).
- `npm run typecheck` zero errors; no `@ts-ignore`/`allowJs` (SC-1).
- Full build byte-identical to baseline after all three conversions (SC-4).
</success_criteria>

<output>
Create `.planning/phases/36-eleventy-data-files-config-migration/36-02-SUMMARY.md` when done.
</output>
