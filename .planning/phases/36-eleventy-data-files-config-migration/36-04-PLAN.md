---
phase: 36-eleventy-data-files-config-migration
plan: 04
type: execute
wave: 3
depends_on: ["36-01", "36-02", "36-03"]
files_modified: []
autonomous: false
requirements: [MIG-03]

must_haves:
  truths:
    - "Zero .js source files remain in src/_data/ or at the config root"
    - "npm run typecheck exits 0 with zero errors; no @ts-ignore, no allowJs, no unguarded as unknown as in any converted file"
    - "The full test suite passes via node --test, including eleventy.config.test.ts"
    - "npm run build produces _site/ byte-identical to _site_baseline/ (HTML + Parquet)"
    - "npm run dev serves a species page with assets resolving under / (local, no double-prefix); GITHUB_PAGES build resolves under /pnwmoths/ (SC-3)"
  artifacts: []
  key_links:
    - from: "package.json build:eleventy / dev"
      to: "eleventy.config.ts"
      via: "--config flag resolves the .ts config under both env states"
      pattern: "--config=eleventy.config.ts"
---

<objective>
Phase-gate verification for the Eleventy data-files + config migration. After Plans 01–03, all `src/_data/*.js` and `eleventy.config.js` are converted to `.ts`, the config test is `.ts` and asserts the GITHUB_PAGES conditional, and the build is byte-identical. This plan runs the consolidated MIG-03 acceptance gate across all four ROADMAP success criteria and the one manual-only check (SC-3 local-dev pathPrefix) that `node --test` cannot cover.

Purpose: prove MIG-03 / SC-1..SC-4 hold for the whole phase before `/gsd-verify-work`. No new source — this is the validation and human-verify checkpoint.

Output: a green typecheck + full test suite + byte-identical build, and an operator confirmation that `npm run dev` serves assets under `/` (local) with no double-prefix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-eleventy-data-files-config-migration/36-CONTEXT.md
@.planning/phases/36-eleventy-data-files-config-migration/36-RESEARCH.md
@.planning/phases/36-eleventy-data-files-config-migration/36-VALIDATION.md
@CLAUDE.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 04 portion)

None — this plan produces no new source symbols. It is the consolidated verification gate over the artifacts produced by Plans 01–03 (listed in their `<artifacts_produced>` sections).
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Consolidated MIG-03 acceptance gate (SC-1, SC-2, SC-4 automated)</name>
  <files>(verification-only — no source files modified)</files>
  <read_first>
    - .planning/phases/36-eleventy-data-files-config-migration/36-VALIDATION.md §"Sampling Rate" + §"Validation Sign-Off" (the phase-gate command set)
    - .planning/phases/36-eleventy-data-files-config-migration/36-RESEARCH.md §"Byte-Identical Gate" (diff command + Vite-hashed-asset caveat) + §"Phase Requirements → Test Map"
    - .planning/ROADMAP.md §"Phase 36" success criteria (SC-1..SC-4)
    - package.json (the `test`, `typecheck`, `build`, `build:eleventy` scripts)
  </read_first>
  <action>
    Run the full phase gate and confirm every clause of SC-1, SC-2, and SC-4:

    (a) SC-1 — completeness + cleanliness: `find src/_data -name '*.js'` returns empty (only `speciesSlugs.json` remains, which is JSON not `.js` source); `test ! -f eleventy.config.js` and `test ! -f eleventy.config.test.js`; `test ! -f src/_data/taxon.d.ts`. Grep all converted files for prohibited constructs: `grep -RnE '@ts-ignore|as unknown as' eleventy.config.ts eleventy.config.test.ts src/_data/*.ts` returns empty, and `grep -RnE 'allowJs' tsconfig.node.json tsconfig.browser.json tsconfig.json` returns empty. Run `npm run typecheck` (must exit 0). Run `npm test` (full suite, must pass, including `eleventy.config.test.ts`).

    (b) SC-2 — pathPrefix preserved + test-asserted: `grep -q 'process.env.GITHUB_PAGES ? "/pnwmoths/" : "/"' eleventy.config.ts` succeeds; the config test's GITHUB_PAGES assertion is in the passing suite from (a).

    (c) SC-4 — byte-identical: `npm run build` (the full pipeline: build:data → build:eleventy → copy-parquet → copy-images → species-states → pagefind → validate-links → check-weight), then `diff -r _site/ _site_baseline/`. The only acceptable differences are Vite content-hashed asset filenames (non-deterministic across builds, documented Phase 34/35). Confirm `find _site -path '*species*index.html' | wc -l` equals `1433`. Zero HTML-prose differences and zero Parquet differences = PASS; any other difference is a FAIL to investigate.

    Do not modify source in this task. If any gate fails, report the specific failing clause (do not patch over it here — a failure means a Plan 01–03 task regressed and must be fixed at the source).
  </action>
  <verify>
    <automated>find src/_data -name '*.js' | head -1 | grep -qv . ; A=$? ; test ! -f eleventy.config.js && test ! -f src/_data/taxon.d.ts && ! grep -RnE '@ts-ignore|as unknown as' eleventy.config.ts eleventy.config.test.ts src/_data/*.ts && ! grep -RnE 'allowJs' tsconfig.node.json tsconfig.browser.json tsconfig.json && grep -q 'process.env.GITHUB_PAGES ? "/pnwmoths/" : "/"' eleventy.config.ts && npm run typecheck && npm test && npm run build && find _site -path '*species*index.html' | wc -l | grep -qx 1433 && test $A -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `find src/_data -name '*.js' | head -1` returns empty; `eleventy.config.js`, `eleventy.config.test.js`, `src/_data/taxon.d.ts` do NOT exist.
    - `grep -RnE '@ts-ignore|as unknown as' eleventy.config.ts eleventy.config.test.ts src/_data/*.ts` returns empty; `grep -RnE 'allowJs' tsconfig.*.json tsconfig.json` returns empty.
    - `npm run typecheck` exits 0; `npm test` passes the full suite including the GITHUB_PAGES assertion.
    - `grep -q 'process.env.GITHUB_PAGES ? "/pnwmoths/" : "/"' eleventy.config.ts` succeeds.
    - `npm run build` succeeds; `find _site -path '*species*index.html' | wc -l` equals `1433`; `diff -r _site/ _site_baseline/` shows only Vite content-hashed asset-name differences.
  </acceptance_criteria>
  <done>SC-1, SC-2, and SC-4 are proven green by the automated gate; the phase is conversion-complete with no prohibited constructs and a byte-identical build.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify local dev pathPrefix (SC-3 local `/` branch, no double-prefix)</name>
  <files>(verification-only — runs npm run dev; no source files modified)</files>
  <what-built>
    `eleventy.config.ts` is loaded via `--config=eleventy.config.ts` in the `dev` script. The `pathPrefix` is `process.env.GITHUB_PAGES ? "/pnwmoths/" : "/"`, and Vite's `base:` is wired to that `pathPrefix`. The GitHub Pages branch (`/pnwmoths/`) is verified automatically by the byte-identical build gate in Task 1 (the production build exercises GITHUB_PAGES). The local-dev `/` branch runs through a long-lived `--serve` process that `node --test` cannot assert, so it needs a one-time human confirmation that assets are not double-prefixed under local dev.
  </what-built>
  <action>
    Automation is complete (Plans 01–03 + Task 1). This is a human-verify checkpoint for the one behavior `node --test` cannot assert: the local-dev `/` pathPrefix branch served by the long-lived `eleventy --serve` process. Present the verification steps in `<how-to-verify>` to the operator and pause for the resume signal. Do not modify source.
  </action>
  <how-to-verify>
    1. Run `npm run dev` (this runs `build:data` then `eleventy --serve --config=eleventy.config.ts`). Wait for the server to report its local URL (typically `http://localhost:8080/`).
    2. Open a species fact-sheet page in a browser, e.g. `http://localhost:8080/species/abagrotis-apposita/`.
    3. View source / open devtools Network tab and confirm CSS/JS/component asset URLs resolve under `/` (e.g. `/assets/...`, `/components/...`, `/styles/...`) — NOT under `/pnwmoths/...` and NOT double-prefixed (no `//` or `/pnwmoths/pnwmoths/`).
    4. Confirm the page renders (styles applied, no 404s in the Network tab for first-party assets).
    5. Stop the dev server (Ctrl-C).
  </how-to-verify>
  <verify>
    <human-check>Operator confirms in a live `npm run dev` browser session that first-party CSS/JS/component asset URLs on a species page resolve under `/` with no `/pnwmoths/` prefix and no double-prefix (`//` or `/pnwmoths/pnwmoths/`), and the page renders with no first-party 404s.</human-check>
  </verify>
  <resume-signal>Type "approved" if local-dev assets resolve cleanly under `/` with no double-prefix, or describe any 404 / double-prefix / wrong-base issue observed.</resume-signal>
  <done>Operator has confirmed local-dev (`/`) asset resolution with no double-prefix; SC-3's local branch is verified (the `/pnwmoths/` branch is covered by Task 1's build gate).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → dev server | The human-verify step runs a local `--serve` process and loads pages in a browser. Localhost only; no external exposure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-08 | Information Disclosure | local `npm run dev` server | accept | Binds localhost for operator verification only; not deployed. No new surface vs. prior phases. |
| T-36-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed this phase (RESEARCH §Package Legitimacy Audit). Gate N/A. |

Verification-only plan; no source change, nothing ships to the browser bundle. No ASVS L1 category applies (RESEARCH §Security Domain). No high-severity threats.
</threat_model>

<verification>
- SC-1: `find src/_data -name '*.js'` empty; config + test `.ts`; no `@ts-ignore`/`allowJs`/`as unknown as`; `npm run typecheck` 0; `npm test` green.
- SC-2: `grep` GITHUB_PAGES conditional in `eleventy.config.ts`; config-test assertion in the passing suite.
- SC-3: GITHUB_PAGES `/pnwmoths/` branch via the byte-identical build gate; local `/` branch via the human-verify dev checkpoint.
- SC-4: `npm run build && diff -r _site/ _site_baseline/` byte-identical (HTML + Parquet).
</verification>

<success_criteria>
- All four ROADMAP success criteria (SC-1..SC-4) for Phase 36 are proven: full conversion with no `.js` source, GITHUB_PAGES conditional preserved + test-asserted, both build/dev pathPrefix branches correct with no double-prefix, byte-identical `_site/`.
- Full test suite + typecheck green; no prohibited constructs.
- Operator has confirmed local-dev asset resolution.
</success_criteria>

<output>
Create `.planning/phases/36-eleventy-data-files-config-migration/36-04-SUMMARY.md` when done.
</output>
