---
phase: 36-eleventy-data-files-config-migration
plan: 03
type: execute
wave: 2
depends_on: ["36-01"]
files_modified:
  - src/_data/images.ts
  - src/_data/images.js
  - src/_data/plates.ts
  - src/_data/plates.js
  - src/_data/speciesPhotos.ts
  - src/_data/speciesPhotos.js
autonomous: true
requirements: [MIG-03]

must_haves:
  truths:
    - "images.ts, plates.ts, speciesPhotos.ts exist; their .js predecessors are deleted"
    - "Each file-I/O data file uses a local interface or a derived type for its emitted shape (no DuckDB boundary, no guard required)"
    - "speciesPhotos.ts soft-fails to {} when the manifest file is missing (behavior preserved)"
    - "plates.ts preserves both code paths (committed-JSON fallback and filesystem-XML) with no behavior change"
    - "All three load via the addDataExtension(\"ts\") mechanism from Plan 01 and produce the same template data as the .js versions"
  artifacts:
    - path: "src/_data/images.ts"
      provides: "Images-by-species loader: hand-rolled CSV parse → Record<string, ImageRow[]> sorted by weight"
      contains: "parseCSV"
    - path: "src/_data/plates.ts"
      provides: "Plates loader: committed JSON fallback OR filesystem Zoomify-XML scan → PlateEntry[]"
      contains: "PlateEntry"
    - path: "src/_data/speciesPhotos.ts"
      provides: "High-res photo manifest loader: JSON readFile → Record<string, SpeciesPhoto>, {} on missing file"
      contains: "SpeciesPhoto"
  key_links:
    - from: "src/_data/speciesPhotos.ts"
      to: "data/species-photos.json"
      via: "readFile + JSON.parse with existsSync soft-fail"
      pattern: "existsSync"
    - from: "src/_data/speciesPhotos.ts"
      to: "src/types/index.ts"
      via: "SpeciesPhoto derived type"
      pattern: "SpeciesPhoto"
---

<objective>
Convert the three file-I/O `src/_data` files — `images.js`, `plates.js`, `speciesPhotos.js` — to TypeScript. None of these use DuckDB, so per D-03/PATTERNS they need a typed local interface (or a clean derived type) for the emitted shape, NOT a `getRowObjectsJS()` guard. This plan touches a disjoint file set from Plan 02 (the DuckDB files), so the two run in parallel in Wave 2.

Purpose: MIG-03 (SC-1) — complete the `.js` → `.ts` conversion of `src/_data/`; zero `tsc --noEmit` errors; no `@ts-ignore`/`allowJs`/double-casts. (`speciesSlugs.json` stays JSON; not converted.)

Output: `src/_data/images.ts`, `src/_data/plates.ts`, `src/_data/speciesPhotos.ts`; the three `.js` predecessors deleted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-eleventy-data-files-config-migration/36-CONTEXT.md
@.planning/phases/36-eleventy-data-files-config-migration/36-RESEARCH.md
@.planning/phases/36-eleventy-data-files-config-migration/36-PATTERNS.md
@src/types/schemas.ts
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 03 portion)

New symbols (exclude from drift verification):

- **`src/_data/images.ts`** — `export default function (): Record<string, ImageRow[]>`. Typed local helpers `parseCSV(text): Record<string, string>[]`, `toInt(v): number | null`, `toFloat(v): number | null`, and a local `interface ImageRow` describing the EMITTED row (note: `photographer` is emitted as `string | null` via `row.photographer || null`, which diverges from the non-null `SpeciesImage.photographer` schema field — use the emitted local interface, not `SpeciesImage` directly).
- **`src/_data/plates.ts`** — `export default async function (): Promise<PlateEntry[]>`. New local `interface PlateEntry`, typed `DESCRIPTIONS: Record<string, string>`, typed `bySlug: Map<string, PlateEntry & { dirName: string }>`, helpers `parseDirName`, `toSlug`, `readDimensions` annotated.
- **`src/_data/speciesPhotos.ts`** — `export default async function (): Promise<Record<string, SpeciesPhoto>>` using the `SpeciesPhoto` derived type from `src/types/`.
</artifacts_produced>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Convert speciesPhotos.js → speciesPhotos.ts (trivial, derived SpeciesPhoto type) and images.js → images.ts (typed CSV accumulator)</name>
  <files>src/_data/speciesPhotos.ts, src/_data/speciesPhotos.js (delete), src/_data/images.ts, src/_data/images.js (delete)</files>
  <read_first>
    - src/_data/speciesPhotos.js (the file being converted — full read; note the existsSync soft-fail returning {} and the JSON.parse)
    - src/_data/images.js (the file being converted — full read; note the hand-rolled parseCSV, toInt/toFloat, the `photographer: row.photographer || null` emit, and the per-species weight sort)
    - .planning/phases/36-eleventy-data-files-config-migration/36-PATTERNS.md §"src/_data/speciesPhotos.ts" (full converted pattern) + §"src/_data/images.ts" (typed accumulator + noUncheckedIndexedAccess ?? '' note)
    - src/types/schemas.ts §"SpeciesPhoto / Specimen" (SpeciesPhoto = { high_res_available, specimens, photographer, license }) and §"SpeciesImage" (note photographer is z.string() non-null in the schema — the images emit diverges to string|null)
  </read_first>
  <action>
    **speciesPhotos.ts** (trivial conversion — PATTERNS §speciesPhotos.ts is the literal full pattern): copy `speciesPhotos.js`, import `type { SpeciesPhoto } from '../types/index.ts'`, keep the `MANIFEST_PATH` URL constant and the `existsSync` soft-fail (`console.warn` + `return {}`) unchanged, annotate the default export as `Promise<Record<string, SpeciesPhoto>>`, and cast the `JSON.parse(...)` result with a single `as Record<string, SpeciesPhoto>` (safe: `species-photos.json` is generated by `scripts/generate-species-photos.ts` against the same `SpeciesPhoto` shape; no runtime validation at this build-locked boundary per D-11). Delete `speciesPhotos.js`.

    **images.ts**: copy `images.js`. Annotate `parseCSV(text: string): Record<string, string>[]`, `toInt(v: string): number | null`, `toFloat(v: string): number | null`. Define a local `interface ImageRow` for the EMITTED row shape — do NOT reuse `SpeciesImage` directly, because the file emits `photographer: row.photographer || null` (string|null) and numeric `weight`/`latitude`/`longitude`/`elevation_ft`/`year`/`month`/`day` via toInt/toFloat (number|null), whereas the `SpeciesImage` schema types these as non-null/VARCHAR strings. The local `ImageRow` reflects what templates actually consume. Type the accumulator `const bySpecies: Record<string, ImageRow[]> = {}` and the pushed objects as `ImageRow`. Under `noUncheckedIndexedAccess`, `row['field']` returns `string | undefined`; preserve the existing `|| null` / coalesce semantics (`row['species_slug'] ?? ''`, `row['photographer'] || null`) so emitted values match the `.js` exactly. Keep the per-species `.sort((a,b) => (a.weight ?? 0) - (b.weight ?? 0))` unchanged. Annotate the default export as `Record<string, ImageRow[]>`. Delete `images.js`.

    No DuckDB boundary → no `getRowObjectsJS()` guard needed for either file (PATTERNS: file-read path is local logic, not an external API). No Zod runtime, no `@ts-ignore`, no `allowJs`, no `as unknown as`.
  </action>
  <verify>
    <automated>test -f src/_data/speciesPhotos.ts && test ! -f src/_data/speciesPhotos.js && test -f src/_data/images.ts && test ! -f src/_data/images.js && grep -q 'SpeciesPhoto' src/_data/speciesPhotos.ts && grep -q 'parseCSV' src/_data/images.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/_data/speciesPhotos.ts` and `src/_data/images.ts` exist; both `.js` predecessors do NOT.
    - `grep -q 'existsSync' src/_data/speciesPhotos.ts` succeeds (soft-fail preserved); `grep -q 'return {}' src/_data/speciesPhotos.ts` succeeds.
    - `grep -q 'interface ImageRow' src/_data/images.ts` succeeds (emitted-shape interface, not raw SpeciesImage reuse).
    - `grep -RnE '@ts-ignore|as unknown as|allowJs' src/_data/speciesPhotos.ts src/_data/images.ts` returns empty.
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>`speciesPhotos.ts` (derived SpeciesPhoto, {} soft-fail) and `images.ts` (typed CSV accumulator, emitted ImageRow interface) typecheck clean with no `.js` remaining.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Convert plates.js → plates.ts (PlateEntry interface, both code paths); byte-identical build gate</name>
  <files>src/_data/plates.ts, src/_data/plates.js (delete)</files>
  <read_first>
    - src/_data/plates.js (the file being converted — full read; note the TWO code paths: committed-JSON fallback when PLATES_Z_SOURCE missing, and the filesystem Zoomify-XML scan; the DESCRIPTIONS record indexed by plate number; the bySlug Map with dirName; the numeric plate-number sort)
    - .planning/phases/36-eleventy-data-files-config-migration/36-PATTERNS.md §"src/_data/plates.ts" (PlateEntry interface covering both paths + `DESCRIPTIONS: Record<string,string>` annotation + `bySlug: Map<string, PlateEntry & { dirName: string }>`)
    - .planning/phases/36-eleventy-data-files-config-migration/36-RESEARCH.md §"Per-Data-File plates.js → plates.ts" + §"Byte-Identical Gate"
  </read_first>
  <action>
    Create `src/_data/plates.ts` from `plates.js`. Keep both code paths, `parseDirName`, `toSlug`, `readDimensions`, the `MANIFEST_PATH` URL constant, the `DESCRIPTIONS` table, and the numeric sort byte-for-byte unchanged.

    Apply D-03 (no DuckDB boundary → local interface, no guard; PATTERNS §plates.ts is the literal template):
    - Define a local `interface PlateEntry` covering all consumed/returned fields: `number: string; family: string; title: string; description: string | null; slug: string; width: number; height: number`.
    - Annotate `DESCRIPTIONS` as `Record<string, string>` so `DESCRIPTIONS[number]` does not trip `noUncheckedIndexedAccess` (it returns `string | undefined`; the existing `?? null` coalesce stays).
    - Annotate `parseDirName(dirName: string): { number: string; family: string } | null`, `toSlug(number: string, family: string): string`, `readDimensions(dirPath: string): Promise<{ width: number; height: number }>`.
    - Type `const bySlug = new Map<string, PlateEntry & { dirName: string }>()`. In the JSON-fallback path, type `JSON.parse(...)` as `PlateEntry[]` via a single `as` cast (committed file; build-locked).
    - Annotate the default export as `Promise<PlateEntry[]>`.

    Under `noUncheckedIndexedAccess` use `for...of` and guard array/Map access. Then DELETE `src/_data/plates.js`.

    Finally, run the byte-identical build gate proving Plan 03's three file-I/O conversions produce identical output. NOTE on parallelism: Plans 02 and 03 both run in Wave 2 and converge on the same `_site/`. If Plan 02 has already merged, this gate reflects the full conversion; if Plan 03 runs first, `species/glossary/taxon` are still `.js` (still loaded fine via the Plan 01 extension glob) — either way the build must be byte-identical to baseline. Command: `npm run build:data && npm run build:eleventy && diff -r _site/ _site_baseline/`. Expect only Vite content-hashed asset-name diffs (Phase 34/35 behavior); any HTML/Parquet difference is a FAIL.

    No Zod runtime, no `@ts-ignore`, no `allowJs`, no double-casts.
  </action>
  <verify>
    <automated>test -f src/_data/plates.ts && test ! -f src/_data/plates.js && grep -q 'interface PlateEntry' src/_data/plates.ts && npm run typecheck && node --test eleventy.config.test.ts && npm run build:data && npm run build:eleventy && find _site -path '*species*index.html' | wc -l | grep -qx 1433</automated>
  </verify>
  <acceptance_criteria>
    - `src/_data/plates.ts` exists; `src/_data/plates.js` does NOT.
    - `grep -q 'interface PlateEntry' src/_data/plates.ts` succeeds; `grep -q 'Record<string, string>' src/_data/plates.ts` succeeds (DESCRIPTIONS annotated).
    - `grep -RnE '@ts-ignore|as unknown as|allowJs' src/_data/plates.ts` returns empty.
    - `npm run typecheck` exits 0; `node --test eleventy.config.test.ts` exits 0.
    - `npm run build:eleventy` succeeds; `find _site -path '*species*index.html' | wc -l` equals `1433`.
    - `diff -r _site/ _site_baseline/` reports only Vite content-hashed asset-name differences — zero HTML-prose and zero Parquet differences.
  </acceptance_criteria>
  <done>`plates.ts` preserves both code paths with a typed `PlateEntry` shape, typechecks clean, no `.js` remaining, and the build is byte-identical to baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build → filesystem | `images.ts` reads `data/images.csv`; `plates.ts` reads `data/plates.json` or a local Zoomify directory (`PLATES_Z_SOURCE`); `speciesPhotos.ts` reads `data/species-photos.json`. All committed/build-locked repo files. No network, no user input. |
| build-locked JSON → app code | `JSON.parse` of committed/build-generated JSON, narrowed via a single `as` to a derived type (no runtime validation — build-locked per D-11). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-06 | Tampering | `JSON.parse` of `species-photos.json` / `plates.json` cast to a typed shape | accept | Files are build-generated (`generate-species-photos.ts`, Phase 35) or committed repo data — build-locked, not CDN-fetched. Trust-by-immutability (REQUIREMENTS validation principle): build-locked data is covered by static types, not runtime validation. No high-severity surface. |
| T-36-07 | Information Disclosure | `plates.ts` reading `PLATES_Z_SOURCE` filesystem path | accept | Default path is an operator-local legacy directory; env-overridable. Read-only file access at build time; no secrets, no network. Behavior unchanged from prior phases. |
| T-36-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed this phase (RESEARCH §Package Legitimacy Audit). Gate N/A. |

Build-side only; nothing ships to the browser bundle. No ASVS L1 category applies (RESEARCH §Security Domain). No high-severity threats.
</threat_model>

<verification>
- `npm run typecheck` — zero errors after all three conversions.
- `node --test eleventy.config.test.ts` + `npm test` — full suite green.
- `find src/_data -name '*.js'` — empty for these three files (combined with Plan 02, no `.js` source remains except `speciesSlugs.json`).
- `npm run build:eleventy && diff -r _site/ _site_baseline/` — byte-identical (HTML + Parquet).
</verification>

<success_criteria>
- `images.ts`, `plates.ts`, `speciesPhotos.ts` exist; all three `.js` predecessors deleted (SC-1).
- Each file-I/O file uses a typed local interface or derived type for its emitted shape — no DuckDB guard, no `as unknown as T` (SC-1, D-03).
- `speciesPhotos.ts` `{}` soft-fail and `plates.ts` dual-path behavior preserved unchanged.
- `npm run typecheck` zero errors; no `@ts-ignore`/`allowJs` (SC-1).
- Build byte-identical to baseline (SC-4).
</success_criteria>

<output>
Create `.planning/phases/36-eleventy-data-files-config-migration/36-03-SUMMARY.md` when done.
</output>
