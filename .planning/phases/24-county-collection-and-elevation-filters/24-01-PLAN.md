---
phase: 24-county-collection-and-elevation-filters
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/parquet-cache.js
  - src/components/filters.test.js
autonomous: true
requirements: [FILT-01, FILT-02, FILT-03, FILT-04]
tags: [lit, filters, parquet, web-components, tdd]

must_haves:
  truths:
    - "filterRecords({ county: 'King' }, recs) excludes records whose county !== 'King'"
    - "filterRecords({ county: 'all' }, recs) returns all records including null-county records"
    - "filterRecords({ collection: 'UW' }, recs) excludes records whose collection !== 'UW'"
    - "filterRecords({ collection: 'all' }, recs) returns all records including null-collection records"
    - "filterRecords({ elevationMin: 500 }, recs) excludes records with elevation_ft < 500"
    - "filterRecords({ elevationMax: 1000 }, recs) excludes records with elevation_ft > 1000"
    - "filterRecords({ elevationMin: 0, elevationMax: 15000 }, recs) includes records with null elevation_ft"
    - "Combined filter { county: 'King', collection: 'UW', elevationMax: 500 } yields exactly the King+UW record with elevation 100"
  artifacts:
    - path: "src/components/parquet-cache.js"
      provides: "Extended filterRecords() with county/collection/elevationMin/elevationMax conditions"
      contains: "filters.county"
    - path: "src/components/filters.test.js"
      provides: "New test cases covering county, collection, elevation dimensions, null behavior, and combined filters"
      contains: "filterRecords(geoRecords"
  key_links:
    - from: "src/components/parquet-cache.js"
      to: "src/components/filters.test.js"
      via: "import { filterRecords } from './parquet-cache.js'"
      pattern: "filterRecords"
---

<objective>
Extend `filterRecords()` in `parquet-cache.js` with four new filter dimensions — county, collection, elevationMin, elevationMax — following the exact null-guard patterns already established for state/recordType (dropdowns) and yearMin/yearMax (range bounds). Append new test cases to `filters.test.js` that lock in these behaviors before any UI wiring is added.

Purpose: This is the data-layer foundation for FILT-01..04. The filter-bar component (Plan 02) and the existing map and phenology chart components consume `filterRecords()` directly; extending it here makes both visualizations respond to the new filter dimensions automatically once the filter-bar dispatches the new event-detail keys.

Output: A `filterRecords()` function that handles eight filter dimensions, with automated test coverage that proves each new dimension's correctness (including the null-elevation passthrough at default bounds).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-county-collection-and-elevation-filters/24-CONTEXT.md
@.planning/phases/24-county-collection-and-elevation-filters/24-RESEARCH.md
@.planning/phases/24-county-collection-and-elevation-filters/24-PATTERNS.md

<interfaces>
<!-- Current contract that this plan extends. Read parquet-cache.js for full source. -->

From `src/components/parquet-cache.js` (current `filterRecords` signature and body):

- Signature today:
  `filterRecords(records: Array, filters: { state?, recordType?, yearMin?, yearMax? }) => Array`
- Existing guard styles to copy:
  - Dropdown guard: `if (filters.X && filters.X !== 'all' && r.field !== filters.X) return false;`
  - Range guard:    `if (filters.X != null && r.field < filters.X) return false;` (and the symmetric `>` for the max bound)
- The function is a pure array filter — no side effects, no async — and is called by both `pnwm-occurrence-map.js` and `pnwm-phenology-chart.js`.

From `src/components/filters.test.js` (existing test framework):
- Imports: `import { describe, it } from 'node:test'; import assert from 'node:assert/strict';`
- Test runner: `node --test src/components/filters.test.js`
- Existing `describe('filterRecords edge cases', () => { ... })` block — extend or add a sibling `describe`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing test cases for county, collection, and elevation filter dimensions</name>
  <files>src/components/filters.test.js</files>
  <read_first>
    - src/components/filters.test.js (current test structure, fixture style, `describe`/`it` patterns)
    - src/components/parquet-cache.js (current `filterRecords` signature — confirms what symbols to import)
    - .planning/phases/24-county-collection-and-elevation-filters/24-PATTERNS.md (Pattern map — test cases to add are spelled out under `filters.test.js`)
    - .planning/phases/24-county-collection-and-elevation-filters/24-RESEARCH.md (sections "Test Cases to Add in filters.test.js" and "Pitfall 1" for null-elevation behavior)
  </read_first>
  <behavior>
    - Add a sibling `describe('filterRecords — geo and elevation dimensions', () => { ... })` block at the bottom of the file (do NOT modify the existing fixture or existing `it()` cases).
    - Fixture (5 records) inside the new describe:
      - { county: 'King',    collection: 'UW',  elevation_ft: 100 }
      - { county: 'Pierce',  collection: 'PSU', elevation_ft: 500 }
      - { county: 'King',    collection: 'UW',  elevation_ft: 2000 }
      - { county: null,      collection: null,  elevation_ft: null }
      - { county: 'Whatcom', collection: 'UW',  elevation_ft: 5000 }
    - Test cases (each is a separate `it()`):
      1. `filters by county`: `{ county: 'King' }` → result.length === 2; every result has county === 'King'.
      2. `county "all" returns all records`: `{ county: 'all' }` → result.length === 5.
      3. `null county record included when county is "all"`: `{ county: 'all' }` → result has at least one row with county === null.
      4. `filters by collection`: `{ collection: 'UW' }` → result.length === 3.
      5. `collection "all" returns all records`: `{ collection: 'all' }` → result.length === 5.
      6. `filters by elevationMin`: `{ elevationMin: 500 }` → every result row has `elevation_ft === null || elevation_ft >= 500`; null row is EXCLUDED (because null coerces to 0; 0 < 500 is true). Assert null is NOT in result.
      7. `filters by elevationMax`: `{ elevationMax: 1000 }` → null row IS included (null > 1000 → false in JS); rows 100, 500, null pass; rows 2000, 5000 excluded; result.length === 3.
      8. `elevation range excludes out-of-range`: `{ elevationMin: 200, elevationMax: 3000 }` → result.length === 2; result rows are exactly 500 and 2000.
      9. `null elevation_ft passes through at default bounds`: `{ elevationMin: 0, elevationMax: 15000 }` → result has a row with elevation_ft === null.
      10. `combined county + collection + elevation`: `{ county: 'King', collection: 'UW', elevationMin: 0, elevationMax: 500 }` → result.length === 1; result[0].elevation_ft === 100.
    - All assertions use `assert.equal` or `assert.ok` (match the style of existing tests).
  </behavior>
  <action>
    Open `src/components/filters.test.js`. After the existing `describe('filterRecords edge cases', () => { ... });` block (do not edit that block, do not edit its fixture), append a new sibling `describe('filterRecords — geo and elevation dimensions', () => { ... });` block. Inside it declare a local `geoRecords` fixture exactly as specified in the `<behavior>` section, then add the ten `it()` cases described above. Use existing assertion idioms (`assert.equal(result.length, N)`, `assert.ok(result.every(...))`, `assert.ok(result.some(...))`). After saving, run `node --test src/components/filters.test.js` and confirm the ten new tests are present and FAILING (the current `filterRecords()` does not implement the new dimensions, so the assertions on result counts will fail). Do NOT touch `parquet-cache.js` in this task — Task 2 makes them pass.
  </action>
  <verify>
    <automated>node --test src/components/filters.test.js 2>&amp;1 | tee /tmp/24-01-task1-test.log; grep -c 'geo and elevation dimensions' /tmp/24-01-task1-test.log</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/filters.test.js` contains the literal string `describe('filterRecords — geo and elevation dimensions'` (exactly one occurrence)
    - `src/components/filters.test.js` contains the literal string `const geoRecords = [`
    - `node --test src/components/filters.test.js` output includes ten new test names from the list above (e.g., `filters by county`, `filters by collection`, `filters by elevationMin`, `null elevation_ft passes through at default bounds`, `combined county + collection + elevation`)
    - `node --test src/components/filters.test.js` exit code is non-zero (RED — new assertions fail because Task 2 has not extended `filterRecords` yet)
    - Existing `describe('filterRecords edge cases', ...)` block is unchanged: `git diff src/components/filters.test.js` shows only insertions after the existing block, no modifications to existing lines or fixture
  </acceptance_criteria>
  <done>Ten new failing assertions are present in the test output under the new describe block; existing tests still pass; the only modification to filters.test.js is appended lines.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend filterRecords() with county, collection, and elevation conditions</name>
  <files>src/components/parquet-cache.js</files>
  <read_first>
    - src/components/parquet-cache.js (current `filterRecords` body and JSDoc — lines 38–52)
    - src/components/filters.test.js (the geo/elevation describe block added in Task 1 — defines the contract this task must satisfy)
    - .planning/phases/24-county-collection-and-elevation-filters/24-PATTERNS.md (`parquet-cache.js` section — exact lines to append)
    - .planning/phases/24-county-collection-and-elevation-filters/24-CONTEXT.md (D-08 condition semantics)
  </read_first>
  <behavior>
    - After the change, `filterRecords()` accepts an extended `filters` object with optional keys `county`, `collection`, `elevationMin`, `elevationMax` in addition to the existing four.
    - Guard semantics (D-08):
      - county/collection use the dropdown guard: skip the condition when value is falsy or `'all'`; otherwise reject any record whose field !== filter value.
      - elevationMin/elevationMax use the range guard: skip when filter value is `!= null` is false (i.e., null/undefined); otherwise compare `r.elevation_ft` numerically against the bound. Do NOT add a `!= null` guard on `r.elevation_ft` itself; the existing null-coercion behavior is the locked contract.
    - All ten failing tests from Task 1 turn GREEN.
    - The existing four tests for state/recordType/year continue to pass — no regressions.
  </behavior>
  <action>
    Open `src/components/parquet-cache.js`. Update the JSDoc above `filterRecords` (currently lines 38–43) so its summary reads "Filter records by state, record type, year range, county, collection, and elevation range." and its `@param` typedef adds the four new optional keys: `county?: string, collection?: string, elevationMin?: number, elevationMax?: number`. Inside the existing `filterRecords` body, between the last `yearMax` guard and the `return true;` line, append exactly four conditions in this order:
      1. county dropdown guard — truthy + `!== 'all'` + `r.county !== filters.county`
      2. collection dropdown guard — same shape using `r.collection` and `filters.collection`
      3. elevationMin range guard — `filters.elevationMin != null && r.elevation_ft < filters.elevationMin`
      4. elevationMax range guard — `filters.elevationMax != null && r.elevation_ft > filters.elevationMax`
    Use `return false;` as the rejection action, matching surrounding lines. Do not introduce any helper functions, do not refactor the existing four conditions, do not add a `!= null` guard on `r.elevation_ft`. Run `node --test src/components/filters.test.js` after the edit and confirm all tests pass (both the existing four and the ten added in Task 1).
  </action>
  <verify>
    <automated>node --test src/components/filters.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test src/components/filters.test.js` exits 0 (GREEN)
    - `src/components/parquet-cache.js` contains the literal substring `filters.county !== 'all' && r.county !== filters.county`
    - `src/components/parquet-cache.js` contains the literal substring `filters.collection !== 'all' && r.collection !== filters.collection`
    - `src/components/parquet-cache.js` contains the literal substring `filters.elevationMin != null && r.elevation_ft < filters.elevationMin`
    - `src/components/parquet-cache.js` contains the literal substring `filters.elevationMax != null && r.elevation_ft > filters.elevationMax`
    - JSDoc `@param` typedef on `filterRecords` mentions all of: `county`, `collection`, `elevationMin`, `elevationMax`
    - `npm test` exits 0 (full suite green)
    - Diff shows no removed lines from the existing four guards or `return true;` — only inserted lines and JSDoc edits
  </acceptance_criteria>
  <done>All filterRecords tests are green, full `npm test` passes, JSDoc reflects the eight-dimension signature.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → client-side filter | Already-loaded Parquet data + user dropdown/slider input feed `filterRecords()`. No server crosses this boundary; output is consumed only by in-page Lit components (map, chart). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Tampering | `filterRecords` `filters` argument | accept | Filter values originate from the same Lit component's reactive state; no external/untrusted source enters this function. No `eval`, no template interpolation, no DOM write in this code path. |
| T-24-02 | Information Disclosure | `r.county` / `r.collection` strings | accept | Parquet data is public species record data already shipped to every client; no PII or restricted content. |
| T-24-03 | Denial of Service | Array filter over Parquet records | accept | Records arrays per species are bounded (≤ a few thousand); a pure synchronous `.filter` over them is O(n) with constant-cost predicates. No regex compilation, no recursion. |

This plan modifies only pure JS helpers and tests — no DOM writes, no `innerHTML`, no event handlers introduced, no third-party calls. STRIDE risk for this plan is residual/accept.
</threat_model>

<verification>
- `node --test src/components/filters.test.js` — all 7 existing + 10 new tests pass
- `npm test` — full suite green
- `git diff src/components/filters.test.js` shows additions only (no edits to existing describe block)
- `git diff src/components/parquet-cache.js` shows only: JSDoc edits + four appended condition lines inside `filterRecords`
</verification>

<success_criteria>
- All ten new tests under `describe('filterRecords — geo and elevation dimensions', ...)` pass
- All existing tests still pass (`describe('filterRecords edge cases', ...)`)
- `filterRecords()` accepts the eight-key filters object documented in JSDoc
- No regressions in `npm test`
</success_criteria>

<output>
Create `.planning/phases/24-county-collection-and-elevation-filters/24-01-SUMMARY.md` when done.
</output>
