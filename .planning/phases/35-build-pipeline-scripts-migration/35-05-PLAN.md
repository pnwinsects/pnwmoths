---
phase: 35-build-pipeline-scripts-migration
plan: 05
type: execute
wave: 3
depends_on: ["35-01", "35-02", "35-03", "35-04"]
files_modified:
  - _instructions/ADDING_PLATE.md
autonomous: false
requirements: [MIG-02, SCHEMA-04, SCHEMA-05, SCHEMA-06, SCHEMA-07]
must_haves:
  truths:
    - "Zero .js source files remain in scripts/ (only .ts + tile-config.json)"
    - "npm run typecheck exits 0 across both tsconfigs; no @ts-ignore, no allowJs, no unguarded as-unknown-as casts"
    - "Full npm test suite passes via node --test (all converted scripts run under Node 24 type-stripping)"
    - "time npm run build:data completes under 60s and the SCHEMA-04 check runs"
    - "npm run verify:parquet validates the full dataset standalone and exits 0"
    - "Rebuilt _site/ is byte-identical to the pre-migration baseline"
    - "ADDING_PLATE.md operator workflow confirmed correct with the maintainer after upload-plates deletion"
  artifacts: []
  key_links:
    - from: "rebuilt _site/"
      to: "_site_baseline/"
      via: "diff -r"
      pattern: "diff -r"
---

<objective>
The Phase 35 phase gate: prove the full producer-side migration is complete and behavior-preserving. Run the complete verification battery (typecheck, full test suite, build:data under 60s with SCHEMA-04 active, verify:parquet over the full dataset), confirm zero `.js` source / `@ts-ignore` / `allowJs` / unguarded casts remain in `scripts/`, and assert the rebuilt `_site/` is byte-identical to the pre-migration baseline (SC-5). Finally, a human-verify checkpoint resolves RESEARCH Open Question 1 / Assumption A3: confirm with the maintainer that the `_instructions/ADDING_PLATE.md` workflow is still correct now that `upload-plates.js` is deleted (and adjust if photographic-plate uploads are no longer an operator workflow).

Purpose: A single goal-backward gate that ties the five ROADMAP success criteria together. It runs after Waves 1-2 (all conversions + deletes + verify-parquet scaffold) are merged. The byte-identity diff is the milestone's CI-02 guarantee that the conversion changed types, not behavior.

Output: a green verification battery, a byte-identity confirmation, and a maintainer-confirmed (or corrected) ADDING_PLATE.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-build-pipeline-scripts-migration/35-CONTEXT.md
@.planning/phases/35-build-pipeline-scripts-migration/35-RESEARCH.md
@.planning/phases/35-build-pipeline-scripts-migration/35-VALIDATION.md
</context>

<artifacts_this_phase_produces>
This plan produces no new code symbols. It verifies the phase deliverables and may amend `_instructions/ADDING_PLATE.md` per the maintainer's answer.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Full verification battery — typecheck, tests, build budget, verify:parquet, no-.js gate</name>
  <files>(no source changes — verification only)</files>
  <read_first>
    - .planning/phases/35-build-pipeline-scripts-migration/35-VALIDATION.md §Per-Task Verification Map (phase-gate row) and §Sampling Rate (phase-gate command)
    - .planning/ROADMAP.md §Phase 35 success criteria (the 5 criteria this gate proves)
    - .planning/phases/35-build-pipeline-scripts-migration/35-RESEARCH.md §Environment Availability (build:data 3.8s baseline, verify:parquet 0.5s) and §Validation Architecture
    - package.json — final `test`, `build:data`, `verify:parquet`, `typecheck` scripts (post plans 01-04)
  </read_first>
  <action>
    Run the full phase-gate battery and confirm every check passes:

    1. `npm run typecheck` → exits 0 (both tsconfigs; SC-1).
    2. `npm test` → all tests pass via `node --test` (converted `*.test.ts` files run under Node 24 type-stripping; SC-1). Confirm the test count is consistent with the prior baseline minus the deleted migrate-species tests.
    3. `time npm run build:data` → succeeds, prints `Parquet schema OK:` (SCHEMA-04 ran; SC-3), wall time under 60s (SC-5 / CI-03).
    4. `npm run verify:parquet` → exits 0 with a single `OK: N species, M rows validated` line (SC-4 / SCHEMA-07).
    5. No-`.js` gate: `find scripts -maxdepth 1 -name '*.js'` returns nothing (only `.ts` + `tile-config.json` remain; SC-1).
    6. No banned constructs: grep `scripts/*.ts` for `@ts-ignore` (none), `allowJs` in any tsconfig (none), and unguarded `as unknown as` double-casts (none beyond the D-10-guarded patterns; non-comment lines).

    If any check fails, STOP and report which criterion failed with the exact command output — do not mask a failure. (Conversion fixes belong to plans 03/04; this plan only gates.)
  </action>
  <verify>
    <automated>npm run typecheck && npm test && npm run build:data 2>&1 | grep -q 'Parquet schema OK' && npm run verify:parquet | grep -qE '^OK: [0-9]+ species, [0-9]+ rows validated$' && [ -z "$(find scripts -maxdepth 1 -name '*.js')" ] && ! grep -rn '@ts-ignore' scripts/ && ! grep -l 'allowJs' tsconfig.node.json tsconfig.browser.json tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` exits 0.
    - `npm test` passes (all converted `.test.ts` run via node --test).
    - `npm run build:data` prints `Parquet schema OK:` and completes under 60s (capture `time` output in the SUMMARY).
    - `npm run verify:parquet` exits 0 with exactly one `OK:` summary line.
    - `find scripts -maxdepth 1 -name '*.js'` returns nothing.
    - `grep -rn '@ts-ignore' scripts/` returns nothing; no `allowJs` in any tsconfig.
    - No unguarded `as unknown as T` in any `scripts/*.ts` (D-10 boundary guards are the only narrowing).
  </acceptance_criteria>
  <done>All five success-criteria automated checks pass; no .js source, @ts-ignore, allowJs, or unguarded casts remain; build under 60s; verify:parquet green.</done>
</task>

<task type="auto">
  <name>Task 2: Byte-identity gate — rebuild _site/ and diff against the pre-migration baseline</name>
  <files>(no source changes — verification only)</files>
  <read_first>
    - .planning/phases/35-build-pipeline-scripts-migration/35-VALIDATION.md §Manual-Only Verifications (byte-identity row) — diff method
    - .planning/STATE.md "Phase 34 Plan 01: _site_baseline/ gitignored ... working-tree snapshot for SC-4 byte-identity gate"
    - .planning/STATE.md "Phase 34 Plan 03: Vite content-hash filename changes between builds are non-deterministic ... byte-identity gate assesses HTML prose content, not asset filenames"
    - .planning/REQUIREMENTS.md CI-02 (build-side phases produce byte-identical _site/)
  </read_first>
  <action>
    Confirm `_site_baseline/` exists (captured in Phase 34; present in the working tree, gitignored). If it is missing or known-stale relative to the current data, regenerate it from the pre-migration commit before diffing — but Phase 35 is build-side only, so the existing Phase 34 baseline is the correct reference.

    Run a clean full build: `npm run build`. Then `diff -r _site/ _site_baseline/`. Phase 35 touches only build-side producer scripts (no client bundle, no template change), so the expectation is ZERO diff for build-generated data files (Parquet/JSON) and rendered HTML. Per the Phase 34 finding, Vite content-hashed asset filenames may differ non-deterministically between builds; if the ONLY differences are content-hashed asset filenames (and the HTML prose/data is otherwise identical), that is acceptable under CI-02. Any difference in `records.parquet`, `species-states.json`, `species-photos.json`, or HTML prose content is a FAILURE — report it and stop.

    Record the diff result (clean, or asset-hash-only) in the SUMMARY.
  </action>
  <verify>
    <automated>test -d _site_baseline && npm run build && diff -r _site/species _site_baseline/species > /tmp/site-diff.txt 2>&1; echo "exit=$?"; [ ! -s /tmp/site-diff.txt ] && echo "BYTE_IDENTICAL_SPECIES" || cat /tmp/site-diff.txt</automated>
  </verify>
  <acceptance_criteria>
    - `_site_baseline/` exists and was used as the reference.
    - `npm run build` completes successfully end-to-end.
    - `diff -r _site/species _site_baseline/species` is empty (the per-species Parquet + HTML are byte-identical — these contain no content-hashed asset filenames).
    - Any full-tree `diff -r _site/ _site_baseline/` differences are limited to content-hashed Vite asset filenames (documented in the SUMMARY); no data-file or HTML-prose difference.
  </acceptance_criteria>
  <done>Rebuilt _site/ is byte-identical to the baseline for all build-generated data and species HTML; any diff is confined to non-deterministic Vite asset hashes (SC-5 / CI-02).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirm ADDING_PLATE.md operator workflow after upload-plates.js deletion (D-02 / Open Question 1)</name>
  <files>_instructions/ADDING_PLATE.md</files>
  <what-built>
    Phase 35 deleted `scripts/upload-plates.js` (the one-time photographic-plate upload tool, D-01) and updated `_instructions/ADDING_PLATE.md` (plan 01) to replace its Step 4 with a manual `curl PUT` recipe. RESEARCH Open Question 1 / Assumption A3 flags that we cannot determine from the code alone whether adding photographic plates is still a supported operator workflow or was a one-time migration.
  </what-built>
  <how-to-verify>
    1. Open `_instructions/ADDING_PLATE.md` and read the (updated) upload step.
    2. Answer: Is adding new photographic plates still something you (the maintainer) expect to do?
       - If NO (it was a one-time migration): confirm the doc should drop the upload step entirely, or keep the manual `curl PUT` note for reference. The executor will adjust ADDING_PLATE.md accordingly.
       - If YES (still an operator workflow): confirm the manual `curl PUT` recipe (mirroring `_instructions/UPLOADING_TILES.md`'s bunny.net PUT pattern) is sufficient, or specify what the replacement workflow should be. The executor will finalize the doc to match.
    3. Confirm no remaining `_instructions/` doc references the deleted `upload-plates.js` (the executor verified `! grep -rl 'upload-plates' _instructions/`).
  </how-to-verify>
  <action>PAUSE for the maintainer. Present the upload-plates deletion and the updated `_instructions/ADDING_PLATE.md` step; ask whether photographic-plate uploads remain an operator workflow (Open Question 1 / Assumption A3). Based on the answer, finalize ADDING_PLATE.md: drop the upload step if plates were a one-time migration, or keep/refine the manual `curl PUT` recipe if uploads are ongoing. Do not proceed until the maintainer responds via the resume signal.</action>
  <verify><human-check>Maintainer has answered the plates-workflow question and the executor has finalized ADDING_PLATE.md to match; `grep -rl 'upload-plates' _instructions/` returns nothing.</human-check></verify>
  <done>ADDING_PLATE.md reflects the maintainer's confirmed plates workflow; no _instructions doc references the deleted upload-plates.js (D-02 closed).</done>
  <resume-signal>Reply with: "plates one-time, drop step" OR "plates ongoing, curl recipe ok" OR describe the correct workflow — then the executor finalizes ADDING_PLATE.md and the phase completes.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Verification-and-docs plan. No new data flow, no new external boundary. It runs the already-converted scripts and diffs local build output. |

Build/operator-side, verification-only. ASVS V2/V3/V4 (auth/session/access-control) and V6 (crypto) are **N/A**. V5 (Input Validation) is exercised transitively by running `build:data` and `verify:parquet`, both of which carry the input gates established in plans 03/01.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35P5-01 | Tampering | byte-identity diff masking a real behavior change | mitigate | The `diff -r _site/species` gate fails on ANY data-file or HTML-prose change; only non-deterministic Vite asset-hash filenames are tolerated, and those are explicitly documented in the SUMMARY (no blanket "ignore diffs"). |
| T-35P5-02 | Information Disclosure | ADDING_PLATE.md publishing an upload recipe | accept | The recipe uses `BUNNY_API_KEY` from the operator's env (never hard-coded); identical to the already-published UPLOADING_TILES.md pattern. No secret committed. |
| T-35P5-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed this plan. No legitimacy checkpoint required. |
</threat_model>

<verification>
- `npm run typecheck` + `npm test` + `npm run build:data` (under 60s, SCHEMA-04 prints OK) + `npm run verify:parquet` (single OK line) all green.
- No `.js` source / `@ts-ignore` / `allowJs` / unguarded casts in `scripts/`.
- `diff -r _site/species _site_baseline/species` empty; any full-tree diff limited to Vite asset hashes.
- Maintainer has confirmed (or corrected) the ADDING_PLATE.md workflow; no doc references `upload-plates`.
</verification>

<success_criteria>
- SC-1: all scripts converted, tests pass, zero tsc errors, no @ts-ignore/allowJs (MIG-02).
- SC-3: SCHEMA-04 build-time column check active and passing (SCHEMA-04); SCHEMA-05 output typing in place (verified in plan 04).
- SC-4: verify:parquet runs standalone over the full dataset (SCHEMA-07).
- SC-5: build:data under 60s; _site/ byte-identical to baseline (CI-03 / CI-02).
- D-02 docs finalized with maintainer confirmation.
</success_criteria>

<output>
Create `.planning/phases/35-build-pipeline-scripts-migration/35-05-SUMMARY.md` when done.
</output>
