---
phase: 35-build-pipeline-scripts-migration
plan: 04
type: execute
wave: 2
depends_on: ["35-01", "35-02"]
files_modified:
  - scripts/ingest-photos.ts
  - scripts/ingest-photos.test.ts
  - scripts/tile-photos.ts
  - scripts/tile-photos.test.ts
  - scripts/upload-tiles.ts
  - scripts/upload-tiles.test.ts
  - scripts/generate-species-photos.ts
  - scripts/generate-species-photos.test.ts
  - package.json
autonomous: true
requirements: [MIG-02, SCHEMA-05]
must_haves:
  truths:
    - "ingest-photos, tile-photos, upload-tiles, generate-species-photos are all .ts with no .js source remaining"
    - "view and match_bucket use the imported View/MatchBucket unions everywhere they flow (D-09)"
    - "External boundaries (Dropbox API, Bunny CDN, csv-parse) use minimal interface + runtime guard, not Zod, not unguarded casts (D-10)"
    - "generate-species-photos.ts output object is typed Record<string, SpeciesPhoto> at authoring (SCHEMA-05)"
    - "redact() secret-masking helpers preserved and type-annotated"
    - "All four photo-pipeline test files pass via node --test"
    - "Decisions covered by this plan: D-09 (View/MatchBucket unions threaded through all four photo scripts), D-10 (external-boundary minimal interface + lightweight runtime guard, not Zod), D-13 (build-locked JSON output — species-photos.json — typed via static SpeciesPhoto at authoring, SCHEMA-05)"
  artifacts:
    - path: "scripts/generate-species-photos.ts"
      provides: "Manifest → species-photos.json materializer, output typed via SpeciesPhoto (SCHEMA-05)"
      contains: "SpeciesPhoto"
    - path: "scripts/tile-photos.ts"
      provides: "TILEABLE_BUCKETS typed Set<MatchBucket>"
      contains: "Set<MatchBucket>"
  key_links:
    - from: "scripts/{ingest,tile,upload,generate}-photos.ts"
      to: "scripts/lib/parse-photo-filename.ts View/MatchBucket"
      via: "import type"
      pattern: "import type.*MatchBucket"
    - from: "scripts/generate-species-photos.ts result object"
      to: "src/types SpeciesPhoto"
      via: "Record<string, SpeciesPhoto> annotation"
      pattern: "Record<string, SpeciesPhoto>"
---

<objective>
Convert the four photo-pipeline scripts that consume the manifest and the `view`/`match_bucket` types — `ingest-photos.js`, `tile-photos.js`, `upload-tiles.js`, `generate-species-photos.js` (plus their four existing test files) — from `.js` to strict `.ts`. Apply the Phase 34 boundary-guard pattern at every external boundary (D-10), use the imported `View`/`MatchBucket` unions from plan 02 everywhere they flow (D-09), and type `generate-species-photos.ts`'s JSON output as `Record<string, SpeciesPhoto>` to satisfy SCHEMA-05. Update the `photos:*` package.json invocations and the four `test`-glob tokens to `.ts`.

Purpose: This is the photo-pipeline half of the producer-side conversion. Its file set is disjoint from plan 03, so the two run in parallel within Wave 2. It depends on plan 02 (the `View`/`MatchBucket` exports) and plan 01 (package.json baseline: migrate:* removed, test-token cleanup). package.json edits here touch ONLY the `photos:*` keys and the four photo test tokens — no overlap with plan 03's `build:*` keys.

Output: 4 converted scripts, 4 converted tests, SCHEMA-05 typing on generate, union types threaded through, updated package.json.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-build-pipeline-scripts-migration/35-CONTEXT.md
@.planning/phases/35-build-pipeline-scripts-migration/35-RESEARCH.md
@.planning/phases/35-build-pipeline-scripts-migration/35-PATTERNS.md
@.claude/skills/spike-findings-pnwmoths/SKILL.md
</context>

<artifacts_this_phase_produces>
This plan threads existing types through converted code and produces:

- `TILEABLE_BUCKETS: Set<MatchBucket>` — re-typed existing constant in `scripts/tile-photos.ts` (now typed against the imported union from plan 02).
- `result: Record<string, SpeciesPhoto>` — the typed JSON output object in `scripts/generate-species-photos.ts` (SCHEMA-05; `SpeciesPhoto` imported from `src/types`).

No NEW exported symbols are introduced; all four scripts keep their existing public surface (the same exports the tests already import). `View`/`MatchBucket` come from plan 02; `SpeciesPhoto`/`ManifestRow` already exist (Phases 33/34).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Convert ingest-photos.js + ingest-photos.test.js to strict .ts (boundary guards + unions)</name>
  <files>scripts/ingest-photos.ts, scripts/ingest-photos.test.ts, package.json</files>
  <read_first>
    - scripts/ingest-photos.js (full) — source; lines 23-30 imports, 41-44 env-var constants, 44-48 redact(), 218-272 classify() (returns the 7 match_bucket values), the dbxCall usage, the csv-parse of data/species.csv
    - scripts/lib/parse-photo-filename.ts — now exports `View` and `MatchBucket` (plan 02); also `extractBinomial`, `parseSpecimenAndView`, `toSpeciesSlug`, `ParseSpecimenAndViewResult`
    - scripts/lib/dropbox-list.ts:27-52 — the minimal-interface + `is`-guard boundary pattern (D-10 template); `dbxCall` returns `unknown`
    - scripts/lib/manifest.ts:57-66, 97-101 — `ManifestRow`, `ManifestStatus`, readManifest/writeManifest/sortForInvestigation
    - .planning/phases/35-build-pipeline-scripts-migration/35-PATTERNS.md §ingest-photos.ts (255-321), §Pattern A (boundary guard), §Pattern F (unions)
    - .planning/phases/35-build-pipeline-scripts-migration/35-RESEARCH.md §Pattern 3, §Pattern 4, §Pitfall 2/3
  </read_first>
  <action>
    Rename ingest-photos.js → ingest-photos.ts. Update all internal import specifiers to `.ts` (Pitfall 2): `./lib/parse-photo-filename.ts`, `./lib/dropbox-list.ts`, `./lib/manifest.ts`. Add `import type { MatchBucket } from './lib/parse-photo-filename.ts'` (and `View`/`ParseSpecimenAndViewResult` as needed). Type `classify()`'s return so `match_bucket` is `MatchBucket` (D-09) — the 7 returned literals must satisfy the union with no cast. Type env-var constants per Pattern (`const DROPBOX_TOKEN: string = process.env['DROPBOX_TOKEN'] ?? ''`, etc.). Annotate `redact(msg: string): string` and PRESERVE its token-masking behavior (Security: Information Disclosure mitigation).

    External boundaries (D-10): for the csv-parse output of data/species.csv and any Dropbox API JSON narrowed from `dbxCall` (`unknown`), define a minimal consumed-field interface + an `is`-guard following scripts/lib/dropbox-list.ts:27-52 (e.g. `interface SpeciesCsvRow { genus: string; species: string; ... }` + `isSpeciesCsvRow`). NO Zod, NO unguarded `as unknown as T`.

    Convert ingest-photos.test.js → ingest-photos.test.ts: update the import specifier to `./ingest-photos.ts`; add noUncheckedIndexedAccess guards for any array indexing (Pitfall 3).

    In package.json: update `photos:ingest` → `node scripts/ingest-photos.ts`, `photos:investigate` → `RESORT_ONLY=1 node scripts/ingest-photos.ts`, and the `test` token `scripts/ingest-photos.test.js` → `.ts`. No `@ts-ignore`, no `allowJs`.
  </action>
  <verify>
    <automated>npm run typecheck && node --test scripts/ingest-photos.test.ts && ! ls scripts/ingest-photos.js scripts/ingest-photos.test.js 2>/dev/null && grep -q "import type.*MatchBucket" scripts/ingest-photos.ts && grep -q "node scripts/ingest-photos.ts" package.json</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` exits 0; neither ingest-photos.js nor ingest-photos.test.js remains.
    - `node --test scripts/ingest-photos.test.ts` passes.
    - `grep -q 'MatchBucket' scripts/ingest-photos.ts` — classify() return typed with the union (D-09).
    - A boundary guard (`is`-function) exists for csv-parse / Dropbox output; `grep -cE 'as unknown as' scripts/ingest-photos.ts` (filtered to non-comment lines) is 0.
    - `redact(` is present and type-annotated (`grep -q 'function redact' scripts/ingest-photos.ts`).
    - package.json `photos:ingest`/`photos:investigate` reference `.ts`; test token updated.
    - No `@ts-ignore`, no `allowJs`.
  </acceptance_criteria>
  <done>ingest-photos and its test converted to strict .ts; classify() returns MatchBucket; boundary guards applied (D-10); redact preserved; invocations updated; test green.</done>
</task>

<task type="auto">
  <name>Task 2: Convert tile-photos and upload-tiles (+ their tests) to strict .ts</name>
  <files>scripts/tile-photos.ts, scripts/tile-photos.test.ts, scripts/upload-tiles.ts, scripts/upload-tiles.test.ts, package.json</files>
  <read_first>
    - scripts/tile-photos.js (full) — source; the `TILEABLE_BUCKETS` Set, manifest loop, vips invocation, redact/withRetry/logStage helpers, env-var constants
    - scripts/upload-tiles.js (full) — source; the curl/fetch PUT loop, `tileUploadPath` (uses view), Bunny CDN response handling, footprint pre-flight, redact
    - scripts/lib/manifest.ts:60-101 — `ManifestRow`, `ManifestStatus`, `advanceStatus`, readManifest/writeManifest
    - scripts/lib/parse-photo-filename.ts — `MatchBucket`, `View` (from plan 02)
    - scripts/lib/dropbox-download.ts:79-86 — the `!res.ok` fetch error + redact pattern (D-10 boundary for Bunny CDN response)
    - .planning/phases/35-build-pipeline-scripts-migration/35-PATTERNS.md §tile-photos.ts (336-364), §upload-tiles.ts (367-394), §Pattern B (ManifestRow), §Pattern F (unions)
    - .planning/phases/35-build-pipeline-scripts-migration/35-RESEARCH.md §Pattern 4 (TILEABLE_BUCKETS typing), §Pitfall 2/3
  </read_first>
  <action>
    Rename tile-photos.js → tile-photos.ts. Import `import type { ManifestRow, ManifestStatus } from './lib/manifest.ts'` and `import type { MatchBucket } from './lib/parse-photo-filename.ts'`. Type `TILEABLE_BUCKETS` as `Set<MatchBucket>` (currently an untyped Set of 'clean-match'/'slug-match'/'resolved-via-synonym') — the members must satisfy the union (D-09). Annotate redact/withRetry/logStage helpers; preserve secret masking. Type env-var constants per Pattern. Update internal import specifiers to `.ts`.

    Rename upload-tiles.js → upload-tiles.ts. Import `ManifestRow` (and `View` where `row.view` is used in `tileUploadPath`). Apply the D-10 boundary pattern to the Bunny CDN response (`!res.ok` → read text → `redact()` → throw, per dropbox-download.ts:79-86). Type `tileUploadPath(row: ManifestRow): string` using the `View` union for `row.view`. Preserve footprint pre-flight and idempotency-by-status behavior. Update internal import specifiers to `.ts`.

    Convert tile-photos.test.js → tile-photos.test.ts and upload-tiles.test.js → upload-tiles.test.ts: update import specifiers to the `.ts` siblings; add noUncheckedIndexedAccess guards (Pitfall 3).

    In package.json: update `photos:tile` → `node scripts/tile-photos.ts`, `photos:upload` → `node scripts/upload-tiles.ts`, `photos:rethumbnail` → `THUMBNAIL_ONLY=1 node scripts/tile-photos.ts`, and the `test` tokens for tile-photos.test and upload-tiles.test → `.ts`. No `@ts-ignore`, no `allowJs`, no unguarded `as unknown as T`.
  </action>
  <verify>
    <automated>npm run typecheck && node --test scripts/tile-photos.test.ts scripts/upload-tiles.test.ts && ! ls scripts/tile-photos.js scripts/tile-photos.test.js scripts/upload-tiles.js scripts/upload-tiles.test.js 2>/dev/null && grep -q "Set<MatchBucket>" scripts/tile-photos.ts && grep -q "node scripts/tile-photos.ts" package.json && grep -q "node scripts/upload-tiles.ts" package.json</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` exits 0; none of the four .js files remain.
    - `node --test scripts/tile-photos.test.ts scripts/upload-tiles.test.ts` passes.
    - `grep -q 'Set<MatchBucket>' scripts/tile-photos.ts` (TILEABLE_BUCKETS typed via the union, D-09).
    - upload-tiles.ts uses the `View` union for `row.view` and a D-10 boundary check on the Bunny response; `grep -cE 'as unknown as' scripts/upload-tiles.ts scripts/tile-photos.ts` (non-comment) is 0.
    - package.json `photos:tile`/`photos:upload`/`photos:rethumbnail` reference `.ts`; test tokens updated.
    - redact helpers present in both scripts.
    - No `@ts-ignore`, no `allowJs`.
  </acceptance_criteria>
  <done>tile-photos and upload-tiles (and their tests) converted to strict .ts; TILEABLE_BUCKETS typed Set<MatchBucket>; view union threaded; Bunny boundary guarded; redact preserved; invocations updated; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Convert generate-species-photos.js + test to .ts with SCHEMA-05 output typing</name>
  <files>scripts/generate-species-photos.ts, scripts/generate-species-photos.test.ts, package.json</files>
  <behavior>
    - The JSON output object is typed `Record<string, SpeciesPhoto>` at authoring time; if the populated shape ever diverges from `SpeciesPhoto`, `tsc --noEmit` fails (SCHEMA-05 compile-time guarantee — no runtime validation added).
    - `view` on each specimen uses the `View` union; `match_bucket` in `isMaterializable` uses `MatchBucket` (D-09).
    - The materialized species-photos.json content is unchanged vs the .js version (byte-identical output).
  </behavior>
  <read_first>
    - scripts/generate-species-photos.js (full) — source; lines 19-20 imports, 44-48 redact(), the readManifest → filter uploaded → group by slug → sort → write JSON flow; the output object shape
    - scripts/lib/manifest.ts:60-101 — `ManifestRow`, readManifest
    - src/types/index.ts / src/types/schemas.ts — `SpeciesPhoto` (the z.infer type matching the output shape; SCHEMA-05)
    - scripts/lib/parse-photo-filename.ts — `View`, `MatchBucket` (from plan 02)
    - .planning/phases/35-build-pipeline-scripts-migration/35-PATTERNS.md §generate-species-photos.ts (397-433), §Pattern 6 SCHEMA-05
    - .planning/phases/35-build-pipeline-scripts-migration/35-RESEARCH.md §Pattern 6 (lines 384-389), §SCHEMA-05
  </read_first>
  <action>
    Rename generate-species-photos.js → generate-species-photos.ts. Import `import { readManifest } from './lib/manifest.ts'`, `import type { ManifestRow } from './lib/manifest.ts'`, `import type { SpeciesPhoto } from '../src/types/index.ts'`, and `import type { View, MatchBucket } from './lib/parse-photo-filename.ts'` as needed. Type the output object as `const result: Record<string, SpeciesPhoto> = {}` (SCHEMA-05 / D-13 — build-locked JSON like species-photos.json is covered by static TS types at authoring; no runtime validator; species-states.json is validated at load time in Phase 37, not here). Use `View` for specimen `view` fields and `MatchBucket` for the `isMaterializable` match_bucket check (D-09). Annotate `redact(msg: string): string`; preserve masking. Update internal import specifiers to `.ts`.

    IMPORTANT: do not change the output content — the materialized species-photos.json must stay byte-identical (SC-5). This is a type-only conversion.

    Convert generate-species-photos.test.js → generate-species-photos.test.ts: update import specifier to `./generate-species-photos.ts`; add noUncheckedIndexedAccess guards (Pitfall 3).

    In package.json: update `photos:materialize` → `node scripts/generate-species-photos.ts`, and the `test` token → `.ts`. No `@ts-ignore`, no `allowJs`, no unguarded `as unknown as T`.
  </action>
  <verify>
    <automated>npm run typecheck && node --test scripts/generate-species-photos.test.ts && ! ls scripts/generate-species-photos.js scripts/generate-species-photos.test.js 2>/dev/null && grep -q "Record<string, SpeciesPhoto>" scripts/generate-species-photos.ts && grep -q "node scripts/generate-species-photos.ts" package.json</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` exits 0; neither generate-species-photos.js nor its test remains.
    - `node --test scripts/generate-species-photos.test.ts` passes.
    - `grep -q 'Record<string, SpeciesPhoto>' scripts/generate-species-photos.ts` (SCHEMA-05 output typing).
    - `view`/`match_bucket` use the imported unions; `grep -cE 'as unknown as' scripts/generate-species-photos.ts` (non-comment) is 0.
    - package.json `photos:materialize` references `.ts`; test token updated.
    - No `@ts-ignore`, no `allowJs`.
  </acceptance_criteria>
  <done>generate-species-photos and its test converted to strict .ts; output typed Record<string, SpeciesPhoto> (SCHEMA-05); unions threaded; redact preserved; invocation updated; test green; output content unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dropbox HTTP API JSON → ingest-photos.ts (via `dbxCall`) | Untyped external response; narrowed by a minimal-interface `is`-guard (D-10), never Zod, never unguarded cast. |
| bunny.net Storage HTTP response → upload-tiles.ts | External PUT response; `!res.ok` branch reads text and `redact()`s before throwing (D-10). |
| csv-parse output of `data/species.csv` → ingest-photos.ts | Operator-local CSV; consumed-field interface + guard. |
| `DROPBOX_TOKEN` / `BUNNY_API_KEY` env secrets → error/log output | Masked by `redact()` before any message is logged or thrown. |

Build/operator-side CLI tooling. ASVS V2/V3/V4 (auth/session/access-control) **N/A** — no web surface, no sessions; the API tokens authenticate the operator's own outbound calls, not inbound users. V6 (Crypto) N/A. V5 (Input Validation) applies at the external boundaries above.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35P4-01 | Tampering / Information Disclosure | Untyped Dropbox/Bunny API JSON consumed by ingest/upload | mitigate | Minimal consumed-field interface + runtime `is`-guard per D-10 (Phase 34 template); reject unexpected shapes before use. No unguarded `as unknown as T`. |
| T-35P4-02 | Information Disclosure | `DROPBOX_TOKEN` / `BUNNY_API_KEY` leaking into logs or error messages | mitigate | Preserve `redact(msg: string): string` in ingest/tile/upload/generate; it masks the token via regex replace before logging/throwing. Type-annotate, do not remove. |
| T-35P4-03 | Tampering | match_bucket / view values flowing through pipeline | mitigate | String-literal unions (View/MatchBucket) constrain values at compile time; classify() output and TILEABLE_BUCKETS are union-typed (D-09); no invented values. |
| T-35P4-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed (csv-parse/csv-stringify/hyparquet already audited [OK] in Phase 33). No legitimacy checkpoint required. |
</threat_model>

<verification>
- `npm run typecheck` exits 0.
- `node --test scripts/ingest-photos.test.ts scripts/tile-photos.test.ts scripts/upload-tiles.test.ts scripts/generate-species-photos.test.ts` passes.
- No `.js` source for any of the four photo scripts (or their four tests) remains.
- `View`/`MatchBucket` imported and used in all four scripts; `Record<string, SpeciesPhoto>` in generate; `Set<MatchBucket>` in tile.
- redact() preserved in ingest/tile/upload/generate.
- package.json `photos:*` invocations + the four test tokens reference `.ts`.
</verification>

<success_criteria>
- All four photo-pipeline scripts (+ 4 tests) converted to strict .ts; no .js source remains for them (SC-1 / MIG-02).
- `view`/`match_bucket` use the imported string-literal unions everywhere they flow (D-09 / MIG-02 type-safety).
- External boundaries guarded per D-10; no unguarded casts; redact() preserved.
- generate-species-photos.ts output typed `Record<string, SpeciesPhoto>` (SC-3 / SCHEMA-05); materialized JSON byte-identical.
</success_criteria>

<output>
Create `.planning/phases/35-build-pipeline-scripts-migration/35-04-SUMMARY.md` when done.
</output>
