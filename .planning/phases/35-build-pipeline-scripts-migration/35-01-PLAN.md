---
phase: 35-build-pipeline-scripts-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/migrate-species.js
  - scripts/migrate-species.test.js
  - scripts/migrate-images.js
  - scripts/migrate-species-accounts.js
  - scripts/cdn-copy-reclassified.js
  - scripts/cdn-fix-bad-slugs.js
  - scripts/upload-plates.js
  - scripts/add-image-metadata.js
  - scripts/test-redirect.js
  - scripts/verify-parquet.ts
  - package.json
  - _instructions/ADDING_PLATE.md
  - _instructions/UPLOADING_TILES.md
autonomous: true
requirements: [MIG-02, SCHEMA-07]
must_haves:
  truths:
    - "All eight spent one-off scripts are deleted; none remain in scripts/"
    - "migrate-species.test.js is deleted so its cross-import into build-data is gone before any build-data conversion"
    - "migrate:images and migrate:species npm scripts are removed"
    - "npm run verify:parquet runs standalone over data/parquet/, prints one summary line, exits 0"
    - "No _instructions doc references the deleted upload-plates.js"
    - "Decisions covered by this plan: D-01 (delete spent one-offs + remove migrate:* npm scripts), D-02 (update _instructions docs referencing deleted scripts), D-03 (verify-parquet.ts + verify:parquet npm script, hyparquet, all rows), D-04 (scan-all-then-summarize, no fail-fast), D-05 (single quiet summary line, exit non-zero), D-11 (Zod per-row only in verify-parquet — the sanctioned offline check), D-14 (test glob updated for .ts)"
  artifacts:
    - path: "scripts/verify-parquet.ts"
      provides: "Standalone full-dataset per-row Parquet validation against OccurrenceRecordSchema (SCHEMA-07)"
      contains: "OccurrenceRecordSchema"
    - path: "package.json"
      provides: "verify:parquet npm script; migrate:* removed"
      contains: "verify:parquet"
  key_links:
    - from: "package.json verify:parquet"
      to: "scripts/verify-parquet.ts"
      via: "node scripts/verify-parquet.ts"
      pattern: "node scripts/verify-parquet\\.ts"
    - from: "scripts/verify-parquet.ts"
      to: "src/types/schemas.ts OccurrenceRecordSchema"
      via: "import"
      pattern: "OccurrenceRecordSchema"
---

<objective>
Lay the Phase 35 foundation in one shared-file-ownership plan: delete the eight spent one-off scripts (D-01), strip the dangling `migrate:*` npm scripts, create the new standalone `scripts/verify-parquet.ts` plus its `verify:parquet` npm script (SCHEMA-07), and update the two `_instructions/` docs that reference the now-deleted `upload-plates.js` (D-02).

Purpose: This plan owns ALL edits to `package.json` and the deletion of `migrate-species.test.js`. Doing so here unblocks the Wave 2 conversion plans (which would otherwise conflict on `package.json`) and removes the `migrate-species.test.js → build-data.js` cross-import BEFORE build-data is converted (RESEARCH Pitfall 6). `verify-parquet.ts` is a brand-new file with no `.js` source to convert, so it is scaffolded here and exercised immediately against the already-built `data/parquet/`.

Output: 9 deleted scripts, a working `scripts/verify-parquet.ts`, an updated `package.json`, and two updated operator docs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-build-pipeline-scripts-migration/35-CONTEXT.md
@.planning/phases/35-build-pipeline-scripts-migration/35-RESEARCH.md
@.planning/phases/35-build-pipeline-scripts-migration/35-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan introduces the following NEW symbols (downstream executors and Wave 2/3 plans depend on them):

- `scripts/verify-parquet.ts` — NEW file; standalone validator (SCHEMA-07). No exports (it is a CLI entry script with top-level scan loop).
- `verify:parquet` — NEW `package.json` script key: `"verify:parquet": "node scripts/verify-parquet.ts"`.

(The `MatchBucket` union, the SCHEMA-04 `verifySampleParquetSchema` function in build-data.ts, and the `SpeciesPhoto`-typed output are produced by sibling plans 02/03/04 — not this plan.)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Delete the eight spent one-off scripts and remove migrate:* npm scripts</name>
  <files>scripts/migrate-species.js, scripts/migrate-species.test.js, scripts/migrate-images.js, scripts/migrate-species-accounts.js, scripts/cdn-copy-reclassified.js, scripts/cdn-fix-bad-slugs.js, scripts/upload-plates.js, scripts/add-image-metadata.js, scripts/test-redirect.js, package.json</files>
  <read_first>
    - scripts/migrate-species.test.js:1 — confirms `import { validateCsv } from '../scripts/build-data.js'` (the cross-import removed by deleting this file; RESEARCH Pitfall 6)
    - package.json scripts block — current `migrate:images`, `migrate:species` keys; current `test` glob still lists `scripts/migrate-species.test.js`
    - .planning/phases/35-build-pipeline-scripts-migration/35-RESEARCH.md §Recommended Project Structure → "Deleted scripts" list (the exact 8 files)
    - .planning/phases/35-build-pipeline-scripts-migration/35-CONTEXT.md D-01 (delete spent one-offs) and D-14 (test glob)
  </read_first>
  <action>
    Delete these eight script files (D-01): scripts/migrate-species.js, scripts/migrate-species.test.js, scripts/migrate-images.js, scripts/migrate-species-accounts.js, scripts/cdn-copy-reclassified.js, scripts/cdn-fix-bad-slugs.js, scripts/upload-plates.js, scripts/add-image-metadata.js, scripts/test-redirect.js. Use `git rm` so history records the removal.

    In package.json `scripts`: remove the `migrate:images` and `migrate:species` keys entirely. ALSO remove the `scripts/migrate-species.test.js` token from the `test` script string (it is being deleted now; leaving it would make `node --test` fail on a missing file). Do NOT yet rename the other `.test.js` tokens to `.test.ts` and do NOT yet change the `.js`→`.ts` pipeline invocations — those are owned by Task 2 of this plan and the Wave 2 conversion plans respectively. Keep all `photos:*`, `build:*`, `dev`, and `typecheck` keys unchanged in this task.

    Per D-01: deleting a dead `.js` satisfies the milestone "no `.js` source remains" end-state just as conversion would; git history preserves these one-offs.
  </action>
  <verify>
    <automated>! ls scripts/migrate-species.js scripts/migrate-species.test.js scripts/migrate-images.js scripts/migrate-species-accounts.js scripts/cdn-copy-reclassified.js scripts/cdn-fix-bad-slugs.js scripts/upload-plates.js scripts/add-image-metadata.js scripts/test-redirect.js 2>/dev/null && ! grep -qE '"migrate:(images|species)"' package.json && ! grep -q 'migrate-species.test.js' package.json</automated>
  </verify>
  <acceptance_criteria>
    - `ls scripts/migrate-*.js scripts/cdn-*.js scripts/upload-plates.js scripts/add-image-metadata.js scripts/test-redirect.js 2>/dev/null` lists nothing.
    - `grep -E '"migrate:(images|species)"' package.json` returns no match.
    - `grep 'migrate-species.test.js' package.json` returns no match.
    - The `test` script still lists the other converted-script test files (still `.test.js` at this point — Task 2 will not touch them either; Wave 2 conversions rename them).
  </acceptance_criteria>
  <done>Eight one-off scripts deleted; migrate:* npm scripts and the migrate-species.test.js test token removed from package.json; all active pipeline npm scripts untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create scripts/verify-parquet.ts and add the verify:parquet npm script (SCHEMA-07)</name>
  <files>scripts/verify-parquet.ts, package.json</files>
  <behavior>
    - On a clean dataset: reading every `data/parquet/{slug}/records.parquet`, validating each row against OccurrenceRecordSchema, exits 0 and prints exactly one line `OK: N species, M rows validated` (D-05).
    - On any validation failure: scans ALL species first (does NOT fail fast), prints one `FAIL ...` line per failing row, then a final `FAIL: ...` summary line, and exits non-zero (D-04).
    - Small Parquet files (e.g. abagrotis-brunneipennis ~2932 bytes) do NOT throw `parquet file invalid (footer != PAR1)` — the ArrayBuffer pool slice fix is applied (RESEARCH Pitfall 1).
  </behavior>
  <read_first>
    - .planning/phases/35-build-pipeline-scripts-migration/35-PATTERNS.md §`scripts/verify-parquet.ts` (lines 437-501) — the complete verified script pattern, including the `raw.buffer.slice(raw.byteOffset, raw.byteOffset + raw.byteLength)` fix
    - .planning/phases/35-build-pipeline-scripts-migration/35-RESEARCH.md §Pattern 2 (lines 237-315) and §Pitfall 1 (lines 425-438)
    - src/components/parquet-cache.js:24-29 — the hyparquet `parquetReadObjects` + `file` object shape this mirrors (browser uses fetch; Node adapts with readFileSync + pool fix)
    - scripts/profile-data.ts:200-205 — the scan-all + process.exit structure analog
    - src/types/schemas.ts:11 — `OccurrenceRecordSchema` (the per-row schema validated against)
  </read_first>
  <action>
    Create scripts/verify-parquet.ts following the verified pattern in 35-PATTERNS.md §verify-parquet.ts. Import `parquetReadObjects` from `hyparquet`, `readFileSync`/`readdirSync` from `node:fs`, and `OccurrenceRecordSchema` from `'../src/types/schemas.ts'` (explicit `.ts` extension — Pitfall 2). Read from `PARQUET_DIR = 'data/parquet'` (Claude's Discretion in CONTEXT resolved by RESEARCH: `data/parquet/`, NOT `_site/species/` which holds only index.html). Sort the species dir listing for determinism.

    CRITICAL (Pitfall 1): isolate the ArrayBuffer from Node's shared pool — `const ab = raw.buffer.slice(raw.byteOffset, raw.byteOffset + raw.byteLength)`. Never pass `raw.buffer` directly. Build the hyparquet `file` object as `{ byteLength: ab.byteLength, slice: (s, e) => ab.slice(s, e) }`.

    Per D-03 (verify:parquet reads every species' Parquet with hyparquet and validates ALL rows against OccurrenceRecordSchema) and D-11 (this is the ONE sanctioned place full per-row Zod runs — the build side otherwise consumes only z.infer types): validation loop accumulates `totalRows`; for each record call `OccurrenceRecordSchema.safeParse(rec)` and on `!result.success` push `{ slug, row: i, issues }` (join `result.error.issues` as `path: message`) into a `failures` array — do NOT throw or exit mid-scan (D-04 scan-all-then-summarize).

    After the full scan: if `failures.length > 0`, print every `FAIL {slug} row {n}: {issues}` to stderr, then a final `FAIL: {count} validation error(s) across {species} species, {rows} rows`, and `process.exit(1)`. Otherwise print exactly one line to stdout: `OK: {species.length} species, {totalRows} rows validated` (D-05 single quiet summary line).

    Type the hyparquet return as `unknown[]` and `forEach((rec, i) => ...)` — `safeParse` accepts `unknown`, so no cast is needed. Annotate `FailureSummary` as `{ slug: string; row: number; issues: string }`. No `@ts-ignore`, no `allowJs`, no unguarded `as unknown as T`.

    In package.json add the key `"verify:parquet": "node scripts/verify-parquet.ts"` (Wave 0 gap; SCHEMA-07).
  </action>
  <verify>
    <automated>npm run typecheck && npm run verify:parquet | tee /tmp/vp.out && grep -qE '^OK: [0-9]+ species, [0-9]+ rows validated$' /tmp/vp.out && [ $(grep -cE '^(OK|FAIL):' /tmp/vp.out) -eq 1 ]</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` exits 0 (verify-parquet.ts type-checks under tsconfig.node.json).
    - `npm run verify:parquet` exits 0 against the current built `data/parquet/`.
    - stdout on a clean run is exactly one summary line matching `^OK: [0-9]+ species, [0-9]+ rows validated$` (D-05).
    - `grep -q 'verify:parquet' package.json` and the script body invokes `node scripts/verify-parquet.ts`.
    - `grep -q 'raw.buffer.slice' scripts/verify-parquet.ts` (the Pitfall-1 pool fix is present).
    - `grep -q 'OccurrenceRecordSchema' scripts/verify-parquet.ts` (validates against the canonical schema).
  </acceptance_criteria>
  <done>verify-parquet.ts validates the full dataset standalone, prints a single OK summary line, exits 0; verify:parquet npm script wired; ArrayBuffer pool fix applied.</done>
</task>

<task type="auto">
  <name>Task 3: Update _instructions docs that reference the deleted upload-plates.js (D-02)</name>
  <files>_instructions/ADDING_PLATE.md, _instructions/UPLOADING_TILES.md</files>
  <read_first>
    - _instructions/ADDING_PLATE.md (full) — Step 4 "Upload the new tiles to CDN" references `node scripts/upload-plates.js` (lines 43, 50) and `.upload-plates-progress` (lines 47-49)
    - _instructions/UPLOADING_TILES.md:42 — context-only reference to `scripts/upload-plates.js`
    - .planning/phases/35-build-pipeline-scripts-migration/35-RESEARCH.md §Doc Update Map (lines 497-505) and §Assumption A3 (line 604)
    - ~/.claude global CLAUDE.md "keep docs current before pushing"; keep READMEs concise, link to source rather than duplicate
  </read_first>
  <action>
    In _instructions/ADDING_PLATE.md: replace the upload step that invoked the now-deleted `scripts/upload-plates.js` with a manual `curl PUT` recipe, mirroring the bunny.net HTTP PUT pattern already documented in _instructions/UPLOADING_TILES.md (the Phase 30 pattern — `BUNNY_API_KEY` env, PUT each file under the storage path). Remove the `.upload-plates-progress` resume-file references (lines 47-49) since the progress-tracking script no longer exists. Do not remove the step entirely without a replacement — Task 4's checkpoint resolves whether plate uploads are still an operator workflow; provide the manual recipe so the doc stays actionable either way.

    In _instructions/UPLOADING_TILES.md: update or remove the line-42 reference to `scripts/upload-plates.js`; clarify that this doc covers DZI species-tile uploads (the active `scripts/upload-tiles.ts` / `photos:upload` path), not the deleted plates one-off.

    Per D-02 and the global "keep docs current" rule: no `_instructions/` file may reference `upload-plates.js` after this task.
  </action>
  <verify>
    <automated>! grep -rl 'upload-plates' _instructions/ && ! grep -rl 'upload-plates-progress' _instructions/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rl 'upload-plates' _instructions/` returns nothing (no doc references the deleted script).
    - `grep -rl 'upload-plates-progress' _instructions/` returns nothing.
    - ADDING_PLATE.md still describes a workable upload path (manual `curl PUT` recipe present — `grep -qi 'curl' _instructions/ADDING_PLATE.md`).
    - UPLOADING_TILES.md no longer points at the deleted plates script.
  </acceptance_criteria>
  <done>Both operator docs updated; no reference to upload-plates.js or its progress file remains; ADDING_PLATE.md retains an actionable manual upload recipe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSV/Parquet files on local disk → verify-parquet.ts | Operator-controlled local build artifacts (`data/parquet/`); not web-facing, no untrusted network input. |

This is build/operator-side tooling. ASVS V2 (Authentication), V3 (Session), V4 (Access Control), V6 (Cryptography) are **N/A** — no auth, no sessions, no access control, no crypto. V5 (Input Validation) applies only in the benign sense that verify-parquet.ts reads local build output.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35P1-01 | Tampering | verify-parquet.ts reading `data/parquet/` | accept | Inputs are operator-built local Parquet from the same machine; no untrusted source. hyparquet read is isolated via the ArrayBuffer slice (correctness, not security). |
| T-35P1-02 | Information Disclosure | Failure output may print field values from records | accept | Records are public occurrence data already published on the site; no PII or secrets. No redaction needed. |
| T-35P1-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed this plan (hyparquet/zod already audited [OK] in Phase 33). No legitimacy checkpoint required. |
</threat_model>

<verification>
- `npm run typecheck` exits 0 (verify-parquet.ts compiles under tsconfig.node.json).
- `npm run verify:parquet` exits 0 with a single `OK:` summary line.
- No deleted one-off script remains in `scripts/`.
- No `_instructions/` doc references `upload-plates`.
- `git status` shows the 9 deletions, the new verify-parquet.ts, and the package.json + doc edits.
</verification>

<success_criteria>
- All eight spent one-off scripts (and migrate-species.test.js) deleted; migrate:* npm scripts removed (contributes to SC-1: no .js source remains in scripts/).
- `scripts/verify-parquet.ts` + `verify:parquet` npm script exist and run standalone over the full dataset (SC-4 / SCHEMA-07).
- Both upload-plates-referencing docs updated (D-02).
</success_criteria>

<output>
Create `.planning/phases/35-build-pipeline-scripts-migration/35-01-SUMMARY.md` when done.
</output>
