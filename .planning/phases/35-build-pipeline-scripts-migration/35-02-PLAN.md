---
phase: 35-build-pipeline-scripts-migration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/lib/parse-photo-filename.ts
autonomous: true
requirements: [MIG-02]
must_haves:
  truths:
    - "A View string-literal union ('D' | 'V' | '') is exported from the parser source of truth"
    - "A MatchBucket string-literal union covering exactly the 7 classify() values is exported"
    - "No enum is introduced (TS-03); both are string-literal unions"
    - "Existing parse-photo-filename tests still pass after the change"
  artifacts:
    - path: "scripts/lib/parse-photo-filename.ts"
      provides: "Exported View and MatchBucket union types consumed by all photo-pipeline scripts (D-09)"
      contains: "export type MatchBucket"
  key_links:
    - from: "scripts/lib/parse-photo-filename.ts MatchBucket"
      to: "scripts/ingest-photos.ts / tile-photos.ts / generate-species-photos.ts / upload-tiles.ts"
      via: "import type (Wave 2 plan 04)"
      pattern: "export type MatchBucket"
---

<objective>
Lift the `view` and `match_bucket` values to exported string-literal union types in their source of truth, `scripts/lib/parse-photo-filename.ts` (D-09). This is the smallest possible Wave 1 change and the only edit to a `scripts/lib/*.ts` file in this phase. It exists as its own plan because every Wave 2 photo-pipeline conversion (ingest/tile/upload/generate) imports these unions; defining them first prevents the conversion plans from re-deriving (or worse, inventing) the value set.

Purpose: Establish the type contract before consumers are converted — interface-first ordering. `view` is already typed inline (`'D' | 'V' | ''`) on `ParseSpecimenAndViewResult.view`; this plan promotes it to a named exported `View` alias and adds the `MatchBucket` union whose complete value set is derived from `scripts/ingest-photos.js`'s `classify()` (the source of truth named in D-09). No `enum` (TS-03).

Output: two new exported type aliases in an already-converted lib file; no behavior change; existing tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-build-pipeline-scripts-migration/35-CONTEXT.md
@.planning/phases/35-build-pipeline-scripts-migration/35-RESEARCH.md
@.planning/phases/35-build-pipeline-scripts-migration/35-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan introduces the following NEW exported symbols (Wave 2 plan 04 imports them):

- `export type View = 'D' | 'V' | ''` — named alias for the dorsal/ventral/parse-failed view code, exported from `scripts/lib/parse-photo-filename.ts`.
- `export type MatchBucket` — string-literal union of exactly these 7 values (derived from `ingest-photos.js` `classify()`, verified by code read): `'resolved-via-synonym' | 'provisional' | 'unparseable' | 'clean-match' | 'slug-match' | 'genus-only' | 'likely-synonym'`. Exported from `scripts/lib/parse-photo-filename.ts`.

Export site: `scripts/lib/parse-photo-filename.ts`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add exported View and MatchBucket string-literal unions to parse-photo-filename.ts</name>
  <files>scripts/lib/parse-photo-filename.ts</files>
  <read_first>
    - scripts/lib/parse-photo-filename.ts:47-55 — `ParseSpecimenAndViewResult` interface where `view: 'D' | 'V' | ''` is currently an inline literal; lines 152-162 use the same inline type on the function return
    - scripts/ingest-photos.js:218-272 — the `classify()` function; SOURCE OF TRUTH for the complete match_bucket value set (returns exactly: resolved-via-synonym, provisional, unparseable, clean-match, slug-match, genus-only, likely-synonym)
    - .planning/phases/35-build-pipeline-scripts-migration/35-PATTERNS.md §Pattern F (lines 599-620) — the exact union definition to add
    - .planning/phases/35-build-pipeline-scripts-migration/35-RESEARCH.md §Pattern 4 (lines 325-348) — the 7-value union with per-value provenance comments; §Anti-Pattern "Inventing match_bucket values" (line 406) and "Using enum" (line 407)
    - .planning/phases/35-build-pipeline-scripts-migration/35-CONTEXT.md D-09
  </read_first>
  <action>
    In scripts/lib/parse-photo-filename.ts add two exported type aliases at module scope (near the existing interface definitions, before or after `ParseSpecimenAndViewResult`):

    `export type View = 'D' | 'V' | '';` — then refactor `ParseSpecimenAndViewResult.view` (line ~54) and the `parseSpecimenAndView` return cast (line ~162, currently `as 'D' | 'V' | ''`) to reference `View` instead of repeating the inline literal. This is a pure type-alias substitution — runtime behavior unchanged.

    `export type MatchBucket =` with exactly these seven members and nothing else (the complete set returned by `classify()` in ingest-photos.js — do NOT add or remove values per the anti-pattern): `'resolved-via-synonym' | 'provisional' | 'unparseable' | 'clean-match' | 'slug-match' | 'genus-only' | 'likely-synonym'`. Keep the brief per-value provenance comments from RESEARCH Pattern 4 for maintainability.

    Use string-literal unions ONLY — no `enum` (TS-03 prohibition, enforced by isolatedModules/erasable-syntax). Do not change any function logic; this plan only adds/promotes types.
  </action>
  <verify>
    <automated>npm run typecheck && grep -q 'export type MatchBucket' scripts/lib/parse-photo-filename.ts && grep -q "export type View = 'D' | 'V' | ''" scripts/lib/parse-photo-filename.ts && ! grep -qE '\benum\b' scripts/lib/parse-photo-filename.ts && node --test scripts/lib/parse-photo-filename.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` exits 0.
    - `grep -q 'export type MatchBucket' scripts/lib/parse-photo-filename.ts` succeeds.
    - `grep -q "export type View" scripts/lib/parse-photo-filename.ts` succeeds.
    - The MatchBucket union contains exactly 7 quoted literal members and no others (verify by reading; values match classify()).
    - `grep -E '\benum\b' scripts/lib/parse-photo-filename.ts` returns nothing (no enum; TS-03).
    - `node --test scripts/lib/parse-photo-filename.test.ts` passes (no behavior regression).
  </acceptance_criteria>
  <done>View and MatchBucket exported as string-literal unions from the parser source of truth; ParseSpecimenAndViewResult.view references View; existing parser tests green; no enum.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Type-only change to an existing lib file; no new data flow, no I/O, no external boundary. |

Build/operator-side, type-only. ASVS V2/V3/V4/V5/V6 are **all N/A** — this plan adds compile-time type aliases with zero runtime footprint.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35P2-01 | Tampering | parse-photo-filename.ts type aliases | accept | Compile-time-only change; types are erased under Node 24 type-stripping; no runtime behavior, no attack surface. |
| T-35P2-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed this plan. No legitimacy checkpoint required. |
</threat_model>

<verification>
- `npm run typecheck` exits 0.
- Both unions exported; no enum present.
- Existing parse-photo-filename tests pass.
</verification>

<success_criteria>
- `View` and `MatchBucket` exported from `scripts/lib/parse-photo-filename.ts` as string-literal unions (D-09), MatchBucket value set identical to `classify()`.
- No enum (TS-03); no behavior change; tests green. (Contributes to MIG-02 type-safety.)
</success_criteria>

<output>
Create `.planning/phases/35-build-pipeline-scripts-migration/35-02-SUMMARY.md` when done.
</output>
