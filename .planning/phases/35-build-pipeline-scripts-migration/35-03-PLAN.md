---
phase: 35-build-pipeline-scripts-migration
plan: 03
type: execute
wave: 2
depends_on: ["35-01"]
files_modified:
  - scripts/build-data.ts
  - scripts/build-data.test.ts
  - scripts/copy-parquet.ts
  - scripts/copy-images.ts
  - scripts/emit-species-states.ts
  - scripts/check-page-weight.ts
  - scripts/check-page-weight.test.ts
  - package.json
autonomous: true
requirements: [MIG-02, SCHEMA-04, SCHEMA-06]
must_haves:
  truths:
    - "build-data, copy-parquet, copy-images, emit-species-states, check-page-weight are all .ts with no .js source remaining"
    - "build-data.ts reads back one species' Parquet via DuckDB DESCRIBE and fails the build on a column-schema mismatch (SCHEMA-04)"
    - "DuckDB typed read_csv remains the CSV input gate; no per-row Zod added in the hot path (SCHEMA-06)"
    - "build-data.test.ts and check-page-weight.test.ts pass via node --test"
    - "npm run build:data completes (and stays well under 60s)"
    - "Decisions covered by this plan: D-06 (build-time one-sample Parquet column-schema check, fails build on mismatch), D-07 (sample the first deterministic species), D-08 (use DuckDB DESCRIBE on build-data's existing connection), D-11 (build side consumes derived z.infer types only — no validator invocation in hot path), D-12 (DuckDB typed read_csv + integrity SQL is the CSV gate; nullstr handling per 33 D-08)"
  artifacts:
    - path: "scripts/build-data.ts"
      provides: "DuckDB CSV→Parquet pipeline + SCHEMA-04 verifySampleParquetSchema check"
      contains: "verifySampleParquetSchema"
    - path: "scripts/emit-species-states.ts"
      provides: "species-states.json emitter (converted)"
    - path: "scripts/copy-parquet.ts"
      provides: "data/parquet → _site/species copy (converted)"
  key_links:
    - from: "scripts/build-data.ts verifySampleParquetSchema"
      to: "src/types/schemas.ts OccurrenceRecordSchema.shape"
      via: "Object.keys comparison against DESCRIBE columns"
      pattern: "OccurrenceRecordSchema.shape"
    - from: "package.json build:data / build:* / dev"
      to: "scripts/*.ts"
      via: "node scripts/<name>.ts invocations"
      pattern: "node scripts/build-data\\.ts"
---

<objective>
Convert the five data/build scripts that do NOT consume the photo manifest — `build-data.js`, `copy-parquet.js`, `copy-images.js`, `emit-species-states.js`, `check-page-weight.js` (plus the two existing test files) — from `.js` to strict `.ts`, and add the SCHEMA-04 build-time Parquet column-schema sanity check inside `build-data.ts`. Update the `package.json` invocations for THESE scripts (`build:data`, `build:copy-parquet`, `build:copy-images`, `build:species-states`, `build:check-weight`, `dev`, and the `test` glob tokens for the two converted tests) from `.js` to `.ts`.

Purpose: This is the core producer-side conversion. It owns `build-data.ts` (where SCHEMA-04 lives) and the DuckDB CSV gate that already satisfies SCHEMA-06. Its file set is disjoint from the photo-pipeline conversion (plan 04), so the two run in parallel within Wave 2 — except both edit `package.json`. To avoid a write conflict, plan 03 edits ONLY its own `build:*`/`dev`/`test`-token invocations and plan 04 edits ONLY its `photos:*`/`test`-token invocations; the two edit-sets touch different keys. (If the executor detects the file is locked, treat package.json edits as additive per-key — no overlapping keys exist between the two plans.)

Output: 5 converted scripts, 2 converted tests, a working SCHEMA-04 check, updated package.json invocations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-build-pipeline-scripts-migration/35-CONTEXT.md
@.planning/phases/35-build-pipeline-scripts-migration/35-RESEARCH.md
@.planning/phases/35-build-pipeline-scripts-migration/35-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan introduces the following NEW symbol:

- `verifySampleParquetSchema(conn, firstSlug)` — async function in `scripts/build-data.ts` implementing the SCHEMA-04 one-sample column-schema check (DuckDB `DESCRIBE` vs `Object.keys(OccurrenceRecordSchema.shape)`; throws on mismatch to fail the build). Defined and called inside `scripts/build-data.ts`, after the Parquet export loop, before `conn.closeSync()`.

(`copy-parquet.ts`, `copy-images.ts`, `emit-species-states.ts`, `check-page-weight.ts` are renames of existing scripts — no new public symbols.)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Convert build-data.js → build-data.ts, add SCHEMA-04 check, convert build-data.test.ts</name>
  <files>scripts/build-data.ts, scripts/build-data.test.ts, package.json</files>
  <behavior>
    - SCHEMA-04: after Parquet generation, build-data reads ONE species' Parquet (first species in deterministic alphabetical slug order) via DuckDB `DESCRIBE` and compares column names to `Object.keys(OccurrenceRecordSchema.shape)`; if any column is missing or extra, it throws and the build exits non-zero.
    - On the current correct dataset, the check passes and prints `Parquet schema OK: N columns match OccurrenceRecordSchema`.
    - SCHEMA-06: bad CSV coercion still fails at the DuckDB `read_csv` step (existing integrity behavior preserved — no per-row Zod added).
    - validateCsv still rejects empty/missing-column CSVs (existing tests).
  </behavior>
  <read_first>
    - scripts/build-data.js (full) — source to convert; lines 17-49 `validateCsv`, lines 60-66 `validateSlugComponent`, lines 97-137 DuckDB typed `read_csv` columns (records WITHOUT nullstr='', species WITH nullstr=''), the Parquet export loop, the self-invocation guard
    - scripts/build-data.test.js (full) — source test; line 10 `import { validateCsv } from '../scripts/build-data.js'` (update to `'./build-data.ts'`); any `rows[0]` accesses need the noUncheckedIndexedAccess guard
    - scripts/profile-data.ts (full) — the converted DuckDB analog: imports (lines 1-21), `main()` + self-invocation guard (lines 200-205), `getRowObjectsJS() as ...` typing (lines 133-148)
    - src/types/schemas.ts:11 — `OccurrenceRecordSchema`; `.shape` gives the 14 expected column names
    - .planning/phases/35-build-pipeline-scripts-migration/35-PATTERNS.md §build-data.ts (lines 36-176) — imports, main(), read_csv, validateCsv/validateSlugComponent annotations, the COMPLETE verifySampleParquetSchema function + firstSlug derivation, noUncheckedIndexedAccess guard
    - .planning/phases/35-build-pipeline-scripts-migration/35-RESEARCH.md §Pattern 1 (lines 186-235), §Pitfall 2/3/6/7, §SCHEMA-06 (lines 391-399)
  </read_first>
  <action>
    Rename build-data.js → build-data.ts. Add TS types throughout following 35-PATTERNS.md §build-data.ts: import `DuckDBInstance` and `import type { DuckDBConnection }` from `@duckdb/node-api`; import `OccurrenceRecordSchema` from `'../src/types/schemas.ts'` (explicit `.ts`, Pitfall 2). Annotate `validateCsv(filePath: string, requiredColumns: string[]): Record<string, string>[]` and `validateSlugComponent(value: string, fieldName: string): void`. Preserve the DuckDB `read_csv` typed-columns SQL VERBATIM (records.csv without `nullstr=''`, species.csv with it) — this is the SCHEMA-06 / D-12 input gate (CSV correctness via DuckDB typed read_csv + integrity SQL, no per-row Zod in the hot path); do NOT add per-row Zod. PRESERVE the string-interpolated `COPY ... TO parquet` SQL and the `validateSlugComponent` guard exactly (Pitfall 7 — `COPY TO parquet` cannot be parameterized; the slug guard is the correct mitigation; do not "fix" it).

    Add the SCHEMA-04 check (D-06: read back ONE species' Parquet and validate its COLUMN schema, O(columns), not a per-row pass; D-11: build side consumes z.infer types and never invokes the validators here — column comparison only): define `async function verifySampleParquetSchema(conn: DuckDBConnection, firstSlug: string): Promise<void>` exactly as in 35-PATTERNS.md (DuckDB `DESCRIBE SELECT * FROM read_parquet('data/parquet/${firstSlug}/records.parquet')`, narrow `getRowObjectsJS()` to `Array<{ column_name: string }>`, compare to `Object.keys(OccurrenceRecordSchema.shape)`, throw on `missing`/`extra`). Derive `firstSlug` deterministically from the already-fetched `speciesRows` (D-07): sort by `(genus + species).toLowerCase()`, take `[firstSp]` with a `if (!firstSp) throw` guard, build the slug `${genus}-${species}`.toLowerCase().replace(/\s+/g,'-'). Call `await verifySampleParquetSchema(conn, firstSlug)` AFTER the Parquet export loop and BEFORE `conn.closeSync()`. Use DuckDB for this read (D-08) — no new dependency.

    Convert build-data.test.js → build-data.test.ts: update the import specifier to `'./build-data.ts'` (Pitfall 6); add noUncheckedIndexedAccess guards for any `rows[0]`/`speciesRows[0]` accesses (destructure `const [first] = rows; assert.ok(first !== undefined)`).

    In package.json: update `build:data` to `node scripts/build-data.ts`, and the `test` script token `scripts/build-data.test.js` → `scripts/build-data.test.ts`. No `@ts-ignore`, no `allowJs`, no unguarded `as unknown as T`.
  </action>
  <verify>
    <automated>npm run typecheck && node --test scripts/build-data.test.ts && time npm run build:data && grep -q 'verifySampleParquetSchema' scripts/build-data.ts && grep -q 'OccurrenceRecordSchema.shape' scripts/build-data.ts && ! ls scripts/build-data.js 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` exits 0; no `scripts/build-data.js` remains.
    - `node --test scripts/build-data.test.ts` passes.
    - `npm run build:data` succeeds and prints `Parquet schema OK:` (SCHEMA-04 check ran and passed); wall time well under 60s.
    - `grep -q 'verifySampleParquetSchema' scripts/build-data.ts` and `grep -q 'OccurrenceRecordSchema.shape' scripts/build-data.ts` (SCHEMA-04 wired to the canonical schema).
    - DuckDB typed `read_csv` SQL is unchanged (SCHEMA-06 gate intact); no per-row Zod in build-data.ts hot path (`grep -c 'safeParse\|\.parse(' scripts/build-data.ts` filtered for non-comment lines is 0).
    - `grep -q "node scripts/build-data.ts" package.json` (invocation updated).
    - No `@ts-ignore`, no `allowJs` in build-data.ts.
  </acceptance_criteria>
  <done>build-data converted to strict .ts; SCHEMA-04 DESCRIBE check added and passing; SCHEMA-06 DuckDB gate preserved; test converted and green; build:data invocation updated; build under 60s.</done>
</task>

<task type="auto">
  <name>Task 2: Convert copy-parquet, copy-images, emit-species-states to .ts</name>
  <files>scripts/copy-parquet.ts, scripts/copy-images.ts, scripts/emit-species-states.ts, package.json</files>
  <read_first>
    - scripts/copy-parquet.js (full, ~15 lines) — top-level await, no exports, no test (Pitfall 4 — convert as-is, no main() wrapper)
    - scripts/copy-images.js (full) — same top-level-await shape (Pitfall 4)
    - scripts/emit-species-states.js (full) — DuckDB query + writeFileSync; no test (its DuckDB queries are covered inline by build-data.test.ts)
    - scripts/profile-data.ts:13-22, 133-148, 200-205 — DuckDB imports, `getRowObjectsJS() as Record<string, unknown>[]`, main()/self-invocation guard analog
    - .planning/phases/35-build-pipeline-scripts-migration/35-PATTERNS.md §copy-parquet.ts (180-195), §copy-images.ts (199-204), §emit-species-states.ts (207-234), §Pattern G DuckDB typing
    - .planning/phases/35-build-pipeline-scripts-migration/35-RESEARCH.md §Pitfall 4 (copy-* top-level await), §Pitfall 5 (emit-species-states untested)
  </read_first>
  <action>
    Rename copy-parquet.js → copy-parquet.ts and copy-images.js → copy-images.ts. Both are top-level-`await` scripts with no exports and no tests — convert AS-IS (Pitfall 4): keep the top-level await, do NOT add a `main()` wrapper (that would change behavior). Add minimal type annotations only where TS requires them (these use `cp`/`resolve` from node builtins which are already typed). Update any internal import specifiers to `.ts` if present.

    Rename emit-species-states.js → emit-species-states.ts. Add types following profile-data.ts: `DuckDBInstance` import, `main(): Promise<void>` if it already has that structure (otherwise keep its existing top-level shape — match the source), narrow `getRowObjectsJS()` to `Record<string, unknown>[]` (Pattern G). Preserve all SQL and output behavior.

    In package.json: update `build:copy-parquet` → `node scripts/copy-parquet.ts`, `build:copy-images` → `node scripts/copy-images.ts`, `build:species-states` → `node scripts/emit-species-states.ts`. No new tests required (Deferred: new coverage for these is out of scope). No `@ts-ignore`, no `allowJs`.
  </action>
  <verify>
    <automated>npm run typecheck && node scripts/emit-species-states.ts && node scripts/copy-parquet.ts && ! ls scripts/copy-parquet.js scripts/copy-images.js scripts/emit-species-states.js 2>/dev/null && grep -q "node scripts/copy-parquet.ts" package.json && grep -q "node scripts/emit-species-states.ts" package.json</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` exits 0; none of copy-parquet.js / copy-images.js / emit-species-states.js remain.
    - `node scripts/emit-species-states.ts` runs without error and writes its JSON output (requires a prior `build:data` — run it in sequence if needed).
    - `node scripts/copy-parquet.ts` runs without error.
    - package.json `build:copy-parquet`, `build:copy-images`, `build:species-states` all reference `.ts`.
    - copy-parquet.ts / copy-images.ts retain top-level await with no main() wrapper (Pitfall 4).
    - No `@ts-ignore`, no `allowJs`.
  </acceptance_criteria>
  <done>Three file-I/O / DuckDB scripts converted to .ts as-is; package.json build:* invocations updated; typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 3: Convert check-page-weight.js + check-page-weight.test.js to .ts</name>
  <files>scripts/check-page-weight.ts, scripts/check-page-weight.test.ts, package.json</files>
  <read_first>
    - scripts/check-page-weight.js (full) — the script to convert (request-response style; reads _site, exits 0/warn)
    - scripts/check-page-weight.test.js (full) — uses `spawnSync('node', ['scripts/check-page-weight.js', ...])`; update the subprocess path to `.ts` (Pitfall 5)
    - .planning/phases/35-build-pipeline-scripts-migration/35-PATTERNS.md §check-page-weight.ts (237-251) — subprocess invocation update
    - .planning/phases/35-build-pipeline-scripts-migration/35-RESEARCH.md §Pitfall 5 (subprocess path), §Pitfall 3 (noUncheckedIndexedAccess in tests)
  </read_first>
  <action>
    Rename check-page-weight.js → check-page-weight.ts; add type annotations (it does not export to other modules — keep its existing entry shape). Rename check-page-weight.test.js → check-page-weight.test.ts; update the `spawnSync` invocation argument from `'scripts/check-page-weight.js'` to `'scripts/check-page-weight.ts'` (Pitfall 5 — `node` must reference the renamed file under Node 24 type-stripping). Add noUncheckedIndexedAccess guards for any array indexing in the test (Pitfall 3).

    In package.json: update `build:check-weight` → `node scripts/check-page-weight.ts`, and the `test` script token `scripts/check-page-weight.test.js` → `scripts/check-page-weight.test.ts`. No `@ts-ignore`, no `allowJs`.
  </action>
  <verify>
    <automated>npm run typecheck && node --test scripts/check-page-weight.test.ts && ! ls scripts/check-page-weight.js scripts/check-page-weight.test.js 2>/dev/null && grep -q "node scripts/check-page-weight.ts" package.json && grep -q "scripts/check-page-weight.test.ts" package.json</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` exits 0; neither check-page-weight.js nor check-page-weight.test.js remains.
    - `node --test scripts/check-page-weight.test.ts` passes (subprocess invokes the `.ts` file successfully).
    - package.json `build:check-weight` references `.ts`; the `test` glob lists `check-page-weight.test.ts`.
    - No `@ts-ignore`, no `allowJs`.
  </acceptance_criteria>
  <done>check-page-weight script and test converted to .ts; subprocess path updated to .ts; package.json invocations updated; test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `data/*.csv` (operator-controlled local files) → build-data.ts DuckDB read_csv | The CSV input gate. Typed `read_csv` rejects bad coercion; `validateCsv` checks UTF-8 + required columns; `validateSlugComponent` rejects `[^a-zA-Z0-9 -]` before SQL interpolation. |
| Local `data/parquet/` → SCHEMA-04 DESCRIBE read | Build artifact written by the same script moments earlier; operator-local, not untrusted. |

Build/operator-side. ASVS V2/V3/V4 (auth/session/access-control) and V6 (crypto) are **N/A**. V5 (Input Validation) applies and is already implemented (DuckDB typed read_csv + validateCsv + validateSlugComponent) — preserved unchanged in conversion.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35P3-01 | Tampering | build-data.ts `COPY ... TO parquet` string-interpolated SQL (Pitfall 7) | mitigate | `validateSlugComponent()` rejects any genus/species value outside `[a-zA-Z0-9 -]` before it is interpolated into the SQL path; `COPY TO parquet` does not support parameterized file paths, so the regex guard is the correct mitigation. Inputs are operator-controlled local CSVs (low severity). Preserve the guard verbatim; do NOT parameterize. |
| T-35P3-02 | Tampering | DuckDB typed `read_csv` CSV coercion (SCHEMA-06) | mitigate | Typed columns fail the build on bad coercion; integrity SQL + validateCsv pre-flight retained. No per-row Zod added (hot-path budget). |
| T-35P3-03 | Information Disclosure | build error output | accept | Errors print local file paths and column names only; no secrets (no env tokens read by these scripts). |
| T-35P3-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed (DuckDB/zod/csv-parse already audited [OK] in Phase 33). No legitimacy checkpoint required. |
</threat_model>

<verification>
- `npm run typecheck` exits 0.
- `node --test scripts/build-data.test.ts scripts/check-page-weight.test.ts` passes.
- `npm run build:data` prints `Parquet schema OK:` and completes under 60s.
- No `.js` source for any of the five converted scripts (or their two tests) remains.
- package.json `build:*`/`dev`/converted-test tokens reference `.ts`.
</verification>

<success_criteria>
- All five data/build scripts (+ 2 tests) converted to strict .ts; no .js source remains for them (SC-1 / MIG-02).
- SCHEMA-04 one-sample column check added to build-data.ts and fails the build on mismatch (SC-3 / SCHEMA-04).
- SCHEMA-06 DuckDB typed read_csv gate preserved with no hot-path Zod (SC-2 / SCHEMA-06).
- build:data under 60s (SC-5 / CI-03 contribution).
</success_criteria>

<output>
Create `.planning/phases/35-build-pipeline-scripts-migration/35-03-SUMMARY.md` when done.
</output>
