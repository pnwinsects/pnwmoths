---
phase: 43-character-illustration-images
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/types/schemas.ts
  - src/types/schemas.test.ts
  - src/components/pnwm-identify.test.ts
  - package.json
autonomous: true
requirements: [CIMG-02, CIMG-03]
must_haves:
  truths:
    - "CharacterSchema accepts and round-trips an alt_text field (nullable string)"
    - "The CharacterSchema test asserts a character with alt_text set is valid and a missing alt_text is rejected"
    - "npm run key:upload-images is a defined script that invokes scripts/upload-images.ts"
    - "node --test discovers scripts/upload-images.test.ts and scripts/match-character-images.test.ts (registered in the test script's explicit file list)"
    - "The existing pnwm-identify suite stays green after the schema field is added"
  artifacts:
    - path: "src/types/schemas.ts"
      provides: "CharacterSchema with alt_text: z.nullable(z.string())"
      contains: "alt_text"
    - path: "src/types/schemas.test.ts"
      provides: "alt_text coverage on CharacterSchema"
      contains: "alt_text"
    - path: "package.json"
      provides: "key:upload-images script + registered new test files"
      contains: "key:upload-images"
  key_links:
    - from: "package.json test script"
      to: "scripts/upload-images.test.ts"
      via: "explicit file in node --test arg list"
      pattern: "scripts/upload-images.test.ts"
    - from: "src/components/pnwm-identify.test.ts makeChar factory"
      to: "CharacterSchema alt_text field"
      via: "factory default alt_text: null"
      pattern: "alt_text"
---

<objective>
Lay the contract foundation for Phase 43: extend `CharacterSchema` with the `alt_text` field that the curator CSV will carry to the client (D-06, RESEARCH Pitfall 5), and wire the test harness so the new Wave 1 test files actually run. Without this wave, the `build-key.ts` populate step (Plan 03) has no schema home for `alt_text`, and the new uploader/matcher tests (Plan 02) would silently never execute — `node --test` lists files explicitly (RESEARCH Validation Architecture / VALIDATION Wave 0).

Purpose: A single contract everyone downstream depends on — the `Character.alt_text` shape, the registered test files, and the `key:upload-images` npm script.
Output: Updated `CharacterSchema` + test, updated `package.json` (`key:upload-images` script + two registered test files), and `makeChar`/inline-literal fixtures updated so the existing `pnwm-identify` suite stays green with the new field.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-character-illustration-images/43-RESEARCH.md
@.planning/phases/43-character-illustration-images/43-PATTERNS.md

# Files this plan modifies (read the exact line ranges named in each task)
@src/types/schemas.ts
@src/types/schemas.test.ts
@src/components/pnwm-identify.test.ts
@package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add alt_text to CharacterSchema and cover it in schemas.test.ts (D-06)</name>
  <files>src/types/schemas.ts, src/types/schemas.test.ts</files>
  <read_first>
    - src/types/schemas.ts:157-165 (the existing CharacterSchema; image_filename already present at :163)
    - src/types/schemas.test.ts:84-125 (the CharacterSchema describe block; validCharacter fixture at ~:91; existing image_filename cases at :104-105)
  </read_first>
  <behavior>
    - A character object with `alt_text: 'A black forewing'` passes `CharacterSchema.safeParse` (success === true).
    - A character object with `alt_text: null` passes (nullable, mirrors image_filename — RESEARCH Pitfall 5 / D-06).
    - A character object that omits `alt_text` entirely fails `safeParse` (required-but-nullable, same posture as image_filename).
  </behavior>
  <action>
    Per D-06, add `alt_text: z.nullable(z.string())` to `CharacterSchema` in src/types/schemas.ts immediately after `image_filename` (see PATTERNS.md schemas.ts block, lines 200-208), with the trailing comment `// Phase 43; null → render derives alt from state`. This makes curator alt_text reach the client and resolves RESEARCH Pitfall 5 (alt_text had no schema home). Do NOT use `.optional()` — match the required-but-nullable posture of `image_filename` so the build always emits the key.

    In src/types/schemas.test.ts, update the `validCharacter` fixture (~:91) to include `alt_text: null`, then add three `it(...)` cases inside the existing `describe('CharacterSchema', ...)` block (mirror the existing image_filename cases at :104-105): (a) accepts `alt_text` set to a non-empty string; (b) accepts `alt_text: null`; (c) rejects a character missing `alt_text` (delete the key from a spread copy, assert `result.success === false`). Use the existing `safeParse`/`assert` idiom already in the file.
  </action>
  <verify>
    <automated>node --test src/types/schemas.test.ts</automated>
  </verify>
  <done>schemas.test.ts passes; CharacterSchema requires a nullable alt_text field; the three new assertions all pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Keep pnwm-identify fixtures green under the widened Character type</name>
  <files>src/components/pnwm-identify.test.ts</files>
  <read_first>
    - src/components/pnwm-identify.test.ts:24-37 (the makeChar factory at :24 — defaults image_filename: null at :27 — and the sampleChars array at :33-37)
    - src/components/pnwm-identify.test.ts:213-214 (inline full Character literals that already spell out image_filename: null)
  </read_first>
  <behavior>
    - Every Character produced by `makeChar` and every inline Character literal carries `alt_text`, so the file typechecks against the widened Character type.
    - All existing pnwm-identify tests continue to pass unchanged (this task is type-compat only; no behavioral assertion changes).
  </behavior>
  <action>
    Add `alt_text: null` to the `makeChar` factory default object (alongside `image_filename: null` at :27) so callers inherit it. Add `alt_text: null` to each inline Character literal at :213-214 (and any other inline literal in the file that enumerates Character fields — grep the file for `image_filename:` to find them all). This is a pure type-compatibility update — do NOT alter any assertion. The render-specific `<details>` assertions are added later in Plan 03, not here. Rationale: `Character` is `z.infer<typeof CharacterSchema>`, so Task 1's schema widening requires every fully-spelled-out literal to include the new key (the factory spread covers the rest).
  </action>
  <verify>
    <automated>node --test src/components/pnwm-identify.test.ts</automated>
  </verify>
  <done>The full pnwm-identify suite passes with alt_text present in all fixtures; no assertion changed.</done>
</task>

<task type="auto">
  <name>Task 3: Register new test files and add the key:upload-images script in package.json</name>
  <files>package.json</files>
  <read_first>
    - package.json scripts block: the `test` script (a `node --test ...` line listing each test file explicitly, including `scripts/upload-tiles.test.ts` and `src/components/*.test.ts`) and the `photos:upload` script (`node scripts/upload-tiles.ts`)
  </read_first>
  <action>
    Two edits, both per PATTERNS.md package.json section:
    1. Append `scripts/upload-images.test.ts` and `scripts/match-character-images.test.ts` to the explicit file list in the `test` script. `node --test` here lists files individually and does NOT glob `scripts/*.test.ts`, so an unregistered test file silently never runs (VALIDATION Wave 0; RESEARCH Validation Architecture). Note: `src/components/*.test.ts` glob already covers `pnwm-identify.test.ts`, and `src/types/schemas.test.ts` is already listed — do NOT duplicate those.
    2. Add `"key:upload-images": "node scripts/upload-images.ts"` adjacent to `photos:upload`. Invoke with `node` (native type-stripping), matching `build:key`/`photos:upload`. Do NOT add this to the `build` script — upload is a manual, credentialed, network operator task (RESEARCH Anti-Pattern; same posture as `photos:upload`, which is also out of `build`).

    The two referenced test files do not exist yet (Plan 02 creates them). Registering them now means a full `npm test` errors on the missing files until Plan 02 lands — that is the expected Wave 0 → Wave 1 ordering. The verify below scopes to package.json structure only; full-suite green is asserted at the Plan 02 wave merge.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); const t=p.scripts.test; if(!t.includes('scripts/upload-images.test.ts')||!t.includes('scripts/match-character-images.test.ts')) throw new Error('new test files not registered'); if(p.scripts['key:upload-images']!=='node scripts/upload-images.ts') throw new Error('key:upload-images script missing/wrong'); if(p.scripts.build.includes('upload-images')) throw new Error('upload must not be in build'); console.log('package.json wiring OK');"</automated>
  </verify>
  <done>Both new test files appear in the `test` script's file list; `key:upload-images` runs `node scripts/upload-images.ts`; the script is absent from `build`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| curator CSV → build → client | `alt_text` originates in a curator-authored CSV; this plan only establishes the schema slot. Rendering/escaping is enforced in Plan 03 (Lit auto-escape) and validation in Plan 03 (build-key). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-01 | Tampering | `alt_text` string field on CharacterSchema | accept | This plan only declares a nullable string field; no rendering occurs here. The XSS/CSV-injection surface is handled where the value is consumed: Lit auto-escape in Plan 03 render, `csv-stringify` quoting in Plan 02 matcher, `char_id` range check in Plan 03 build-key. No new high-severity exposure introduced by a schema field. |
| T-43-SC | Tampering | npm/pip/cargo installs | accept | Zero new package installs this phase (RESEARCH Package Legitimacy Audit: "slopcheck not applicable — zero new package installs"). No supply-chain checkpoint required. |
</threat_model>

<verification>
- `node --test src/types/schemas.test.ts` and `node --test src/components/pnwm-identify.test.ts` both green.
- `npm run typecheck` clean (the widened `Character` type ripples into `pnwm-identify.test.ts`; fixtures updated in Task 2 keep it green).
- package.json wiring assertion (Task 3 verify) passes.
</verification>

<success_criteria>
- `CharacterSchema` carries `alt_text: z.nullable(z.string())`; schemas.test.ts proves valid/null/missing behavior.
- `package.json` lists `scripts/upload-images.test.ts` and `scripts/match-character-images.test.ts` in `test`, defines `key:upload-images`, and keeps upload out of `build`.
- `npm run typecheck` is clean and the schemas + pnwm-identify suites are green.
</success_criteria>

<output>
Create `.planning/phases/43-character-illustration-images/43-01-SUMMARY.md` when done
</output>
