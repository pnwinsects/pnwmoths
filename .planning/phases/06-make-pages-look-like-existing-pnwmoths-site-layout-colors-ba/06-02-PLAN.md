---
phase: 06-make-pages-look-like-existing-pnwmoths-site-layout-colors-ba
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/_includes/base.njk
  - src/index.njk
autonomous: false
requirements: []

must_haves:
  truths:
    - "Every page has the moth-strip banner image visible at the top"
    - "Every page has a black header containing banner + nav"
    - "Every page has a black footer with licensing text"
    - "Every page has cream background with white content wrapper"
    - "Every page loads Open Sans and Spinnaker from Google Fonts"
    - "Homepage shows welcome paragraph and single Browse CTA link"
    - "All asset paths use the Eleventy | url filter for pathPrefix compatibility"
  artifacts:
    - path: "src/_includes/base.njk"
      provides: "Site-wide layout with header, banner, nav, content wrapper, footer, Google Fonts, theme.css"
      contains: "header.png"
    - path: "src/index.njk"
      provides: "Homepage with welcome text and Browse CTA"
      contains: "Browse all species"
  key_links:
    - from: "src/_includes/base.njk"
      to: "src/styles/theme.css"
      via: "link rel=stylesheet with | url filter"
      pattern: "styles/theme.css.*url"
    - from: "src/_includes/base.njk"
      to: "src/images/header.png"
      via: "img src with | url filter"
      pattern: "images/header.png.*url"
    - from: "src/_includes/base.njk"
      to: "fonts.googleapis.com"
      via: "link href in head"
      pattern: "fonts.googleapis.com"
    - from: "src/index.njk"
      to: "/browse/"
      via: "anchor href with | url filter"
      pattern: "browse.*url"
---

<objective>
Update the base layout template and homepage to apply the pnwmoths visual identity to every page.

Purpose: This is the user-visible plan — after completion, all ~700 generated pages will have the cream background, black header with moth-strip banner, navigation bar, white content wrapper, footer, and correct fonts. The homepage will show the welcome paragraph and Browse CTA per D-02/D-03/D-04.

Output: Updated `src/_includes/base.njk` (Google Fonts link, theme.css link, header with banner + nav, content wrapper div, footer) and updated `src/index.njk` (welcome text + single CTA). Visual checkpoint confirms the result.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-make-pages-look-like-existing-pnwmoths-site-layout-colors-ba/06-CONTEXT.md
@.planning/phases/06-make-pages-look-like-existing-pnwmoths-site-layout-colors-ba/06-RESEARCH.md
@.planning/phases/06-make-pages-look-like-existing-pnwmoths-site-layout-colors-ba/06-PATTERNS.md
@.planning/phases/06-make-pages-look-like-existing-pnwmoths-site-layout-colors-ba/06-UI-SPEC.md

<interfaces>
<!-- Current base.njk that will be rewritten (from 06-PATTERNS.md) -->

From src/_includes/base.njk (current, complete file):
```njk
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title or "PNW Moths" }}</title>
  <link rel="stylesheet" href="/css/pico.min.css">
  {% if pagefindUi %}<link rel="stylesheet" href="{{ '/pagefind/pagefind-ui.css' | url }}">{% endif %}
</head>
<body>
  <nav data-pagefind-ignore>
    <ul>
      <li><a href="{{ '/' | url }}">Home</a></li>
      <li><a href="{{ '/browse/' | url }}">Browse</a></li>
      <li><a href="{{ '/search/' | url }}">Search</a></li>
      <li><a href="{{ '/glossary/' | url }}">Glossary</a></li>
    </ul>
  </nav>
  <main>
    {{ content | safe }}
  </main>
  <script type="module" src="/components/main.js"></script>
</body>
</html>
```

From src/index.njk (current, complete file):
```njk
---
layout: base.njk
title: PNW Moths
permalink: /index.html
---
<h1>PNW Moths</h1>
<p>A natural history catalog of Pacific Northwest moths. Browse by family and genus, search by name, or look up terms in the glossary.</p>
<ul>
  <li><a href="{{ '/browse/' | url }}">Browse all species</a></li>
  <li><a href="{{ '/search/' | url }}">Search</a></li>
  <li><a href="{{ '/glossary/' | url }}">Glossary</a></li>
</ul>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update base.njk with header, banner, footer, fonts, and theme.css</name>
  <files>src/_includes/base.njk</files>
  <read_first>src/_includes/base.njk, src/styles/theme.css</read_first>
  <action>
Rewrite `src/_includes/base.njk` to the following complete content. This adds Google Fonts, theme.css, wraps nav in a header with banner image, wraps content in .content-wrapper div, and adds a footer.

```njk
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title or "PNW Moths" }}</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Open+Sans:ital,wght@0,400;0,600&family=Spinnaker&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/pico.min.css">
  <link rel="stylesheet" href="{{ '/styles/theme.css' | url }}">
  {% if pagefindUi %}<link rel="stylesheet" href="{{ '/pagefind/pagefind-ui.css' | url }}">{% endif %}
</head>
<body>
  <header>
    <img src="{{ '/images/header.png' | url }}"
         alt="Pacific Northwest moths — a row of specimen photographs on black">
    <nav data-pagefind-ignore>
      <ul>
        <li><a href="{{ '/' | url }}">Home</a></li>
        <li><a href="{{ '/browse/' | url }}">Browse</a></li>
        <li><a href="{{ '/search/' | url }}">Search</a></li>
        <li><a href="{{ '/glossary/' | url }}">Glossary</a></li>
      </ul>
    </nav>
  </header>
  <main>
    <div class="content-wrapper">
      {{ content | safe }}
    </div>
  </main>
  <footer>
    <p>Pacific Northwest Moths. Licensed under <a href="https://creativecommons.org/licenses/by-nc-sa/4.0/">Creative Commons Attribution-NonCommercial-ShareAlike 4.0</a>.</p>
  </footer>
  <script type="module" src="/components/main.js"></script>
</body>
</html>
```

Key changes from the current file:
1. Google Fonts `<link>` tags added BEFORE pico.min.css (3 lines: 2 preconnect + 1 stylesheet)
2. `theme.css` `<link>` added AFTER pico.min.css, using `| url` filter
3. Bare `<nav>` replaced by `<header>` wrapping banner `<img>` + `<nav>` (per D-05)
4. Banner `<img src>` uses `| url` filter for pathPrefix compatibility
5. `{{ content | safe }}` wrapped in `<div class="content-wrapper">` (per D-01)
6. `<footer>` added after `<main>` with licensing text (per UI-SPEC footer contract)
7. All existing elements preserved: pagefindUi conditional, nav links with `| url`, script tag
  </action>
  <verify>
    <automated>npx @11ty/eleventy 2>&1 && grep -q "fonts.googleapis.com" _site/index.html && grep -q "header.png" _site/index.html && grep -q "theme.css" _site/index.html && grep -q "content-wrapper" _site/index.html && grep -q "Creative Commons" _site/index.html && echo "PASS: base.njk changes applied to all pages"</automated>
  </verify>
  <acceptance_criteria>
- `src/_includes/base.njk` contains `fonts.googleapis.com/css2?family=Open+Sans`
- `src/_includes/base.njk` contains `family=Spinnaker&display=swap`
- `src/_includes/base.njk` contains `'/styles/theme.css' | url`
- `src/_includes/base.njk` contains `'/images/header.png' | url`
- `src/_includes/base.njk` contains `<header>` wrapping both `<img` and `<nav`
- `src/_includes/base.njk` contains `<div class="content-wrapper">`
- `src/_includes/base.njk` contains `<footer>` with `Creative Commons Attribution-NonCommercial-ShareAlike 4.0`
- `src/_includes/base.njk` still contains `{% if pagefindUi %}` conditional
- `src/_includes/base.njk` still contains `<script type="module" src="/components/main.js">`
- Built `_site/index.html` contains all of: `fonts.googleapis.com`, `header.png`, `theme.css`, `content-wrapper`, `Creative Commons`
  </acceptance_criteria>
  <done>base.njk updated with complete pnwmoths visual identity: Google Fonts, theme.css, header with banner + nav, content wrapper, and footer. All ~700 generated pages inherit the changes. All asset paths use | url filter.</done>
</task>

<task type="auto">
  <name>Task 2: Update homepage content to welcome text + Browse CTA</name>
  <files>src/index.njk</files>
  <read_first>src/index.njk</read_first>
  <action>
Replace the body content of `src/index.njk` (below the frontmatter). Keep the frontmatter unchanged. Replace the `<ul>` with three `<li>` links with a single standalone `<a>` CTA link. The `<h1>` and `<p>` already match D-03 and stay as-is.

New complete file content:

```njk
---
layout: base.njk
title: PNW Moths
permalink: /index.html
---
<h1>PNW Moths</h1>
<p>A natural history catalog of Pacific Northwest moths. Browse by family and genus, search by name, or look up terms in the glossary.</p>
<a href="{{ '/browse/' | url }}">Browse all species</a>
```

Changes from current:
- Removed `<ul>` block with 3 list items (Browse, Search, Glossary links)
- Added single `<a>` CTA link per D-02 and D-04
- Kept `<h1>` unchanged
- Kept `<p>` with D-03 exact welcome text unchanged
- Kept frontmatter unchanged
  </action>
  <verify>
    <automated>npx @11ty/eleventy 2>&1 && grep -q "Browse all species" _site/index.html && ! grep -q "<ul>" _site/index.html && echo "PASS: homepage updated"</automated>
  </verify>
  <acceptance_criteria>
- `src/index.njk` does NOT contain `<ul>` or `<li>` tags
- `src/index.njk` contains `<a href="{{ '/browse/' | url }}">Browse all species</a>` as a standalone element (not inside a list)
- `src/index.njk` contains the exact text `A natural history catalog of Pacific Northwest moths. Browse by family and genus, search by name, or look up terms in the glossary.`
- Built `_site/index.html` contains `Browse all species` and does NOT contain `<ul>`
  </acceptance_criteria>
  <done>Homepage updated per D-02/D-03/D-04: welcome paragraph retained, three-link list replaced with single "Browse all species" CTA link.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of pnwmoths visual identity</name>
  <action>Run `npx @11ty/eleventy --serve` and open in browser for visual comparison against the existing pnwmoths.biol.wwu.edu site.</action>
  <files>n/a</files>
  <what-built>Applied the pnwmoths.biol.wwu.edu visual identity to all pages: cream background, black header with moth-strip banner image, navigation bar, white content wrapper with box-shadow, black footer, Open Sans + Spinnaker fonts. Updated homepage to show welcome text and single Browse CTA.</what-built>
  <how-to-verify>
1. Start the dev server: `npx @11ty/eleventy --serve`
2. Open `http://localhost:8080/pnwmoths/` in a browser
3. Compare side-by-side with `https://pnwmoths.biol.wwu.edu/`
4. Verify these visual elements:
   - Cream/tan page background (#f3e8ba)
   - Moth-strip banner image at top of page
   - Black navigation bar below banner with white link text
   - White content area with subtle box-shadow
   - Black footer at bottom with licensing text
   - Open Sans body font, Spinnaker heading font
5. Navigate to a species page (e.g., /pnwmoths/species/acronicta-americana/) and confirm same layout
6. Navigate to /pnwmoths/browse/ and confirm same layout
7. Navigate to /pnwmoths/search/ and confirm search still works
8. Check homepage shows welcome paragraph + single "Browse all species" link (no bullet list)
  </how-to-verify>
  <verify>
    <automated>npx @11ty/eleventy 2>&1 | tail -1</automated>
  </verify>
  <done>User has visually confirmed the site matches the pnwmoths.biol.wwu.edu visual identity.</done>
  <resume-signal>Type "approved" if the visual identity matches the existing pnwmoths site, or describe any issues that need fixing.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Google Fonts CDN -> browser | External font files loaded at page view time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02 | Information Disclosure | Google Fonts CDN | accept | Google Fonts sends visitor IP to Google servers. Standard practice for public sites; no PII beyond IP address. No credentials involved. |
</threat_model>

<verification>
- `npx @11ty/eleventy` completes without errors
- All generated HTML pages contain: Google Fonts link, banner img, theme.css link, content-wrapper div, footer
- Homepage has welcome paragraph + single Browse CTA (no `<ul>`)
- Visual inspection confirms cream background, black header/footer, white content wrapper, correct fonts
</verification>

<success_criteria>
- All ~700 generated pages share the same visual identity: cream background, black header with banner, white content wrapper, black footer
- Homepage shows D-03 welcome text and D-04 Browse CTA link only (no multi-link list)
- Build succeeds without errors
- Visual checkpoint approved by user
</success_criteria>

<output>
After completion, create `.planning/phases/06-make-pages-look-like-existing-pnwmoths-site-layout-colors-ba/06-02-SUMMARY.md`
</output>
