---
phase: 13-cdn-provisioning
plan: 05
type: execute
wave: 4
depends_on:
  - 13-04
files_modified:
  - .planning/phases/13-cdn-provisioning/13-CONTEXT.md
  - .planning/phases/13-cdn-provisioning/13-04-SUMMARY.md
autonomous: false

requirements:
  - CDN-01
  - CDN-02
  - CDN-04

must_haves:
  truths:
    - "13-CONTEXT.md accurately documents that Image Classes are disabled and direct Optimizer query params must be used instead"
    - "Phase 14 authors reading 13-CONTEXT.md will not attempt to use Image Class names in template URLs"
    - "All plan 04 outputs are committed to git"
    - "CDN delivers images at HTTP 200 with WebP conversion active"
    - "Optimizer query params (?height=186 and ?width=188&height=225&crop_gravity=north) return resized/cropped WebP images"
  artifacts:
    - path: ".planning/phases/13-cdn-provisioning/13-CONTEXT.md"
      provides: "Updated decisions D-10, D-11, D-18 documenting Image Classes disable and Phase 14 implications"
      contains: "D-18"
    - path: ".planning/phases/13-cdn-provisioning/13-04-SUMMARY.md"
      provides: "Plan 04 completion record"
      contains: "13-04"
  key_links:
    - from: ".planning/phases/13-cdn-provisioning/13-CONTEXT.md"
      to: "Phase 14 planning"
      via: "D-18 decision documenting Image Classes disabled state"
      pattern: "D-18"
    - from: "bunny.net Pull Zone"
      to: "CDN delivery"
      via: "HTTP GET with Optimizer query params"
      pattern: "pnwmoths.b-cdn.net"
---

<objective>
Close out Phase 13 CDN Provisioning: update CONTEXT.md to document the Image Classes support-ticket resolution, commit all outstanding plan 04 artifacts, and complete the CDN spot-check now that the 403 blocker is resolved.

Purpose: Phase 14 template migration depends on knowing that Image Classes are disabled and that Optimizer params must be passed directly in template URLs. CONTEXT.md is the authoritative source for this decision. The CDN spot-check is the final gate before Phase 14 can start.

Output: Updated 13-CONTEXT.md (D-10, D-11 revised, D-18 added), 13-04-SUMMARY.md, committed artifacts, CDN delivery confirmed.
</objective>

<execution_context>
@/Users/rainhead/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rainhead/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/rainhead/dev/pnwmoths/.planning/phases/13-cdn-provisioning/13-CONTEXT.md
@/Users/rainhead/dev/pnwmoths/.planning/phases/13-cdn-provisioning/13-03-SUMMARY.md
@/Users/rainhead/dev/pnwmoths/.planning/phases/13-cdn-provisioning/13-04-PLAN.md
@/Users/rainhead/dev/pnwmoths/.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update 13-CONTEXT.md and create 13-04-SUMMARY.md</name>
  <files>
    .planning/phases/13-cdn-provisioning/13-CONTEXT.md
    .planning/phases/13-cdn-provisioning/13-04-SUMMARY.md
  </files>
  <read_first>
    - .planning/phases/13-cdn-provisioning/13-CONTEXT.md (full file — read before editing; understand current D-10, D-11 wording in the "Image Classes" section)
    - .planning/phases/13-cdn-provisioning/13-03-SUMMARY.md (background on the 403 blocker and Image Classes config)
    - .planning/phases/13-cdn-provisioning/13-04-PLAN.md (task list to document in 13-04-SUMMARY.md)
  </read_first>
  <action>
**Edit 1: Update 13-CONTEXT.md — Image Classes section**

Replace the current "Image Classes" section (D-10 and D-11) with the following revised text. The section header and decision IDs stay the same; the content changes to reflect that Image Classes are now disabled and direct query params are used instead. Add D-18 immediately after D-11.

---

### Image Classes

- **D-10:** Glossary portrait dimensions: 188 × 225 px, north crop. Query params: `?width=188&height=225&crop_gravity=north`. These params work directly on the Optimizer without a named Image Class. The Image Class named `glossaryportrait` was defined during Phase 13 but is now disabled (see D-18). Phase 14 templates MUST use direct query params, NOT the class name.
- **D-11:** Nav thumbnail dimensions: height 186 px, width auto-proportioned. Query param: `?height=186`. This works directly on the Optimizer without a named Image Class. The Image Class named `navthumb` was defined during Phase 13 but is now disabled (see D-18). Phase 14 templates MUST use `?height=186` directly in CDN URLs, NOT the class name.
- **D-18:** bunny.net Image Classes are currently **disabled** on the Pull Zone Optimizer. Root cause: when Image Classes are enabled, bunny.net requires every request to include a valid class name in the URL — requests without a class name receive 403. During Phase 13 execution, all CDN requests were returning 403 because no class name was appended. bunny.net support disabled Image Classes to restore delivery. Resolution: the same resize/crop operations are achieved by passing Optimizer query params directly (`?width=`, `?height=`, `?crop_gravity=`). **Do not re-enable Image Classes** unless Phase 14 templates are simultaneously updated to append class names to every CDN URL — failing to do so will return 403 sitewide.

---

**Edit 2: Create 13-04-SUMMARY.md**

Create `.planning/phases/13-cdn-provisioning/13-04-SUMMARY.md` with this exact content:

```markdown
---
phase: 13-cdn-provisioning
plan: "04"
subsystem: cdn-documentation
tags: [cdn, bunny-net, documentation, contributor-workflow]
dependency_graph:
  requires:
    - 13-03 (CDN provisioning and image upload)
  provides:
    - _instructions/UPLOADING_IMAGES.md (contributor-facing rclone upload guide)
  affects:
    - Phase 14 (CDN spot-check gate; CONTEXT.md decisions D-10, D-11, D-18)
key_files:
  created:
    - _instructions/UPLOADING_IMAGES.md
  modified: []
decisions:
  - Task 2 (CDN spot-check checkpoint) was superseded by Plan 13-05 — the spot-check was blocked by a 403 during Plan 03 execution, which was resolved by bunny.net support disabling Image Classes
metrics:
  completed: "2026-04-22"
  tasks_completed: 1/2
  tasks_superseded: 1
---

# Phase 13 Plan 04: Summary

**One-liner:** Contributor upload doc written; CDN spot-check deferred to Plan 05 (was blocked by Image Classes 403 that required bunny.net support intervention).

## Tasks Completed

| Task | Name | Status |
|------|------|--------|
| 1 | Write _instructions/UPLOADING_IMAGES.md | Done |
| 2 | Spot-check CDN delivery end-to-end | Superseded by Plan 13-05 |

## What Was Built

### _instructions/UPLOADING_IMAGES.md

Contributor-facing guide covering:
- How to request FTP credentials and API key from the project owner
- rclone FTP remote setup for bunny.net (LA region: `la.storage.bunnycdn.com`)
- `rclone copy --ignore-times` for new and replacement uploads
- Upload one file at a time (directory upload causes 450 errors)
- Cache invalidation via `curl` to `api.bunny.net/purge`
- Prominent WARNING: never use `rclone sync`

## Deviations from Plan

### CDN spot-check deferred to Plan 13-05

The Task 2 checkpoint (CDN spot-check) was blocked by the same 403 that stopped Plan 03. bunny.net support resolved the issue by disabling Image Classes on the Optimizer. The spot-check runs in Plan 13-05 once the fix is confirmed. Plan 04 is considered complete with Task 1 done; Task 2 is superseded.

## Known Issues

None. The 403 blocker that caused Task 2 to be deferred was resolved by bunny.net support before Plan 13-05 was executed.
```
  </action>
  <verify>
    <automated>grep "D-18" /Users/rainhead/dev/pnwmoths/.planning/phases/13-cdn-provisioning/13-CONTEXT.md && grep "Do not re-enable Image Classes" /Users/rainhead/dev/pnwmoths/.planning/phases/13-cdn-provisioning/13-CONTEXT.md && test -f /Users/rainhead/dev/pnwmoths/.planning/phases/13-cdn-provisioning/13-04-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "D-18" .planning/phases/13-cdn-provisioning/13-CONTEXT.md` exits 0
    - `grep "Do not re-enable Image Classes" .planning/phases/13-cdn-provisioning/13-CONTEXT.md` exits 0
    - `grep "direct query params" .planning/phases/13-cdn-provisioning/13-CONTEXT.md` exits 0 (D-10 and D-11 updated to reference query params, not class names)
    - `grep "glossaryportrait.*disabled\|disabled.*glossaryportrait\|now disabled" .planning/phases/13-cdn-provisioning/13-CONTEXT.md` exits 0
    - `grep "navthumb.*disabled\|disabled.*navthumb\|now disabled" .planning/phases/13-cdn-provisioning/13-CONTEXT.md` exits 0
    - File `.planning/phases/13-cdn-provisioning/13-04-SUMMARY.md` exists
    - `grep "Superseded by Plan 13-05\|superseded by Plan 05\|superseded" .planning/phases/13-cdn-provisioning/13-04-SUMMARY.md` exits 0
    - `grep "tasks_completed: 1/2" .planning/phases/13-cdn-provisioning/13-04-SUMMARY.md` exits 0
  </acceptance_criteria>
  <done>13-CONTEXT.md has D-10, D-11 revised to reference direct query params (not class names), D-18 added documenting Image Classes disabled state and re-enable danger. 13-04-SUMMARY.md exists and records task 2 as superseded.</done>
</task>

<task type="auto">
  <name>Task 2: Commit all outstanding Phase 13 artifacts</name>
  <files>
    _instructions/UPLOADING_IMAGES.md
    .planning/phases/13-cdn-provisioning/13-03-SUMMARY.md
    .planning/phases/13-cdn-provisioning/13-04-SUMMARY.md
    .planning/phases/13-cdn-provisioning/13-CONTEXT.md
  </files>
  <read_first>
    - Run `git status` to confirm the four files above are the only changed/untracked files to commit
  </read_first>
  <action>
Stage and commit the following four files in a single commit:

1. `_instructions/UPLOADING_IMAGES.md` — plan 04 task 1 output (untracked)
2. `.planning/phases/13-cdn-provisioning/13-03-SUMMARY.md` — plan 03 execution summary (untracked)
3. `.planning/phases/13-cdn-provisioning/13-04-SUMMARY.md` — plan 04 completion record (created in Task 1)
4. `.planning/phases/13-cdn-provisioning/13-CONTEXT.md` — updated decisions D-10, D-11, D-18 (modified in Task 1)

Commit command:
```bash
git add _instructions/UPLOADING_IMAGES.md \
  .planning/phases/13-cdn-provisioning/13-03-SUMMARY.md \
  .planning/phases/13-cdn-provisioning/13-04-SUMMARY.md \
  .planning/phases/13-cdn-provisioning/13-CONTEXT.md
git commit -m "docs(phase-13): record Image Classes disable, plan 04 artifacts, contributor upload guide"
```

Do NOT stage any other files. Do NOT use `git add .` or `git add -A`.
  </action>
  <verify>
    <automated>git log --oneline -1 | grep "phase-13" && git status --short | grep -v "^?" | wc -l | awk '{if ($1 == 0) exit 0; else exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `git log --oneline -1` shows a commit message containing "phase-13"
    - `git show --stat HEAD` lists exactly the four files: `_instructions/UPLOADING_IMAGES.md`, `13-03-SUMMARY.md`, `13-04-SUMMARY.md`, `13-CONTEXT.md`
    - `git status --short` shows no staged changes (clean working tree aside from untracked files not in this commit)
    - `git show HEAD -- _instructions/UPLOADING_IMAGES.md | head -5` shows the file content (confirms it was committed)
  </acceptance_criteria>
  <done>All four Phase 13 outstanding artifacts are committed in a single git commit. Working tree is clean for those files.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Spot-check CDN delivery and Optimizer query params</name>
  <read_first>
    - .planning/phases/13-cdn-provisioning/13-CONTEXT.md (D-10, D-11, D-18 — the query params to verify)
    - .planning/phases/13-cdn-provisioning/13-03-SUMMARY.md (what was uploaded; slugs and filenames available)
  </read_first>
  <what-built>
    bunny.net support disabled Image Classes on the Pull Zone Optimizer. CDN delivery should now return 200. The Optimizer's direct query params (?height=, ?width=, ?crop_gravity=) are the mechanism Phase 14 will use — this checkpoint confirms they work correctly.
  </what-built>
  <how-to-verify>
Run each check. Replace `GLOSSARY_FILENAME` with an actual filename from the `glossary/` directory (use `rclone ls bunny:glossary/ | head -5` to find one if needed).

```bash
# 1. Basic CDN delivery — species photo, no Optimizer params
curl -sI "https://pnwmoths.b-cdn.net/acronicta-americana/Acronicta%20americana-A-D.jpg" \
  | grep -E "^HTTP|^content-type"
# Expected: HTTP/2 200 and content-type: image/webp

# 2. Optimizer resize — nav thumbnail equivalent (Phase 14 will use this)
curl -sI "https://pnwmoths.b-cdn.net/acronicta-americana/Acronicta%20americana-A-D.jpg?height=186" \
  | grep -E "^HTTP|^content-type|^content-length"
# Expected: HTTP/2 200, content-type: image/webp

# 3. Optimizer crop — glossary portrait equivalent (Phase 14 will use this)
curl -sI "https://pnwmoths.b-cdn.net/glossary/GLOSSARY_FILENAME?width=188&height=225&crop_gravity=north" \
  | grep -E "^HTTP|^content-type"
# Expected: HTTP/2 200, content-type: image/webp

# 4. Confirm build still passes
cd /Users/rainhead/dev/pnwmoths && npm run build:data
# Expected: exits 0
```

Also open in a browser (browser signals WebP support via Accept header, which triggers Optimizer conversion):
- `https://pnwmoths.b-cdn.net/acronicta-americana/Acronicta%20americana-A-D.jpg?height=186`
  - Network tab: Status 200, Content-Type: image/webp, image visible
- `https://pnwmoths.b-cdn.net/glossary/GLOSSARY_FILENAME?width=188&height=225&crop_gravity=north`
  - Network tab: Status 200, Content-Type: image/webp, image cropped to portrait shape

Check that NO 403 responses appear in any of the above. A 403 would indicate Image Classes were re-enabled or another access rule was added.
  </how-to-verify>
  <acceptance_criteria>
    - curl check 1 (basic delivery): HTTP 200 + content-type: image/webp (no 403, no 404)
    - curl check 2 (?height=186): HTTP 200 + content-type: image/webp
    - curl check 3 (?width=188&height=225&crop_gravity=north): HTTP 200 + content-type: image/webp
    - npm run build:data exits 0
    - Browser confirms resized WebP for ?height=186 (image visible, not broken)
    - Browser confirms portrait-cropped WebP for ?width=188&height=225&crop_gravity=north
    - No 403 response on any request
  </acceptance_criteria>
  <resume-signal>Type "cdn-verified" when all curl checks return 200 + image/webp and the browser confirms both Optimizer param variants work. If any check returns 403, type "403-again" and describe which check failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 13-CONTEXT.md → Phase 14 planner | Decision doc informs Phase 14 template URL construction |
| CDN URL → browser | Public CDN delivery; no auth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-05-01 | Tampering | Image Classes re-enabled without updating Phase 14 templates | mitigate | D-18 in 13-CONTEXT.md explicitly warns against re-enabling Image Classes without simultaneous Phase 14 template update; CDN spot-check (Task 3) confirms current disabled state works |
| T-13-05-02 | Information Disclosure | CDN delivers images publicly without auth | accept | Public site; images are intentionally public; no PII in image content |
| T-13-05-03 | Repudiation | Committed CONTEXT.md does not match what bunny.net support actually did | accept | D-18 records the support interaction and outcome; low risk since project owner observed the fix |
</threat_model>

<verification>
```bash
# CONTEXT.md updated with D-18
grep "D-18" .planning/phases/13-cdn-provisioning/13-CONTEXT.md
grep "Do not re-enable Image Classes" .planning/phases/13-cdn-provisioning/13-CONTEXT.md

# All artifacts committed
git show --stat HEAD

# CDN delivers without 403
curl -sI "https://pnwmoths.b-cdn.net/acronicta-americana/Acronicta%20americana-A-D.jpg" | grep "^HTTP"
# Expected: HTTP/2 200

# Optimizer params work
curl -sI "https://pnwmoths.b-cdn.net/acronicta-americana/Acronicta%20americana-A-D.jpg?height=186" | grep "^HTTP"
# Expected: HTTP/2 200
```
</verification>

<success_criteria>
1. `13-CONTEXT.md` contains D-18 documenting Image Classes disabled, the 403 root cause, and the Phase 14 implication (use direct query params, not class names)
2. D-10 and D-11 in `13-CONTEXT.md` reference direct query params and explicitly state the class names are disabled
3. `_instructions/UPLOADING_IMAGES.md`, `13-03-SUMMARY.md`, `13-04-SUMMARY.md`, and `13-CONTEXT.md` are all committed in a single git commit
4. CDN spot-check passes: `curl -sI https://pnwmoths.b-cdn.net/acronicta-americana/Acronicta%20americana-A-D.jpg` returns HTTP 200 + `content-type: image/webp`
5. Optimizer query params verified: `?height=186` and `?width=188&height=225&crop_gravity=north` both return HTTP 200 + `content-type: image/webp` in browser
</success_criteria>

<output>
After completion, create `.planning/phases/13-cdn-provisioning/13-05-SUMMARY.md` documenting:
- D-10, D-11, D-18 written to CONTEXT.md
- Four files committed
- CDN spot-check results (curl HTTP codes, Optimizer param verification)
- Phase 13 status: complete and unblocked for Phase 14
</output>
