---
phase: 28
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/28-end-to-end-vertical-slice-pilot-one-species/TILE-RECIPE.md
autonomous: false
requirements:
  - PILOT-01
must_haves:
  truths:
    - "Operator has a documented vips dzsave recipe sufficient to produce DZI tiles from one TIFF on their own hardware"
    - "Running the recipe on a TIFF produces an output_prefix.dzi descriptor + output_prefix_files/ directory pyramid"
    - "Operator has produced 2 to 6 tile pyramids (one per specimen+view pair) for one hand-picked species under their working directory"
  artifacts:
    - path: ".planning/phases/28-end-to-end-vertical-slice-pilot-one-species/TILE-RECIPE.md"
      provides: "Operator-runnable vips dzsave recipe; species selection guidance; per-pair invocation pattern; manual verification block"
      min_lines: 40
    - path: "(operator-local) tile output directories"
      provides: "{specimen_id}-{view}.dzi + {specimen_id}-{view}_files/ pyramids on operator hardware for the chosen pilot species (uncommitted; uploaded in Plan 03)"
  key_links:
    - from: ".planning/phases/28-end-to-end-vertical-slice-pilot-one-species/TILE-RECIPE.md"
      to: ".planning/phases/28-end-to-end-vertical-slice-pilot-one-species/28-RESEARCH.md Pattern 3 (vips dzsave recipe)"
      via: "vips dzsave invocation block with --tile-size 256 --overlap 1 --suffix .jpg[Q=85] --layout dz"
      pattern: "vips dzsave .* --tile-size 256 .* --suffix \\.jpg\\[Q=85\\] .* --layout dz"
    - from: "TILE-RECIPE.md output prefix convention"
      to: "URL convention {{ cdnBaseUrl }}/species-tiles/{slug}/{specimen_id}-{view}/"
      via: "output prefix is {specimen_id}-{view}"
      pattern: "\\{specimen_id\\}-\\{view\\}"
---

<objective>
Produce a TIFF-to-DZI tile-generation recipe and use it to tile one hand-picked pilot species
on the operator's local hardware. This plan is the operator-side tile production step of the
pilot; it does NOT upload to bunny.net (Plan 03) and it does NOT wire OSD in the browser
(Plan 04). The deliverable is (a) a committed recipe document and (b) a local set of DZI
tile pyramids ready for upload.

Purpose: Surface the first integration risk seam (vips parameter choices, file layout, naming
collisions with the {specimen_id}-{view} convention) on a single species before any bulk work.

Output:
- `.planning/phases/28-end-to-end-vertical-slice-pilot-one-species/TILE-RECIPE.md` (committed)
- Local-only tile pyramid directories on operator hardware (NOT committed; uploaded in Plan 03)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/28-end-to-end-vertical-slice-pilot-one-species/28-RESEARCH.md
@.planning/phases/28-end-to-end-vertical-slice-pilot-one-species/28-PATTERNS.md
@.claude/skills/spike-findings-pnwmoths/SKILL.md
@CLAUDE.md

<interfaces>
Source-of-truth knowledge for tile naming and URL convention.

From ROADMAP.md / RESEARCH.md (URL convention):
- Production URL pattern: `{{ cdnBaseUrl }}/species-tiles/{species-slug}/{specimen_id}-{view}/`
- DZI descriptor URL: `{cdnBaseUrl}/species-tiles/{slug}/{specimen_id}-{view}/{specimen_id}-{view}.dzi`
- OSD derives tile URLs as `{descriptor_without_extension}_files/{level}/{col}_{row}.{format}`

From RESEARCH.md (Pilot Species Selection):
- Candidates with 1 specimen, 2 files (D+V): `abagrotis-apposita`, `abagrotis-dickeli`, `abrostola-urentis`
- Candidate with 3 files / mixed specimen IDs: `feltia-herilis` (specimen A + WWUC0000003275)
- Operator selects based on TIFF availability on their local machine

From manifest (`data/species-photos-manifest.csv` — Phase 26 output):
- Each row carries `specimen_id`, `view` (D|V), `species_slug`, `match_bucket`
- Pilot species MUST be in `clean-match` bucket with both D and V views, 1–3 specimens
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author TILE-RECIPE.md operator runbook</name>
  <files>.planning/phases/28-end-to-end-vertical-slice-pilot-one-species/TILE-RECIPE.md</files>
  <read_first>
    - .planning/phases/28-end-to-end-vertical-slice-pilot-one-species/28-RESEARCH.md (especially `## Architecture Patterns` Pattern 3 — vips dzsave recipe — and `## Pilot Species Selection`)
    - .planning/phases/28-end-to-end-vertical-slice-pilot-one-species/28-PATTERNS.md (`### Operator runbook style` section — borrows shape from `_instructions/ADDING_PLATE.md` and `_instructions/INGESTING_HIGH_RES_PHOTOS.md`)
    - _instructions/ADDING_PLATE.md (closest analog operator runbook for OSD+bunny.net workflows)
    - _instructions/INGESTING_HIGH_RES_PHOTOS.md (closest analog for "real env vars, real verification" runbook style)
    - .claude/skills/spike-findings-pnwmoths/SKILL.md (project-wide v2.2 constraints — folder convention, manifest as source of truth)
  </read_first>
  <action>
    Create `.planning/phases/28-end-to-end-vertical-slice-pilot-one-species/TILE-RECIPE.md` as an
    operator-runbook-style markdown document with H1 `# Task: Produce DZI tiles for the pilot species`.
    Borrow the section structure from `_instructions/ADDING_PLATE.md`: `## What This Changes`, `## Prerequisites`,
    `## Pick the pilot species`, `## Steps`, `## Verify`, `## When Things Go Wrong`.

    Required content:
    - `## Prerequisites`: vips/libvips installed locally (homebrew `brew install vips` or apt `sudo apt install libvips-tools`); operator has TIFF files for the candidate species on local disk; the species' slug + manifest rows are known (point operator at `data/species-photos-manifest.csv`).
    - `## Pick the pilot species`: state the constraint (clean-match bucket, 1–3 specimens, both D and V views). Surface the three single-specimen candidates from RESEARCH.md (`abagrotis-apposita`, `abagrotis-dickeli`, `abrostola-urentis`) and the 3-file candidate `feltia-herilis`. State the operator should pick based on which TIFFs they have locally; tell them to record the chosen slug at the top of `PILOT-LESSONS.md` later (Plan 05).
    - `## Steps`: one copy-pasteable `vips dzsave` invocation per specimen+view pair. The output prefix MUST be `{specimen_id}-{view}` (with literal hyphen). The required flags are exactly: `--tile-size 256`, `--overlap 1`, `--suffix .jpg[Q=85]`, `--layout dz`. The output argument is the prefix path (e.g. `/tmp/tiles/{slug}/A-D`); vips writes `{prefix}.dzi` + `{prefix}_files/`. Show a shell loop / per-pair pattern (NOT a node script — this is a one-shot operator workflow).
    - `## Verify`: per pair, the operator should confirm `{prefix}.dzi` exists, the `{prefix}_files/0/0_0.jpg` exists, and the `.dzi` descriptor's `Format` attribute is `jpg`. Provide the `grep` / `head` command to inspect the descriptor (e.g. `head -20 /tmp/tiles/{slug}/A-D.dzi` and assert `Format="jpg"`).
    - `## When Things Go Wrong`: cover (a) vips not installed → install instructions; (b) wrong output extension (.png instead of .jpg) — fix by re-running with explicit `--suffix .jpg[Q=85]`; (c) operator typo'd the prefix — vips will write to whatever the last path component is, so the prefix MUST match the upload URL convention (`{specimen_id}-{view}` exact).
    - Do NOT include the bunny.net upload step (that is Plan 03's domain) — point to Plan 03 / `_instructions/ADDING_PHOTO.md`-style follow-up.
    - Do NOT include OSD wiring instructions — out of scope here.
    - Add a closing `## After this step` note: "Tile pyramids are local-only at this point. Next: Plan 03 (operator upload) moves them to bunny.net Storage; Plan 04 wires OSD; Plan 05 hand-edits `data/species-photos.json` and captures lessons in PILOT-LESSONS.md."
    Per RESEARCH.md `## Assumptions Log` A1, libvips is not installed on this researcher machine — explicitly note `[ASSUMED]` next to any default-flag claim that the operator should verify the first time they run the recipe.
  </action>
  <verify>
    <automated>test -f .planning/phases/28-end-to-end-vertical-slice-pilot-one-species/TILE-RECIPE.md &amp;&amp; grep -q "vips dzsave" .planning/phases/28-end-to-end-vertical-slice-pilot-one-species/TILE-RECIPE.md &amp;&amp; grep -q -- "--tile-size 256" .planning/phases/28-end-to-end-vertical-slice-pilot-one-species/TILE-RECIPE.md &amp;&amp; grep -q -- "--suffix .jpg\[Q=85\]" .planning/phases/28-end-to-end-vertical-slice-pilot-one-species/TILE-RECIPE.md &amp;&amp; grep -q -- "--layout dz" .planning/phases/28-end-to-end-vertical-slice-pilot-one-species/TILE-RECIPE.md &amp;&amp; grep -q "{specimen_id}-{view}" .planning/phases/28-end-to-end-vertical-slice-pilot-one-species/TILE-RECIPE.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/28-end-to-end-vertical-slice-pilot-one-species/TILE-RECIPE.md` exists and is at least 40 lines.
    - File contains all four required vips flags: `--tile-size 256`, `--overlap 1`, `--suffix .jpg[Q=85]`, `--layout dz`.
    - File contains the literal token `{specimen_id}-{view}` documenting the required output-prefix convention.
    - File has H2 sections `## Prerequisites`, `## Pick the pilot species`, `## Steps`, `## Verify`, `## When Things Go Wrong`.
    - File mentions the three single-specimen candidate slugs (`abagrotis-apposita`, `abagrotis-dickeli`, `abrostola-urentis`) and `feltia-herilis` for the 3-file case.
    - File does NOT contain a `curl` / bunny.net upload step (verify with `! grep -q 'curl.*storage.bunnycdn.com' TILE-RECIPE.md`); upload is Plan 03's scope.
  </acceptance_criteria>
  <done>TILE-RECIPE.md is committed, contains the four required vips flags, and a fresh operator could read it end-to-end without consulting RESEARCH.md.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Operator selects pilot species and produces DZI tile pyramids locally</name>
  <files>(operator-local working directory; no committed files)</files>
  <read_first>
    - .planning/phases/28-end-to-end-vertical-slice-pilot-one-species/TILE-RECIPE.md (just authored in Task 1)
    - data/species-photos-manifest.csv (filter by match_bucket="clean-match" and both D+V views present, 1–3 specimens)
  </read_first>
  <what-built>
    Task 1 authored TILE-RECIPE.md. Now the operator runs it: pick one species, run vips dzsave once per (specimen_id, view) pair, and produce local tile pyramids ready for Plan 03's upload.
  </what-built>
  <how-to-verify>
    1. Open TILE-RECIPE.md and confirm vips/libvips is installed locally (`vips --version` returns 8.x or later).
    2. Open `data/species-photos-manifest.csv` and pick one species matching the constraint: `match_bucket=clean-match`, both D and V views, 1–3 specimens, TIFFs available on your local disk. Note the chosen slug here (it will be needed for Plan 05).
    3. For each (specimen_id, view) pair of the chosen species (typically 2–6 pairs total), run the documented `vips dzsave` invocation, substituting the source TIFF path and output prefix `{slug}/{specimen_id}-{view}`.
    4. Verify each pair: `{prefix}.dzi` exists; `{prefix}_files/0/0_0.jpg` exists; `head -20 {prefix}.dzi` shows `Format="jpg"`.
    5. Report back: chosen species slug, number of pairs tiled, approximate total tile count and disk size (for Phase 30 footprint extrapolation). Note any vips warnings or unexpected output.
  </how-to-verify>
  <resume-signal>Type "done — pilot slug: {slug}, N pairs tiled, ~{X} MB total" or describe any blockers (e.g. vips install failure, no clean-match species with local TIFFs).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator host → local disk | Tile output goes to operator-controlled tmp dir; nothing committed; no auth surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01-01 | Information Disclosure | TIFF source files on operator hardware | accept | Operator-local; data is public moth specimen photography; no PII; no credentials referenced in TILE-RECIPE.md |
| T-28-01-02 | Tampering | vips invocation flags | mitigate | Recipe pins the exact flags (`--tile-size 256 --overlap 1 --suffix .jpg[Q=85] --layout dz`) so output is reproducible; operator records actual flags used in PILOT-LESSONS.md (Plan 05) |
| T-28-01-03 | Denial of Service | Operator disk exhaustion from tile output | accept | One species × 2–6 pairs; ~tens of MB total; trivial relative to source TIFFs already on disk |

Block-severity threshold: high. All threats here are low; documented for completeness.
</threat_model>

<verification>
- TILE-RECIPE.md committed (verified by Task 1 automated check).
- Operator confirms local tile production via checkpoint (Task 2).
- No code changes in this plan — no `npm test` impact expected.
</verification>

<success_criteria>
- Recipe document exists, is operator-runnable, and pins the vips parameters.
- Operator has local tile pyramids for one pilot species ready for Plan 03 upload.
- Pilot species slug is recorded for downstream plans (operator reports it at checkpoint resume).
</success_criteria>

<output>
Create `.planning/phases/28-end-to-end-vertical-slice-pilot-one-species/28-01-SUMMARY.md` when done.
Summary must record: the pilot species slug (from operator checkpoint), how many (specimen_id, view) pairs were tiled, and any vips surprises (default tile-format quirks, file-permission issues, etc.) that should inform Phase 29's committed config.
</output>
