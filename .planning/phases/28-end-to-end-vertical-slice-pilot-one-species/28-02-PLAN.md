---
phase: 28
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - data/species-photos.json
  - src/_data/species-photos.js
autonomous: true
requirements:
  - PILOT-01
must_haves:
  truths:
    - "data/species-photos.json exists as a committed JSON file with an empty object literal `{}` (no entries yet — real entry is hand-edited in Plan 05)"
    - "src/_data/species-photos.js exports the JSON contents as a by-slug map; missing-file path soft-fails with a console.warn (does not crash the build)"
    - "Running `npm run build:eleventy` still produces 1,364 species pages and exits 0 (no regression introduced by the new data file)"
    - "The Eleventy template variable `speciesPhotos` is reachable from Nunjucks templates (verified via a one-line probe in species.njk that is reverted, OR by reading the data file's export shape)"
  artifacts:
    - path: "data/species-photos.json"
      provides: "Committed JSON manifest keyed by species slug; entries follow the {high_res_available: bool, specimens: [{specimen_id, view, tiles_path}]} schema; Phase 31 replaces with manifest-derived version"
      contains: "{}"
    - path: "src/_data/species-photos.js"
      provides: "Eleventy 11ty-data loader (ESM) that reads data/species-photos.json and returns the by-slug object; soft-fails to {} when file missing"
      exports: ["default"]
      min_lines: 15
  key_links:
    - from: "src/_data/species-photos.js"
      to: "data/species-photos.json"
      via: "readFile(new URL('../../data/species-photos.json', import.meta.url).pathname, 'utf8')"
      pattern: "new URL\\('\\.\\./\\.\\./data/species-photos\\.json'"
    - from: "Eleventy build"
      to: "Nunjucks templates via {{ speciesPhotos }} global"
      via: "11ty-data file convention (kebab filename → camelCase template variable)"
      pattern: "speciesPhotos"
---

<objective>
Create the data-layer plumbing for high-res species entries — the JSON manifest file and the
Eleventy `_data/` loader that exposes it to Nunjucks templates — WITHOUT yet adding any real
entries (Plan 05 hand-edits the real pilot entry) and WITHOUT modifying templates or the
slideshow component (Plans 04 and 05 own those).

Purpose: De-risk the data-layer integration seam early. If the 11ty filename → template
variable mapping fails (kebab → camelCase open question in PATTERNS.md), surface it now
before Plan 04 depends on it. The empty-object shape proves the build is still green and
the loader is wired before any real data flows.

Output:
- `data/species-photos.json` (committed; empty object `{}`)
- `src/_data/species-photos.js` (committed; ESM loader)
- Build still produces 1,364 species pages
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/28-end-to-end-vertical-slice-pilot-one-species/28-RESEARCH.md
@.planning/phases/28-end-to-end-vertical-slice-pilot-one-species/28-PATTERNS.md
@.claude/skills/spike-findings-pnwmoths/SKILL.md
@CLAUDE.md

<interfaces>
Existing patterns this plan mirrors verbatim. Use these directly — no codebase exploration needed.

From `src/_data/plates.js` (Phase 18 — closest OSD-manifest analog):
```javascript
import { readdir, readFile } from 'node:fs/promises';
import { existsSync } from 'node:fs';
import { join } from 'node:path';

const MANIFEST_PATH = new URL('../../data/plates.json', import.meta.url).pathname;

export default async function () {
  if (!existsSync(PLATES_Z_SOURCE)) {
    if (existsSync(MANIFEST_PATH)) {
      const raw = JSON.parse(await readFile(MANIFEST_PATH, 'utf8'));
      return raw.map(...);
    }
    console.warn(`[plates] Source not found: ${PLATES_Z_SOURCE} — skipping plate data`);
    return [];
  }
  // ...
}
```

From `eleventy.config.js` (line 24 onward — relevant: `dir.data: "_data"`):
- 11ty-data files live in `src/_data/`
- Filename → variable: `plates.js` → `{{ plates }}`, `images.js` → `{{ images }}`
- For kebab names: `species-photos.js` → `{{ speciesPhotos }}` (Eleventy camelCases automatically). PATTERNS.md flags this for verification — Plan 04 will rely on `speciesPhotos`.

From `package.json`:
- `"type": "module"` — use ESM imports.
- Test command: `node --test ... src/_lib/*.test.js src/components/*.test.js` (no glob for `src/_data/*.test.js`; existing 11ty-data files have no unit tests; this plan does NOT add one).

Phase 28 JSON schema (from RESEARCH.md Pattern 2 — keyed by slug, NOT array):
```json
{
  "{species-slug}": {
    "high_res_available": true,
    "specimens": [
      { "specimen_id": "A", "view": "D", "tiles_path": "species-tiles/{slug}/A-D" },
      { "specimen_id": "A", "view": "V", "tiles_path": "species-tiles/{slug}/A-V" }
    ]
  }
}
```
This plan ships the file with `{}` (no entries). Plan 05 adds the real entry.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create data/species-photos.json with empty object literal</name>
  <files>data/species-photos.json</files>
  <read_first>
    - data/plates.json (committed JSON manifest analog — confirms 2-space indentation and trailing newline conventions)
    - .planning/phases/28-end-to-end-vertical-slice-pilot-one-species/28-PATTERNS.md (section: `### data/species-photos.json (committed JSON data manifest)`)
    - .planning/phases/28-end-to-end-vertical-slice-pilot-one-species/28-RESEARCH.md (Pattern 2 — schema)
  </read_first>
  <action>
    Create `data/species-photos.json` containing exactly the JSON literal `{}` followed by a trailing newline (two characters total before the newline). No comments — JSON doesn't support them; document the schema in PATTERNS.md / RESEARCH.md, not in the file itself. 2-space indentation convention applies (irrelevant for empty object but established for when Plan 05 adds an entry).
    The file MUST be valid JSON parseable by `JSON.parse`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const d=JSON.parse(fs.readFileSync('data/species-photos.json','utf8')); if(typeof d !== 'object' || d === null || Array.isArray(d)) {process.exit(1)} console.log('OK: empty object,', Object.keys(d).length, 'keys')"</automated>
  </verify>
  <acceptance_criteria>
    - File `data/species-photos.json` exists.
    - File parses as valid JSON via `JSON.parse`.
    - Parsed value is a plain object (not array, not null), with 0 keys.
    - File ends with a trailing newline.
  </acceptance_criteria>
  <done>data/species-photos.json exists, contains `{}` + newline, parses to an empty object.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create src/_data/species-photos.js Eleventy data loader</name>
  <files>src/_data/species-photos.js</files>
  <read_first>
    - src/_data/plates.js (closest analog — lines 149-167 manifest-only branch; copy the `MANIFEST_PATH` + `existsSync` + `JSON.parse(await readFile)` idiom exactly)
    - src/_data/images.js (by-slug shape reference — confirms returning a plain object keyed by slug is the project pattern)
    - .planning/phases/28-end-to-end-vertical-slice-pilot-one-species/28-PATTERNS.md (section: `### src/_data/species-photos.js (new — Eleventy data file)`)
    - package.json (confirm `"type": "module"` — use ESM imports)
    - eleventy.config.js (confirm `dir.data` setting)
  </read_first>
  <action>
    Create `src/_data/species-photos.js` as an ESM module with a default async export following the
    `src/_data/plates.js` manifest-only-branch idiom:
    - Import `readFile` from `node:fs/promises` and `existsSync` from `node:fs`.
    - Define `MANIFEST_PATH = new URL('../../data/species-photos.json', import.meta.url).pathname` (file-URL resolution — Phase 18 plates.js pattern).
    - Export `default async function ()` that:
      - If `!existsSync(MANIFEST_PATH)`: log `console.warn('[species-photos] Manifest not found: <path> — no high-res species')` and return `{}`. (Soft-fail; matches plates.js missing-source behavior; does not crash the build.)
      - Otherwise: return `JSON.parse(await readFile(MANIFEST_PATH, 'utf8'))`.
    - No transform of the parsed object — the JSON shape IS the data shape (in contrast to plates.js which maps fields). This matches RESEARCH.md Pattern 2.
    - No top-level `console.log`; warn only when file is missing.
    Do NOT add a test file. PATTERNS.md (`### Test conventions`) confirms no existing 11ty-data file has unit tests; the loader's correctness is verified by the build still producing 1364 pages (Task 3).
  </action>
  <verify>
    <automated>node -e "const fn=(await import('./src/_data/species-photos.js')).default; const result=await fn(); if(typeof result !== 'object' || result === null) process.exit(1); console.log('OK: loader returned object,', Object.keys(result).length, 'keys')" --input-type=module 2>&amp;1 || node --input-type=module -e "const m = await import('./src/_data/species-photos.js'); const r = await m.default(); if (typeof r !== 'object' || r === null) process.exit(1); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File `src/_data/species-photos.js` exists and is at least 15 lines.
    - File uses ESM imports (`import { readFile } from 'node:fs/promises'`).
    - File defines `MANIFEST_PATH` using `new URL('../../data/species-photos.json', import.meta.url).pathname` (verify by `grep -q "new URL.*species-photos.json.*import.meta.url" src/_data/species-photos.js`).
    - File has a default async export (verify by `grep -q "export default async function" src/_data/species-photos.js`).
    - When invoked, the default export returns a plain object (the empty `{}` from Task 1's file).
    - When `data/species-photos.json` is renamed/missing, the loader returns `{}` and emits a `console.warn` containing `[species-photos]` (verify by temporarily renaming the JSON, invoking, restoring — OR by code review against the analog).
  </acceptance_criteria>
  <done>src/_data/species-photos.js exists, mirrors src/_data/plates.js manifest-load idiom, returns the JSON contents (empty object at this point), soft-fails on missing file.</done>
</task>

<task type="auto">
  <name>Task 3: Build verification — no regression to 1,364 species pages</name>
  <files>(no files modified — verification only)</files>
  <read_first>
    - .planning/phases/28-end-to-end-vertical-slice-pilot-one-species/28-RESEARCH.md (section: `## Validation Architecture` — page count target 1,364)
    - .planning/ROADMAP.md (Phase 17 success criteria: "1,364 species pages")
    - package.json (the `build:eleventy` script)
  </read_first>
  <action>
    Run `npm run build:data` then `npm run build:eleventy` from the repo root. Both must exit 0.
    After the build, count generated species pages: `find _site/species -name 'index.html' | wc -l` must equal 1364.
    Then run `npm test` to confirm no test broke. The new `_data` loader has no tests; the existing suite should pass unchanged.
    If the species page count differs from 1364, the new data loader is interfering with Eleventy's page generation — diagnose by reading the build output for warnings mentioning `species-photos`, then revisit Task 2 (likely cause: throwing where it should soft-fail, or returning a shape that confuses Eleventy's data merging).
  </action>
  <verify>
    <automated>npm run build:data &amp;&amp; npm run build:eleventy &amp;&amp; test "$(find _site/species -name 'index.html' | wc -l)" = "1364" &amp;&amp; npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build:eleventy` exits 0.
    - `find _site/species -name 'index.html' | wc -l` returns exactly `1364`.
    - `npm test` exits 0 (no test regressions from the new data loader).
    - Build output does NOT contain an Eleventy error mentioning `species-photos` (a warning about missing manifest is acceptable only if the JSON file was deleted; with Task 1's `{}` in place, no warning is expected).
  </acceptance_criteria>
  <done>Full build is green, species page count is unchanged at 1,364, test suite passes. Data-layer plumbing is wired without disturbing any existing behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time → committed JSON | `data/species-photos.json` is operator-authored, committed to git, read at build time; same trust model as `data/plates.json` |
| 11ty-data loader → Nunjucks | Loader output flows into template context; no user-supplied input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-02-01 | Tampering | data/species-photos.json content | mitigate | File is operator-authored and committed; reviewed via PR; OSD `tileSources` URL string in Plan 04 does not execute code. RESEARCH.md V5 (Input Validation) confirms no user-supplied input reaches OSD config. |
| T-28-02-02 | Denial of Service | Build crash on malformed JSON | mitigate | Loader uses `JSON.parse` which throws on invalid JSON. Task 1's verify command parses the file before commit; Task 3 runs the full build before plan completion, surfacing any parse error immediately. |
| T-28-02-03 | Information Disclosure | Missing-manifest warning leaks filesystem path | accept | `console.warn` includes a fully-resolved path; build logs are operator-only; consistent with existing `[plates]` warning pattern in src/_data/plates.js line 162 |

Block-severity threshold: high. All threats low; no new attack surface introduced.
</threat_model>

<verification>
- Task 1 automated check: JSON parses to empty object.
- Task 2 automated check: loader returns an object when invoked.
- Task 3 automated checks: build exits 0, page count = 1364, `npm test` passes.
</verification>

<success_criteria>
- `data/species-photos.json` exists (committed) with empty object content.
- `src/_data/species-photos.js` exists (committed) and follows the plates.js manifest-load idiom.
- Full Eleventy build is green; 1,364 species pages produced; existing tests pass.
- The `speciesPhotos` Eleventy template variable is reachable (validated transitively by Plan 04, which is the first consumer).
</success_criteria>

<output>
Create `.planning/phases/28-end-to-end-vertical-slice-pilot-one-species/28-02-SUMMARY.md` when done.
Summary must record: file paths created, exact loader signature, and confirmation that page count is 1,364 (matching Phase 17's v1.4 baseline).
</output>
