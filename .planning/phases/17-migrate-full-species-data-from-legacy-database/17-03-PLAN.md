---
phase: 17-migrate-full-species-data-from-legacy-database
plan: 03
type: execute
wave: 2
depends_on:
  - 17-02
files_modified:
  - scripts/build-data.test.js
autonomous: true
requirements:
  - SC-3
  - SC-4

must_haves:
  truths:
    - "npm test passes with full dataset (all tests green)"
    - "npm run build:data completes without errors"
    - "npm run build completes without errors"
    - "build-data.test.js tests do not hardcode stub-only assumptions"
  artifacts:
    - path: "scripts/build-data.test.js"
      provides: "Updated integration tests compatible with full dataset"
  key_links:
    - from: "scripts/build-data.test.js"
      to: "data/species.csv"
      via: "validateCsv() call + execSync build-data.js"
      pattern: "validateCsv.*species\\.csv"
    - from: "scripts/build-data.test.js"
      to: "data/parquet/"
      via: "existsSync assertions on parquet output"
      pattern: "existsSync.*parquet"
---

<objective>
Run the full test suite and build pipeline with the migrated dataset. Fix any build-data.test.js assertions that fail because they were written against the 11-row stub. Confirm SC-3 (npm run build) and SC-4 (npm test) both pass.

Purpose: Close the migration with a verified, working build. The migration is only complete when the full pipeline (data → Parquet → Eleventy → Pagefind → link check) runs clean.
Output: Updated build-data.test.js (if needed), green npm test, green npm run build.
</objective>

<execution_context>
@/Users/rainhead/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rainhead/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/rainhead/dev/pnwmoths/.planning/phases/17-migrate-full-species-data-from-legacy-database/17-PATTERNS.md
@/Users/rainhead/dev/pnwmoths/.planning/phases/17-migrate-full-species-data-from-legacy-database/17-RESEARCH.md
</context>

<interfaces>
<!-- Key context the executor needs about what may have changed. -->

From PATTERNS.md "Update Required: build-data.test.js After Migration":
```javascript
// These two assertions in build-data.test.js (lines ~158-166) reference stub slugs.
// Both acronicta-americana and hyles-lineata exist in the full DB, so they MAY still pass.
// Verify before touching:
assert.ok(
  existsSync(resolve(ROOT, 'data/parquet/acronicta-americana/records.parquet')),
  'data/parquet/acronicta-americana/records.parquet should exist'
);
assert.ok(
  existsSync(resolve(ROOT, 'data/parquet/hyles-lineata/records.parquet')),
  'data/parquet/hyles-lineata/records.parquet should exist'
);
```

From RESEARCH.md Pitfall 7:
- Integration tests use process.chdir() for alternate data dirs
- Unit tests call validateCsv() with the real data/species.csv path
- Both acronicta-americana and hyles-lineata are real PNW species present in the full DB

npm test command (from package.json):
  node --test eleventy.config.test.js scripts/build-data.test.js scripts/check-page-weight.test.js scripts/migrate-species.test.js src/components/*.test.js
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Run npm test — diagnose and fix any stub-specific test failures</name>
  <files>scripts/build-data.test.js</files>
  <read_first>
    - scripts/build-data.test.js — full file; read before touching to understand current assertions
    - .planning/phases/17-migrate-full-species-data-from-legacy-database/17-PATTERNS.md — "Update Required" section for exact lines to check
  </read_first>
  <action>
Run `npm test` to discover which tests fail with the full dataset. Expected outcome: most tests pass; only tests that hardcoded stub-specific row counts or slug names (that don't exist in the full DB) would fail.

**Diagnosis procedure:**
1. Run `npm test 2>&1` and capture output
2. For each failing test, read the assertion to determine if it's stub-specific

**Known safe assertions** (both slugs exist in full DB — do NOT change these):
- `existsSync(resolve(ROOT, 'data/parquet/acronicta-americana/records.parquet'))` — acronicta-americana exists in full DB
- `existsSync(resolve(ROOT, 'data/parquet/hyles-lineata/records.parquet'))` — hyles-lineata exists in full DB

**What may need updating:**
- Any assertion that checks an exact row count that matches the stub (e.g. `assert.strictEqual(rows.length, 11)` for species or `assert.strictEqual(rows.length, 667)` for records)
- Any test that expects a specific species to NOT exist in species.csv (would now exist)

**What must NOT be changed:**
- validateCsv() tests (column presence checks) — these are schema tests, not data tests
- The build pipeline integration test structure — only update expected values, not test logic

**Update rule:** If a test asserts an exact count that matches stub size, replace with a range assertion:
- Old: `assert.strictEqual(rows.length, 11)` → New: `assert.ok(rows.length >= 1300, ...)`
- Old: `assert.strictEqual(rows.length, 667)` → New: `assert.ok(rows.length >= 3000, ...)`

After fixing, run `npm test` again to confirm green.

If ALL tests pass on the first run without any changes, make no edits to build-data.test.js and document that in the summary.
  </action>
  <verify>
    <automated>npm test 2>&1; echo "Exit: $?"</automated>
  </verify>
  <done>
    - npm test exits 0
    - All tests in build-data.test.js pass
    - All tests in migrate-species.test.js pass
    - All tests in eleventy.config.test.js pass
    - All component tests pass
  </done>
  <acceptance_criteria>
    - `npm test` exits 0
    - `npm test 2>&1 | grep -E "^(not ok|Error)"` returns no output (no failures)
    - If build-data.test.js was modified: `grep "rows.length >= 1300\|rows.length >= 3000" scripts/build-data.test.js` matches any updated assertions
    - If build-data.test.js was NOT modified: document in summary that no changes were needed
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Run npm run build — verify full pipeline with full dataset</name>
  <files>scripts/migrate-species.js (conditionally modified if build:data reports data quality issues requiring script fixes; no writes in happy path)</files>
  <read_first>
    - package.json — build script sequence to understand what runs
  </read_first>
  <action>
Run the full build pipeline to confirm SC-3. The build sequence is:
```
npm run build:data      # DuckDB validation + Parquet export
npm run build:eleventy  # Eleventy static site generation
npm run build:copy-parquet
npm run build:copy-images
npm run build:species-states
npm run build:pagefind
npm run build:validate-links  # lychee link checker
npm run build:check-weight
```

Run: `npm run build`

If it fails, diagnose the failing step:
- `build:data` failure → data quality issue; run `npm run build:data 2>&1` alone to see DuckDB validation errors
- `build:eleventy` failure → template error with new data (e.g. missing field, null in template); check Eleventy error output
- `build:validate-links` → link checker finds broken similar_species links; fix in migrate-species.js (but note: lychee excludes image extensions per lychee.toml, so CDN image 404s won't surface here)
- `build:check-weight` → page weight over threshold; not expected with species data change

The build will take longer than with stub data (1,374 species pages instead of 11). Expect 2-5 minutes total.

If build succeeds: confirm _site/ contains ≥ 1,300 species HTML pages:
```bash
ls _site/species/ | wc -l
```

Document the final build output in the summary.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20; echo "Build exit: $?"</automated>
  </verify>
  <done>
    - npm run build exits 0
    - _site/ contains species pages for the full dataset
    - No errors in any build step
  </done>
  <acceptance_criteria>
    - `npm run build` exits 0
    - `npm run build:data` exits 0 (DuckDB validation passes with full dataset)
    - `ls _site/species/ | wc -l` shows ≥ 1300 (one directory per species)
    - `npm run build 2>&1 | grep -i "error\|failed\|exception"` returns no output (clean build)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSV→DuckDB | build:data reads species.csv and records.csv into DuckDB; validation queries run against full dataset |
| DuckDB→Parquet | ~1,374 species get individual Parquet files written to data/parquet/ |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-05 | Denial of Service | Build time with 1,374 species (vs 11 stub) | accept | Build takes longer but completes; not a production runtime concern |
| T-17-06 | Tampering | Test suite modifications to suppress failures | mitigate | Only update exact count assertions; never delete tests or lower thresholds below full-data expected values |
</threat_model>

<verification>
```bash
npm test
npm run build
ls _site/species/ | wc -l
```

All three commands must succeed. _site/species/ must have ≥ 1,300 entries.
</verification>

<success_criteria>
- npm test exits 0 — all tests green including migrate-species.test.js
- npm run build exits 0 — all pipeline steps complete without error
- _site/species/ contains ≥ 1,300 species HTML pages
- Phase 17 is complete: full production dataset in data/species.csv and data/records.csv
</success_criteria>

<output>
After completion, create .planning/phases/17-migrate-full-species-data-from-legacy-database/17-03-SUMMARY.md
</output>
