---
phase: 40-filter-logic-tdd-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/schemas.ts
  - src/types/events.ts
autonomous: true
requirements: [MATCH-01, MATCH-02, MATCH-03]
must_haves:
  truths:
    - "KeyMatrixMetaSchema exists and validates { totalKeySpecies, matchedSpecies, unmatchedSpecies, generatedAt }"
    - "KeyMatrixSchema includes a required meta field typed as KeyMatrixMeta"
    - "KeyFilterChangeDetail interface exists and pnwm-key-filter-change is typed on HTMLElementEventMap"
    - "FilterChangeDetail is unchanged (event-bus isolation preserved)"
    - "MATCH-01/02/03 Phase 39 artifacts remain green (key-matrix.json + coverage report unchanged here)"
  artifacts:
    - path: "src/types/schemas.ts"
      provides: "KeyMatrixMetaSchema, KeyMatrixMeta type, meta field on KeyMatrixSchema"
      contains: "KeyMatrixMetaSchema"
    - path: "src/types/events.ts"
      provides: "KeyFilterChangeDetail interface + pnwm-key-filter-change event map entry"
      contains: "KeyFilterChangeDetail"
  key_links:
    - from: "src/types/schemas.ts"
      to: "KeyMatrixSchema"
      via: "meta field references KeyMatrixMetaSchema"
      pattern: "meta:\\s*KeyMatrixMetaSchema"
    - from: "src/types/events.ts"
      to: "HTMLElementEventMap"
      via: "pnwm-key-filter-change augmentation"
      pattern: "pnwm-key-filter-change"
---

<objective>
Add the two new type artifacts that Phases 40–42 depend on, and confirm the Phase 39
data artifacts (MATCH-01/02/03) remain green.

This plan establishes the type contracts consumed by the rest of the phase:
- `KeyMatrixMetaSchema` (the one SC4 schema not built in Phase 39) plus a `meta` field on
  `KeyMatrixSchema`, so `build-key.ts` (Plan 02) can emit meta and the browser load-guard
  validates it for free.
- `KeyFilterChangeDetail` event detail + `pnwm-key-filter-change` augmentation, the transport
  the Phase 41 panel will dispatch and the Phase 42 grid will listen for.

Per the event-bus isolation rule, `FilterChangeDetail` is NOT touched — the new detail type is
fully separate.

Purpose: Provide the stable type surface so the filter-logic plan (03) and the build-key meta
plan (02) compile and validate against a single source of truth.
Output: Updated `src/types/schemas.ts` and `src/types/events.ts`; both tsconfigs typecheck clean.
</objective>

<artifacts_this_phase_produces>
This plan introduces these NEW symbols (consumed by Plans 02–03 and Phases 41–42):
- `KeyMatrixMetaSchema` (zod/mini schema) — `src/types/schemas.ts`
- `KeyMatrixMeta` (`z.infer` type alias) — `src/types/schemas.ts`
- `meta` field on `KeyMatrixSchema` (typed `KeyMatrixMetaSchema`) — `src/types/schemas.ts`
- `KeyFilterChangeDetail` (interface) — `src/types/events.ts`
- `pnwm-key-filter-change` event key on `HTMLElementEventMap` — `src/types/events.ts`
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/40-filter-logic-tdd-contract/40-CONTEXT.md
@.planning/phases/40-filter-logic-tdd-contract/40-RESEARCH.md
@.planning/phases/40-filter-logic-tdd-contract/40-PATTERNS.md
@src/types/schemas.ts
@src/types/events.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add KeyMatrixMetaSchema + meta field to schemas.ts</name>
  <files>src/types/schemas.ts</files>
  <read_first>
    - src/types/schemas.ts (the existing Key Matrix block, lines 155-183: CharacterSchema,
      KeySpeciesSchema, KeyMatrixSchema; mirror this declaration style)
    - 40-RESEARCH.md section "Pattern 3: KeyMatrixMetaSchema" (field set + rationale)
    - 40-PATTERNS.md section "src/types/schemas.ts" (exact placement; zod/mini import already at line 6)
  </read_first>
  <action>
    Using the existing `import * as z from 'zod/mini'` already at the top of the file, add a new
    exported schema `KeyMatrixMetaSchema = z.object({ ... })` with four fields:
    `totalKeySpecies` (z.number, 1228 — all key.csv binomials incl. unmatched),
    `matchedSpecies` (z.number, 1192 — species resolved to slugs), `unmatchedSpecies`
    (z.number, 36), and `generatedAt` (z.string, ISO 8601 timestamp). Immediately after the
    schema constant, declare `export type KeyMatrixMeta = z.infer<typeof KeyMatrixMetaSchema>`
    (project convention: type alias paired right after every schema). Place these immediately
    BEFORE the existing `KeyMatrixSchema` so the schema is in scope. Then modify
    `KeyMatrixSchema` to add `meta: KeyMatrixMetaSchema` as the FIRST property (before
    `characters`). Do NOT change the existing `characters`/`species`/`matrix` fields or the
    `KeyMatrix` type export. Do NOT touch CharacterSchema or KeySpeciesSchema. Keep the
    explanatory comments above KeyMatrixSchema accurate. This adds SC4's remaining schema and
    plumbs the "showing N of 1,228" affordance.
  </action>
  <verify>
    <automated>cd /Users/rainhead/dev/pnwmoths && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'KeyMatrixMetaSchema' src/types/schemas.ts` returns at least 2 (schema decl + reference in KeyMatrixSchema)
    - `grep -E 'export type KeyMatrixMeta = z.infer' src/types/schemas.ts` matches
    - `grep -E 'meta:\s*KeyMatrixMetaSchema' src/types/schemas.ts` matches (meta field present on KeyMatrixSchema)
    - KeyMatrixMetaSchema declares exactly: totalKeySpecies, matchedSpecies, unmatchedSpecies, generatedAt
    - `npm run typecheck` exits 0 (both browser + node tsconfigs)
    - CharacterSchema and KeySpeciesSchema declarations are unchanged
  </acceptance_criteria>
  <done>KeyMatrixMetaSchema + KeyMatrixMeta type exist; KeyMatrixSchema requires a meta field; typecheck passes both configs.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add KeyFilterChangeDetail + pnwm-key-filter-change to events.ts</name>
  <files>src/types/events.ts</files>
  <read_first>
    - src/types/events.ts (the full file, lines 1-22: existing FilterChangeDetail + the single
      declare global HTMLElementEventMap block — mirror exactly; do NOT extend FilterChangeDetail)
    - 40-RESEARCH.md section "Pattern 4: pnwm-key-filter-change Event Type" and Pitfall 4
      (verbatimModuleSyntax module-boundary requirement)
    - 40-PATTERNS.md section "src/types/events.ts" (separate interface; augment the existing declare global block)
  </read_first>
  <action>
    Add a NEW exported interface `KeyFilterChangeDetail` with three fields: `matchedSlugs`
    (string[] — current matching species slugs, mirrors computeMatching's return), `count`
    (number — matchedSlugs.length convenience for the counter), and `hasSelection` (boolean —
    true iff any question has at least one selected state; distinguishes empty-filter-result vs
    no-filters-yet UX). Do NOT extend, modify, or inherit from `FilterChangeDetail` (event-bus
    isolation, PITFALLS Pitfall 12 — extending it would leak key-filter events to the
    occurrence-filter listeners). Add a single new entry to the EXISTING `declare global`
    `HTMLElementEventMap` block: `'pnwm-key-filter-change': CustomEvent<KeyFilterChangeDetail>`.
    Do NOT create a second declare global block and do NOT alter the existing
    `'pnwm-filter-change'` entry. The existing export already makes the file a module, satisfying
    verbatimModuleSyntax — no structural change is required.
  </action>
  <verify>
    <automated>cd /Users/rainhead/dev/pnwmoths && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E 'export interface KeyFilterChangeDetail' src/types/events.ts` matches
    - KeyFilterChangeDetail declares exactly: matchedSlugs (string[]), count (number), hasSelection (boolean)
    - `grep -c 'pnwm-key-filter-change' src/types/events.ts` returns at least 1 (event-map entry present)
    - The existing `'pnwm-filter-change': CustomEvent<FilterChangeDetail>` line is still present and unchanged
    - `grep -c 'declare global' src/types/events.ts` returns exactly 1 (no second global block)
    - FilterChangeDetail interface body is byte-for-byte unchanged
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>KeyFilterChangeDetail exists; pnwm-key-filter-change is typed on HTMLElementEventMap; FilterChangeDetail untouched; typecheck passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Confirm MATCH-01/02/03 Phase 39 artifacts remain green</name>
  <files>(no files modified — verification only)</files>
  <read_first>
    - .planning/REQUIREMENTS.md (MATCH-01/02/03 — already Complete via Phase 39; this task asserts they stay green)
    - 40-CONTEXT.md section "Already delivered by Phase 39 (do NOT redo)"
  </read_first>
  <action>
    Do NOT re-build or re-plan the data pipeline. This is a confirmation-only task: assert that
    Phase 39's deliverables backing MATCH-01 (slug resolution), MATCH-02 (coverage report), and
    MATCH-03 (nav-image join) are still present and pass. Run the build-key test suite and
    confirm `data/key-matrix.json` and `data/key-coverage-report.json` exist with the expected
    shape (1,192 matched species each carrying slug + nav_image; coverage report listing
    unmatched binomials). No code changes. If any assertion fails, STOP and report — do not
    attempt to regenerate here; the regen belongs to Plan 02.
  </action>
  <verify>
    <automated>cd /Users/rainhead/dev/pnwmoths && node --test scripts/build-key.test.ts && test -f data/key-coverage-report.json && node -e "const d=require('./data/key-matrix.json'); const ok=d.species.length===1192 && d.species.every(s => ('slug' in s) && ('nav_image' in s)); process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `node --test scripts/build-key.test.ts` exits 0 (Phase 39 slug-resolution + join tests still pass)
    - `data/key-coverage-report.json` exists (MATCH-02)
    - `data/key-matrix.json` has exactly 1,192 species entries, each with a `slug` and `nav_image` field (MATCH-01 + MATCH-03)
    - No source files were modified by this task
  </acceptance_criteria>
  <done>MATCH-01/02/03 confirmed green: build-key tests pass, coverage report present, 1,192 species carry slug + nav_image.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build output → browser load | `data/key-matrix.json` (incl. new `meta`) is parsed by `validateKeyMatrix` at the client boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-01 | Tampering | meta block on KeyMatrix at browser load | mitigate | `KeyMatrixSchema.parse()` (via existing `validateKeyMatrix`) now also validates the four meta fields against `KeyMatrixMetaSchema`; malformed meta fails closed (ASVS V5, trivially covered) |

No other threat surface: these are pure type/schema declarations. No SQL, eval, innerHTML, auth,
crypto, or network input introduced. RESEARCH section "Security Domain" concluded no meaningful
threat surface for this phase.
</threat_model>

<verification>
- `npm run typecheck` exits 0 (browser + node tsconfigs) — the new schema and event types compile.
- `node --test scripts/build-key.test.ts` exits 0 — Phase 39 data artifacts (MATCH-01/02/03) remain green.
- `grep` confirms KeyMatrixMetaSchema, the meta field, KeyFilterChangeDetail, and pnwm-key-filter-change all present; FilterChangeDetail unchanged.
</verification>

<success_criteria>
- `KeyMatrixMetaSchema` + `KeyMatrixMeta` exported; `KeyMatrixSchema` requires `meta`.
- `KeyFilterChangeDetail` exported; `pnwm-key-filter-change` typed on `HTMLElementEventMap`; `FilterChangeDetail` untouched.
- MATCH-01/02/03 confirmed still green (no pipeline rebuild).
- `npm run typecheck` passes with zero errors.
</success_criteria>

<output>
Create `.planning/phases/40-filter-logic-tdd-contract/40-01-SUMMARY.md` when done.
</output>
