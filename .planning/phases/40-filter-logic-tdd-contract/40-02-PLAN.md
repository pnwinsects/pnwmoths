---
phase: 40-filter-logic-tdd-contract
plan: 02
type: execute
wave: 2
depends_on: [40-01]
files_modified:
  - scripts/build-key.ts
  - data/key-matrix.json
autonomous: true
requirements: [IDENT-04]
must_haves:
  truths:
    - "build-key.ts emits a meta block with totalKeySpecies, matchedSpecies, unmatchedSpecies, generatedAt"
    - "Regenerated data/key-matrix.json contains a meta object with those four fields"
    - "data/key-matrix.json still validates against the updated KeyMatrixSchema (incl. meta)"
    - "characters (237), species (1192), and matrix (237) are unchanged by the regeneration"
  artifacts:
    - path: "scripts/build-key.ts"
      provides: "meta field populated at the KeyMatrixSchema.parse() callsite"
      contains: "totalKeySpecies"
    - path: "data/key-matrix.json"
      provides: "meta block on the emitted artifact"
      contains: "meta"
  key_links:
    - from: "scripts/build-key.ts"
      to: "KeyMatrixSchema"
      via: "meta passed into KeyMatrixSchema.parse()"
      pattern: "meta:\\s*\\{"
    - from: "data/key-matrix.json"
      to: "KeyMatrixMetaSchema"
      via: "meta object validated at parse + browser load"
      pattern: "\"meta\""
---

<objective>
Emit the `meta` block from `build-key.ts` and regenerate `data/key-matrix.json` so the artifact
carries `{ totalKeySpecies, matchedSpecies, unmatchedSpecies, generatedAt }` — the data backing
Phase 42's "N of 1,228 species match" affordance.

This is a 5-line addition at the existing `KeyMatrixSchema.parse({ ... })` callsite in
`build-key.ts` `main()` (line 277), using variables already in scope at that point, followed by
a regeneration of the committed artifact via `npm run build:key`.

Purpose: Complete the data side of SC4 — the schema (Plan 01) now has a `meta` field, and the
build emits it. The browser load-guard (`validateKeyMatrix`) inherits validation for free.
Output: Updated `scripts/build-key.ts`; regenerated `data/key-matrix.json` with a meta block.
</objective>

<artifacts_this_phase_produces>
This plan produces:
- The populated `meta` block at the `KeyMatrixSchema.parse()` callsite in `scripts/build-key.ts`
- A regenerated `data/key-matrix.json` whose top-level object now includes a `meta` object with
  `totalKeySpecies`, `matchedSpecies`, `unmatchedSpecies`, `generatedAt`
(The `meta` field on `KeyMatrixSchema` and `KeyMatrixMetaSchema` themselves are produced by Plan 01.)
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/40-filter-logic-tdd-contract/40-CONTEXT.md
@.planning/phases/40-filter-logic-tdd-contract/40-RESEARCH.md
@.planning/phases/40-filter-logic-tdd-contract/40-PATTERNS.md
@scripts/build-key.ts
@src/types/schemas.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Emit meta block from build-key.ts</name>
  <files>scripts/build-key.ts</files>
  <read_first>
    - scripts/build-key.ts (the artifact-build + Zod validation block; the
      `const artifact = KeyMatrixSchema.parse({ characters, species, matrix });` call at line 277,
      and the in-scope variables: `speciesBinomials` at line 200 = 1228, `matchedSlugs` at
      line 245 = 1192, `unmatchedBinomials` at line 224 = 36)
    - src/types/schemas.ts (the KeyMatrixSchema with the new meta field from Plan 01 — meta is now required)
    - 40-RESEARCH.md section "4. KeyMatrixMetaSchema: fields and build-key.ts changes"
    - 40-PATTERNS.md section "scripts/build-key.ts (add meta emission)" (exact change location + variable references)
  </read_first>
  <action>
    At the existing `KeyMatrixSchema.parse({ characters, species, matrix })` call in `main()`
    (line 277), add a `meta` property as the first key of the parsed object literal. Populate it
    from variables already in scope at that point: `totalKeySpecies: speciesBinomials.length`
    (1228), `matchedSpecies: matchedSlugs.length` (1192), `unmatchedSpecies:
    unmatchedBinomials.length` (36), and `generatedAt: new Date().toISOString()`. Do NOT add new
    imports, recompute counts, or change how `characters`/`species`/`matrix` are built. Do NOT
    introduce a separate meta file or a second fetch — meta is embedded in the existing artifact.
    Because Plan 01 made `meta` a required field on `KeyMatrixSchema`, the `parse()` call now
    fails closed if meta is malformed (this is the intended V5 guard). Use `noUncheckedIndexedAccess`-safe
    code consistent with the rest of build-key.ts.
  </action>
  <verify>
    <automated>cd /Users/rainhead/dev/pnwmoths && npm run typecheck && node --test scripts/build-key.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E 'totalKeySpecies' scripts/build-key.ts` matches and appears inside the KeyMatrixSchema.parse() argument
    - `grep -E 'matchedSpecies|unmatchedSpecies|generatedAt' scripts/build-key.ts` matches all three
    - The meta object is passed into `KeyMatrixSchema.parse(...)` (not assembled into a separate file)
    - No new top-level imports were added to build-key.ts
    - `npm run typecheck` exits 0
    - `node --test scripts/build-key.test.ts` exits 0
  </acceptance_criteria>
  <done>build-key.ts populates meta from in-scope counts at the parse callsite; typecheck and build-key tests pass.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Regenerate data/key-matrix.json with the meta block</name>
  <files>data/key-matrix.json</files>
  <read_first>
    - scripts/build-key.ts (after Task 1 — the meta-emitting build)
    - 40-PATTERNS.md section "scripts/build-key.ts" (regenerate via npm run build:key)
    - .claude/skills/spike-findings-pnwmoths/SKILL.md (project runtime: Node 24 type-stripping; all pipeline ops run locally — never "run on server")
  </read_first>
  <action>
    Run `npm run build:key` locally to regenerate `data/key-matrix.json` from the meta-emitting
    `build-key.ts`. The regenerated artifact must now include a top-level `meta` object with the
    four fields. Confirm that `characters` (237), `species` (1192), and `matrix` (237) are
    structurally unchanged versus before — only the addition of `meta` should differ (and the
    `generatedAt` timestamp will be new). Do NOT hand-edit `data/key-matrix.json`; it must be the
    direct output of the build script. This runs entirely locally (no server).
  </action>
  <verify>
    <automated>cd /Users/rainhead/dev/pnwmoths && node -e "const d=require('./data/key-matrix.json'); const m=d.meta||{}; const ok = d.characters.length===237 && d.species.length===1192 && d.matrix.length===237 && m.totalKeySpecies===1228 && m.matchedSpecies===1192 && m.unmatchedSpecies===36 && typeof m.generatedAt==='string'; process.exit(ok?0:1)" && node -e "const {KeyMatrixSchema}=await import('./src/types/schemas.ts'); KeyMatrixSchema.parse(require('./data/key-matrix.json')); console.log('schema parse ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `data/key-matrix.json` contains a `meta` object with exactly: totalKeySpecies (1228), matchedSpecies (1192), unmatchedSpecies (36), generatedAt (ISO 8601 string)
    - `characters.length === 237`, `species.length === 1192`, `matrix.length === 237` (unchanged)
    - `KeyMatrixSchema.parse(data/key-matrix.json)` succeeds (validates incl. meta)
    - The artifact is the direct output of `npm run build:key` (not hand-edited)
  </acceptance_criteria>
  <done>data/key-matrix.json regenerated with a valid meta block; species/characters/matrix counts unchanged; schema parse succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build script → committed artifact | `build-key.ts` writes `data/key-matrix.json` consumed by the browser at load |
| committed artifact → browser load | `validateKeyMatrix` parses the artifact (now incl. meta) via `KeyMatrixSchema` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-02 | Tampering | meta block in key-matrix.json | mitigate | `KeyMatrixSchema.parse()` validates meta at both build time (build-key.ts) and browser load (validateKeyMatrix); malformed/missing meta fails closed (ASVS V5) |

No package installs in this plan. No network input. The only data flow is build-time computation
of integer counts already derived in Phase 39. RESEARCH section "Security Domain" confirmed no
meaningful threat surface.
</threat_model>

<verification>
- `npm run typecheck` exits 0 — meta literal type-matches KeyMatrixMetaSchema.
- `node --test scripts/build-key.test.ts` exits 0 — build pipeline still green.
- Regenerated `data/key-matrix.json` parses against `KeyMatrixSchema` and contains the four meta fields with expected values (1228 / 1192 / 36 / ISO timestamp).
- characters/species/matrix counts (237 / 1192 / 237) unchanged.
</verification>

<success_criteria>
- `build-key.ts` emits the meta block at the parse callsite from in-scope counts.
- `data/key-matrix.json` regenerated with a valid meta object; rest of artifact unchanged.
- typecheck + build-key tests pass.
</success_criteria>

<output>
Create `.planning/phases/40-filter-logic-tdd-contract/40-02-SUMMARY.md` when done.
</output>
