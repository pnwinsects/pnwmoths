---
phase: 42-results-grid
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/components/key-results-grid.ts
  - src/components/key-results-grid.test.ts
  - src/components/pnwm-identify.test.ts
autonomous: true
requirements: [GRID-01, GRID-02, GRID-03, GRID-04]

must_haves:
  truths:
    - "A failing (RED) test file exists for the grid's pure helpers before the component is built"
    - "Count-text logic is locked: 'N species match' when filtering, 'Showing all 1,192 species' at rest"
    - "CDN URL construction is locked with encodeURIComponent and ?height=320"
    - "Placeholder condition is locked: nav_image === null means no <img>, a gray block instead"
    - "Empty-state condition is locked: hasSelection && matchedSpecies.length === 0"
    - "pnwm-identify reset path (Clear all) is asserted to clear matched display state (D-09)"
  artifacts:
    - path: "src/components/key-results-grid.test.ts"
      provides: "Wave 0 unit tests for GRID-01..04 pure logic (count text, CDN URL, placeholder, empty-state, real-data null-nav-image gate)"
      contains: "buildCardUrl"
      min_lines: 60
    - path: "src/components/key-results-grid.ts"
      provides: "Pure-helper stubs (buildCardUrl, buildCountText) exported for Node unit tests"
      exports: ["buildCardUrl", "buildCountText"]
    - path: "src/components/pnwm-identify.test.ts"
      provides: "Extended tests asserting _dispatchFilterChange populates matched state and _clearAll resets it"
  key_links:
    - from: "src/components/key-results-grid.test.ts"
      to: "src/components/key-results-grid.ts"
      via: "import { buildCardUrl, buildCountText }"
      pattern: "from './key-results-grid"
    - from: "src/components/key-results-grid.test.ts"
      to: "data/key-matrix.json"
      via: "readFileSync real-data gate (exactly 2 null nav_image species)"
      pattern: "key-matrix\\.json"
---

<objective>
Establish the Wave 0 RED test scaffolds for Phase 42's results grid before any implementation
exists. Per VALIDATION.md, the four GRID-01..04 behaviors that ARE Node-testable (count text,
CDN URL construction, placeholder condition, empty-state condition) must have failing unit tests
first, plus extensions to `pnwm-identify.test.ts` asserting the new matched-state plumbing and the
shared "Clear all" reset (D-09).

This plan deliberately ships ONLY the minimal pure-helper stubs that the test file imports
(`buildCardUrl`, `buildCountText`) so the test file can be authored against real contracts and run
RED. The full `key-results-grid` LitElement and the `pnwm-identify` matrix wiring are built in
Plan 42-02 (Wave 1), which turns these tests GREEN.

Purpose: Lock the testable GRID-01..04 contracts (exact copy strings, exact CDN URL shape, exact
placeholder/empty conditions) in code before implementation, so Wave 1 is graded against assertions
rather than judgment. Satisfies VALIDATION.md "Wave 0 Requirements".
Output: `key-results-grid.test.ts` (new), pure-helper stubs in `key-results-grid.ts` (new),
extended `pnwm-identify.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-results-grid/42-RESEARCH.md
@.planning/phases/42-results-grid/42-PATTERNS.md
@.planning/phases/42-results-grid/42-UI-SPEC.md
@.planning/phases/42-results-grid/42-VALIDATION.md
</context>

<artifacts_this_phase_produces>
New symbols introduced by Phase 42 (exclude from source-drift verification — they do not exist before this phase):

- **Component / custom element:** `key-results-grid` (tag), `KeyResultsGrid` (class) — created in Plan 42-02.
- **Exported pure helpers (this plan creates the stubs, 42-02 completes them):**
  - `buildCardUrl(slug: string, navImage: string, height: number): string` → CDN thumbnail URL.
  - `buildCountText(hasSelection: boolean, count: number, total: number): string` → the count-line string.
- **New reactive properties on `pnwm-identify` (created in 42-02):** `_keyMatrix`, `_questionGroups`, `_matchedSpecies`, `_matchedCount`, and the `path-prefix` attribute + `_prefix` getter.
- **New event:** `pnwm-key-clear-all` (CustomEvent dispatched by the grid's empty-state CTA; created in 42-02).
- **New CSS classes (42-02):** `.pnwm-identify-layout`, `.pnwm-identify-panel`, `.pnwm-identify-grid-area`, `.pnwm-krg-count`, `.pnwm-krg-grid`, `.pnwm-krg-card`, `.pnwm-krg-label`, `.pnwm-krg-common`, `.pnwm-krg-placeholder-wrap`, `.pnwm-krg-prompt`, `.pnwm-krg-empty`.
- **New file paths:** `src/components/key-results-grid.ts`, `src/components/key-results-grid.test.ts`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create key-results-grid pure-helper stubs + RED unit test file (GRID-01..04 testable logic)</name>
  <files>src/components/key-results-grid.ts, src/components/key-results-grid.test.ts</files>
  <read_first>
    - src/components/key-results-grid.ts (does not exist yet — this task creates it)
    - src/components/pnwm-identify.test.ts (test-harness analog: node:test + node:assert/strict, no DOM, real-data gate via readFileSync of data/key-matrix.json)
    - src/components/pnwm-taxon-browser.ts (CDN URL mechanics: CDN_BASE_URL const, encodeURIComponent, ?height=, loading="lazy")
    - .planning/phases/42-results-grid/42-PATTERNS.md (§ "key-results-grid.test.ts" — file header, no-DOM instance pattern, real-data gate pattern)
    - .planning/phases/42-results-grid/42-RESEARCH.md (§ RQ6 testing patterns; § "Code Examples" exact CDN URL pattern; § Validation Architecture)
    - .planning/phases/42-results-grid/42-UI-SPEC.md (§ "Grid Anatomy" — ?height=320 CDN value; § "Full Copywriting Contract" — exact count/prompt/empty strings)
    - data/key-matrix.json (confirm: meta.matchedSpecies=1192; exactly 2 species with nav_image:null — autographa-v-alba, xestia-c-nigrum)
  </read_first>
  <action>
    Create `src/components/key-results-grid.ts` containing ONLY two exported pure helpers (no LitElement yet — that is Plan 42-02). Export `buildCardUrl(slug, navImage, height)` returning `${CDN_BASE_URL}/${slug}/${encodeURIComponent(navImage)}?height=${height}` with a module-level `const CDN_BASE_URL = 'https://pnwmoths.b-cdn.net'` (hardcoded, mirroring taxon-browser, per D-07). Export `buildCountText(hasSelection, count, total)` returning `"${count.toLocaleString('en-US')} species match"` when hasSelection is true, else `"Showing all ${total.toLocaleString('en-US')} species"` (exact copy from UI-SPEC Copywriting Contract, D-01/D-03; both numbers are thousands-separated so a 4-digit total renders "1,192" with a comma, matching the UI-SPEC and Plan 42-02 truths — NOT "1192"). These stubs are intentionally minimal but CORRECT — they exist now so the test file imports real symbols and Wave 1 reuses them inside the component render.

    Create `src/components/key-results-grid.test.ts` using the `node:test` + `node:assert/strict` harness (no DOM), mirroring `pnwm-identify.test.ts` header. Import `buildCardUrl` and `buildCountText` from `./key-results-grid.ts`. Write describe/test blocks:
    - GRID-02: `buildCardUrl('habrosyne-scripta', 'Habrosyne scripta-A-D.jpg', 320)` equals `'https://pnwmoths.b-cdn.net/habrosyne-scripta/Habrosyne%20scripta-A-D.jpg?height=320'` (asserts encodeURIComponent encodes the space and ?height=320 from UI-SPEC).
    - GRID-01: `buildCountText(false, 0, 1192)` equals `'Showing all 1,192 species'` (comma-formatted total); `buildCountText(true, 47, 1192)` equals `'47 species match'`; `buildCountText(true, 0, 1192)` equals `'0 species match'` (the zero-match count line per UI-SPEC State 3); `buildCountText(true, 1190, 1192)` equals `'1,190 species match'` (locks the comma form on a 4-digit match count too).
    - GRID-03 (placeholder condition): a test documenting the rule that `nav_image === null` means no `<img>` is emitted — assert the predicate `(sp.nav_image === null)` directly on the 2 known null-nav-image entries loaded from real data (see real-data gate below). The render-side proof is HUMAN-UAT; this locks the data condition.
    - GRID-04 (empty-state condition): a test documenting the predicate `hasSelection && matchedSpecies.length === 0` selects the empty state — assert this boolean expression for the three state combinations (no selection / selection+results / selection+zero). Keep it as a pure boolean assertion (the component render is built in 42-02).
    - Real-data gate (GRID-03): `readFileSync(resolve(ROOT, 'data/key-matrix.json'))`, parse, and assert `species.filter(s => s.nav_image === null)` has length exactly 2, and that those two slugs are `autographa-v-alba` and `xestia-c-nigrum`. This guards against silent data drift in the null-nav-image set.
    - Real-data gate (GRID-01): assert `meta.matchedSpecies === 1192` so the at-rest count string stays truthful if the dataset changes.

    Do NOT import or instantiate any LitElement in this test file — these are pure-function and data-shape assertions only (Node, no DOM). Browser render behaviors are HUMAN-UAT in Plan 42-02.
  </action>
  <verify>
    <automated>node --test src/components/key-results-grid.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `key-results-grid.ts` contains `export function buildCardUrl` and `export function buildCountText` and `const CDN_BASE_URL = 'https://pnwmoths.b-cdn.net'`.
    - `key-results-grid.test.ts` imports both helpers from `./key-results-grid.ts` and contains assertions for the exact strings: `'https://pnwmoths.b-cdn.net/habrosyne-scripta/Habrosyne%20scripta-A-D.jpg?height=320'`, `'Showing all 1,192 species'`, `'47 species match'`, `'0 species match'`, `'1,190 species match'`.
    - Real-data gate asserts exactly 2 species with `nav_image === null` (`autographa-v-alba`, `xestia-c-nigrum`) and `meta.matchedSpecies === 1192`.
    - `node --test src/components/key-results-grid.test.ts` runs and all tests in the file PASS (the helpers are correct stubs, so this file is GREEN on its own — the RED in this plan comes from the pnwm-identify extensions in Task 2).
    - No LitElement import in the test file; no DOM usage.
  </acceptance_criteria>
  <done>The grid's testable GRID-01..04 contracts are locked in `key-results-grid.test.ts` and pass against the exported pure helpers; the helpers are reused unchanged by the component in Plan 42-02.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend pnwm-identify.test.ts — RED tests for matched-state plumbing + Clear-all reset (D-09)</name>
  <files>src/components/pnwm-identify.test.ts</files>
  <read_first>
    - src/components/pnwm-identify.test.ts (the file being modified — follow the existing `_clearAll` describe block at lines ~159–190; reuse its `c._dispatchFilterChange = () => {}` override trick and the FIXTURE)
    - src/components/pnwm-identify.ts (current `_dispatchFilterChange` placeholder lines ~140–149; `_clearAll` lines ~102–105; `_selection` shape)
    - src/_lib/key-filter.ts (computeMatching signature → MatchResult { matchedSlugs, count }; buildQuestionGroups)
    - .planning/phases/42-results-grid/42-PATTERNS.md (§ "pnwm-identify.test.ts (MODIFY — extend)" — exact new test descriptions)
    - .planning/phases/42-results-grid/42-RESEARCH.md (§ RQ7 "Clear all shared reset path"; Pitfall 3)
    - data/key-matrix.json (source of a minimal matrix fixture: meta, characters, species, matrix base64 strings)
  </read_first>
  <action>
    Add two new `describe`/`test` blocks to `pnwm-identify.test.ts` that assert behavior NOT YET IMPLEMENTED in `pnwm-identify.ts` (these are RED until Plan 42-02 turns them GREEN):

    1. `_dispatchFilterChange sets _matchedSpecies after the matrix is loaded` — Instantiate `new PnwmIdentify()`. Assign a minimal in-memory `KeyMatrix` fixture to `c._keyMatrix` (small hand-built or sliced-from-real object with `meta`, a couple `characters`, 2–3 `species`, and matching base64 `matrix` strings) and the corresponding `c._questionGroups = buildQuestionGroups(fixture.characters)`. Set `c._selection` to a Map that selects a state matching at least one species. Call `c._dispatchFilterChange()`. Assert `c._matchedSpecies` is a non-empty `KeySpecies[]` and `c._matchedCount` equals its length. (Override any DOM-touching dispatch as needed, mirroring the existing `c._dispatchFilterChange = () => {}` pattern is NOT applicable here since we are testing `_dispatchFilterChange` itself — instead avoid `this.dispatchEvent` brittleness by accepting that `LitElement.dispatchEvent` is safe to call on a detached instance; if it throws in Node, guard the test by stubbing `c.dispatchEvent = () => true`.)

    2. `_clearAll resets _matchedSpecies and _matchedCount (D-09)` — Extend coverage of the reset path: instantiate `new PnwmIdentify()`, seed `c._selection` with a non-empty Map AND seed `c._matchedSpecies` with a dummy non-empty array and `c._matchedCount` to a non-zero number, stub `c._dispatchFilterChange = () => {}` to isolate, call `c._clearAll()`, then assert `c._selection.size === 0`, `c._matchedSpecies.length === 0`, and `c._matchedCount === 0`. This locks Pitfall 3 (clear must reset display state regardless of matrix load).

    Reference the new property names exactly: `_keyMatrix`, `_questionGroups`, `_matchedSpecies`, `_matchedCount`. These are the same names Plan 42-02 will declare in `pnwm-identify.ts` properties().

    Run the full suite to confirm these two tests FAIL (RED) because `_dispatchFilterChange` still dispatches the Phase 41 placeholder and `_clearAll` does not yet reset matched state. A RED result here is the expected, correct outcome of this plan.
  </action>
  <verify>
    <automated>node --test src/components/pnwm-identify.test.ts; test $? -ne 0 && echo "RED-as-expected" || echo "UNEXPECTED-GREEN"</automated>
  </verify>
  <acceptance_criteria>
    - `pnwm-identify.test.ts` contains a test asserting `_matchedSpecies` is populated and `_matchedCount` equals its length after `_dispatchFilterChange()` with a loaded `_keyMatrix` fixture.
    - `pnwm-identify.test.ts` contains a test asserting `_clearAll()` sets `_selection.size === 0`, `_matchedSpecies.length === 0`, and `_matchedCount === 0`.
    - The two new tests reference exactly `_keyMatrix`, `_questionGroups`, `_matchedSpecies`, `_matchedCount`.
    - Running `node --test src/components/pnwm-identify.test.ts` FAILS on the two new assertions (RED) — confirmed by the verify command printing `RED-as-expected`. Pre-existing tests that don't depend on new props still pass.
    - No new npm dependency added; no test glob change needed (`src/components/*.test.ts` already covers both files).
  </acceptance_criteria>
  <done>The matched-state plumbing and the shared Clear-all reset (D-09) are asserted by failing tests, ready for Plan 42-02 to satisfy. The RED state is verified, not assumed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test files → real `data/key-matrix.json` | Read-only fixture load; no external input, no network, no untrusted data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-01 | Tampering | `data/key-matrix.json` shape drift breaking the at-rest count / null-nav-image set | mitigate | Real-data gate tests assert `meta.matchedSpecies === 1192` and exactly 2 null-nav-image species; drift fails the suite |
| T-42-02 | Information Disclosure | none — static read-only test data, no secrets, no PII | accept | No user input, no network in this plan; static-site test scaffolds only |
| T-42-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this plan (all from existing `lit@3.3.2`, `zod@^4`, `node:test`); RESEARCH § Package Legitimacy Audit confirms no installs |
</threat_model>

<verification>
- `node --test src/components/key-results-grid.test.ts` passes (helpers correct, contracts locked).
- `node --test src/components/pnwm-identify.test.ts` shows the two new tests RED (expected pre-implementation state).
- No new files outside the three listed in `files_modified`.
- `git grep -n "buildCardUrl" src/components/key-results-grid.test.ts` confirms the test imports the real helper.
</verification>

<success_criteria>
- Wave 0 test scaffolds exist for all four GRID requirements' Node-testable logic.
- The exact locked copy strings (D-01/D-03/D-09) and the exact CDN URL shape (D-07, ?height=320) are encoded as assertions.
- `pnwm-identify` matched-state plumbing and Clear-all reset (D-09) are asserted and currently RED.
- VALIDATION.md "Wave 0 Requirements" checklist is satisfied (test file created; pure helpers exported; pnwm-identify additions present; test glob already covers the file).
</success_criteria>

<output>
Create `.planning/phases/42-results-grid/42-01-SUMMARY.md` when done.
</output>
