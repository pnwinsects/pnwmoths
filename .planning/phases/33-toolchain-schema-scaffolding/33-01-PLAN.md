---
phase: 33-toolchain-schema-scaffolding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - tsconfig.json
  - tsconfig.node.json
  - tsconfig.browser.json
  - src/types/eleventy.d.ts
autonomous: true
requirements: [TS-01, TS-02, TS-03, TS-04, TS-05]
must_haves:
  truths:
    - "npm run typecheck runs tsc --noEmit across both tsconfigs and exits zero with no source files converted"
    - "typescript, @types/node@^24, and @types/leaflet are in devDependencies; zod stays in dependencies; @types/openseadragon is removed"
    - "A local src/types/eleventy.d.ts shim types every eleventyConfig.* method actually called in eleventy.config.js"
    - "Static TypeScript types alone carry the build-locked data path — the strict tsconfigs plus a green typecheck are what make 'if tsc passes, the shape is known' true, with no runtime validator on the build side (D-01)"
  artifacts:
    - path: "tsconfig.node.json"
      provides: "Node 24 nodenext type-check target with isolatedModules + allowImportingTsExtensions"
      contains: "NodeNext"
    - path: "tsconfig.browser.json"
      provides: "Vite bundler type-check target with Lit-required flags"
      contains: "useDefineForClassFields"
    - path: "tsconfig.json"
      provides: "Root references config driving a single typecheck command"
      contains: "references"
    - path: "src/types/eleventy.d.ts"
      provides: "EleventyConfig API shim (~30 lines)"
      contains: "addTransform"
    - path: "package.json"
      provides: "typecheck npm script + corrected dependency classification"
      contains: "typecheck"
  key_links:
    - from: "package.json scripts.typecheck"
      to: "tsconfig.json + tsconfig.node.json"
      via: "tsc --noEmit invocations"
      pattern: "tsc --noEmit"
    - from: "tsconfig.browser.json"
      to: "src/types/eleventy.d.ts"
      via: "include glob src/types/**/*.ts"
      pattern: "src/types"
---

<objective>
Install-correct and configure the TypeScript toolchain so `npm run typecheck` runs `tsc --noEmit` across a Node target and a browser target and exits zero — before any `.js` source file is converted. This includes repairing the uncommitted, partially-wrong package install left by the research-time slopcheck run.

Purpose: This is the foundation every downstream migration phase (34–38) depends on. The three-config layout, the erasable-syntax constraints, and the Lit-specific browser flags must all be locked in here so that when source files start converting in Phase 34 they immediately type-check under the correct compiler settings.

Output: Corrected `package.json`/`package-lock.json`, three tsconfig files (`tsconfig.json`, `tsconfig.node.json`, `tsconfig.browser.json`), a local `src/types/eleventy.d.ts` shim, and a `typecheck` npm script that exits zero.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-toolchain-schema-scaffolding/33-CONTEXT.md
@.planning/phases/33-toolchain-schema-scaffolding/33-RESEARCH.md
@CLAUDE.md
</context>

<artifacts_produced>
## Artifacts this plan produces

New files:
- `tsconfig.json` — root references config (`files: []`, `references` array pointing at the two sub-configs); drives `tsc --noEmit`
- `tsconfig.node.json` — Node 24 `nodenext` target
- `tsconfig.browser.json` — Vite `bundler` target with Lit flags
- `src/types/eleventy.d.ts` — `EleventyConfig` + `EleventyTransformContext` interfaces and `declare module '@11ty/eleventy'`

New/changed symbols:
- `npm run typecheck` script in `package.json`

Modified files (dependency-classification repair):
- `package.json` — `typescript`, `@types/node` (pinned `^24`), `@types/leaflet` moved to `devDependencies`; `zod` stays in `dependencies`; `@types/openseadragon` removed
- `package-lock.json` — regenerated to match
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Repair the dependency install and add the typecheck script</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - package.json (current state — note all five research-installed packages landed in `dependencies`; see lines 35–47)
    - .planning/phases/33-toolchain-schema-scaffolding/33-RESEARCH.md § "Corrections to package.json after slopcheck install" and § "Package Legitimacy Audit"
    - .nvmrc (confirms Node 24 — drives the @types/node major pin)
  </read_first>
  <action>
    Repair the partially-wrong dependency classification from the research-time install, then add the typecheck script. The five packages `typescript`, `zod`, `@types/node`, `@types/leaflet`, `@types/openseadragon` currently sit in `dependencies` and must be corrected to the final state asserted by RESEARCH.md.

    Run the corrections via npm so `package-lock.json` regenerates consistently (per RESEARCH.md "Installation command (corrected)"):
    - `npm uninstall @types/openseadragon` — OSD 6.0.2 ships its own types at `types/index.d.ts`; the `@types/openseadragon` stub uses enum syntax that violates the TS-03 no-enum constraint. It must NOT appear in either dependency block.
    - `npm install zod@^4` — keep `zod` in `dependencies` (build scripts consume it at build time; it is not dev-only).
    - `npm install -D typescript@^6 @types/node@^24 @types/leaflet@^1.9.21` — move all three to `devDependencies`. Pin `@types/node` to `^24` (NOT `^25`) to match the Node 24 runtime in `.nvmrc`. Keep `typescript` at the already-installed `^6` major (research A1: TS 6.0.3 is compatible — `strict` default and dropped `node10`/`es5` do not affect this codebase).

    Then add a `"typecheck"` entry to the `scripts` block, value exactly: `tsc -p tsconfig.browser.json --noEmit && tsc -p tsconfig.node.json --noEmit`. Each sub-config is invoked explicitly because TypeScript does NOT follow `references` in plain `--noEmit` (non-`--build`) mode — a bare `tsc --noEmit` on the root solution config (`files: []` + `references`) silently checks nothing and exits 0 (verified against tsc 6.0.3), which would leave the browser target — and in Phase 37 every `src/components/*.ts` — unchecked. The root `tsconfig.json` remains the IDE/editor solution config that ties both projects together. Place the script alphabetically near the other `build:*`/`test` entries; do not reorder unrelated scripts.

    Do NOT add `tsx`, `ts-node`, or `vite-plugin-checker` (RESEARCH.md "Packages NOT to install"). Do NOT touch any of the existing runtime dependencies (`@11ty/eleventy`, `hyparquet`, `lit`, `openseadragon`, etc.).
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); const dev=p.devDependencies, dep=p.dependencies; const fail=[]; if(!/^\^?24/.test(dev['@types/node']||'')) fail.push('@types/node not devDep ^24: '+dev['@types/node']); if(!dev['typescript']) fail.push('typescript not in devDependencies'); if(!dev['@types/leaflet']) fail.push('@types/leaflet not in devDependencies'); if(!dep['zod']) fail.push('zod not in dependencies'); if(dep['typescript']||dep['@types/node']||dep['@types/leaflet']) fail.push('type pkg leaked into dependencies'); if(dep['@types/openseadragon']||dev['@types/openseadragon']) fail.push('@types/openseadragon still present'); if(p.scripts.typecheck!=='tsc -p tsconfig.browser.json --noEmit && tsc -p tsconfig.node.json --noEmit') fail.push('typecheck script wrong: '+p.scripts.typecheck); if(fail.length){console.error(fail.join('\n'));process.exit(1)} console.log('package.json OK')"</automated>
  </verify>
  <done>
    `package.json` has `typescript`, `@types/node@^24`, and `@types/leaflet` in `devDependencies`; `zod` in `dependencies`; no `@types/openseadragon` anywhere; a `typecheck` script with the exact two-config command; `package-lock.json` regenerated. The verify command prints `package.json OK` and exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create the three tsconfigs and the Eleventy type shim</name>
  <files>tsconfig.json, tsconfig.node.json, tsconfig.browser.json, src/types/eleventy.d.ts</files>
  <read_first>
    - .planning/phases/33-toolchain-schema-scaffolding/33-RESEARCH.md § "Open Technical Questions Resolved" Q1 (tsconfig drafts — copy the exact flag values) and Q4 (Eleventy shim outline + method table)
    - eleventy.config.js (source of truth for which eleventyConfig.* methods exist: addPlugin, addFilter, addTransform, addGlobalData, addPassthroughCopy, on; and that the transform uses `this.page.outputPath`)
    - .planning/phases/33-toolchain-schema-scaffolding/33-CONTEXT.md § "Claude's Discretion" (locked: useDefineForClassFields:false + experimentalDecorators:true browser; isolatedModules:true both; allowImportingTsExtensions not rewriteRelativeImportExtensions)
  </read_first>
  <action>
    Create the three tsconfig files using the exact flag values from RESEARCH.md Q1. Do NOT add `composite: true` to any config (creates stale `.tsbuildinfo`; only needed for `tsc --build`, which this project never runs — Pitfall in RESEARCH.md).

    `tsconfig.node.json` compilerOptions: `target` ES2022, `module` NodeNext, `moduleResolution` NodeNext, `lib` ["ES2022"], `strict` true, `noUnusedLocals` true, `noUnusedParameters` true, `noImplicitReturns` true, `noFallthroughCasesInSwitch` true, `noUncheckedIndexedAccess` true, `isolatedModules` true, `verbatimModuleSyntax` true, `allowImportingTsExtensions` true (per D-08 discretion: `noEmit:true` ⇒ allowImportingTsExtensions, NOT rewriteRelativeImportExtensions), `resolveJsonModule` true, `skipLibCheck` true, `noEmit` true. `include`: `scripts/**/*.ts`, `src/_data/**/*.ts`, `src/_lib/**/*.ts`, `src/types/**/*.ts`, `eleventy.config.ts`, `**/*.test.ts`.

    `tsconfig.browser.json` compilerOptions: `target` ES2022, `module` ESNext, `moduleResolution` bundler, `lib` ["ES2022","DOM","DOM.Iterable"], `strict` true, plus the same four extra-strict flags (`noUnusedLocals`, `noUnusedParameters`, `noImplicitReturns`, `noFallthroughCasesInSwitch`, `noUncheckedIndexedAccess`), `isolatedModules` true, `verbatimModuleSyntax` true, `experimentalDecorators` true (LOCKED — Lit reactive props), `useDefineForClassFields` false (LOCKED — Pitfall 1: omitting it silently breaks Lit reactivity at runtime), `types` ["vite/client"], `skipLibCheck` true, `noEmit` true. `include`: `src/components/**/*.ts`, `src/types/**/*.ts`. NOTE: `src/types/**/*.ts` MUST appear in BOTH sub-config includes so the shared schema module is checked under both resolution modes (Pitfall 5).

    `tsconfig.json` (root): `files` [], `references` [{ "path": "./tsconfig.node.json" }, { "path": "./tsconfig.browser.json" }]. This is the IDE/editor solution config tying both projects together; the `typecheck` script invokes each sub-config explicitly (`references` are NOT followed in `--noEmit` mode), so no `composite`/`--build` is required.

    Create `src/types/eleventy.d.ts` (~30 lines) per RESEARCH.md Q4. Export an `EleventyTransformContext` interface with `page: { outputPath: string | false }`. Export an `EleventyConfig` interface declaring exactly the six methods called in eleventy.config.js: `addPlugin(plugin: unknown, options?: unknown): void`, `addFilter(name: string, fn: (this: unknown, ...args: unknown[]) => unknown): void`, `addTransform(name: string, fn: (this: EleventyTransformContext, content: string) => string): void`, `addGlobalData(name: string, value: unknown): void`, `addPassthroughCopy(pathOrRecord: string | Record<string, string>, opts?: unknown): void`, `on(event: string, callback: (data: { runMode: string }) => Promise<void> | void): void`. Add `declare module '@11ty/eleventy'` exporting a class `EleventyRenderPlugin` with `configFunction(config: EleventyConfig): void`. Use `unknown` (never `any`) for opaque values. No `enum`, no `namespace` (TS-03).

    After writing all four files, run `npm run typecheck`. With zero `.ts` source files yet (only the `.d.ts` shim and the still-empty `src/types/` dir), both configs must exit 0. If tsc reports `error TS18003: No inputs were found` for an include glob, that is acceptable behavior only if it does not cause a nonzero exit — if it does exit nonzero, ensure each `include` has at least the matching pattern present (the shim satisfies `src/types/**/*.ts`); do not add placeholder source files.
  </action>
  <verify>
    <automated>npm run typecheck && node -e "const fs=require('fs'); const b=JSON.parse(fs.readFileSync('tsconfig.browser.json','utf8')).compilerOptions; const n=JSON.parse(fs.readFileSync('tsconfig.node.json','utf8')).compilerOptions; const r=JSON.parse(fs.readFileSync('tsconfig.json','utf8')); const f=[]; if(b.useDefineForClassFields!==false)f.push('browser useDefineForClassFields!==false'); if(b.experimentalDecorators!==true)f.push('browser experimentalDecorators!==true'); if(b.isolatedModules!==true)f.push('browser isolatedModules!==true'); if(n.isolatedModules!==true)f.push('node isolatedModules!==true'); if(n.allowImportingTsExtensions!==true)f.push('node allowImportingTsExtensions!==true'); if(String(n.module).toLowerCase()!=='nodenext')f.push('node module!==NodeNext'); if(String(b.moduleResolution).toLowerCase()!=='bundler')f.push('browser moduleResolution!==bundler'); if(b.composite||n.composite)f.push('composite must be absent'); if(!Array.isArray(r.references)||r.references.length!==2)f.push('root references not 2 entries'); if(!fs.existsSync('src/types/eleventy.d.ts'))f.push('eleventy.d.ts missing'); const shim=fs.readFileSync('src/types/eleventy.d.ts','utf8'); for(const m of ['addPlugin','addFilter','addTransform','addGlobalData','addPassthroughCopy','on(']){if(!shim.includes(m))f.push('shim missing '+m)} if(f.length){console.error(f.join('\n'));process.exit(1)} console.log('tsconfigs + shim OK')"</automated>
  </verify>
  <done>
    Three tsconfig files exist with the exact locked flags; `src/types/eleventy.d.ts` types all six called methods plus the transform context; `npm run typecheck` exits 0 with no source files converted; the verify command prints `tsconfigs + shim OK`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → devDependencies | Newly-classified dev tooling (`typescript`, `@types/*`) enters the build toolchain |

This phase adds NO network endpoints, NO user input, NO auth surface, and ships NO runtime/browser code (the browser-side validator and bundle impact land in Phase 37). The schemas authored in Plan 02 are type definitions only.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01 | Tampering | npm dev-tooling supply chain (`typescript`, `@types/node`, `@types/leaflet`) | accept | slopcheck rated all `[OK]` (RESEARCH.md Package Legitimacy Audit); all are 8–12-yr-old packages with 1M–60M weekly downloads from microsoft/TypeScript and DefinitelyTyped; no `[ASSUMED]`/`[SUS]`/`[SLOP]` verdicts → no blocking-human checkpoint required |
| T-33-02 | Information Disclosure | type packages misclassified into `dependencies` | mitigate | Task 1 moves `typescript`/`@types/*` to `devDependencies` so no type info or dev tooling ships in any production install |
| T-33-03 | Tampering | obsolete `@types/openseadragon` stub (enum syntax, no source repo) | mitigate | Task 1 removes it entirely; OSD 6.0.2 self-hosts types |
| T-33-SC | Tampering | npm installs in Task 1 | accept | All packages pre-audited `[OK]` by slopcheck; this plan only re-classifies already-installed, already-audited packages and removes one — no new unaudited package is introduced |

No high-severity threats: this phase adds dev-only tooling and type definitions; no runtime/browser code is shipped.
</threat_model>

<verification>
- `npm run typecheck` exits 0 (both configs) with no `.ts` source files converted.
- `package.json` dependency classification matches RESEARCH.md corrected final state (Task 1 verify).
- The three tsconfigs carry the locked flags and the root config uses `references` (Task 2 verify).
- `grep -rnE '\benum\b' src/types/` returns empty (the shim must not use enum — TS-03). Run: `grep -rnE '\benum\b' src/types/ | grep -v '^[^:]*:[0-9]*:[[:space:]]*//' || true` should print nothing.
</verification>

<success_criteria>
- Three tsconfig files exist: `tsconfig.node.json` (`module: NodeNext`), `tsconfig.browser.json` (`moduleResolution: bundler`, `useDefineForClassFields: false`, `experimentalDecorators: true`), root `tsconfig.json` (`references` both) — satisfies TS-01, TS-02.
- Both sub-configs set `isolatedModules: true`; no `enum` in `src/types/` — satisfies TS-03.
- `typescript`, `@types/node@^24`, `@types/leaflet` in devDependencies; `@types/openseadragon` removed; `src/types/eleventy.d.ts` shim covers every called Eleventy method — satisfies TS-04.
- `npm run typecheck` runs `tsc --noEmit` across both configs and exits zero with no source files converted — satisfies TS-05.
</success_criteria>

<output>
Create `.planning/phases/33-toolchain-schema-scaffolding/33-01-SUMMARY.md` when done.
</output>
