---
phase: 31-data-species-photos-json-build-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/species/species.njk
autonomous: false
requirements: [DATA-03]
tags: [eleventy, nunjucks, template, photos]

must_haves:
  truths:
    - "For a species with `high_res_available: true` in speciesPhotos, the rendered HTML contains zero low-res `<figure>` elements (suppressed entirely)"
    - "For a species without a high-res entry (or with `high_res_available: false`), the existing low-res `<figure>` loop renders unchanged"
    - "The empty-slot `<figure>` (`No photos on file for this species.`) still renders only when there are no spImages AND no high-res entry"
    - "`npm run build:eleventy` succeeds and produces the same species page count as before (1,364)"
  artifacts:
    - path: "src/species/species.njk"
      provides: "DATA-03 template guard suppressing low-res figures when high-res is available"
      contains: "(not (highResEntry and highResEntry.high_res_available)) and spImages and spImages.length > 0"
  key_links:
    - from: "src/species/species.njk line ~47 (guard)"
      to: "data/species-photos.json via src/_data/speciesPhotos.js"
      via: "speciesPhotos[sp.slug].high_res_available"
      pattern: "highResEntry.high_res_available"
---

<objective>
Replace the single Nunjucks conditional in `src/species/species.njk` that wraps the `images.csv` `<figure>` loop with the D-04 guard so that low-res `<figure>` elements are suppressed entirely for species where `high_res_available` is true. This is the one-line template change required by DATA-03.

Purpose: Implements the "high-res replaces, not coexists" rule (D-03/D-04). For high-res species, the `<pnwm-image-slideshow>` web component still receives `high-res-available` and `high-res-specimens` attributes (so Phase 32 can fill the slot with OSD-driven thumbnails), but the `<figure>` slot content is empty in the static HTML — no double rendering.

Output: A one-line change at `src/species/species.njk` line 47, plus a human-verify checkpoint over a fresh build to confirm no regression to non-high-res species and the correct suppression for `abagrotis-apposita`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/31-data-species-photos-json-build-integration/31-CONTEXT.md
@.planning/phases/31-data-species-photos-json-build-integration/31-RESEARCH.md
@.planning/phases/31-data-species-photos-json-build-integration/31-PATTERNS.md
@.planning/phases/31-data-species-photos-json-build-integration/31-UI-SPEC.md
@.planning/phases/31-data-species-photos-json-build-integration/31-VALIDATION.md

@src/species/species.njk
@src/_data/speciesPhotos.js
@data/species-photos.json

<interfaces>
<!-- Template variables in scope at species.njk line 47 -->
- `spImages` — array of image rows from `images[sp.slug]` (legacy `data/images.csv`); may be `undefined` or `[]`
- `highResEntry` — `speciesPhotos[sp.slug]` (from data/species-photos.json); may be `undefined`; when defined, has shape `{ high_res_available: boolean, specimens: [...] }`
- `cdnBaseUrl`, `sp`, `img` — standard Eleventy context vars

<!-- Existing conditional at species.njk lines 40-46 that already uses the highResEntry variable: -->
{% if highResEntry and highResEntry.high_res_available %}
high-res-available
high-res-specimens="{{ highResEntry.specimens | tojson | escape }}"
cdn-base-url="{{ cdnBaseUrl }}"
prefix-url="{{ '/osd-images/' | url }}"
{% endif %}

<!-- Current target line (species.njk line 47) -->
{% if spImages and spImages.length > 0 %}

<!-- Replacement (D-04 EXACT WORDING — both the `not` keyword and the explicit inner parentheses are required to avoid Nunjucks operator-precedence bugs per RESEARCH.md Pitfall 3) -->
{% if (not (highResEntry and highResEntry.high_res_available)) and spImages and spImages.length > 0 %}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add DATA-03 guard to species.njk (single conditional rewrite)</name>
  <files>src/species/species.njk</files>
  <read_first>
    - src/species/species.njk (full file — must see the full `<pnwm-image-slideshow>` block at lines 38-71 in context; the guard sits at line 47 and the matching `{% endif %}` sits at line 70)
    - .planning/phases/31-data-species-photos-json-build-integration/31-CONTEXT.md (D-03 high-res-replaces decision; D-04 exact guard wording — copy verbatim)
    - .planning/phases/31-data-species-photos-json-build-integration/31-RESEARCH.md (Pitfall 3 on Nunjucks `not` precedence; Pattern 6 Nunjucks guard syntax)
    - .planning/phases/31-data-species-photos-json-build-integration/31-PATTERNS.md (`src/species/species.njk` analog section — confirms only line 47 changes and the closing `{% endif %}` at line 70 stays)
    - .planning/phases/31-data-species-photos-json-build-integration/31-UI-SPEC.md (Template Guard section confirms before/after and that no CSS/HTML beyond this line changes)
    - data/species-photos.json (the pilot entry `abagrotis-apposita` has `high_res_available: true` and is the species this guard suppresses low-res figures for)
  </read_first>
  <action>
    In `src/species/species.njk`, locate the line currently reading:
      `{% if spImages and spImages.length > 0 %}`
    (At the time of writing this plan it is line 47, inside the `<pnwm-image-slideshow>` element opened at line 38.)

    Replace that single line with the D-04 exact wording (note the explicit inner parentheses around the high-res check — required by Nunjucks precedence per RESEARCH.md Pitfall 3):
      `{% if (not (highResEntry and highResEntry.high_res_available)) and spImages and spImages.length > 0 %}`

    Do not change any other line. The matching `{% endif %}` (currently line 70, immediately before `</pnwm-image-slideshow>`) stays exactly as it is. The `{% else %}` branch that renders the "No photos on file for this species." placeholder also stays exactly as it is — it now also takes effect when a high-res entry exists but `spImages` is empty. The `<pnwm-image-slideshow>` opening tag and its existing `{% if highResEntry and highResEntry.high_res_available %}` attribute block (lines 40-46) are unchanged.

    Semantics after change:
      - Species with no high-res entry AND with images.csv rows: render the `<figure>` loop as before.
      - Species with `high_res_available: true`: skip the `<figure>` loop entirely; the component receives `high-res-available` + `high-res-specimens` attributes but no slot children. (Phase 32 will populate the slot via component-side rendering — this empty-slot state is the correct intermediate state per UI-SPEC.md.)
      - Species with no high-res entry AND no images.csv rows: render the existing "No photos on file" placeholder figure (unchanged behavior).
  </action>
  <verify>
    <automated>grep -F '(not (highResEntry and highResEntry.high_res_available)) and spImages and spImages.length > 0' src/species/species.njk</automated>
    <automated>test $(grep -c '{% if spImages and spImages.length > 0 %}' src/species/species.njk) -eq 0</automated>
    <automated>npm run build:eleventy</automated>
    <automated>test -f _site/species/abagrotis-apposita/index.html</automated>
    <automated>test $(find _site/species -mindepth 2 -name 'index.html' | wc -l) -ge 1364</automated>
  </verify>
  <acceptance_criteria>
    - `src/species/species.njk` contains the literal substring `(not (highResEntry and highResEntry.high_res_available)) and spImages and spImages.length > 0` exactly once.
    - The original substring `{% if spImages and spImages.length > 0 %}` no longer appears in `src/species/species.njk` (count is 0).
    - The `{% endif %}` count in `src/species/species.njk` is unchanged from before the edit (use `git diff --stat src/species/species.njk` to confirm a single-line modification with no net add/remove of structural tags).
    - `npm run build:eleventy` exits 0.
    - `_site/species/abagrotis-apposita/index.html` exists and:
        - contains `high-res-available` attribute on the `pnwm-image-slideshow` element
        - contains the substring `species-tiles/abagrotis-apposita/A-D` (or the equivalent manifest-derived value) in the `high-res-specimens` attribute
        - does NOT contain `<figure>` followed by `<img src="https://...abagrotis-apposita/...` for low-res images inside the `pnwm-image-slideshow` element. (Verify: `grep -A 30 'pnwm-image-slideshow' _site/species/abagrotis-apposita/index.html | grep -c '<img src='` returns 0 within that element scope, OR the only `<img>` is for a similar-species row outside the slideshow.)
    - A non-high-res species page (e.g., pick any species whose slug is NOT in `data/species-photos.json` keys — run `head -50 _site/browse/index.html` to find a candidate, or pick `abagrotis-alternata` if absent from species-photos.json) still renders `<figure>` elements with low-res `<img src="...{slug}/...">` images inside its `<pnwm-image-slideshow>`. Confirm via `grep -c '<figure>' _site/species/<non-high-res-slug>/index.html` returns ≥ 1.
    - Total species page count (`find _site/species -mindepth 2 -name 'index.html' | wc -l`) is at least 1,364 (matches v2.1 baseline per Phase 31 SC-4).
  </acceptance_criteria>
  <done>
    Template guard committed; build green; abagrotis-apposita renders with empty `<figure>` slot but live `high-res-*` attributes; at least one non-high-res species still renders its low-res `<figure>` loop unchanged.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification — high-res species suppresses low-res figures; non-high-res species unchanged</name>
  <files>src/species/species.njk, _site/species/abagrotis-apposita/index.html</files>
  <action>Pause for the operator to visually verify the rendered species pages in a browser. No autonomous code changes; the previous task already shipped the template edit and proved it via build + grep. This checkpoint exists for human-eye confirmation of intended UX on both code paths (high-res and non-high-res species).</action>
  <verify>Operator inspects abagrotis-apposita and a non-high-res species page in a browser; confirms no low-res photo renders on the high-res species, and the Phase 23 carousel behavior is preserved on non-high-res species.</verify>
  <done>Operator types "approved" after both pages render as expected with no regression.</done>
  <what-built>
    The DATA-03 guard at `src/species/species.njk` line 47 now suppresses the legacy low-res `<figure>` loop for any species whose `data/species-photos.json` entry has `high_res_available: true`. The `<pnwm-image-slideshow>` element still receives `high-res-available` and `high-res-specimens` attributes so Phase 32 can wire OSD into the empty slot. Non-high-res species are unchanged.

    The previous task's automated verification already confirmed the source change, the build success, and the HTML output shape via grep. This checkpoint exists for human-visual confirmation that:
      (a) the abagrotis-apposita page does not double-render photos (no low-res next to high-res placeholder), and
      (b) a sample non-high-res species still shows its carousel of low-res photos correctly.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` (or `npm run build:eleventy && npx serve _site`) and open the abagrotis-apposita species page in a browser: `http://localhost:8080/species/abagrotis-apposita/` (or whatever URL the dev server prints).
    2. Confirm visually that the photos area shows NO low-res image — the slot is empty (or shows whatever placeholder browsers render for an empty custom element). Inspect element on `<pnwm-image-slideshow>` and confirm `high-res-available` and `high-res-specimens="[...]"` attributes are present.
    3. Open a non-high-res species page (any species not in `data/species-photos.json` keys — e.g., `http://localhost:8080/species/abagrotis-alternata/` if that species is not in the JSON, or pick any species from `/browse/`). Confirm the existing Phase 23 carousel + low-res photos render unchanged: thumbnails, click-to-lightbox, carousel navigation.
    4. Confirm the species page count claim from the previous task — open `_site` via the browser at `/browse/` and spot-check that browse listings still work.
    5. (Optional) Verify the `_site` build's total page count: `find _site/species -mindepth 2 -name 'index.html' | wc -l` returns 1,364 (or higher if new species were added).
  </how-to-verify>
  <resume-signal>Type "approved" if the abagrotis-apposita page suppresses low-res photos correctly AND a non-high-res species still renders its Phase 23 carousel unchanged. Report any visual regression in non-high-res rendering or any unexpected layout shift in the species-photos area.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `data/species-photos.json` → Nunjucks template | Build-time trusted data; values flow into HTML attributes via `tojson | escape` |
| Nunjucks template → emitted HTML | Static build output served from CDN; no runtime user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-05 | Tampering | Nunjucks operator-precedence bug in guard | mitigate | D-04 mandates explicit parentheses `(not (highResEntry and highResEntry.high_res_available))`; acceptance criteria assert exact substring match — wrong parenthesization would not contain the literal string and would fail the grep gate. |
| T-31-06 | Information Disclosure | high-res-specimens JSON attribute rendered into HTML | accept | Already established in Phase 28; values are public CDN paths (specimen_id + view + tiles_path). `tojson | escape` safely encodes for HTML attribute context. Existing line 42 (unchanged by this plan) handles this. |
| T-31-07 | Denial of Service | Empty-slot rendering for high-res species breaks visible UX until Phase 32 | accept | Documented intermediate state per `31-UI-SPEC.md`. Only impacts species in `data/species-photos.json` (currently just `abagrotis-apposita` for the pilot). Phase 32 will fill the slot. Operator can revert the template change in a hotfix branch if rollback is needed before Phase 32 ships. |
| T-31-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this plan. |
</threat_model>

<verification>
- The exact D-04 substring is present once in `src/species/species.njk`; the pre-edit substring is absent.
- `npm run build:eleventy` succeeds.
- `_site/species/abagrotis-apposita/index.html` carries the high-res attributes and emits no low-res `<img>` inside the slideshow element.
- A non-high-res species page still renders its low-res `<figure>` loop.
- Species page count ≥ 1,364.
- Human visual checkpoint confirms intended UX on both code paths.
</verification>

<success_criteria>
- DATA-03: For species where `high_res_available` is true, the build emits no low-res `<figure>` elements inside the species photos slideshow — no double rendering.
- Non-regression: Species without high-res still render Phase 23's carousel + lightbox exactly as before.
- Build count preserved (1,364 species pages).
- Pilot species (abagrotis-apposita) demonstrates the suppression in real HTML output.
</success_criteria>

<output>
Create `.planning/phases/31-data-species-photos-json-build-integration/31-02-SUMMARY.md` when done, summarizing the one-line template change, the verified before/after HTML for both the high-res and non-high-res code paths, and any observation worth carrying into Phase 32 (e.g., "empty slot renders as zero-height region — Phase 32 must fill it before shipping").
</output>
