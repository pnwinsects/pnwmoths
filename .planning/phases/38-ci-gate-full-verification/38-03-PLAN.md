---
phase: 38-ci-gate-full-verification
plan: 03
type: execute
wave: 3
depends_on: ["38-01", "38-02"]
files_modified:
  - .planning/phases/38-ci-gate-full-verification/MILESTONE-EVIDENCE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [CI-02, CI-03, MIG-05, MIG-06, CI-01]
must_haves:
  truths:
    - "The byte-identical one-shot proof has been RUN locally and its real output is recorded in MILESTONE-EVIDENCE.md"
    - "The build:data timing has been OBSERVED once and the measured seconds recorded (no timer assertion added)"
    - "All five requirement IDs (MIG-05, MIG-06, CI-01, CI-02, CI-03) are marked Complete and v3.0 is declared shipped"
  artifacts:
    - path: ".planning/phases/38-ci-gate-full-verification/MILESTONE-EVIDENCE.md"
      provides: "Committed milestone evidence: CI-02 byte-identical proof result, CI-03 build:data timing, MIG-06 guard result, test count, typecheck result, v3.0 completion declaration"
      min_lines: 40
      contains: "byte-identical"
    - path: ".planning/REQUIREMENTS.md"
      provides: "MIG-05/MIG-06/CI-01/CI-02/CI-03 marked Complete"
      contains: "MIG-06"
  key_links:
    - from: ".planning/phases/38-ci-gate-full-verification/MILESTONE-EVIDENCE.md"
      to: "scripts/compare-sites.sh output"
      via: "recorded real two-bucket diff result"
      pattern: "byte-identical"
    - from: ".planning/phases/38-ci-gate-full-verification/MILESTONE-EVIDENCE.md"
      to: "time npm run build:data output"
      via: "recorded measured seconds"
      pattern: "build:data"
---

<objective>
Run the two one-shot milestone proofs locally, record their REAL output in a committed evidence doc, and declare the v3.0 TypeScript migration milestone complete.

This plan delivers: (1) the CI-02 byte-identical proof — run `bash scripts/compare-sites.sh` locally against the gitignored `_site_baseline/` snapshot and record the real two-bucket result (D-01/D-02: one-shot LOCAL proof, NOT a recurring CI step); (2) the CI-03 timing observation — `time npm run build:data`, record the measured seconds (D-07: observe once, NO hard-failing timer); (3) `MILESTONE-EVIDENCE.md` capturing both proofs plus the guard result, test count, and typecheck result; (4) milestone-completion updates to ROADMAP.md / REQUIREMENTS.md declaring v3.0 done.

Purpose: SC-3 is satisfied by this recorded local proof (NOT a workflow step — D-01). The evidence doc is the permanent artifact; the byte-identical check is deliberately not promoted to a recurring gate (it would fail forever after the next legitimate content change). The MIG-06 guard from 38-01/38-02 is the ONE permanent gate.
Output: `MILESTONE-EVIDENCE.md` (new), updated ROADMAP.md and REQUIREMENTS.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/38-ci-gate-full-verification/38-RESEARCH.md
@.planning/phases/38-ci-gate-full-verification/38-PATTERNS.md
@.planning/phases/34-scripts-lib-src-lib-migration/BASELINE.md
@.planning/phases/38-ci-gate-full-verification/38-01-SUMMARY.md
@.planning/phases/38-ci-gate-full-verification/38-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Newly-created files (exclude from drift verification):
- `.planning/phases/38-ci-gate-full-verification/MILESTONE-EVIDENCE.md` — this plan
- (depends on) `scripts/compare-sites.sh`, `scripts/check-ts-only.sh` from 38-01; the wired workflow steps from 38-02
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild _site/, run the one-shot byte-identical proof, and observe build:data timing</name>
  <files>.planning/phases/38-ci-gate-full-verification/MILESTONE-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/34-scripts-lib-src-lib-migration/BASELINE.md — the `_site_baseline/` protocol, the staleness caveat, and the table+commands+notes style to mirror
    - .planning/phases/38-ci-gate-full-verification/38-RESEARCH.md §"D-03 Content-Hash Normalization", §"Common Pitfalls" (esp. Pitfall 3 baseline staleness + Pitfall 4 test-count drift), §"D-06 Budget Fit"
    - .planning/phases/38-ci-gate-full-verification/38-PATTERNS.md §"MILESTONE-EVIDENCE.md" — the structure to mirror and the "populate with REAL output" instruction
    - scripts/compare-sites.sh (from 38-01) — the proof helper being run
  </read_first>
  <action>
    First, confirm the baseline is not stale (RESEARCH.md Pitfall 3): verify `_site_baseline/` exists in the working tree and that no `data/` change occurred since it was built 2026-06-09 (`git log --oneline -- data/ | head` and check dates; if data changed, STOP and flag — the baseline must be rebuilt before the proof is meaningful). Then rebuild the current `_site/` with the same build command BASELINE.md used (`npm run build:data && npm run build:eleventy && npm run build:copy-parquet && npm run build:copy-images && npm run build:species-states`) so the comparison reflects the post-migration tree. Run `bash scripts/compare-sites.sh` and CAPTURE its real stdout (Bucket A `DATA: byte-identical`, Bucket B `HTML: identical modulo content-hash`, Bucket C exclusion line). Separately, observe timing ONCE: `time npm run build:data` and capture the real wall-clock seconds — do NOT add any timer assertion or fail-on-threshold logic (D-07). Also capture: `npm test 2>&1 | tail` (real passing test count — record the ACTUAL number, expected 225 not the stale "~191" per RESEARCH.md Pitfall 4), `npm run typecheck` result, and `bash scripts/check-ts-only.sh` result. Write all captured REAL output into MILESTONE-EVIDENCE.md (see Task 2 for structure). If Bucket A or B reports any unexpected diff, do NOT fabricate a PASS — record the actual diff and flag it; an unexpected data/HTML diff means a regression to investigate, not a proof to rubber-stamp.
  </action>
  <verify>
    <automated>test -f _site/species-states.json && bash scripts/compare-sites.sh; echo "compare-exit=$?"; { time npm run build:data ; } 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/compare-sites.sh` runs and reports `DATA: byte-identical` (Bucket A) and `HTML: identical modulo content-hash` (Bucket B); if not, the real diff is captured and flagged
    - `time npm run build:data` was run and the real measured seconds captured (no timer assertion added anywhere)
    - The actual `npm test` passing count was captured (expected 225, recorded as the real number)
    - `bash scripts/check-ts-only.sh` was run and its exit-0 result captured
    - Baseline staleness was checked (no `data/` change since 2026-06-09) before trusting the proof
  </acceptance_criteria>
  <done>The two one-shot proofs are run locally with real output captured; baseline freshness confirmed; no recurring CI gate or timer assertion was introduced.</done>
</task>

<task type="auto">
  <name>Task 2: Write the committed MILESTONE-EVIDENCE.md with real captured output</name>
  <files>.planning/phases/38-ci-gate-full-verification/MILESTONE-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/34-scripts-lib-src-lib-migration/BASELINE.md — the style analog (header + table + commands + notes)
    - .planning/phases/38-ci-gate-full-verification/38-PATTERNS.md §"MILESTONE-EVIDENCE.md" — the six required sections
    - .planning/phases/38-ci-gate-full-verification/38-CONTEXT.md §decisions — to caption WHY the byte-identical check is one-shot (D-01) and WHY there is no timer assertion (D-07)
  </read_first>
  <action>
    Write `.planning/phases/38-ci-gate-full-verification/MILESTONE-EVIDENCE.md` mirroring BASELINE.md's table+commands+notes style, extended with the six sections from PATTERNS.md, populated with the REAL output captured in Task 1 (NOT placeholder text — the evidence value is in the actual measurements). Required sections: (1) "Phase 38 Milestone Summary" header naming v3.0; (2) a measured-timings table — typecheck, npm test (with the REAL test count), verify:parquet, build:data — using the captured numbers; (3) "CI-02 — Byte-identical `_site/` proof (one-shot, local — D-01/D-02/D-03)": paste the real `scripts/compare-sites.sh` output, state the two-bucket method (Parquet/JSON strict, HTML normalized via the Perl hash canonicalization, assets/ excluded), and caption that this is a ONE-SHOT proof recorded as committed evidence, NOT a recurring CI step (D-01 rationale: a pre-migration baseline stops being meaningful after the milestone ships); (4) "CI-03 — `build:data` timing (D-07)": the real measured seconds, captioned as an observation with NO hard-failing timer assertion (CI variance makes 60s flaky), confirming the under-5-min total budget holds; (5) "MIG-06 — TS-only guard result": the real `check-ts-only.sh` exit-0 output, noting this IS the one permanent gate (wired into pr-check.yml in 38-02); (6) "v3.0 Milestone Complete" declaration listing all five reqs (MIG-05, MIG-06, CI-01, CI-02, CI-03) as satisfied with their evidence. Reference `scripts/compare-sites.sh` and `scripts/check-ts-only.sh` by path rather than re-pasting them (CLAUDE.md: link, do not duplicate).
  </action>
  <verify>
    <automated>test -f .planning/phases/38-ci-gate-full-verification/MILESTONE-EVIDENCE.md && grep -ciE "byte-identical|build:data|MIG-06|one-shot" .planning/phases/38-ci-gate-full-verification/MILESTONE-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - `MILESTONE-EVIDENCE.md` exists in the phase directory
    - It records the CI-02 byte-identical proof result with the real `compare-sites.sh` output and explicitly labels it a ONE-SHOT local proof (not a recurring CI step)
    - It records the CI-03 `build:data` timing in seconds as an observation with NO timer assertion
    - It records the real `npm test` count (the actual number, not the stale "~191")
    - It records the `check-ts-only.sh` exit-0 result and identifies it as the one permanent gate
    - It contains a "v3.0 Milestone Complete" declaration covering all five requirement IDs
    - It references the two scripts by path rather than duplicating their contents
  </acceptance_criteria>
  <done>The committed evidence doc records both one-shot proofs (real output), the guard result, and the v3.0 completion declaration, preserving the one-shot-vs-permanent distinction.</done>
</task>

<task type="auto">
  <name>Task 3: Mark requirements Complete and declare v3.0 done in ROADMAP.md and REQUIREMENTS.md</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md lines ~38-45 (the five `[ ]` req entries) and lines ~93-97 (the status table rows: MIG-05, MIG-06, CI-01, CI-02, CI-03 = Pending)
    - .planning/ROADMAP.md §"Phase 38" (line ~581) and the Progress table — to mark the phase complete
    - .planning/phases/38-ci-gate-full-verification/MILESTONE-EVIDENCE.md (from Task 2) — the evidence backing each completion
  </read_first>
  <action>
    Update REQUIREMENTS.md: change the five phase-38 requirement checkboxes from `[ ]` to `[x]` (MIG-05, MIG-06, CI-01, CI-02, CI-03), and in the status table change their rows from `Pending` to `Complete`. Note: CI-02 is satisfied by the recorded local one-shot proof (D-01 deviation from SC-3's "CI step" wording) — when annotating, reference MILESTONE-EVIDENCE.md, and do NOT reword the requirement to claim a recurring CI gate exists. Update ROADMAP.md: mark the Phase 38 entry complete (check the `[ ]` at line ~123 and update the Progress table row for Phase 38 to `Complete` with today's date 2026-06-10), and note the test count is 225 (not the stale "191" still in the goal text) if updating goal prose — but do NOT rewrite locked success criteria; SC-3 remains satisfied-by-evidence per D-01. Keep edits surgical — checkboxes, status cells, and the completion date only; do not restructure these planning docs.
  </action>
  <verify>
    <automated>grep -nE "MIG-05|MIG-06|CI-01|CI-02|CI-03" .planning/REQUIREMENTS.md | grep -i "complete"; grep -c "\[x\] \*\*Phase 38" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - All five req entries (MIG-05, MIG-06, CI-01, CI-02, CI-03) are `[x]` in REQUIREMENTS.md
    - The REQUIREMENTS.md status table shows all five as `Complete`
    - ROADMAP.md Phase 38 entry is checked and the Progress table marks Phase 38 Complete with 2026-06-10
    - CI-02's completion references the local one-shot evidence (not a recurring CI step) — no rewording claims a recurring CI gate
    - Edits are surgical (checkboxes/status/date only; no doc restructuring)
  </acceptance_criteria>
  <done>All five Phase 38 requirements are marked Complete with the evidence doc as backing, the phase is marked complete in ROADMAP, and the D-01 one-shot framing is preserved in the annotations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer shell → working tree | Proofs run locally (D-02 — no datacenter server, per project memory); read `_site/`, `_site_baseline/`, `data/`; write only the evidence doc and planning-doc edits |
| evidence doc → git history | The committed evidence records measurements; no secrets or runtime surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-03-REP | Repudiation / integrity of evidence | MILESTONE-EVIDENCE.md fabricated PASS | mitigate | Task 1 mandates capturing REAL `compare-sites.sh`/`build:data`/`npm test` output; an unexpected diff must be recorded and flagged, never rubber-stamped. The proof is reproducible by re-running `bash scripts/compare-sites.sh` locally. LOW. |
| T-38-03-STALE | Tampering (false proof) | stale `_site_baseline/` | mitigate | Task 1 checks `_site_baseline/` freshness (no `data/` change since 2026-06-09, RESEARCH.md Pitfall 3) before trusting the proof; a stale baseline would yield Parquet diffs unrelated to the migration. LOW. |
| T-38-03-TMP | Information disclosure | `compare-sites.sh` mktemp tree | accept | Inherited from 38-01: `mktemp -d` (0700), public site content only, `rm -rf` cleanup. LOW. |
| T-38-03-SC | Tampering | npm/pip/cargo installs | N/A | No packages installed (RESEARCH.md audit N/A). No runtime surface added — this plan only runs existing commands and writes docs. |

No high-severity findings. This plan adds no runtime surface and no CI changes — it runs existing local commands and records their honest output. The only integrity concern (fabricated evidence) is mitigated by requiring real captured output and a reproducible proof.
</threat_model>

<verification>
- `bash scripts/compare-sites.sh` reports byte-identical data + HTML-identical-modulo-hash (or the real diff is flagged)
- `time npm run build:data` measured once; seconds recorded; NO timer assertion added anywhere in the repo
- MILESTONE-EVIDENCE.md exists, records both proofs with real output, and preserves the one-shot (CI-02) vs permanent (MIG-06 guard) distinction
- All five req IDs marked Complete in REQUIREMENTS.md; Phase 38 marked complete in ROADMAP.md
- `bash scripts/check-ts-only.sh` exits 0 and `npm test` (225) green at milestone close
</verification>

<success_criteria>
- The CI-02 byte-identical proof is run LOCALLY and recorded as committed evidence (SC-3 satisfied per D-01 — NOT a recurring CI step)
- The CI-03 build:data timing is observed once and recorded (no flaky timer assertion — D-07)
- MILESTONE-EVIDENCE.md captures real measurements, the guard result, and a v3.0 completion declaration
- All five Phase 38 requirements (MIG-05, MIG-06, CI-01, CI-02, CI-03) marked Complete; v3.0 declared shipped
- The one-shot vs permanent distinction is preserved throughout the evidence and req annotations
</success_criteria>

<output>
Create `.planning/phases/38-ci-gate-full-verification/38-03-SUMMARY.md` when done
</output>
