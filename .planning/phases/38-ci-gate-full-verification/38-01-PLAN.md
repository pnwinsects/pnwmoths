---
phase: 38-ci-gate-full-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/check-ts-only.sh
  - scripts/compare-sites.sh
  - package.json
autonomous: true
requirements: [MIG-05, MIG-06, CI-02]
must_haves:
  truths:
    - "Running `bash scripts/check-ts-only.sh` on the current clean tree exits 0 and prints the OK line"
    - "Running `bash scripts/compare-sites.sh` performs the two-bucket diff (data strict, HTML normalized) against _site_baseline/"
    - "`npm test` still discovers and passes all 225 tests after the glob cleanup"
  artifacts:
    - path: "scripts/check-ts-only.sh"
      provides: "Permanent MIG-06 TS-only invariant guard (4 guards: .js sources, allowJs, @ts-ignore, double-casts)"
      min_lines: 35
    - path: "scripts/compare-sites.sh"
      provides: "One-shot D-03 two-bucket byte-identical proof helper (Parquet/JSON strict + HTML hash-normalized)"
      min_lines: 20
    - path: "package.json"
      provides: "MIG-05 test glob cleanup ({js,ts} alternations removed)"
      contains: "scripts/lib/*.test.ts"
  key_links:
    - from: "scripts/check-ts-only.sh"
      to: "scripts/ src/_lib src/_data src/components tsconfig*.json"
      via: "find/grep over converted source dirs"
      pattern: "find scripts src/_lib src/_data src/components"
    - from: "scripts/compare-sites.sh"
      to: "_site/ and _site_baseline/"
      via: "diff -r with Perl content-hash normalization"
      pattern: "perl -pe"
---

<objective>
Create the two new shell helpers and the package.json test-glob cleanup that the rest of Phase 38 depends on. None of these touch CI, so they form the foundation wave.

This plan delivers: (1) `scripts/check-ts-only.sh` — the PERMANENT MIG-06 cleanliness guard that pr-check.yml will invoke (38-02); (2) `scripts/compare-sites.sh` — the ONE-SHOT D-03 two-bucket byte-identical proof helper that the milestone proof (38-03) will run locally; (3) the MIG-05 package.json test-glob cleanup that drops the now-pointless `{js,ts}` alternations.

Purpose: All verification logic already exists in RESEARCH.md (every grep pattern and the Perl normalization are verified against the live codebase). This plan's job is to PLACE the verified scripts correctly, not to invent logic.
Output: `scripts/check-ts-only.sh`, `scripts/compare-sites.sh`, edited `package.json`.

Preserves the one-shot vs permanent distinction (D-01 vs D-08): `check-ts-only.sh` is permanent (wired into CI in 38-02); `compare-sites.sh` is a one-shot proof helper (run locally once in 38-03, never wired into CI).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/38-ci-gate-full-verification/38-RESEARCH.md
@.planning/phases/38-ci-gate-full-verification/38-PATTERNS.md
</context>

<artifacts_this_phase_produces>
Newly-created symbols/files (exclude from drift/source-grounding verification — they do not exist in the pre-phase tree):
- `scripts/check-ts-only.sh` — permanent MIG-06 guard (this plan)
- `scripts/compare-sites.sh` — one-shot D-03 proof helper (this plan)
- (later plans) the new pr-check.yml/deploy.yml steps; `.planning/phases/38-ci-gate-full-verification/MILESTONE-EVIDENCE.md`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Create the permanent MIG-06 guard script (scripts/check-ts-only.sh)</name>
  <files>scripts/check-ts-only.sh</files>
  <read_first>
    - .planning/phases/38-ci-gate-full-verification/38-RESEARCH.md §"D-08 Cleanliness Guard Script (Resolved)" — the COMPLETE verified script (lines ~301-347) and the four verified grep patterns
    - .planning/phases/38-ci-gate-full-verification/38-PATTERNS.md §"scripts/check-ts-only.sh" — the reproduced complete script + verified edge cases
    - .github/scripts/check-cdn-urls.py — the role-analog (committed CI-support script idiom in this repo)
  </read_first>
  <action>
    Replicate the complete `scripts/check-ts-only.sh` from RESEARCH.md §D-08 verbatim (do not re-derive the grep patterns — they are verified). Shebang `#!/usr/bin/env bash`, `set -e`, and a `FAIL=0` accumulator so all four guards run and report before exiting (per PATTERNS.md "Shell Script Exit Pattern"). The four guards, in order: (1) `find scripts src/_lib src/_data src/components -name "*.js"` must yield zero files; (2) `grep -l "allowJs" tsconfig*.json` must yield nothing; (3) `grep -rn "@ts-ignore" scripts/ src/ --include="*.ts"` filtered through `grep -v node_modules` must be empty; (4) `grep -rn "as unknown as" scripts/ src/ --include="*.ts"` filtered through `grep -v node_modules`, `grep -v "\.test\.ts"`, and `grep -v "\.d\.ts"` must be empty. On all-clean, echo the `OK: TS-only invariant: ...` line and `exit 0`; on any failure echo the offending matches and `exit 1`. Mark the file executable (`chmod +x scripts/check-ts-only.sh`). Do NOT broaden or narrow the verified exclusions — the `.test.ts` and `.d.ts` exclusions for guard 4 are load-bearing (see RESEARCH.md verified edge cases: `openseadragon.d.ts` comment, `verify-parquet.ts` single `as unknown[]` cast, `*.test.ts` mock casts).
  </action>
  <verify>
    <automated>chmod +x scripts/check-ts-only.sh && bash scripts/check-ts-only.sh; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/check-ts-only.sh` exits 0 on the current clean tree
    - `scripts/check-ts-only.sh` contains a `find` over `scripts src/_lib src/_data src/components` for `*.js`
    - `scripts/check-ts-only.sh` contains a grep for `allowJs`
    - `scripts/check-ts-only.sh` contains a grep for `@ts-ignore`
    - `scripts/check-ts-only.sh` contains a grep for `as unknown as` with `grep -v "\.test\.ts"` and `grep -v "\.d\.ts"` exclusions
    - `scripts/check-ts-only.sh` is executable (`test -x scripts/check-ts-only.sh`)
    - The script prints the `OK: TS-only invariant` line on success
  </acceptance_criteria>
  <done>The guard script exists, is executable, exits 0 on the clean tree, and enforces all four MIG-06 invariants with the verified exclusions.</done>
</task>

<task type="auto">
  <name>Task 2: Create the one-shot byte-identical proof helper (scripts/compare-sites.sh)</name>
  <files>scripts/compare-sites.sh</files>
  <read_first>
    - .planning/phases/38-ci-gate-full-verification/38-RESEARCH.md §"D-03 Content-Hash Normalization (Resolved)" — the verified Perl one-liner, the three-bucket classification, and the "Simpler recommended approach for the evidence doc" script
    - .planning/phases/38-ci-gate-full-verification/38-PATTERNS.md §"scripts/compare-sites.sh" — the reproduced complete two-bucket script
    - .planning/phases/34-scripts-lib-src-lib-migration/BASELINE.md — the established `diff -r _site/ _site_baseline/` baseline protocol this script extends
  </read_first>
  <action>
    Replicate the complete `scripts/compare-sites.sh` from PATTERNS.md §"scripts/compare-sites.sh" verbatim. Shebang `#!/usr/bin/env bash`, `set -e`. Three sections: Bucket A — strict data diff: `diff -r _site/species/ _site_baseline/species/ --include="*.parquet"` then `diff _site/species-states.json _site_baseline/species-states.json`, echo `DATA: byte-identical` on success. Bucket B — HTML normalized: create a `mktemp -d` temp dir, `find _site -name "*.html"`, for each file write a normalized copy of BOTH the current and baseline versions through the verified Perl normalizer `perl -pe 's{(-[A-Za-z0-9_-]{8})(\.(js|css|png))}{-HASH$2}g'`, then `diff -r` the two normalized trees, echo `HTML: identical modulo content-hash`, and `rm -rf` the temp dir. Bucket C — echo that `_site/assets/` is EXCLUDED (behavior covered by the test suite, per D-03/CI-02). Use Perl, NOT sed — RESEARCH.md Pitfall 2 documents that macOS BSD sed and Linux GNU sed disagree on `{8}`; Perl is portable. Mark the file executable (`chmod +x scripts/compare-sites.sh`). Note: this script is NOT wired into any workflow — it is run locally once in plan 38-03 (D-01/D-02 one-shot framing).
  </action>
  <verify>
    <automated>chmod +x scripts/compare-sites.sh && head -1 scripts/compare-sites.sh && grep -c "perl -pe" scripts/compare-sites.sh && test -x scripts/compare-sites.sh && echo "executable-ok"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/compare-sites.sh` contains `diff -r _site/species/ _site_baseline/species/ --include="*.parquet"`
    - `scripts/compare-sites.sh` contains `diff _site/species-states.json _site_baseline/species-states.json`
    - `scripts/compare-sites.sh` contains the Perl normalizer `perl -pe 's{(-[A-Za-z0-9_-]{8})(\.(js|css|png))}{-HASH$2}g'` (appears at least twice — current + baseline)
    - `scripts/compare-sites.sh` uses `mktemp -d` and `rm -rf` for the temp normalized trees
    - `scripts/compare-sites.sh` echoes that `_site/assets/` (Bucket C) is excluded
    - `scripts/compare-sites.sh` uses Perl, not sed (no `sed` invocation for hash normalization)
    - `scripts/compare-sites.sh` is executable
  </acceptance_criteria>
  <done>The proof helper exists, is executable, and encodes the verified two-bucket comparison (Bucket A strict, Bucket B Perl-normalized, Bucket C excluded). It is NOT referenced by any CI workflow.</done>
</task>

<task type="auto">
  <name>Task 3: Clean up package.json test globs to .ts-only (MIG-05)</name>
  <files>package.json</files>
  <read_first>
    - package.json line 24 — the current `"test"` script with the `{js,ts}` alternations
    - .planning/phases/38-ci-gate-full-verification/38-PATTERNS.md §"package.json" — the exact two substitutions
    - .planning/phases/38-ci-gate-full-verification/38-RESEARCH.md §"MIG-05 Details" — confirms zero `.js` test files remain, so the cleanup misses nothing
  </read_first>
  <action>
    In the `"test"` script string (package.json line 24), make exactly two substitutions and NO others: replace `'scripts/lib/*.test.{js,ts}'` with `'scripts/lib/*.test.ts'`, and replace `'src/_lib/*.test.{js,ts}'` with `'src/_lib/*.test.ts'`. Leave every other token in the test command unchanged (the explicit `.ts` test filenames and `src/components/*.test.ts` glob stay exactly as-is). Do not touch any other script. RESEARCH.md confirms zero `.js` test files remain, so this cleanup drops no coverage — it documents the completed migration.
  </action>
  <verify>
    <automated>grep -c "test.{js,ts}" package.json; npm test 2>&1 | grep -E "# tests|tests [0-9]+" | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep "test.{js,ts}" package.json` returns nothing (zero `{js,ts}` alternations remain)
    - package.json `"test"` script contains `scripts/lib/*.test.ts`
    - package.json `"test"` script contains `src/_lib/*.test.ts`
    - `npm test` still discovers and passes all 225 tests (0 fail, 0 skip)
  </acceptance_criteria>
  <done>The two `{js,ts}` test globs are narrowed to `.ts`-only; `npm test` continues to pass all 225 tests, proving no coverage was dropped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer shell → repo working tree | Scripts are authored and run locally; they read the source tree and `_site*/` snapshots, write only to `/tmp` (compare-sites) or stdout |
| (deferred to 38-02) CI runner → these scripts | `check-ts-only.sh` will be invoked by pr-check.yml in plan 38-02; threat surface assessed there |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-01-IJ | Injection (shell) | `compare-sites.sh` `find ... | while read f` over `_site/**/*.html` | accept | Filenames are build-generated by Eleventy/Vite (slugs from `data/species.csv`), not attacker-controlled. `read f` without `-r` is acceptable for these controlled paths; no `eval`/no interpolation into a shell command. LOW. |
| T-38-01-GLOB | Tampering | `check-ts-only.sh` glob/path expansion | mitigate | Patterns are fixed literals (`scripts src/_lib ...`), no user input concatenated into commands. `2>/dev/null` suppresses dir-missing noise without hiding match output. The `FAIL` accumulator ensures no silent pass. LOW. |
| T-38-01-TMP | Information disclosure | `compare-sites.sh` `mktemp -d` temp tree | accept | `mktemp -d` creates a private 0700 dir; normalized HTML copies contain only already-public site content; `rm -rf` cleans up. LOW. |
| T-38-01-SC | Tampering | npm/pip/cargo installs | N/A | No packages installed in this phase (RESEARCH.md "Package Legitimacy Audit — N/A"). No new dependencies. |

All threats LOW or N/A — this plan only adds local-only shell helpers and a one-line package.json edit, with no new runtime surface and no new external actions.
</threat_model>

<verification>
- `bash scripts/check-ts-only.sh` exits 0 on the clean tree (MIG-06 guard green)
- `npm test` passes all 225 tests after glob cleanup (MIG-05)
- `scripts/compare-sites.sh` is present, executable, and uses the verified Perl normalizer (CI-02 helper ready for 38-03)
- `npm run typecheck` still exits 0 (no source touched, but confirm no accidental breakage) — sampling per VALIDATION.md
</verification>

<success_criteria>
- Both shell scripts exist, are executable, and replicate the RESEARCH.md-verified logic exactly
- `check-ts-only.sh` enforces all four MIG-06 invariants and exits 0 on the clean tree
- `compare-sites.sh` encodes the two-bucket D-03 comparison and is NOT wired into any workflow
- package.json test globs are `.ts`-only; `npm test` still green (225 tests)
- The one-shot (compare-sites) vs permanent (check-ts-only) distinction is preserved
</success_criteria>

<output>
Create `.planning/phases/38-ci-gate-full-verification/38-01-SUMMARY.md` when done
</output>
