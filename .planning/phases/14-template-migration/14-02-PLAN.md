---
phase: 14-template-migration
plan: 02
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - src/species/species.njk
  - src/glossary/index.njk
autonomous: true
requirements:
  - TMPL-03
  - TMPL-04
  - TMPL-06

must_haves:
  truths:
    - "Species factsheet photos resolve through the CDN (no /images/ relative paths)"
    - "Glossary portrait images resolve through the CDN with Bunny Optimizer params"
    - "Glossary portraits have a srcset with a 2x width descriptor"
    - "The | url filter is absent from all CDN image src expressions"
    - "urlencode filter is applied to all filenames in CDN URL expressions"
  artifacts:
    - path: "src/species/species.njk"
      provides: "CDN URL for species photo img src"
      contains: "cdnBaseUrl"
    - path: "src/glossary/index.njk"
      provides: "CDN URL + srcset for glossary portrait img"
      contains: "srcset"
  key_links:
    - from: "src/species/species.njk"
      to: "https://pnwmoths.b-cdn.net"
      via: "{{ cdnBaseUrl }}/{{ sp.slug }}/{{ img.filename | urlencode }}"
      pattern: "cdnBaseUrl.*urlencode"
    - from: "src/glossary/index.njk"
      to: "https://pnwmoths.b-cdn.net"
      via: "{{ cdnBaseUrl }}/glossary/{{ term.image_filename | urlencode }}?width=188&height=225&crop_gravity=north"
      pattern: "cdnBaseUrl.*crop_gravity"
---

<objective>
Update `src/species/species.njk` and `src/glossary/index.njk` to construct image URLs using the `cdnBaseUrl` Nunjucks global and `urlencode` filter established in Plan 01.

Purpose: Every image served by the Eleventy-built site resolves through the bunny.net CDN. Local `/images/` paths are eliminated from templates. Glossary portraits gain a `srcset` 2x descriptor for HiDPI displays.

Output:
- `src/species/species.njk`: species photo `<img src>` updated to CDN URL
- `src/glossary/index.njk`: glossary portrait `<img src>` updated to CDN URL + Optimizer params + `srcset`
</objective>

<execution_context>
@/Users/rainhead/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rainhead/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/rainhead/dev/pnwmoths/.planning/ROADMAP.md
@/Users/rainhead/dev/pnwmoths/.planning/phases/14-template-migration/14-CONTEXT.md
@/Users/rainhead/dev/pnwmoths/.planning/phases/14-template-migration/14-PATTERNS.md
@/Users/rainhead/dev/pnwmoths/.planning/phases/14-template-migration/14-UI-SPEC.md
@/Users/rainhead/dev/pnwmoths/.planning/phases/14-template-migration/14-01-SUMMARY.md

<interfaces>
<!-- Existing state of template files — the exact lines being replaced. -->

From src/species/species.njk (lines 46–51, target section inside {% for img in spImages %}):
```html
<figure>
  <img src="/images/{{ sp.slug }}/{{ img.filename }}"
       alt="{{ sp.genus }} {{ sp.species }}"
       data-photographer="{{ img.photographer }}">
  <figcaption>{{ img.photographer }}</figcaption>
</figure>
```

From src/glossary/index.njk (lines 39–47, target section inside {% if term.image_filename %}):
```html
<figure>
  <img src="{{ ('/images/glossary/' + term.image_filename) | url }}"
       alt="{{ term.term }}"
       width="188" height="225">
  {% if term.photographer %}
    <figcaption>{{ term.photographer }}</figcaption>
  {% endif %}
</figure>
```

From eleventy.config.js (established in Plan 01):
```js
eleventyConfig.addFilter("urlencode", v => encodeURIComponent(v));
eleventyConfig.addGlobalData("cdnBaseUrl", CDN_BASE_URL);  // "https://pnwmoths.b-cdn.net"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update species.njk species photo img src to CDN URL</name>
  <files>src/species/species.njk</files>
  <read_first>
    - src/species/species.njk (read the full file — locate line 48 with `/images/{{ sp.slug }}/{{ img.filename }}`; confirm the surrounding figure/figcaption structure and the pnwm-image-slideshow context)
  </read_first>
  <action>
Replace the `src` attribute value on the species photo `<img>` element at line 48.

**Before** (line 48):
```html
<img src="/images/{{ sp.slug }}/{{ img.filename }}"
     alt="{{ sp.genus }} {{ sp.species }}"
     data-photographer="{{ img.photographer }}">
```

**After (locked contract from UI-SPEC):**
```html
<img src="{{ cdnBaseUrl }}/{{ sp.slug }}/{{ img.filename | urlencode }}"
     alt="{{ sp.genus }} {{ sp.species }}"
     data-photographer="{{ img.photographer }}">
```

Change ONLY the `src` attribute value. All other attributes (`alt`, `data-photographer`) and surrounding elements (`<figure>`, `<figcaption>`) remain unchanged.

Do NOT add `srcset` — deferred per CONTEXT.md D-04 (pnwm-image-slideshow drops srcset on slotted img elements in connectedCallback; cannot be wired in Phase 14).
Do NOT add Bunny Optimizer query params to species photo URLs — not required by Phase 14 success criteria (per CONTEXT.md, Claude's Discretion; defer to Phase 16).
Do NOT apply `| url` to the CDN URL — absolute URLs must NOT be prefixed by pathPrefix.
  </action>
  <verify>
    <automated>grep -n 'cdnBaseUrl.*sp\.slug.*urlencode' /Users/rainhead/dev/pnwmoths/src/species/species.njk && ! grep -q '/images/{{ sp.slug }}' /Users/rainhead/dev/pnwmoths/src/species/species.njk</automated>
  </verify>
  <acceptance_criteria>
    - `src/species/species.njk` contains `src="{{ cdnBaseUrl }}/{{ sp.slug }}/{{ img.filename | urlencode }}"` (exact string)
    - `grep '/images/{{ sp.slug }}' src/species/species.njk` returns 0 matches (old path eliminated)
    - `alt="{{ sp.genus }} {{ sp.species }}"` attribute is unchanged on the same img element
    - `data-photographer="{{ img.photographer }}"` attribute is unchanged on the same img element
    - No `srcset` attribute on the species photo img element
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Species factsheet photo img src uses CDN URL; no local /images/ path remains in the for-loop; build green</done>
</task>

<task type="auto">
  <name>Task 2: Update glossary/index.njk glossary portrait img src + add srcset</name>
  <files>src/glossary/index.njk</files>
  <read_first>
    - src/glossary/index.njk (read the full file — locate lines 39–47 with the `{% if term.image_filename %}` block and the `| url` expression; confirm surrounding dl/dt/dd and figcaption structure)
  </read_first>
  <action>
Replace the `<img>` element inside `{% if term.image_filename %}` at lines 41–43. The `<figure>` wrapper, `{% if term.photographer %}` block, and `<figcaption>` remain unchanged.

**Before** (lines 41–43):
```html
<img src="{{ ('/images/glossary/' + term.image_filename) | url }}"
     alt="{{ term.term }}"
     width="188" height="225">
```

**After (locked contract from UI-SPEC):**
```html
<img src="{{ cdnBaseUrl }}/glossary/{{ term.image_filename | urlencode }}?width=188&height=225&crop_gravity=north"
     srcset="{{ cdnBaseUrl }}/glossary/{{ term.image_filename | urlencode }}?width=376&height=450&crop_gravity=north 2x"
     alt="{{ term.term }}"
     width="188" height="225">
```

Constraints:
- Strip `| url` entirely from the src expression — it corrupts absolute CDN URLs by prepending pathPrefix.
- Retain `width="188" height="225"` HTML attributes — they match the 1x Optimizer dimensions.
- Bunny Optimizer 1x params: `?width=188&height=225&crop_gravity=north` (per Phase 13 D-10).
- Bunny Optimizer 2x params for srcset: `?width=376&height=450&crop_gravity=north 2x` (per CONTEXT.md D-03).
- `srcset` uses descriptor `2x` only — no pixel-width breakpoints.
- Do NOT use Image Class names in the URL — Image Classes are disabled on the Pull Zone (Phase 13 D-18); use direct query params only.
- Do NOT apply `| url` to any CDN URL expression.
  </action>
  <verify>
    <automated>grep -n 'srcset' /Users/rainhead/dev/pnwmoths/src/glossary/index.njk && grep -n 'crop_gravity=north' /Users/rainhead/dev/pnwmoths/src/glossary/index.njk && grep -c '| url' /Users/rainhead/dev/pnwmoths/src/glossary/index.njk</automated>
  </verify>
  <acceptance_criteria>
    - `src/glossary/index.njk` contains `src="{{ cdnBaseUrl }}/glossary/{{ term.image_filename | urlencode }}?width=188&height=225&crop_gravity=north"` (exact string)
    - `src/glossary/index.njk` contains `srcset="{{ cdnBaseUrl }}/glossary/{{ term.image_filename | urlencode }}?width=376&height=450&crop_gravity=north 2x"` (exact string)
    - `grep '| url' src/glossary/index.njk` returns 0 matches (| url filter fully removed)
    - `width="188" height="225"` HTML attributes present on the same img element
    - `alt="{{ term.term }}"` unchanged
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Glossary portrait img src uses CDN URL with 1x Optimizer params; srcset attribute present with 2x descriptor; | url filter absent; build green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Nunjucks data → HTML | term.image_filename and img.filename flow from build-time CSV data into img src/srcset attributes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03 | Tampering | urlencode filter on filenames | accept | Filenames originate from CSV data controlled by project owner (build-time, not user-supplied); encodeURIComponent is for URL correctness, not injection prevention in a static build context |
| T-14-04 | Information Disclosure | CDN URL in built HTML | accept | CDN URL is intentionally public; bunny.net serves these as public assets; no credentials or private paths in URLs |
</threat_model>

<verification>
After both tasks complete:
1. `npm run build` exits 0
2. `grep -r '/images/' src/species/species.njk` returns 0 matches (CDN URL used instead)
3. `grep 'cdnBaseUrl' src/species/species.njk` returns 1 match
4. `grep '| url' src/glossary/index.njk` returns 0 matches
5. `grep 'srcset' src/glossary/index.njk` returns 1 match with `2x` descriptor
6. `grep 'crop_gravity=north' src/glossary/index.njk` returns 2 matches (1x src + 2x srcset)
7. Build artifact check: `grep -r 'pnwmoths.b-cdn.net' _site/species/acronicta-americana/index.html` returns matches (CDN URLs in built HTML)
8. Build artifact check: `grep -r 'pnwmoths.b-cdn.net' _site/glossary/index.html` returns matches with srcset
</verification>

<success_criteria>
- All species factsheet `<img>` elements in built HTML use `https://pnwmoths.b-cdn.net/{slug}/{encoded-filename}` as src
- All glossary portrait `<img>` elements use CDN URL with `?width=188&height=225&crop_gravity=north` on src and `?width=376&height=450&crop_gravity=north 2x` on srcset
- No `/images/` relative paths remain in species.njk or glossary/index.njk
- No `| url` filter applied to any CDN image URL
- `npm run build` exits 0; `npm test` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/14-template-migration/14-02-SUMMARY.md`
</output>
