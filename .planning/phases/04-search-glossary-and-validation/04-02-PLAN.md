---
phase: 04-search-glossary-and-validation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/search/index.njk
  - src/_includes/base.njk
  - src/species/species.njk
autonomous: true
requirements:
  - SRCH-01
  - SRCH-02
  - SRCH-03
  - SRCH-04
must_haves:
  truths:
    - "Searching for a scientific name returns relevant species pages"
    - "Searching for a common name returns relevant species pages"
    - "Occurrence data (collector, county, coordinates) does not appear in search results"
    - "The search page works entirely client-side with no server"
    - "Navigation text does not pollute search results"
  artifacts:
    - path: "src/search/index.njk"
      provides: "Search page with Pagefind UI widget"
      contains: "pagefind-ui"
    - path: "src/_includes/base.njk"
      provides: "Shared layout with data-pagefind-ignore on nav"
      contains: "data-pagefind-ignore"
  key_links:
    - from: "src/search/index.njk"
      to: "_site/pagefind/pagefind-ui.js"
      via: "script src"
      pattern: "/pagefind/pagefind-ui.js"
    - from: "src/_includes/base.njk"
      to: "Pagefind indexer"
      via: "data-pagefind-ignore attribute"
      pattern: "data-pagefind-ignore"
---

<objective>
Implement the search page using Pagefind's built-in UI widget and configure Pagefind indexing exclusions. The search page replaces the existing stub with the Pagefind widget styled to match Pico CSS. The base layout and species template get data-pagefind-ignore attributes on navigation, web component elements, and noscript blocks to keep search results clean.

Purpose: Deliver SRCH-01 (Pagefind indexing), SRCH-02 (scientific + common name search), SRCH-03 (occurrence data excluded), SRCH-04 (client-side search page).
Output: Working search page at /search/ with Pagefind widget; clean indexing configuration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-search-glossary-and-validation/04-CONTEXT.md
@.planning/phases/04-search-glossary-and-validation/04-RESEARCH.md
@.planning/phases/04-search-glossary-and-validation/04-UI-SPEC.md

<interfaces>
From src/_includes/base.njk (current, needs data-pagefind-ignore on nav):
```html
<nav>
  <ul>
    <li><a href="/">Home</a></li>
    <li><a href="/browse/">Browse</a></li>
    <li><a href="/search/">Search</a></li>
    <li><a href="/glossary/">Glossary</a></li>
  </ul>
</nav>
<main>
  {{ content | safe }}
</main>
<script type="module" src="/components/main.js"></script>
```

From src/species/species.njk (web components needing data-pagefind-ignore, lines 33-39):
```html
<pnwm-filter-bar slug="{{ sp.slug }}"></pnwm-filter-bar>
<pnwm-occurrence-map slug="{{ sp.slug }}"></pnwm-occurrence-map>
<pnwm-phenology-chart slug="{{ sp.slug }}"></pnwm-phenology-chart>
<noscript>
  <p>Enable JavaScript to view the interactive map and filters.</p>
</noscript>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace search page stub with Pagefind UI widget</name>
  <files>src/search/index.njk</files>
  <read_first>
    - src/search/index.njk (current stub to replace)
    - .planning/phases/04-search-glossary-and-validation/04-UI-SPEC.md (search page markup, lines 116-137)
    - .planning/phases/04-search-glossary-and-validation/04-RESEARCH.md (Pattern 2, lines 152-186)
  </read_first>
  <action>
Replace the contents of src/search/index.njk with the Pagefind widget page per UI-SPEC and D-05, D-06, D-07.

The Nunjucks frontmatter block (delimited by triple-dash lines) must contain:
  layout: base.njk
  title: "Search -- PNW Moths"
  permalink: /search/index.html

Then the body content (after the frontmatter closing delimiter):

```html
<h1>Search</h1>
<link href="/pagefind/pagefind-ui.css" rel="stylesheet">
<style>
  #search {
    --pagefind-ui-primary: var(--pico-primary);
    --pagefind-ui-text: var(--pico-color);
    --pagefind-ui-background: var(--pico-background-color);
    --pagefind-ui-border: var(--pico-muted-border-color);
    --pagefind-ui-tag: var(--pico-card-background-color);
    --pagefind-ui-font: system-ui, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
  }
</style>
<div id="search"></div>
<script src="/pagefind/pagefind-ui.js"></script>
<script>
  window.addEventListener('DOMContentLoaded', () => {
    new PagefindUI({ element: '#search', showSubResults: false });
  });
</script>
<noscript data-pagefind-ignore>
  <p>Search requires JavaScript. <a href="/browse/">Browse all species</a> instead.</p>
</noscript>
```

Key details per decisions:
- Use Pagefind's built-in UI widget, NOT a custom wrapper (D-05, D-07)
- Style via --pagefind-ui-* CSS custom properties mapped to Pico CSS tokens (D-06)
- Do NOT exclude Pagefind's default stylesheet, override selectively (D-06)
- Do NOT set --pagefind-ui-scale, inherit Pagefind default (UI-SPEC)
- showSubResults: false prevents sub-headings appearing as separate results
- The noscript block has data-pagefind-ignore so fallback text is not indexed
- The /pagefind/ assets are generated by `pagefind --site _site` post-build (not at Eleventy build time)
  </action>
  <verify>
    <automated>grep -c "pagefind-ui" src/search/index.njk && grep -c "pico-primary" src/search/index.njk</automated>
  </verify>
  <acceptance_criteria>
    - src/search/index.njk contains `<link href="/pagefind/pagefind-ui.css" rel="stylesheet">`
    - src/search/index.njk contains `<script src="/pagefind/pagefind-ui.js"></script>`
    - src/search/index.njk contains `new PagefindUI({ element: '#search', showSubResults: false })`
    - src/search/index.njk contains `--pagefind-ui-primary: var(--pico-primary)`
    - src/search/index.njk contains `--pagefind-ui-text: var(--pico-color)`
    - src/search/index.njk contains `--pagefind-ui-background: var(--pico-background-color)`
    - src/search/index.njk contains `--pagefind-ui-border: var(--pico-muted-border-color)`
    - src/search/index.njk contains `--pagefind-ui-tag: var(--pico-card-background-color)`
    - src/search/index.njk contains `<noscript data-pagefind-ignore>`
    - src/search/index.njk contains `Browse all species</a> instead.`
    - src/search/index.njk does NOT contain "coming soon"
  </acceptance_criteria>
  <done>Search page has Pagefind UI widget with Pico CSS theme overrides and JS-off fallback</done>
</task>

<task type="auto">
  <name>Task 2: Add data-pagefind-ignore attributes to base layout and species template</name>
  <files>src/_includes/base.njk, src/species/species.njk</files>
  <read_first>
    - src/_includes/base.njk (add data-pagefind-ignore to nav)
    - src/species/species.njk (add data-pagefind-ignore to web components and noscript)
    - .planning/phases/04-search-glossary-and-validation/04-UI-SPEC.md (Pagefind Indexing Contract, lines 252-265)
    - .planning/phases/04-search-glossary-and-validation/04-CONTEXT.md (D-08, D-09)
  </read_first>
  <action>
1. In src/_includes/base.njk, add data-pagefind-ignore to the nav element. Change line 10 from:
   `<nav>`
   to:
   `<nav data-pagefind-ignore>`
   This is the only change to base.njk. Per D-09, nav boilerplate must not appear in search results.

2. In src/species/species.njk, add data-pagefind-ignore to the web component elements and noscript block. These render no static text content but are belt-and-suspenders per UI-SPEC indexing contract.

   Modify line 33: add ` data-pagefind-ignore` before the closing `>` on pnwm-filter-bar
   Modify line 34: add ` data-pagefind-ignore` before the closing `>` on pnwm-occurrence-map
   Modify line 35: add ` data-pagefind-ignore` before the closing `>` on pnwm-phenology-chart
   Modify line 36: change `<noscript>` to `<noscript data-pagefind-ignore>`
   Modify line 44: add ` data-pagefind-ignore` before the closing `>` on pnwm-image-slideshow

   The resulting elements should look like:
   ```html
   <pnwm-filter-bar slug="{{ sp.slug }}" data-pagefind-ignore></pnwm-filter-bar>
   <pnwm-occurrence-map slug="{{ sp.slug }}" data-pagefind-ignore></pnwm-occurrence-map>
   <pnwm-phenology-chart slug="{{ sp.slug }}" data-pagefind-ignore></pnwm-phenology-chart>
   <noscript data-pagefind-ignore>
   ```
   And for slideshow:
   ```html
   <pnwm-image-slideshow slug="{{ sp.slug }}" data-pagefind-ignore>
   ```

Note: Per D-08, occurrence data exclusion from search is architecturally guaranteed (data loads from Parquet client-side, never in HTML). The data-pagefind-ignore on web components is defense-in-depth. Species content areas (h1, dl, prose article, similar species section) are indexed by default. Do NOT add data-pagefind-ignore to them (SRCH-02 depends on this content being indexed).
  </action>
  <verify>
    <automated>grep -c "data-pagefind-ignore" src/_includes/base.njk && grep -c "data-pagefind-ignore" src/species/species.njk</automated>
  </verify>
  <acceptance_criteria>
    - src/_includes/base.njk contains `<nav data-pagefind-ignore>`
    - src/species/species.njk contains `pnwm-filter-bar` with `data-pagefind-ignore`
    - src/species/species.njk contains `pnwm-occurrence-map` with `data-pagefind-ignore`
    - src/species/species.njk contains `pnwm-phenology-chart` with `data-pagefind-ignore`
    - src/species/species.njk contains `pnwm-image-slideshow` with `data-pagefind-ignore`
    - src/species/species.njk contains `<noscript data-pagefind-ignore>`
    - src/species/species.njk does NOT have `data-pagefind-ignore` on h1, dl, or similar species section
  </acceptance_criteria>
  <done>Navigation and web component elements are excluded from Pagefind indexing; species content remains indexed</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pagefind bundle to browser | Third-party JS loaded from /pagefind/ directory |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-04-04 | Tampering | Pagefind JS bundle | accept | Bundle is generated locally by pagefind CLI from trusted npm package; not fetched from CDN at runtime |
| T-04-05 | Information Disclosure | Search results expose occurrence data | accept | Architecturally prevented: occurrence data never in HTML (loads from Parquet client-side); data-pagefind-ignore on web components is defense-in-depth |
</threat_model>

<verification>
1. src/search/index.njk contains Pagefind UI widget markup with all 6 CSS custom property overrides
2. src/_includes/base.njk has data-pagefind-ignore on nav
3. src/species/species.njk has data-pagefind-ignore on all 4 web component elements and noscript
4. Eleventy build succeeds: npx @11ty/eleventy produces _site/search/index.html
</verification>

<success_criteria>
- Search page at /search/ contains Pagefind widget with Pico-themed CSS custom properties
- JS-off fallback shows "Search requires JavaScript" with link to /browse/
- Navigation text excluded from Pagefind indexing via data-pagefind-ignore
- Web component elements and noscript blocks excluded from indexing
- Species scientific names, common names, and content remain indexed (no data-pagefind-ignore on content areas)
</success_criteria>

<output>
After completion, create `.planning/phases/04-search-glossary-and-validation/04-02-SUMMARY.md`
</output>
