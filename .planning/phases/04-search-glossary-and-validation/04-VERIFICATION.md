---
phase: 04-search-glossary-and-validation
verified: 2026-04-12T16:51:17Z
status: passed
score: 8/8 must-haves verified
overrides_applied: 0
---

# Phase 4: Search, Glossary, and Validation Verification Report

**Phase Goal:** Users can find species by name via static search, read the glossary, and the build catches bad links, excessive page weight, and data integrity problems before deploy.
**Verified:** 2026-04-12T16:51:17Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Searching for a scientific or common name returns relevant species pages with no server required | ✓ VERIFIED | Human browser confirmation (Plan 04-03 checkpoint): typing "Acronicta" returned species results; /browse/ page appearing in results is expected (genus names legitimately match species queries) |
| 2 | Occurrence record data does not appear as search results | ✓ VERIFIED | Occurrence data never embedded in HTML (loads from Parquet client-side); `data-pagefind-ignore` on all 4 web components and noscript in species.njk |
| 3 | The glossary page renders all terms in alphabetical order from the source data file | ✓ VERIFIED | `_site/glossary/index.html` has 4 letter sections (A, F, H, T) with 5 `id="term-"` anchors; DuckDB orders `ORDER BY term ASC` and groups by first letter |
| 4 | A post-build link check fails the build when any internal link points to a non-existent page | ✓ VERIFIED | `lychee --root-dir _site --offline '_site/**/*.html'` wired in `build:validate-links`; 94 links checked, 0 errors in latest build |
| 5 | The build emits a warning when any HTML page exceeds the page weight threshold | ✓ VERIFIED | `scripts/check-page-weight.js` with THRESHOLD_BYTES = 500 * 1024; exits 0 (warn-only per D-11); wired as final build step |
| 6 | The data validator catches invalid state values | ✓ VERIFIED | `validationChecks` array in `scripts/build-data.js` includes `invalid state values` check with state NOT IN ('WA', 'OR', 'ID', 'BC', 'AB', 'MT'); participates in hard-fail path (process.exit(1)) |
| 7 | The full build pipeline runs all 6 steps in sequence | ✓ VERIFIED | `npm run build` chains: build:data && build:eleventy && build:copy-parquet && build:pagefind && build:validate-links && build:check-weight; exits 0 |
| 8 | Navigation text does not pollute search results | ✓ VERIFIED | `<nav data-pagefind-ignore>` in `src/_includes/base.njk` propagated to all built HTML pages; browser test confirmed nav text did not appear as standalone results |

**Score:** 8/8 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `data/glossary.csv` | Glossary source data with 4 columns | ✓ VERIFIED | 5 data rows, header: `term,definition,image_filename,photographer` |
| `src/_data/glossary.js` | DuckDB query grouping glossary terms by letter | ✓ VERIFIED | Reads `data/glossary.csv`, returns object keyed by letter, `ORDER BY term ASC` |
| `src/glossary/index.njk` | Alphabetic glossary template | ✓ VERIFIED | `aria-label="Alphabetic index"`, `id="term-{{ term.slug }}"`, conditional image block |
| `src/search/index.njk` | Search page with Pagefind UI widget | ✓ VERIFIED | Contains `pagefind-ui.js`, all 6 Pico CSS custom property overrides, noscript fallback |
| `src/_includes/base.njk` | Shared layout with data-pagefind-ignore on nav | ✓ VERIFIED | `<nav data-pagefind-ignore>` present |
| `scripts/check-page-weight.js` | Page weight warning script | ✓ VERIFIED | `THRESHOLD_BYTES = 500 * 1024`, `walkHtml`, `process.env.SITE_DIR \|\| '_site'`, no `process.exit(1)` |
| `scripts/check-page-weight.test.js` | Behavioral tests for page weight check | ✓ VERIFIED | 2 tests pass: oversized triggers WARNING, under-threshold does not |
| `package.json` | Build script chain with all 6 steps | ✓ VERIFIED | `build:pagefind`, `build:validate-links`, `build:check-weight`; pagefind@1.5.2 in devDependencies |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `src/_data/glossary.js` | `data/glossary.csv` | `read_csv('data/glossary.csv'` | ✓ WIRED | Line 9: `read_csv('data/glossary.csv', ...)` |
| `src/glossary/index.njk` | `src/_data/glossary.js` | Eleventy data cascade | ✓ WIRED | `{% for letter, terms in glossary %}` — `glossary` matches data file name |
| `src/search/index.njk` | `_site/pagefind/pagefind-ui.js` | `<script src="/pagefind/pagefind-ui.js">` | ✓ WIRED | Script tag present; `/pagefind/` generated by `pagefind --site _site` post-build |
| `src/_includes/base.njk` | Pagefind indexer | `data-pagefind-ignore` on nav | ✓ WIRED | `<nav data-pagefind-ignore>` at line 10 |
| `package.json build script` | pagefind CLI | `npm run build:pagefind` → `pagefind --site _site` | ✓ WIRED | pagefind@1.5.2 in devDependencies; build step confirmed working |
| `package.json build script` | lychee CLI | `npm run build:validate-links` → `lychee ...` | ✓ WIRED | lychee at `/opt/homebrew/bin/lychee`; build exits 0 |
| `scripts/build-data.js` | records table state validation | `invalid state values` entry in validationChecks | ✓ WIRED | Line 149: description `'invalid state values'`, state NOT IN ('WA', 'OR', 'ID', 'BC', 'AB', 'MT') |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `src/glossary/index.njk` | `glossary` (object keyed by letter) | `src/_data/glossary.js` → DuckDB → `data/glossary.csv` | Yes — 5 CSV rows returned and grouped | ✓ FLOWING |
| `src/search/index.njk` | Search results | Pagefind index at `_site/pagefind/` | Yes — 13 pages indexed (203 words); browser-confirmed species results returned | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Glossary HTML has alphabetic sections | `grep -c "id=\"letter-" _site/glossary/index.html` | 4 | ✓ PASS |
| Glossary HTML has term anchors | `grep -c "id=\"term-" _site/glossary/index.html` | 5 | ✓ PASS |
| Build tests pass (build-data.test.js) | `node --test scripts/build-data.test.js` | 8/8 pass | ✓ PASS |
| Build tests pass (check-page-weight.test.js) | `node --test scripts/check-page-weight.test.js` | 2/2 pass | ✓ PASS |
| Full build pipeline completes | `npm run build` | exits 0, all 6 steps complete | ✓ PASS |
| Pagefind index generated | `ls _site/pagefind/pagefind-ui.js` | exists | ✓ PASS |
| Lychee link check passes | `build:validate-links` step | 94 links, 0 errors | ✓ PASS |
| Page weight check runs | `build:check-weight` step | all pages under 500KB | ✓ PASS |
| Search returns actual results | Browser test — typing "Acronicta" at /search/ | Species results returned; nav text absent from results | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| SRCH-01 | 04-02, 04-03 | Pagefind indexing in build pipeline | ✓ SATISFIED | `pagefind --site _site` in build; `_site/pagefind/` generated; 13 pages indexed |
| SRCH-02 | 04-02 | Scientific + common name search returns species pages | ✓ SATISFIED | Browser-confirmed (Plan 04-03 checkpoint): "Acronicta" returned species results; /browse/ page appearing is expected since genus names legitimately match species queries |
| SRCH-03 | 04-02 | Occurrence data excluded from search index | ✓ SATISFIED | Architectural: occurrence data never in HTML (loaded from Parquet client-side); `data-pagefind-ignore` on web components as defense-in-depth |
| SRCH-04 | 04-02 | Search works without JS (noscript fallback) | ✓ SATISFIED | `<noscript data-pagefind-ignore>` with link to /browse/ in `src/search/index.njk` |
| GLOS-01 | 04-01 | Glossary page renders alphabetized terms from data/glossary.csv | ✓ SATISFIED | Built `_site/glossary/index.html` has 4 letter sections, 5 term anchors; DuckDB sorts ORDER BY term ASC |
| VALD-01 | 04-03 | Post-build link checker hard-fails on broken internal links | ✓ SATISFIED | lychee wired in `build:validate-links`; `--offline` mode; 0 errors in latest build |
| VALD-02 | 04-03 | Page weight check warns on pages over configured threshold | ✓ SATISFIED | `scripts/check-page-weight.js` with 500KB threshold; exits 0 (warn-only per D-11); wired in build |
| VALD-03 | 04-01, 04-03 | Data validator catches invalid state/record_type values, orphaned species_id, out-of-bounds coordinates | ✓ SATISFIED | `validationChecks` array has 5 checks including `invalid state values`; validated with 8-test suite |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| No anti-patterns found | — | — | — | — |

All scan targets checked:
- `data/glossary.csv`: 5 substantive rows, no image references (antenna.jpg correctly removed by plan 03)
- `src/_data/glossary.js`: real DuckDB query, no stubs, data flows from CSV
- `src/glossary/index.njk`: full template, no TODO/placeholder text, no `| safe` on content
- `src/search/index.njk`: Pagefind widget complete, noscript fallback present, "coming soon" removed
- `src/_includes/base.njk`: `data-pagefind-ignore` added, no regressions
- `src/species/species.njk`: 5 `data-pagefind-ignore` attributes, content areas (h1, dl, prose) NOT excluded
- `scripts/check-page-weight.js`: `walkHtml` function, `THRESHOLD_BYTES`, no `process.exit(1)`, testable via env override
- `scripts/build-data.js`: `invalid state values` check in validationChecks, hard-fail path intact

### Human Verification Required

None. All must-haves verified — SRCH-02 confirmed by human browser test at Plan 04-03 checkpoint.

### Gaps Summary

No gaps. All 8 success criteria verified. SRCH-02 (search behavior) was confirmed by the human developer during the Plan 04-03 checkpoint: visiting /search/ after `npm run build`, typing "Acronicta" returned species results and navigation text did not appear as standalone results. The /browse/ page appearing in results is expected behavior since genus names legitimately match species queries.

**Note on _site/pagefind/ state:** The `_site/pagefind/` directory was absent at the start of verification because `npm install` had not been run in this worktree environment. After running `npm install`, `npm run build` succeeded and created `_site/pagefind/` with all Pagefind artifacts. This is expected behavior — pagefind is a devDependency that must be installed before the build step runs.

---

_Verified: 2026-04-12T16:51:17Z_
_Verifier: Claude (gsd-verifier)_
