---
phase: 07-code-quality-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/build-data.test.js
  - scripts/check-page-weight.test.js
  - package.json
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - WR-01
  - WR-02
  - WR-03
  - WR-04

must_haves:
  truths:
    - "npm test passes and covers WR-01 (glossary image_filename rejection)"
    - "npm test passes and covers WR-04 (missing SITE_DIR is handled without exception)"
    - "check-page-weight.test.js is included in the npm test command"
    - "REQUIREMENTS.md shows all four WR items checked off"
  artifacts:
    - path: "scripts/build-data.test.js"
      provides: "Test asserting invalid glossary.csv image_filename causes build-data.js to exit non-zero"
      contains: "WR-01"
    - path: "scripts/check-page-weight.test.js"
      provides: "Test asserting missing SITE_DIR is handled without unhandled exception"
      contains: "WR-04"
    - path: "package.json"
      provides: "npm test command includes check-page-weight.test.js"
      contains: "check-page-weight.test.js"
    - path: ".planning/REQUIREMENTS.md"
      provides: "All four WR items marked complete"
      contains: "[x] WR-04"
  key_links:
    - from: "package.json scripts.test"
      to: "scripts/check-page-weight.test.js"
      via: "node --test glob argument"
      pattern: "check-page-weight\\.test\\.js"
    - from: "scripts/build-data.test.js"
      to: "scripts/build-data.js glossary validation block (lines 80-87)"
      via: "integration test using wrapper script pattern"
      pattern: "glossary.*image_filename|image_filename.*glossary"
---

<objective>
Verify and test all four WR code fixes that were implemented during Phase 4 but never had
corresponding regression tests or requirements sign-off.

Purpose: The code changes for WR-01 through WR-04 are already committed. This plan adds
the missing test coverage that proves those fixes hold, wires the check-page-weight test
file into npm test, and marks all four requirements complete in REQUIREMENTS.md.

Output:
- Two augmented test files with regression tests for WR-01 and WR-04
- Updated package.json test command including check-page-weight.test.js
- Updated REQUIREMENTS.md with all four WR items checked off
</objective>

<execution_context>
@/Users/rainhead/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rainhead/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add regression tests for WR-01 and WR-04, wire check-page-weight.test.js into npm test</name>
  <files>scripts/build-data.test.js, scripts/check-page-weight.test.js, package.json</files>

  <read_first>
    - scripts/build-data.test.js — read the full file to understand the integration test pattern (the wrapper-script approach used in the "bad CSV data" test at the bottom, lines 169-212)
    - scripts/build-data.js — lines 80-87 (the glossary image_filename validation block being tested)
    - scripts/check-page-weight.js — lines 28-31 (the existsSync ENOENT guard being tested)
    - scripts/check-page-weight.test.js — read the full file to understand the spawnSync test pattern already used
    - package.json — read the "test" script to see the current command
  </read_first>

  <behavior>
    WR-01 test (add to build-data.test.js):
    - Test name: "integration: build-data.js rejects invalid image_filename in glossary.csv"
    - Setup: create a tmpDir with copies of data/species.csv, data/images.csv, data/records.csv,
      and a custom glossary.csv whose image_filename column contains "bad file!.jpg" (contains a space and exclamation — fails /^[a-zA-Z0-9._-]+$/)
    - Run: write a wrapper .mjs that imports main() from build-data.js, sets cwd to tmpDir, calls main()
    - Assert: process exits non-zero AND stderr contains "Invalid image_filename"
    - Teardown: rmSync tmpDir

    WR-04 test (add to check-page-weight.test.js):
    - Test name: "check-page-weight.js: handles missing SITE_DIR without unhandled exception"
    - Setup: a SITE_DIR path that does not exist (e.g. join(ROOT, '_nonexistent_dir_wr04'))
    - Run: spawnSync('node', [SCRIPT], { env: { ...process.env, SITE_DIR: nonexistentDir } })
    - Assert: result.status is not null (process exited cleanly — not killed by signal)
      AND (result.stdout + result.stderr) includes "[page-weight]"
      AND result.error is undefined (no spawn error)
    - Do NOT assert exit code 0 — the script exits 1 for missing SITE_DIR, which is correct

    package.json change:
    - Current test script: `node --test scripts/build-data.test.js src/components/*.test.js`
    - New test script: `node --test scripts/build-data.test.js scripts/check-page-weight.test.js src/components/*.test.js`
    - Add scripts/check-page-weight.test.js between build-data.test.js and src/components/*.test.js
  </behavior>

  <action>
    1. Read scripts/build-data.test.js in full, then append a new test after the last test (line 212).
       Use the same wrapper-script pattern as the existing "bad CSV data" integration test:
       - Create tmpDir at resolve(ROOT, '.tmp-glossary-wr01')
       - Create tmpDataDir = resolve(tmpDir, 'data')
       - mkdirSync(tmpDataDir, { recursive: true })
       - Copy species.csv, images.csv, records.csv from resolve(ROOT, 'data/') unchanged
       - Write a custom glossary.csv with an invalid image_filename:
         Content (exact string, no trailing space):
         ```
         term,definition,image_filename,photographer
         alula,A small lobe at the base of a wing,bad file!.jpg,Test Photographer
         ```
       - Write a wrapper .mjs at resolve(tmpDir, 'run-glossary-bad.mjs'):
         ```js
         import { main } from '/absolute/path/to/scripts/build-data.js';
         process.chdir('/absolute/path/to/tmpDir');
         main().catch(err => { console.error(err.message); process.exit(1); });
         ```
         (Use scriptPath = resolve(ROOT, 'scripts/build-data.js') for the absolute path)
       - execSync the wrapper, catch the thrown error (non-zero exit)
       - Assert threw === true
       - Assert stderrOutput includes 'Invalid image_filename'
       - Finally: rmSync(tmpDir, { recursive: true, force: true })

    2. Read scripts/check-page-weight.test.js in full, then append a new test after the last test.
       Use the existing spawnSync pattern:
       - const NONEXISTENT = join(ROOT, '_nonexistent_wr04_dir')
       - const result = spawnSync('node', [SCRIPT], { cwd: ROOT, encoding: 'utf8', env: { ...process.env, SITE_DIR: NONEXISTENT } })
       - assert.ok(result.error === undefined || result.error === null, 'No spawn error')
       - assert.ok(result.status !== null, 'Process exited (not signal-killed)')
       - assert.ok((result.stdout + result.stderr).includes('[page-weight]'), 'Output contains [page-weight] prefix')
       Note: do NOT assert result.status === 0 — the script exits 1 for missing SITE_DIR, which is correct behavior per WR-04

    3. Edit package.json "test" script:
       From: "node --test scripts/build-data.test.js src/components/*.test.js"
       To:   "node --test scripts/build-data.test.js scripts/check-page-weight.test.js src/components/*.test.js"
  </action>

  <verify>
    <automated>cd /Users/rainhead/dev/pnwmoths && npm test 2>&1 | tail -20</automated>
  </verify>

  <done>
    - npm test exits 0
    - Output includes "build-data.js rejects invalid image_filename in glossary.csv" passing
    - Output includes "handles missing SITE_DIR without unhandled exception" passing
    - Output includes check-page-weight.test.js tests running (the two existing tests also pass)
    - package.json "test" script contains "scripts/check-page-weight.test.js"
  </done>
</task>

<task type="auto">
  <name>Task 2: Mark WR-01 through WR-04 complete in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>

  <read_first>
    - .planning/REQUIREMENTS.md — read in full to see the current checkbox state and Traceability table
  </read_first>

  <action>
    Update .planning/REQUIREMENTS.md:

    1. Change all four checkboxes from `- [ ]` to `- [x]`:
       - `- [x] **WR-01**: Build script validates...`
       - `- [x] **WR-02**: Pagefind CSS...`
       - `- [x] **WR-03**: DuckDB connection...`
       - `- [x] **WR-04**: `check-page-weight.js` handles...`

    2. Update the Traceability table — add plan column values:
       | WR-01 | 7 | 07-01 |
       | WR-02 | 7 | 07-01 |
       | WR-03 | 7 | 07-01 |
       | WR-04 | 7 | 07-01 |

    Note on implementation status (for executor awareness, not to include in the file):
    All four fixes were applied in Phase 4 fix commits:
    - WR-01: fix(04) commit 07ba873 — glossary.csv image_filename validated at lines 80-87 of build-data.js
    - WR-02: fix(04) commit 15958d0 — pagefind-ui.css in base.njk <head> at line 12
    - WR-03: fix(04) commit 755673c — conn.closeSync() + db.closeSync() in glossary.js lines 33-34
    - WR-04: fix(04) commit 4080a0f — existsSync guard in check-page-weight.js lines 28-31
    Phase 7 adds regression tests and closes the tracking loop.
  </action>

  <verify>
    <automated>grep -c "\- \[x\]" /Users/rainhead/dev/pnwmoths/.planning/REQUIREMENTS.md</automated>
  </verify>

  <done>
    - All four WR items show `- [x]` (checked) in REQUIREMENTS.md
    - grep -c "\- \[x\]" returns 4
    - Traceability table shows plan 07-01 for all four WR items
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → file system | Tests create/delete temp directories; no user input involved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | Temp test directories | accept | Tests clean up with rmSync in finally blocks; no persistent artifacts |
| T-07-02 | Information Disclosure | build-data.js error messages | accept | Error messages reveal CSV field values — intentional for maintainer debugging, not a security boundary |
</threat_model>

<verification>
Run the full test suite and confirm all tests pass:

```bash
cd /Users/rainhead/dev/pnwmoths && npm test
```

Expected: All pre-existing tests pass plus the two new regression tests:
- "build-data.js rejects invalid image_filename in glossary.csv" — PASS
- "check-page-weight.js: handles missing SITE_DIR without unhandled exception" — PASS

Confirm WR-02 (FOUC fix) via grep — should already be in place:
```bash
grep -n "pagefindUi" /Users/rainhead/dev/pnwmoths/src/_includes/base.njk
```
Expected: line 12 contains `{% if pagefindUi %}<link rel="stylesheet" href="/pagefind/pagefind-ui.css">{% endif %}`

Confirm WR-03 (DuckDB close) via grep:
```bash
grep -n "closeSync" /Users/rainhead/dev/pnwmoths/src/_data/glossary.js
```
Expected: two lines — conn.closeSync() and db.closeSync()
</verification>

<success_criteria>
- npm test exits 0 with all tests passing including the two new regression tests
- REQUIREMENTS.md has all four WR items checked: grep -c "\- \[x\]" returns 4
- package.json test script includes scripts/check-page-weight.test.js
- src/_includes/base.njk line 12 contains pagefind-ui.css link in head (WR-02 confirmed)
- src/_data/glossary.js has both conn.closeSync() and db.closeSync() (WR-03 confirmed)
</success_criteria>

<output>
After completion, create `.planning/phases/07-code-quality-fixes/07-01-SUMMARY.md`
</output>
