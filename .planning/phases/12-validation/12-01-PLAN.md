---
phase: 12-validation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/pnwm-taxon-browser.js
  - src/styles/theme.css
  - src/species/species.njk
  - .planning/config.json
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "npm run build exits 0 with no new errors"
    - "_site/species-states.json is present in the built output"
    - "npm run build:validate-links reports 0 Errors"
    - "npm test shows fail 0 across all 58 tests"
    - "data-pagefind-ignore attribute is present on #taxon-data in _site/browse/index.html"
    - "All v1.3 requirements are marked Complete in REQUIREMENTS.md"
    - "ROADMAP.md marks Phase 12 and the v1.3 milestone complete"
    - "STATE.md shows progress 100% and status Complete"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Traceability table with all v1.3 requirements marked Complete"
      contains: "Complete"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 12 checked off, v1.3 milestone closed"
      contains: "v1.3 Visual Browse"
    - path: ".planning/STATE.md"
      provides: "Milestone close-out state"
      contains: "percent: 100"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "v1.3 phases"
      via: "Traceability table rows"
      pattern: "Complete"
    - from: "_site/browse/index.html"
      to: "#taxon-data"
      via: "data-pagefind-ignore attribute"
      pattern: "data-pagefind-ignore"
---

<objective>
Close out the v1.3 Visual Browse milestone: commit the four UAT toolbar-polish files that are
currently unstaged, run the full verification checklist to confirm all four success criteria are
met, and update the planning documents to mark the milestone complete.

Purpose: The milestone is functionally complete — this plan makes that official by committing
the last changes, verifying every success criterion, and updating the planning record.
Output: A clean git history, verified build output, and updated planning docs marking v1.3 shipped.
</objective>

<execution_context>
@/Users/rainhead/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rainhead/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit UAT toolbar-polish changes</name>
  <files>src/components/pnwm-taxon-browser.js, src/styles/theme.css, src/species/species.njk, .planning/config.json</files>
  <read_first>
    - `src/components/pnwm-taxon-browser.js` — confirm `align-items:baseline` is present in toolbar styles (the UAT fix; was `align-items:center`)
    - `src/styles/theme.css` — confirm `.pnwm-tb-family-row` and related disclosure button rules are present
    - `src/species/species.njk` — confirm image URL uses raw `/images/...` path (not `| url` filter — that caused Vite double-prefix)
    - `.planning/config.json` — confirm `_auto_chain_active: true`
  </read_first>
  <action>
Stage and commit exactly these four files — no others:

```bash
git add src/components/pnwm-taxon-browser.js src/styles/theme.css src/species/species.njk .planning/config.json
git commit -m "fix(11): commit UAT toolbar polish — baseline alignment and button styles"
```

These files contain changes from the Phase 11 UAT session that were verified correct and tested
(58/58 tests green) but never staged. They must be committed before the verification task runs
so the final build reflects the complete Phase 11 + UAT state.

Do NOT use `git add .` or `git add -A` — only the four files listed above.
  </action>
  <verify>
    <automated>git log --oneline -1 | grep "UAT toolbar polish" &amp;&amp; git diff HEAD -- src/components/pnwm-taxon-browser.js src/styles/theme.css src/species/species.njk .planning/config.json | wc -l | grep "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - `git log --oneline -1` contains "UAT toolbar polish"
    - `git diff HEAD -- src/components/pnwm-taxon-browser.js` is empty (file committed, no remaining diff)
    - `git diff HEAD -- src/styles/theme.css` is empty
    - `git diff HEAD -- src/species/species.njk` is empty
    - `git diff HEAD -- .planning/config.json` is empty
    - `git status` shows no unstaged changes for those four files
  </acceptance_criteria>
  <done>All four UAT polish files committed under the Phase 11 fix commit; working tree is clean for those files.</done>
</task>

<task type="auto">
  <name>Task 2: Run verification checklist and update planning docs</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` — read the full Traceability table (lines 51–66) before editing; current statuses are Pending/Partial/Complete
    - `.planning/ROADMAP.md` — read the Phase 12 entry and the v1.3 section header before editing
    - `.planning/STATE.md` — read the frontmatter block and Progress section before editing
  </read_first>
  <action>
**Step 1 — Run verification checklist (in order). Stop and report if any step fails.**

```bash
# SC-1: Clean build
npm run build
# Expected: exits 0; "vite v7.x.x building..." then "✓ built"; "0 errors"
# Pre-existing advisory (not an error): "can't be bundled without type='module'" for pagefind-ui.js

# SC-1b: species-states.json present
ls _site/species-states.json
# Expected: file listed, no "No such file" error

# SC-3: Full test suite
node --test scripts/build-data.test.js scripts/check-page-weight.test.js src/components/*.test.js
# Expected: "# fail 0" and "# pass 58" in output (or higher if tests were added)

# SC-4: pagefind-ignore on taxonomy data
grep "data-pagefind-ignore" _site/browse/index.html
# Expected: at least one line containing both "taxon-data" and "data-pagefind-ignore"

# SC-2: Link checker (runs after build as part of build:validate-links)
npm run build:validate-links
# Expected: "0 Errors" in lychee output
# "2 Unsupported" (base64 data: URIs) and excluded counts are acceptable — only "Errors" matter
```

If any check fails, stop and report the failure before editing any planning docs.

**Step 2 — Update REQUIREMENTS.md**

Edit `.planning/REQUIREMENTS.md`:

1. In the requirements list (lines 10–28), change every `- [ ]` to `- [x]` for the twelve v1.3 requirements (TAXON-01/02/03, BROWSE-01 through BROWSE-07, SFILT-01, SFILT-02). BROWSE-01 is already `[x]` — leave it.

2. In the Traceability table (lines 53–66), update the Status column:
   - TAXON-01 | Phase 8 | Complete  (was Pending)
   - TAXON-02 | Phase 8 | Complete  (was Pending)
   - TAXON-03 | Phase 8 | Complete  (was Pending)
   - BROWSE-01 | Phase 10 | Complete  (already Complete — leave)
   - BROWSE-02 | Phase 11 | Complete  (was Pending)
   - BROWSE-03 | Phase 11 | Complete  (was Pending)
   - BROWSE-04 | Phase 11 | Complete  (was Pending)
   - BROWSE-05 | Phase 11 | Complete  (was Pending)
   - BROWSE-06 | Phase 11 | Complete  (was Pending)
   - BROWSE-07 | Phase 10 | Complete  (was Partial — both clauses now done)
   - SFILT-01 | Phase 9 | Complete  (was Pending)
   - SFILT-02 | Phase 11 | Complete  (was Pending)

3. Update the last-updated comment at the bottom to `2026-04-20 after Phase 12 close-out`.

**Step 3 — Update ROADMAP.md**

Edit `.planning/ROADMAP.md`:

1. Line 11 — change `🚧 **v1.3 Visual Browse**` to `✅ **v1.3 Visual Browse** — Phases 8–12 (shipped 2026-04-20)` (remove "in progress").

2. In the Phase 12 entry under `### 🚧 v1.3 Visual Browse (In Progress)`:
   - Change `- [ ] **Phase 12: Validation**` to `- [x] **Phase 12: Validation**`

3. In the Phase 12 Plans section, replace:
   ```
   Plans:
   - [ ] TBD
   ```
   with:
   ```
   Plans:
   - [x] 12-01-PLAN.md — Commit UAT polish, run verification checklist, update planning docs
   ```

4. Update the Plans count line under Phase 12 to: `**Plans**: 1 plan`

5. In the Progress table (around line 142), update the Phase 12 row:
   ```
   | 12. Validation | v1.3 | 1/1 | Complete | 2026-04-20 |
   ```
   (was `0/? | Not started | -`)

6. Wrap the entire `### 🚧 v1.3 Visual Browse (In Progress)` section (phases 8–12 plus all their Phase Details sections) in a `<details>` block matching the v1.0–v1.2 folded pattern:
   ```markdown
   <details>
   <summary>✅ v1.3 Visual Browse (Phases 8–12) — SHIPPED 2026-04-20</summary>

   ...existing content for phases 8–12...

   </details>
   ```

**Step 4 — Update STATE.md**

Edit `.planning/STATE.md`:

1. Replace the frontmatter block entirely:
   ```yaml
   ---
   gsd_state_version: 1.0
   milestone: v1.3
   milestone_name: Visual Browse
   status: Complete
   stopped_at: ~
   last_updated: "2026-04-20T00:02:00Z"
   last_activity: 2026-04-20
   progress:
     total_phases: 5
     completed_phases: 5
     total_plans: 11
     completed_plans: 11
     percent: 100
   ---
   ```

2. Update the Current Position section (under `## Current Position`):
   ```
   Phase: 12 of 12 (Validation)
   Plan: complete
   Status: Phase 12 complete — all v1.3 success criteria verified
   Last activity: 2026-04-20 — v1.3 milestone closed
   ```

3. Update the progress bar line:
   ```
   Progress: [██████████] 100%
   ```

4. Update the `**Current focus:**` line to: `**Current focus:** v1.3 milestone complete`

**Step 5 — Commit planning doc updates**

```bash
git add .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md
git commit -m "docs(12): mark v1.3 milestone complete — all requirements verified"
```
  </action>
  <verify>
    <automated>grep -c "Complete" .planning/REQUIREMENTS.md | grep -E "^1[0-9]|^[2-9][0-9]" &amp;&amp; grep "percent: 100" .planning/STATE.md &amp;&amp; grep "SHIPPED 2026-04-20" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 (check `echo $?` after run)
    - `ls _site/species-states.json` succeeds (file exists)
    - `node --test ...` output contains `# fail 0`
    - `grep "data-pagefind-ignore" _site/browse/index.html` returns a line containing both "taxon-data" and "data-pagefind-ignore"
    - `npm run build:validate-links` output contains "0 Errors"
    - `grep "^| TAXON-01" .planning/REQUIREMENTS.md` returns a line ending in `Complete`
    - `grep "^| SFILT-02" .planning/REQUIREMENTS.md` returns a line ending in `Complete`
    - `grep "^| BROWSE-07" .planning/REQUIREMENTS.md` returns a line ending in `Complete` (not Partial)
    - `grep "percent: 100" .planning/STATE.md` returns a match
    - `grep "SHIPPED 2026-04-20" .planning/ROADMAP.md` returns a match
    - `grep "\- \[x\] \*\*Phase 12" .planning/ROADMAP.md` returns a match (Phase 12 checked off)
    - `git log --oneline -1` contains "mark v1.3 milestone complete"
  </acceptance_criteria>
  <done>All four SC items verified green; REQUIREMENTS.md, ROADMAP.md, and STATE.md committed with v1.3 marked complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → git | Only four specific files are staged; git add by name prevents accidental secret inclusion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | git commit (Task 1) | accept | Only named files staged; no secrets in those four files; project is non-public hobby site |
| T-12-02 | Information Disclosure | lychee link check output | accept | Link check runs against local _site/; no external network requests for internal links; lychee.toml excludes image extensions |
</threat_model>

<verification>
Run in order after both tasks complete:

```bash
# Full build (SC-1)
npm run build

# Artifact check (SC-1b)
ls _site/species-states.json

# Unit tests (SC-3)
node --test scripts/build-data.test.js scripts/check-page-weight.test.js src/components/*.test.js

# Pagefind index audit (SC-4)
grep "data-pagefind-ignore" _site/browse/index.html

# Link check (SC-2)
npm run build:validate-links
```

Expected: all five commands produce no errors. Lychee "Unsupported" and "Excluded" counts are safe.
</verification>

<success_criteria>
1. `npm run build` exits 0 — no new errors
2. `_site/species-states.json` exists with species-state pairs
3. `npm run build:validate-links` reports `0 Errors`
4. `node --test ...` reports `# fail 0` (58+ pass)
5. `grep "data-pagefind-ignore" _site/browse/index.html` matches `id="taxon-data"`
6. All twelve v1.3 requirements marked Complete in REQUIREMENTS.md
7. ROADMAP.md shows Phase 12 `[x]` and v1.3 milestone as `✅ SHIPPED 2026-04-20`
8. STATE.md frontmatter has `percent: 100` and `status: Complete`
9. Two new commits present in git log (UAT polish + planning docs close-out)
</success_criteria>

<output>
After completion, create `.planning/phases/12-validation/12-01-SUMMARY.md`

Include:
- Commit hash(es) for Task 1 (UAT polish) and Task 2 (planning docs)
- Build output summary: exit code, module count, any warnings
- Test run output: pass/fail counts
- Lychee output summary: Total / OK / Errors / Excluded / Unsupported
- Verification grep results for SC-4 (data-pagefind-ignore line from _site/browse/index.html)
- Confirmation that all four SC items are met
- v1.3 milestone close-out note
</output>
