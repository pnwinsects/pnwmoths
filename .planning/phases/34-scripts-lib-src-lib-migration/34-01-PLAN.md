---
phase: 34-scripts-lib-src-lib-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - .planning/phases/34-scripts-lib-src-lib-migration/BASELINE.md
autonomous: true
requirements: [MIG-01]
must_haves:
  truths:
    - "A pre-migration _site/ baseline is captured to a stable path so the byte-identity gate (SC-4) can run after the conversion plans"
    - "The package.json test script globs match both .js and .test.ts files in scripts/lib/ and src/_lib/ so the suite keeps passing during the .js→.ts transition and after"
    - "Running the full test suite on the still-.js codebase (no files converted yet) is green under the broadened globs — proving the glob change alone introduces no regression"
  artifacts:
    - path: ".planning/phases/34-scripts-lib-src-lib-migration/BASELINE.md"
      provides: "Pre-migration baseline manifest: _site/ snapshot location, species-page count, and the exact diff command for the SC-4 byte-identity gate"
      contains: "species pages"
    - path: "package.json"
      provides: "Broadened test globs covering .test.ts in scripts/lib and src/_lib"
      contains: "scripts/lib/*.test.{js,ts}"
  key_links:
    - from: "package.json"
      to: "scripts/lib/*.test.ts and src/_lib/*.test.ts"
      via: "node --test glob"
      pattern: "scripts/lib/\\*\\.test\\.\\{js,ts\\}"
---

<objective>
Wave-0 setup for the scripts/lib & src/_lib migration: capture the pre-migration `_site/` baseline that the byte-identity gate depends on, and broaden the `package.json` test globs so the converted `.ts` tests run under Node 24 native type-stripping without breaking the still-`.js` suite mid-migration.

Purpose: The phase's no-regression gate (ROADMAP SC-4) requires a frozen pre-migration `_site/` to diff against. That snapshot MUST be taken before any rename. The test-glob change must land before the conversion plans so the renamed `.test.ts` files are picked up — and it must be a no-op on the current all-`.js` suite (proven by a green run here). This plan owns `package.json` exclusively so the parallel Wave-2 conversion plans never touch it.

Output: A committed `BASELINE.md` recording the snapshot path, species-page count, and diff command; a `package.json` with `*.test.{js,ts}` globs for the two migrated directories.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-scripts-lib-src-lib-migration/34-CONTEXT.md
@.planning/phases/34-scripts-lib-src-lib-migration/34-RESEARCH.md
@package.json
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 01 contribution)

- `.planning/phases/34-scripts-lib-src-lib-migration/BASELINE.md` — records the pre-migration `_site/` snapshot path, the species-page count (data-determined; 1,433 as of 2026-06-09), and the exact `diff -r` command the conversion plans use for the SC-4 byte-identity gate.
- `package.json` `test` script — globs `scripts/lib/*.test.js`→`scripts/lib/*.test.{js,ts}` and `src/_lib/*.test.js`→`src/_lib/*.test.{js,ts}` (Node 24 selects both extensions; RESEARCH Finding 2).
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Capture the pre-migration _site/ baseline</name>
  <files>.planning/phases/34-scripts-lib-src-lib-migration/BASELINE.md</files>
  <read_first>
    - package.json (the `build` script chain — to know what produces `_site/`)
    - .planning/phases/34-scripts-lib-src-lib-migration/34-RESEARCH.md §"Validation Architecture" → "Wave 0 Gaps" (baseline-before-rename requirement) and §"Phase Requirements → Test Map" (the `diff -r _site/ _site_baseline/` row)
    - .planning/phases/33-toolchain-schema-scaffolding/33-VERIFICATION.md (the 1,433 species-page count and the page-count note — 1,364 is a stale Phase 17 figure; current is 1,433 and data-determined)
  </read_first>
  <action>
    Run a full clean `npm run build` against the current all-`.js` codebase (no source files converted yet) to produce `_site/`. Snapshot it to a stable sibling path `_site_baseline/` via `cp -R _site _site_baseline` (NOT tracked by git — `_site/` is already gitignored; `_site_baseline/` is a working-tree-only baseline for the duration of the phase, do NOT commit it).

    Record the species-page count with `ls -d _site/species/*/index.html | wc -l` (expect 1,433 as of 2026-06-09 — note in BASELINE.md that this number is data-determined and may grow; the gate is "unchanged vs this snapshot", not a fixed literal).

    Author `.planning/phases/34-scripts-lib-src-lib-migration/BASELINE.md` (this file IS committed) capturing: (a) the snapshot path `_site_baseline/`, (b) the recorded species-page count and the snapshot date, (c) the exact gate command the conversion plans run after migrating — `diff -r _site/ _site_baseline/` must report no differences (allow the documented exception that there are none expected, since Phase 34 changes no templates, data, or output), (d) a one-line note that this snapshot must be regenerated only if it predates an unrelated data change.

    Do NOT place fenced code blocks in this plan's action; the executor writes the commands into BASELINE.md as documentation.
  </action>
  <verify>
    <automated>test -d _site_baseline && test "$(ls -d _site/species/*/index.html | wc -l | tr -d ' ')" = "$(ls -d _site_baseline/species/*/index.html | wc -l | tr -d ' ')" && grep -q "species pages" .planning/phases/34-scripts-lib-src-lib-migration/BASELINE.md</automated>
  </verify>
  <acceptance_criteria>
    - `_site_baseline/` exists and contains the same species-page count as the freshly built `_site/`.
    - `BASELINE.md` exists, names the `_site_baseline/` path, records the species-page count, and contains the exact `diff -r _site/ _site_baseline/` gate command.
    - `_site_baseline/` is NOT staged/committed (it is a working-tree artifact); `BASELINE.md` IS committed.
  </acceptance_criteria>
  <done>A working-tree `_site_baseline/` snapshot exists and a committed BASELINE.md documents the snapshot path, page count, and the byte-identity diff command for downstream plans.</done>
</task>

<task type="auto">
  <name>Task 2: Broaden package.json test globs to cover .test.ts and prove no regression</name>
  <files>package.json</files>
  <read_first>
    - package.json (the `test` script — current globs `scripts/lib/*.test.js` and `src/_lib/*.test.js` at line 26)
    - .planning/phases/34-scripts-lib-src-lib-migration/34-RESEARCH.md §"Finding 2" (mixed `*.test.{js,ts}` glob selects both extensions on Node 24.15.0; no flag needed) and §"Validation Architecture" → "Wave 0 Gaps"
  </read_first>
  <action>
    Edit the `test` script in `package.json`: change the segment `scripts/lib/*.test.js` to `scripts/lib/*.test.{js,ts}` and the segment `src/_lib/*.test.js` to `src/_lib/*.test.{js,ts}`. Leave every other glob in the `test` script unchanged — the scripts-level files (`scripts/*.test.js`, `eleventy.config.test.js`, `src/components/*.test.js`) stay `.js` through Phase 34 and keep their `.js` globs.

    The `{js,ts}` brace glob matches both extensions; during this plan no `.test.ts` files exist yet, so the glob currently resolves to the same `.js` files. This MUST be a no-op on the current suite. Run `npm test` and confirm the full suite is green (same ~191 tests as before this plan), proving the glob change alone introduces no regression. Do NOT add any `--experimental-strip-types` flag — Node 24.15.0 strips `.ts` natively by default (RESEARCH Finding 2).
  </action>
  <verify>
    <automated>grep -q 'scripts/lib/\*\.test\.{js,ts}' package.json && grep -q 'src/_lib/\*\.test\.{js,ts}' package.json && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` `test` script contains `scripts/lib/*.test.{js,ts}` and `src/_lib/*.test.{js,ts}`; no other glob changed.
    - No `--experimental-strip-types` (or equivalent) flag added to the `test` script.
    - `npm test` exits 0 with the same suite green as before the change (still all-`.js` files matched at this point).
  </acceptance_criteria>
  <done>The two migrated-directory globs accept `.test.ts`; `npm test` is green on the unchanged all-`.js` suite, confirming the glob change is a no-op pre-conversion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build script → `_site/` filesystem | The build writes generated HTML/data; no untrusted input crosses here. The baseline snapshot is a local copy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01 | Tampering | `_site_baseline/` snapshot drift | accept | Snapshot is local working-tree only and regenerated per BASELINE.md note; no security value, no network surface. |
| T-34-SC | Tampering | npm/pip/cargo installs | accept | No packages installed in Phase 34 (RESEARCH §"Package Legitimacy Audit" — audit not required); no install task exists in this plan. |
</threat_model>

<verification>
- `_site_baseline/` exists with a matching species-page count to a fresh `_site/`.
- `BASELINE.md` committed with snapshot path, count, and diff command.
- `package.json` globs broadened to `*.test.{js,ts}` for both migrated dirs only.
- `npm test` green on the unchanged all-`.js` suite.
</verification>

<success_criteria>
- A pre-migration `_site/` baseline is frozen for the SC-4 byte-identity gate.
- The test runner is ready to pick up `.test.ts` files without breaking the current suite.
- `package.json` is owned exclusively by this plan; Wave-2 conversion plans do not touch it.
</success_criteria>

<output>
Create `.planning/phases/34-scripts-lib-src-lib-migration/34-01-SUMMARY.md` when done
</output>
