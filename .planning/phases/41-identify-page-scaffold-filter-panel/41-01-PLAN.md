---
phase: 41-identify-page-scaffold-filter-panel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/build-key.ts
  - scripts/build-key.test.ts
  - data/key-matrix.json
autonomous: true
requirements: [IDENT-02]
must_haves:
  truths:
    - "data/key-matrix.json contains exactly 8 distinct category strings (no stray-quote 9th category)"
    - "The Abdomen and thorax category covers all 8 of its questions (7 real + the recovered stray-quote question), 16 states total"
    - "parseCharacterLabel strips leading/trailing double-quotes from labels and states"
  artifacts:
    - path: "scripts/build-key.ts"
      provides: "parseCharacterLabel with quote-stripping normalization"
      contains: "replace(/^\"|\"$/g"
    - path: "scripts/build-key.test.ts"
      provides: "Stray-quote normalization test cases + 8-category invariant test"
      contains: "Abdomen and thorax"
    - path: "data/key-matrix.json"
      provides: "Regenerated key matrix with 8 clean categories"
      contains: "Abdomen and thorax"
  key_links:
    - from: "scripts/build-key.ts"
      to: "data/key-matrix.json"
      via: "npm run build:key regeneration"
      pattern: "parseCharacterLabel"
---

<objective>
Fix the "Abdomen and thorax" stray-quote artifact at its source in `scripts/build-key.ts`, extend the build-key test suite to lock the fix and the 8-category invariant, and regenerate `data/key-matrix.json` so every downstream consumer (this phase's filter panel, Phase 42, Phase 43) sees exactly 8 clean categories.

Purpose: The filter panel (Plan 03) and the no-JS species/character lists (Plan 02) group characters by `category`. The committed `data/key-matrix.json` currently has NINE distinct category strings: `Abdomen and thorax` (7 questions / 14 states) and a spurious `"Abdomen and thorax` (1 question / 2 states) produced by a `csv-parse` `relax_quotes` artifact on an embedded-quote field. The ROADMAP and D-01 require 8 categories. Fixing at the build-script source keeps the artifact out of the committed JSON and out of every downstream phase. This is a hard prerequisite (Wave 0) for ALL Phase 41 UI work.

Output: A one-line normalization in `parseCharacterLabel`, two new test cases, and a regenerated 8-category `data/key-matrix.json`.

Decisions implemented: D-01 (all 8 categories — this fix merges the stray-quote 9th into the single `Abdomen and thorax` category so the panel groups exactly 8 per D-01).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-identify-page-scaffold-filter-panel/41-RESEARCH.md
@.planning/phases/41-identify-page-scaffold-filter-panel/41-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan produces NO new browser-facing symbols. It modifies an existing build-time function and a data file:
- `scripts/build-key.ts` `parseCharacterLabel(label)` — gains a quote-stripping pre-pass (existing exported function, signature unchanged).
- `data/key-matrix.json` — regenerated artifact with 8 distinct `characters[].category` strings (no behavior-format change; only the stray category string is corrected).

(New symbols `pnwm-identify` element + class, `buildCategoryMap`, `_selectionCountForCategory`, `_hasSelection`, `_clearAll`, `keyMatrix.ts` default export / `KeySpeciesWithFamily` + `familyGroups`, and new files `src/identify/index.njk`, `src/components/pnwm-identify.ts`, `src/_data/keyMatrix.ts` are produced by Plans 02 and 03.)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add stray-quote test cases and the 8-category invariant test to build-key.test.ts</name>
  <files>scripts/build-key.test.ts</files>
  <read_first>
    - `scripts/build-key.test.ts` (the existing `describe('parseCharacterLabel', ...)` block at lines 82–106 and the `describe('main (integration)', ...)` block at lines 164–173 — mirror their `node:test` + `node:assert/strict` import and structure)
    - `scripts/build-key.ts` lines 62–87 (current `parseCharacterLabel`) and the `main()` integration path it exercises
    - `.planning/phases/41-identify-page-scaffold-filter-panel/41-RESEARCH.md` § "Q1: Stray-Quote Artifact" (the exact raw label `'"Abdomen and thorax:Abdomen:Does it appear as if the tip of the abdomen was '` and the expected cleaned output)
    - `.planning/phases/41-identify-page-scaffold-filter-panel/41-VALIDATION.md` (the `41-W0-strayquote` row: `node --test scripts/build-key.test.ts` must exit 0; `data/key-matrix.json` must have exactly 8 distinct categories)
  </read_first>
  <behavior>
    - Test: `parseCharacterLabel('"Abdomen and thorax:Abdomen:Does it appear as if the tip of the abdomen was dipped in a different color?:Yes"')` returns `category === 'Abdomen and thorax'` (no leading quote) and `state === 'Yes'` (no trailing quote).
    - Test: `parseCharacterLabel` still parses a clean 3-part label and a clean 4-part label unchanged (regression guard — keep the existing assertions passing).
    - Test (integration, extends the existing `main (integration)` describe block): after `execSync('node scripts/build-key.ts')`, reading `data/key-matrix.json` and computing `new Set(characters.map(c => c.category)).size` equals exactly `8`, and the set does NOT contain any string beginning with a `"` character.
  </behavior>
  <action>
    In `scripts/build-key.test.ts`, add two test cases inside the existing `describe('parseCharacterLabel', ...)` block: one asserting the leading-quote label normalizes to `category: 'Abdomen and thorax'` and `state: 'Yes'`, and one regression case confirming a clean label is unaffected. Then extend the existing `describe('main (integration)', ...)` block with a new `test('emits exactly 8 distinct categories with no stray-quote artifact', ...)` that runs `execSync('node scripts/build-key.ts', { cwd: ROOT, stdio: 'pipe' })`, reads `data/key-matrix.json` via `readFileSync` + `JSON.parse`, builds `const cats = new Set(matrix.characters.map(c => c.category))`, and asserts `cats.size === 8` and `[...cats].every(c => !c.startsWith('"'))`. Use the file's existing `ROOT` constant and `node:assert/strict`. These tests are RED until Task 2 lands the fix and regenerates the JSON.
  </action>
  <verify>
    <automated>node --test scripts/build-key.test.ts 2>&1 | grep -E "Abdomen|8 distinct" || echo "new tests present"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/build-key.test.ts` contains a test case using the literal string `Abdomen and thorax` asserting the cleaned category and state.
    - `scripts/build-key.test.ts` contains an integration assertion that `new Set(characters.map(c => c.category)).size === 8`.
    - Running `node --test scripts/build-key.test.ts` BEFORE Task 2 shows the new category-count / stray-quote assertions FAILING (RED), proving they exercise the fix.
  </acceptance_criteria>
  <done>Two parseCharacterLabel test cases + one 8-category integration assertion exist and are RED against the current (unfixed) code.</done>
</task>

<task type="auto">
  <name>Task 2: Normalize quotes in parseCharacterLabel and regenerate data/key-matrix.json</name>
  <files>scripts/build-key.ts, data/key-matrix.json</files>
  <read_first>
    - `scripts/build-key.ts` lines 62–87 (the `parseCharacterLabel` function body — the fix is one line added before `label.split(':')`)
    - `.planning/phases/41-identify-page-scaffold-filter-panel/41-PATTERNS.md` § "`scripts/build-key.ts` — `parseCharacterLabel`" (the exact minimal diff: `const cleaned = label.replace(/^"|"$/g, ''); const parts = cleaned.split(':');`)
    - `package.json` `build:key` script (`node scripts/build-key.ts`) and the `build` pipeline order (build:key runs before build:eleventy; the regenerated JSON must be committed)
  </read_first>
  <action>
    In `scripts/build-key.ts` `parseCharacterLabel`, insert a quote-stripping pre-pass as the first statements of the function body: `const cleaned = label.replace(/^"|"$/g, '');` then change `const parts = label.split(':');` to `const parts = cleaned.split(':');`. Leave every subsequent reference to `parts` unchanged — the existing `.trim()` on each field already handles surrounding whitespace; the regex strips the leading `"` on `category` and the trailing `"` on `state` that `csv-parse` `relax_quotes` leaves on the embedded-quote field (`...was "dipped" in...`). Do NOT alter `normalizeBinomial`, `binomialToSlug`, `resolveSlug`, `buildBitset`, or `main()`. Then run `npm run build:key` to regenerate `data/key-matrix.json` (and `data/key-coverage-report.json`) and commit the regenerated `data/key-matrix.json` as part of this plan. Do NOT hand-edit the JSON — it must be produced by the fixed script.
  </action>
  <verify>
    <automated>npm run build:key && node -e "const d=require('./data/key-matrix.json'); const c=new Set(d.characters.map(x=>x.category)); if(c.size!==8) throw new Error('expected 8 categories, got '+c.size); if([...c].some(x=>x.startsWith(String.fromCharCode(34)))) throw new Error('stray quote remains'); console.log('OK 8 categories, no stray quote');"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/build-key.ts` `parseCharacterLabel` contains `replace(/^"|"$/g, '')`.
    - `node --test scripts/build-key.test.ts` exits 0 (all parseCharacterLabel + the new 8-category integration assertion now GREEN).
    - `node -e "const d=require('./data/key-matrix.json'); console.log(new Set(d.characters.map(c=>c.category)).size)"` prints `8`.
    - No category string in `data/key-matrix.json` begins with a `"` character.
    - The `Abdomen and thorax` category groups 16 states (the recovered stray-quote question's 2 states are now under the correct category).
  </acceptance_criteria>
  <done>parseCharacterLabel strips surrounding quotes; `npm run build:key` regenerates an 8-category `data/key-matrix.json`; the full build-key test suite is GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Build-time-only data transform; no network, no auth, no persistence, no runtime user input |

No threat surface — this plan modifies a build script that reads a committed CSV/JSON and emits a committed JSON artifact. The only input is `data/key-characters.csv` (committed, Zod-validated downstream by `KeyMatrixSchema.parse()` in `build-key.ts`'s existing `main()`). ASVS: N/A (RESEARCH.md "Security Domain").
</threat_model>

<verification>
- `node --test scripts/build-key.test.ts` exits 0 (parseCharacterLabel cases + 8-category integration assertion green).
- `data/key-matrix.json` has exactly 8 distinct `characters[].category` values, none starting with `"`.
- `npm test` still passes for the full suite (no regression to other build-key behaviors).
</verification>

<success_criteria>
- The stray-quote artifact is eliminated at source; `data/key-matrix.json` is the canonical 8-category artifact every Phase 41/42/43 consumer reads.
- The fix is locked by a regression test so a future `build-key.ts` change cannot silently reintroduce the 9th category.
</success_criteria>

<output>
Create `.planning/phases/41-identify-page-scaffold-filter-panel/41-01-SUMMARY.md` when done.
</output>
