---
phase: 27-synonym-curation-pass
plan: 03
type: execute
wave: 2
depends_on:
  - 27-01
  - 27-02
files_modified:
  - _instructions/CURATING_SPECIES_SYNONYMS.md
autonomous: true
requirements:
  - CURATE-01
  - CURATE-03

tags:
  - operator-doc
  - curator-runbook
  - non-developer-audience

must_haves:
  truths:
    - "L-05: `_instructions/CURATING_SPECIES_SYNONYMS.md` exists at the locked path and follows the v2.x runbook section structure (What This Changes → Before You Start → Steps → Verify → When Things Go Wrong) — mirrors INGESTING_HIGH_RES_PHOTOS.md (Phase 26) and UPLOADING_IMAGES.md (Phase 13)"
    - "The runbook's audience is a non-technical curator (matching the 'non-technical maintainers' core value in PROJECT.md); the explanation never requires the reader to read Plan 02's code or know any classifier internals"
    - "The runbook quotes the top-12 unmatched binomials from CONTEXT.md 'Specifics' verbatim so the curator immediately knows the highest-impact decisions: smerinthus ophthalmica (32 files), grammia nevadensis (10), sericosema wilsonensis (8), pheosia rimosa (8), pero occidentalis (8), macaria signaria (8), iridopsis emasculatum (8), hydriomena irata (8), dysstroma hersiliata (8), drepanulatrix hulstii (8), drepanulatrix bifilata (8), digrammia muscariata (8)"
    - "The runbook documents the locked workflow: open the manifest in a spreadsheet → identify a from_binomial → look up the target species in data/species.csv → add a row to data/species-synonyms.csv → run `npm run photos:investigate` → confirm the promotion → commit synonyms.csv + manifest in the SAME PR (D-01 audit-trail-via-git)"
    - "D-01: the runbook explicitly tells the curator to commit synonyms.csv + manifest together; no `decided_by` / `decided_on` / `note` column instruction appears anywhere"
    - "D-02: the runbook specifies the canonical lowercase forms — `from_binomial` matches manifest's binomial_raw exactly (lowercased, space-separated); `to_species_slug` matches species.csv slug form (lowercased, hyphen-joined)"
    - "D-08: the runbook does NOT instruct the curator to pre-fill any well-known synonyms before normal use — the curation pass IS the first set of rows"
    - "L-04: the runbook references the ≥95% target metric (up from 77.3% baseline) so the curator knows what 'done' looks like for one pass"
    - "L-02: the runbook mentions the new bucket name `resolved-via-synonym` so the curator knows what to look for in the spreadsheet"
    - "The runbook explicitly excludes deferred topics — no mention of GBIF/ITIS, no preview/dry-run flag, no genus-wildcard syntax, no decided_by/decided_on/note column, no *custom/ folder, no DROPBOX_TOKEN requirement (curation is offline)"
    - "The runbook calls out the 22-row unreachable residue (rows with empty binomial_raw) as deferred (D-07) and instructs the curator to leave them alone"
    - "The runbook explains the `synonym-warn target-not-in-species-csv` failure mode and how to fix it (typo in to_species_slug → look up the correct slug in data/species.csv)"
    - "The runbook's command examples reference REAL commands the Plan 02 code ships: `npm run photos:investigate` (the canonical curator command), no `RESORT_ONLY=1` direct invocation in user-facing instructions"
    - "Document length is in the 80-250 line band that matches Phase 26's INGESTING_HIGH_RES_PHOTOS.md (114 lines) and Phase 13's UPLOADING_IMAGES.md (similar)"
  artifacts:
    - path: "_instructions/CURATING_SPECIES_SYNONYMS.md"
      provides: "Curator-facing operator runbook for the synonym-curation workflow; the human-readable companion to Plan 02's code"
      contains: "from_binomial"
  key_links:
    - from: "_instructions/CURATING_SPECIES_SYNONYMS.md"
      to: "data/species-synonyms.csv"
      via: "instructions for adding curator-authored rows"
      pattern: "data/species-synonyms\\.csv"
    - from: "_instructions/CURATING_SPECIES_SYNONYMS.md"
      to: "data/species-photos-manifest.csv"
      via: "instructions for opening the manifest in a spreadsheet and identifying high-impact rows"
      pattern: "data/species-photos-manifest\\.csv"
    - from: "_instructions/CURATING_SPECIES_SYNONYMS.md"
      to: "scripts/ingest-photos.js (via npm alias)"
      via: "command reference `npm run photos:investigate` (Plan 02's load-bearing alias)"
      pattern: "npm run photos:investigate"

decisions_covered:
  - D-01
  - D-02
  - D-07
  - D-08
  - L-04
  - L-05
---

<objective>
Author `_instructions/CURATING_SPECIES_SYNONYMS.md` — the non-technical-curator-facing runbook that turns Plan 01's seed CSV + Plan 02's synonym-aware classify cascade into a workflow a maintainer can actually execute. The runbook is L-05's hard requirement.

Purpose: Phase 27 deliverable #3 from CONTEXT.md "Phase Boundary". CURATE-01 says the curator "can author a flat data/species-synonyms.csv … editable by a non-technical curator following the `_instructions/` pattern" — the `_instructions/` pattern is enforced by this runbook existing in the established shape (mirrors `_instructions/INGESTING_HIGH_RES_PHOTOS.md` from Phase 26 and `_instructions/UPLOADING_IMAGES.md` from Phase 13). CURATE-03 (investigation view) is satisfied by Plan 02 leaving sortForInvestigation untouched (L-03); this runbook surfaces that workflow.

Output: one Markdown file at `_instructions/CURATING_SPECIES_SYNONYMS.md`, ~80-250 lines, following the locked v2.x runbook structure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-synonym-curation-pass/27-CONTEXT.md
@.planning/phases/27-synonym-curation-pass/27-PATTERNS.md
@.planning/phases/27-synonym-curation-pass/27-01-SUMMARY.md
@.planning/phases/27-synonym-curation-pass/27-02-SUMMARY.md
@_instructions/INGESTING_HIGH_RES_PHOTOS.md
@_instructions/UPLOADING_IMAGES.md
@_instructions/ADDING_PHOTO.md
@data/species-synonyms.csv
@data/species.csv

<interfaces>
<!-- The locked v2.x curator-runbook section structure (L-05, mirrored from -->
<!-- INGESTING_HIGH_RES_PHOTOS.md and UPLOADING_IMAGES.md): -->
<!--                                                                                       -->
<!--   # Task: <action-oriented title>                                                     -->
<!--   ## What This Changes                                                                -->
<!--   ## Before You Start                                                                 -->
<!--   ## Steps                                                                            -->
<!--   ###   1. <step>                                                                     -->
<!--   ###   2. <step>                                                                     -->
<!--   ###   ... etc. (numbered sub-headers as the existing analogs use them)              -->
<!--   ## Verify                                                                           -->
<!--   ## When Things Go Wrong                                                             -->
<!--                                                                                       -->
<!-- The commands the runbook references (already shipped by Plan 02): -->
<!--   npm run photos:investigate    — Plan 02 enriched this to also reclassify against    -->
<!--                                   the current synonyms.csv (D-05); no Dropbox calls   -->
<!--   (No DROPBOX_TOKEN is required for the curator workflow — Phase 27 is offline)      -->
<!--                                                                                       -->
<!-- The file paths the runbook references (all exist as committed repo artifacts after -->
<!-- Plan 01 + Plan 02): -->
<!--   data/species-synonyms.csv       — Plan 01 (header-only seed)                       -->
<!--   data/species-photos-manifest.csv — Phase 26 output (Plan 26-04 committed it)        -->
<!--   data/species.csv                — Phase 17 (the slug column is the truth source)    -->
<!--                                                                                       -->
<!-- The locked log-line shapes (from Plan 02 logStage; the runbook quotes these in -->
<!-- "When Things Go Wrong"): -->
<!--   <ISO> <12-char hash prefix> synonym-warn     target-not-in-species-csv  <from> → <to> -->
<!--   <ISO> <12-char hash prefix> reclassify       resolved-via-synonym       <binomial> → <slug> -->
<!--                                                                                       -->
<!-- The L-04 target metric, verbatim from ROADMAP.md success criterion #4: -->
<!--   "clean-or-resolved match rate measurably rises (target ≥ 95%, up from 77.3% baseline)" -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author _instructions/CURATING_SPECIES_SYNONYMS.md mirroring INGESTING_HIGH_RES_PHOTOS.md and UPLOADING_IMAGES.md shape</name>
  <files>_instructions/CURATING_SPECIES_SYNONYMS.md</files>
  <read_first>
    - _instructions/INGESTING_HIGH_RES_PHOTOS.md (entire 114-line file — port the section structure: top-level title, What This Changes, Before You Start, numbered Steps with `###` sub-headers, Verify, When Things Go Wrong; the tone is direct, second-person, no jargon; tables for schema info; fenced code blocks for commands)
    - _instructions/UPLOADING_IMAGES.md (entire file — sibling Phase 13 runbook for the same audience; confirms the section structure and the prose style)
    - _instructions/ADDING_PHOTO.md (the schema-as-Markdown-table pattern if the runbook needs to render either CSV's columns; useful as an analog for the data/species-synonyms.csv schema block)
    - .planning/phases/27-synonym-curation-pass/27-CONTEXT.md (D-01 schema, D-02 form rules, D-07 unreachable residue note, D-08 seed-is-empty, L-04 ≥95% target, L-05 doc requirement, plus the "Specifics" section's verbatim top-12 list)
    - .planning/phases/27-synonym-curation-pass/27-PATTERNS.md (the "_instructions/CURATING_SPECIES_SYNONYMS.md (curator/operator runbook)" section — explicit per-section content rules, the suggested 7-step list, the verbatim top-12 binomial block, the explicit "Concrete things NOT to include" list)
    - data/species-synonyms.csv (Plan 01's committed file — confirm the path and header are stable so the runbook references the actual on-disk artifact)
    - data/species.csv (first ~5 rows — to confirm the genus + species columns and the slug derivation rule the runbook walks the curator through)
    - .planning/phases/27-synonym-curation-pass/27-02-SUMMARY.md (Plan 02's just-completed implementation — confirm the actual logStage shapes and the `[ingest-photos] re-sorted manifest; N rows; M promoted to resolved-via-synonym` line so the runbook's Verify and Steps sections quote real output, not hypothetical)
  </read_first>
  <action>
    Author a new file at `_instructions/CURATING_SPECIES_SYNONYMS.md` mirroring the section structure of `_instructions/INGESTING_HIGH_RES_PHOTOS.md`. The audience is a non-technical maintainer; use second-person prose, avoid implementation jargon, and prefer concrete examples over abstract explanations.

    Required top-level structure (the six section headers must appear in this exact order):

    1. `# Task: Curate Species Synonyms` — the action-oriented title (mirrors `# Task: Ingest High-Res Species Photos`).

    2. `## What This Changes` — a 3-bullet list:
       - `data/species-synonyms.csv` — new rows the curator authors; one per `from_binomial → to_species_slug` decision
       - `data/species-photos-manifest.csv` — rows whose `binomial_raw` matches one of the curator's decisions get re-routed to `match_bucket: resolved-via-synonym` with `binomial_resolved` and `species_slug` populated from `data/species.csv`
       - **No** Dropbox API calls, **no** bunny.net writes, **no** file downloads, **no** DROPBOX_TOKEN required — synonym curation is an in-repo edit + re-classify loop

    3. `## Before You Start` — list of prerequisites in the INGESTING_HIGH_RES_PHOTOS.md style:
       - **Node 24** — matches `.nvmrc`; verify with `node --version`
       - A spreadsheet program (Excel, LibreOffice Calc, or Google Sheets with the CSV imported) for reading `data/species-photos-manifest.csv` and (optionally) `data/species.csv`
       - A text editor for `data/species-synonyms.csv` — direct CSV editing is fine; the file is two columns, header on line 1
       - You do NOT need a Dropbox token for this task. Phase 27 curation is offline.

    4. `## Steps` — seven numbered `### N. <step-title>` sub-sections (mirror INGESTING_HIGH_RES_PHOTOS.md "### 1. Create a Dropbox app and access token", etc.). Use the suggested step list from 27-PATTERNS.md "_instructions/CURATING_SPECIES_SYNONYMS.md" with these specifics:

       ### 1. Open the manifest in a spreadsheet — Open `data/species-photos-manifest.csv` in your spreadsheet of choice (LibreOffice Calc handles flat CSVs cleanly; Excel works too; in Google Sheets use File → Import → Append/Replace). Phase 26's `sortForInvestigation` already moved the `genus-only`, `likely-synonym`, `provisional`, and `unparseable` rows to the top, ordered by binomial frequency. Work top-down — the most-impactful single decision is at the very top.

       ### 2. Identify a `from_binomial` to re-route — The top of the file shows the highest-frequency unmatched binomials in the v2.2 manifest. Quote the top-12 list VERBATIM in a fenced block under this step so the curator sees them without flipping through the spreadsheet. The list is (REPRODUCE EXACTLY as shown in 27-CONTEXT.md "Specifics"):

       ```
       smerinthus ophthalmica   (32 files)
       grammia nevadensis       (10)
       sericosema wilsonensis   (8)
       pheosia rimosa           (8)
       pero occidentalis        (8)
       macaria signaria         (8)
       iridopsis emasculatum    (8)
       hydriomena irata         (8)
       dysstroma hersiliata     (8)
       drepanulatrix hulstii    (8)
       drepanulatrix bifilata   (8)
       digrammia muscariata     (8)
       ```

       Add a brief sentence: clearing the top-12 alone gets the clean-or-resolved match rate from 77.3% to roughly 80%; the long tail of 30–80 unique binomials provides the remaining bar-clearing toward the L-04 ≥95% target. Note that 22 rows have an empty `binomial_raw` — those are unreachable through synonyms.csv (no key to match on; D-07) and the curator should leave them alone.

       ### 3. Look up the target species — In `data/species.csv` find a row with `genus` + `species` columns matching the current name. The slug is `lower(genus + '-' + species)`. Provide ONE worked example (illustrative — NOT a real curatorial decision to commit; D-08 keeps the seed empty): `Apantesis,nevadensis` in `data/species.csv` → slug `apantesis-nevadensis`. Add a one-sentence note that the slug is always lowercase and uses hyphens to join, never underscores, never CamelCase (D-02).

       ### 4. Add a row to `data/species-synonyms.csv` — Open the file in a text editor (NOT a spreadsheet — spreadsheets sometimes rewrite quoting on save and corrupt simple flat CSVs). The schema is exactly two columns:

       ```
       from_binomial,to_species_slug
       ```

       Append your row at the bottom. Both values are lowercase. `from_binomial` matches `binomial_raw` from the manifest EXACTLY (lowercased, space-separated). `to_species_slug` matches the slug column in `data/species.csv` (lowercased, hyphen-joined). Example row (D-08 — illustrative only, not the file's seed content):

       ```
       grammia nevadensis,apantesis-nevadensis
       ```

       Make sure each row ends with a newline. Save.

       ### 5. Re-classify the manifest — Run:

       ```
       npm run photos:investigate
       ```

       This re-runs the script in re-sort-only mode: no Dropbox calls. It re-loads `data/species-synonyms.csv`, walks every manifest row, promotes any row whose `binomial_raw` matches one of your `from_binomial` entries to `match_bucket: resolved-via-synonym`, updates `binomial_resolved` and `species_slug`, leaves `status` and every other column untouched, and re-sorts the file in place. The final log line reports the count of rows promoted: `[ingest-photos] re-sorted manifest; N rows; M promoted to resolved-via-synonym`.

       ### 6. Confirm the promotion — Re-open `data/species-photos-manifest.csv` in your spreadsheet. The rows you re-routed should no longer be at the very top of the file (they have moved into the trailing partition with the `clean-match` rows). Filter or sort by `match_bucket` and confirm:
       - The rows for the binomials you added have `match_bucket = resolved-via-synonym`
       - Their `binomial_resolved` and `species_slug` columns are populated (not empty)
       - Their `status` is still `discovered`
       The first row of the file is now the next-highest-frequency unresolved binomial — work it next.

       ### 7. Commit synonyms.csv AND the manifest in the same PR — D-01 explicitly relies on git history as the audit trail. Do NOT split the curator decision and its effect across two commits. Stage both:

       ```
       git add data/species-synonyms.csv data/species-photos-manifest.csv
       git commit -m "data(27): resolve <N> binomials to species slugs"
       ```

       PR description should briefly note which binomials were added and why (the citation / reference for the synonymy). The diff IS the audit trail; the PR review IS the human checkpoint.

    5. `## Verify` — a checklist the curator can run after a pass:
       - In a spreadsheet view of `data/species-photos-manifest.csv`: the rows you re-routed have `match_bucket = resolved-via-synonym`, `binomial_resolved` and `species_slug` are populated (not empty), `status` is still `discovered`
       - On the command line: `tail` of the `npm run photos:investigate` output shows the new per-bucket distribution and the `promoted` count matches the number of new rows you added (per affected manifest row, not per synonyms.csv row)
       - Over multiple passes: the clean-or-resolved match rate (clean-match + resolved-via-synonym ÷ total rows) climbs toward the L-04 ≥95% target (up from the 77.3% baseline)

    6. `## When Things Go Wrong` — a short troubleshooting block for the two narrow failure modes Plan 02 actually emits:

       **A `synonym-warn target-not-in-species-csv <from> → <to>` line appeared in the log.** Your `to_species_slug` is not present in `data/species.csv`. Open `data/species.csv`, search for the genus you intended, and copy the slug exactly as it appears in the data (lowercase, hyphen-joined). The row in `data/species-synonyms.csv` was dropped and the corresponding manifest rows were NOT promoted; correct the typo and re-run `npm run photos:investigate`.

       **A manifest row you expected to promote did not move.** The `from_binomial` does not exactly match the manifest's `binomial_raw`. Check for: extra trailing whitespace, mixed case (must be all lowercase), use of hyphens instead of spaces (use spaces), or a misspelling. Look at the actual `binomial_raw` value in the spreadsheet and copy it character-for-character into your synonyms.csv row.

    The following content is FORBIDDEN in this runbook (per 27-CONTEXT.md "Deferred Ideas" and 27-PATTERNS.md "Concrete things NOT to include"):
    - Any mention of GBIF, ITIS, or external taxonomic-API auto-resolution (deferred at milestone level)
    - Any reference to a `decided_by`, `decided_on`, `note`, `notes`, `comment`, or `comments` column on `data/species-synonyms.csv` (D-01 rejects extending the schema)
    - Any "preview mode" / "dry-run" / `--dry-run` flag for synonym application (Claude's discretion was not exercised)
    - Genus-wildcard syntax (`Grammia *` → `Apantesis *`) — rejected for v2.2
    - The `*custom/` Dropbox subfolder — irrelevant to curation
    - Any reference to DROPBOX_TOKEN or Dropbox API setup — Phase 27 is offline
    - The literal string `RESORT_ONLY=1` in any curator-facing step — the alias `npm run photos:investigate` is the curator-facing command (mentioning the env-var-flag exposes Plan 02 internals)
    - Pre-filled synonym suggestions to copy into the seed file (D-08 — every row is a deliberate curator PR; the worked example in Step 4 is explicitly labeled illustrative)

    Length target: 80–250 lines of Markdown. INGESTING_HIGH_RES_PHOTOS.md is 114 lines after Phase 26. Aim for the same neighborhood — substantially shorter and the runbook lacks the verbatim top-12 block and the worked example; substantially longer and the runbook is over-explaining and obscures the workflow.

    File line endings: LF (Unix), matching every other file in `_instructions/`. Encoding: UTF-8, no BOM.

    Commit this file in a single commit with the message:

    `docs(27-03): curator runbook for data/species-synonyms.csv`

    Stage only `_instructions/CURATING_SPECIES_SYNONYMS.md`. Do not modify any other file in this plan.
  </action>
  <verify>
    <automated>test -f _instructions/CURATING_SPECIES_SYNONYMS.md && echo FILE_OK; grep -cE '^## (What This Changes|Before You Start|Steps|Verify|When Things Go Wrong)' _instructions/CURATING_SPECIES_SYNONYMS.md; wc -l _instructions/CURATING_SPECIES_SYNONYMS.md; grep -cF 'npm run photos:investigate' _instructions/CURATING_SPECIES_SYNONYMS.md; grep -cE '(GBIF|ITIS|decided_by|decided_on|--dry-run|RESORT_ONLY=1|DROPBOX_TOKEN|\*custom)' _instructions/CURATING_SPECIES_SYNONYMS.md</automated>
  </verify>
  <acceptance_criteria>
    - File `_instructions/CURATING_SPECIES_SYNONYMS.md` exists — `test -f _instructions/CURATING_SPECIES_SYNONYMS.md && echo OK` prints `OK`
    - File has the top-level title — `grep -cF '# Task: Curate Species Synonyms' _instructions/CURATING_SPECIES_SYNONYMS.md` returns 1
    - All five required `##` section headers appear in the document — `grep -cE '^## (What This Changes|Before You Start|Steps|Verify|When Things Go Wrong)' _instructions/CURATING_SPECIES_SYNONYMS.md` returns 5
    - The seven numbered Steps sub-headers appear — `grep -cE '^### [1-7]\.' _instructions/CURATING_SPECIES_SYNONYMS.md` returns at least 7
    - File references the curator command — `grep -cF 'npm run photos:investigate' _instructions/CURATING_SPECIES_SYNONYMS.md` returns at least 1
    - File references the synonyms.csv path — `grep -cF 'data/species-synonyms.csv' _instructions/CURATING_SPECIES_SYNONYMS.md` returns at least 1
    - File references the manifest path — `grep -cF 'data/species-photos-manifest.csv' _instructions/CURATING_SPECIES_SYNONYMS.md` returns at least 1
    - File references the species data path — `grep -cF 'data/species.csv' _instructions/CURATING_SPECIES_SYNONYMS.md` returns at least 1
    - File names the new bucket — `grep -cF 'resolved-via-synonym' _instructions/CURATING_SPECIES_SYNONYMS.md` returns at least 1
    - File references the ≥95% target metric or the 77.3% baseline — `grep -cE '(95%|77\.3%)' _instructions/CURATING_SPECIES_SYNONYMS.md` returns at least 1
    - The verbatim top-12 binomials appear (excluding `smerinthus ophthalmica` which is the count-32 leader, the other 11 should each appear) — `for b in 'grammia nevadensis' 'sericosema wilsonensis' 'pheosia rimosa' 'pero occidentalis' 'macaria signaria' 'iridopsis emasculatum' 'hydriomena irata' 'dysstroma hersiliata' 'drepanulatrix hulstii' 'drepanulatrix bifilata' 'digrammia muscariata' 'smerinthus ophthalmica'; do grep -cF "$b" _instructions/CURATING_SPECIES_SYNONYMS.md; done` returns 12 lines each `>= 1`
    - The two log-line markers from Plan 02 are quoted — `grep -cF 'synonym-warn' _instructions/CURATING_SPECIES_SYNONYMS.md` returns at least 1 (the "When Things Go Wrong" section quotes this exact action name)
    - File does NOT mention any forbidden topic — `grep -cE '(GBIF|ITIS|decided_by|decided_on|--dry-run|RESORT_ONLY=1|DROPBOX_TOKEN|\*custom|genus-wildcard|preview mode)' _instructions/CURATING_SPECIES_SYNONYMS.md` returns 0
    - File length is in the 80-250 line band — `LC=$(wc -l < _instructions/CURATING_SPECIES_SYNONYMS.md); test "$LC" -ge 80 -a "$LC" -le 250 && echo LEN_OK` prints `LEN_OK`
    - File has UTF-8 encoding without BOM — `head -c 3 _instructions/CURATING_SPECIES_SYNONYMS.md | od -An -tx1 | tr -d ' \n'` does NOT return `efbbbf`
    - The commit message matches the locked format — `git log -1 --pretty=%s` returns exactly `docs(27-03): curator runbook for data/species-synonyms.csv`
    - The commit modifies ONLY `_instructions/CURATING_SPECIES_SYNONYMS.md` — `git diff-tree --no-commit-id --name-only -r HEAD` returns `_instructions/CURATING_SPECIES_SYNONYMS.md`
  </acceptance_criteria>
  <done>_instructions/CURATING_SPECIES_SYNONYMS.md exists at the locked path, follows the v2.x runbook section structure with the five required headers and seven numbered Steps, quotes the top-12 unmatched binomials verbatim, references the real `npm run photos:investigate` command Plan 02 shipped, names the new `resolved-via-synonym` bucket, calls out the L-04 ≥95% target, excludes every forbidden topic (no GBIF/ITIS, no decided_by columns, no DROPBOX_TOKEN, no RESORT_ONLY=1, no *custom/), is within the 80-250 line band that matches Phase 26's analog, and is committed under `docs(27-03): curator runbook for data/species-synonyms.csv`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Curator following the runbook → committed CSV + manifest | A non-technical maintainer reads this doc and produces edits that flow through Plan 02's code into committed CSV files. |
| Documentation → curator's mental model | The runbook IS the curator's understanding of the workflow; a misleading or out-of-date instruction can produce wrong data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27.03-01 | Repudiation | Curator commits synonyms.csv and manifest in separate PRs, breaking the D-01 audit-trail guarantee | mitigate | Step 7 explicitly instructs the curator to stage BOTH files together in a single commit. The acceptance criteria pin the presence of `git add data/species-synonyms.csv data/species-photos-manifest.csv` (both names) in Step 7 via the grep checks that confirm both file paths appear in the runbook. |
| T-27.03-02 | Information disclosure | Runbook accidentally documents a Dropbox token / API key requirement when none is needed | mitigate | The doc explicitly states "no DROPBOX_TOKEN required" in "What This Changes" AND in "Before You Start" AND the forbidden-topics grep verifies the literal string `DROPBOX_TOKEN` does NOT appear anywhere in the doc. |
| T-27.03-03 | Tampering | Runbook instructs the curator to edit synonyms.csv in a spreadsheet, which can round-trip-mangle the flat CSV (smart quoting, BOM injection, trailing-whitespace stripping) | mitigate | Step 4 explicitly tells the curator to use a TEXT EDITOR for `data/species-synonyms.csv`, not a spreadsheet. The manifest is read in a spreadsheet (read-only inspection); only the manifest is written to by code, and Plan 02 uses writeManifest's csv-stringify path, not curator edits. |
| T-27.03-04 | Spoofing | Runbook surfaces a forbidden topic (GBIF/ITIS or preview mode) that doesn't exist in code, causing the curator to look for a non-existent feature | mitigate | The forbidden-topics grep in acceptance criteria pins the absence of every deferred-idea keyword. The acceptance pass blocks any drift. |
| T-27.03-05 | Repudiation | The runbook drifts from the actual command output emitted by Plan 02 (e.g. quotes a log shape that does not match the real logStage output) | mitigate | The runbook is authored AFTER Plan 02 (depends_on: 27-02), so the executor reads `.planning/phases/27-synonym-curation-pass/27-02-SUMMARY.md` and can verify the exact log-line shapes against the just-implemented helper. The two log-line action names (`synonym-warn`, `reclassify`) appear in acceptance criteria grep. |
| T-27.03-SC | Tampering | No npm packages installed by this plan | accept | This plan adds zero npm dependencies. files_modified is a single Markdown file under `_instructions/`. |
</threat_model>

<verification>
- `_instructions/CURATING_SPECIES_SYNONYMS.md` exists with the five required `##` section headers
- The runbook references `npm run photos:investigate`, `data/species-synonyms.csv`, `data/species-photos-manifest.csv`, `data/species.csv`, `resolved-via-synonym`, and the L-04 ≥95% target metric or 77.3% baseline
- The verbatim top-12 unmatched binomials from CONTEXT.md "Specifics" all appear in the document
- The runbook does NOT mention any forbidden topic: GBIF, ITIS, decided_by, decided_on, --dry-run, RESORT_ONLY=1, DROPBOX_TOKEN, *custom, genus-wildcard, preview mode
- Document length is in the 80–250 line band that matches Phase 26's analog
- The synonym-warn log-line marker is referenced (so the curator knows how to read "When Things Go Wrong")
- The commit is `docs(27-03): curator runbook for data/species-synonyms.csv` and modifies only the single Markdown file
</verification>

<success_criteria>
- A non-technical curator who reads this document end-to-end can complete one synonym-curation pass without asking clarifying questions: open the manifest, identify a high-impact unresolved binomial, look up the target species in species.csv, add a row to synonyms.csv, run `npm run photos:investigate`, confirm the promotion in the spreadsheet, and commit both files together
- The curator understands what to look for after a pass: `match_bucket = resolved-via-synonym` on the affected rows, `binomial_resolved` + `species_slug` populated, `status` unchanged
- The curator knows the bar to clear: ≥95% clean-or-resolved match rate (L-04) — and the seed top-12 list gives them a concrete starting point
- The curator knows how to recognize and fix the two real failure modes Plan 02 emits (`synonym-warn target-not-in-species-csv` and "expected row didn't move")
- The curator never reaches for a feature that doesn't exist (no GBIF lookup, no preview mode, no decided_by column, no genus wildcard) — the runbook simply doesn't surface them
- The runbook's tone and shape match `_instructions/INGESTING_HIGH_RES_PHOTOS.md` so a curator who's done a Phase 26 ingest run experiences zero cognitive switching cost
</success_criteria>

<output>
Create `.planning/phases/27-synonym-curation-pass/27-03-SUMMARY.md` when done. Include:
- The actual line count of `_instructions/CURATING_SPECIES_SYNONYMS.md`
- The actual count of the five required section headers (must be 5)
- The actual count of numbered Steps sub-headers (must be ≥ 7)
- Confirmation that every top-12 binomial appears (12 names, all grep-matched)
- Confirmation that the forbidden-topics grep returns 0
- The commit hash and message of the docs commit
- A one-line note about anything trimmed for brevity vs. the runbook outline in 27-PATTERNS.md, and why
</output>
</content>
</invoke>