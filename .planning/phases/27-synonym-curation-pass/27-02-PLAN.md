---
phase: 27-synonym-curation-pass
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/ingest-photos.js
  - scripts/ingest-photos.test.js
  - package.json
autonomous: true
requirements:
  - CURATE-02
  - CURATE-03

tags:
  - cli
  - classify-cascade
  - synonym-lookup
  - tdd
  - reclassification

must_haves:
  truths:
    - "D-03: scripts/ingest-photos.js auto-loads data/species-synonyms.csv whenever it exists — no RECLASSIFY_ONLY flag, no separate reclassify script. Synonym-aware classification is the default after Phase 27."
    - "D-04: a synonym pre-pass runs in classify() BEFORE the existing byBinomial/bySlug/genera cascade — when binomial_raw matches a synonyms.csv row the bucket short-circuits to `resolved-via-synonym` with `binomial_resolved` + `species_slug` populated from the resolved species.csv record"
    - "D-04: when a synonyms.csv row's to_species_slug is NOT present in species.bySlug, the row is dropped from the in-memory map and a single `logStage('', 'synonym-warn', 'target-not-in-species-csv', '<from> → <to>')` line is emitted ONCE at load time, not per-manifest-row"
    - "D-05: BOTH `npm run photos:ingest` (full Dropbox ingest) AND `npm run photos:investigate` (RESORT_ONLY=1, no Dropbox) re-run classification against the current synonyms.csv — the curator's workflow is `edit synonyms.csv → npm run photos:investigate → manifest reclassified + re-sorted`"
    - "D-06: any row with non-empty `binomial_raw` is routable through synonyms.csv — INCLUDING provisional and unparseable rows. The synonym pre-pass runs BEFORE the provisional short-circuit and BEFORE the unparseable early return, so curator-routed rows in those buckets get promoted to `resolved-via-synonym`"
    - "D-09: loadSynonyms is a sibling helper next to loadSpecies, taking (csvPath, species) and returning a Map<from_binomial, { binomial_resolved, species_slug }>; the existsSync guard makes it first-run-safe (returns an empty Map when the file is absent — matches the readManifest pattern in scripts/lib/manifest.js)"
    - "L-02: new manifest bucket name is exactly `resolved-via-synonym` (the literal string), extending the Phase 26 bucket set"
    - "L-03: sortForInvestigation in scripts/lib/manifest.js is NOT modified — `resolved-via-synonym` rows trail with `clean-match` rows in the 'not-needs-investigation' partition (the four investigation buckets still surface at the top)"
    - "D-10 / TDD: the test file scripts/ingest-photos.test.js is committed in a RED commit (failing) BEFORE the implementation lands in a GREEN commit — matching Phase 26's `test(26-XX): add failing tests …` then `feat(26-XX): …` shape"
    - "RESORT_ONLY path re-classifies each row against the current synonyms.csv (D-05) using the row's stored `binomial_raw` (not the parser) and logs `logStage(content_hash, 'reclassify', 'resolved-via-synonym', '<binomial> → <slug>')` only when an actual change occurs (idempotent re-runs do not re-log)"
    - "Re-classification updates `binomial_resolved` and `species_slug` columns on promoted rows (Phase 30 reads BOTH fields per CONTEXT.md 'Integration Points')"
    - "Re-classification does NOT modify `status` — D-06 + CONTEXT.md 'Integration Points': Phase 28 reads `status: discovered`, not `match_bucket`, so the status column is left untouched. `dropbox_path`, `content_hash`, `size_bytes`, `server_modified`, `filename_raw`, `binomial_raw`, `specimen_id`, `view`, `last_error` are also left untouched on reclassification"
    - "No source files (TIFFs) are downloaded by `photos:investigate` — RESORT_ONLY=1 reads/writes only the manifest CSV (CURATE-02 mandate)"
    - "package.json test script glob registers scripts/ingest-photos.test.js — verifying that `npm test` picks up the new tests"
  artifacts:
    - path: "scripts/ingest-photos.js"
      provides: "Adds loadSynonyms helper, SYNONYMS_CSV constant, synonym pre-pass in classify(), synonym-aware re-classification in the RESORT_ONLY path; exports classify and loadSynonyms for unit testing"
      contains: "loadSynonyms"
    - path: "scripts/ingest-photos.test.js"
      provides: "Unit tests covering classify()'s synonym pre-pass (D-04, D-06) and loadSynonyms()'s first-run-safety, header-only seed handling, and orphan-target dropping (D-04)"
      contains: "describe"
    - path: "package.json"
      provides: "Test script glob updated to pick up scripts/ingest-photos.test.js"
      contains: "scripts/ingest-photos.test.js"
  key_links:
    - from: "scripts/ingest-photos.js"
      to: "data/species-synonyms.csv"
      via: "csv-parse/sync read inside loadSynonyms"
      pattern: "data/species-synonyms\\.csv"
    - from: "scripts/ingest-photos.js"
      to: "data/species.csv"
      via: "load-time validation of every synonym's to_species_slug against species.bySlug"
      pattern: "species\\.bySlug"
    - from: "scripts/ingest-photos.test.js"
      to: "scripts/ingest-photos.js"
      via: "imports classify and loadSynonyms"
      pattern: "from\\s+['\"]\\./ingest-photos\\.js['\"]"
    - from: "scripts/ingest-photos.js classify()"
      to: "scripts/ingest-photos.js loadSynonyms()"
      via: "synonyms Map passed as 3rd arg into classify() pre-pass"
      pattern: "classify\\([^)]*synonyms"

decisions_covered:
  - D-03
  - D-04
  - D-05
  - D-06
  - D-09
  - D-10
  - L-02
  - L-03
---

<objective>
Extend `scripts/ingest-photos.js` with a synonyms pre-pass that auto-loads `data/species-synonyms.csv` and routes matching `binomial_raw` values to the new `resolved-via-synonym` bucket — applied on BOTH `photos:ingest` (full classification of new Dropbox files) and `photos:investigate` (RESORT_ONLY=1 re-classification of existing manifest rows, with no Dropbox traffic). Land the change RED-first (failing tests committed in one commit) then GREEN (implementation that turns them green, committed in the next).

Purpose: this is the load-bearing code change for Phase 27. CURATE-02 says "Re-running classification against an updated species-synonyms.csv reclassifies affected manifest rows from `genus-only` / `likely-synonym` to `resolved-via-synonym` … and does not redownload the source files." That contract lives entirely in this plan. The seed CSV (Plan 01) provides the file; the runbook (Plan 03) tells the curator how to use it; this plan provides the loader, the pre-pass, the re-classifier, and the unit tests that pin all of it.

Output:
- `scripts/ingest-photos.js`: +loadSynonyms helper, +SYNONYMS_CSV constant, +synonym pre-pass in classify() (3rd arg = synonyms Map), +synonym-aware re-classification in RESORT_ONLY path, +`existsSync` import from node:fs, +`export` keywords on classify and loadSynonyms (and on the SYNONYMS_CSV constant if the tests need it). Net diff target: ~80 lines of added JS, no Phase 26 logic deleted.
- `scripts/ingest-photos.test.js`: new file with `describe('classify (with synonyms pre-pass)')` and `describe('loadSynonyms')` test groups; mirrors `scripts/lib/manifest.test.js` shape (node:test, tmpdir, mkdtempSync/rmSync).
- `package.json`: one edit to the test script glob to add `scripts/ingest-photos.test.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-synonym-curation-pass/27-CONTEXT.md
@.planning/phases/27-synonym-curation-pass/27-PATTERNS.md
@.claude/skills/spike-findings-pnwmoths/SKILL.md
@scripts/ingest-photos.js
@scripts/lib/manifest.js
@scripts/lib/manifest.test.js
@scripts/lib/parse-photo-filename.js
@scripts/lib/parse-photo-filename.test.js
@package.json
@data/species.csv

<interfaces>
<!-- The current shape of classify() (scripts/ingest-photos.js:173):                       -->
<!--   function classify({ binomialFromParser, bucketHintFromParser }, species) → { match_bucket, binomial_resolved, species_slug } -->
<!--                                                                                       -->
<!-- The new shape (D-09 — third arg added; existing callers updated):                     -->
<!--   function classify({ binomialFromParser, bucketHintFromParser }, species, synonyms) → { match_bucket, binomial_resolved, species_slug } -->
<!--                                                                                       -->
<!-- The pre-pass is step −1 (runs BEFORE the existing step 0 / provisional short-circuit  -->
<!-- and BEFORE step 5 / unparseable early return). D-06 honors this widening so          -->
<!-- provisional + unparseable rows route through synonyms.csv when they have a            -->
<!-- non-empty binomial_raw.                                                                -->
<!--                                                                                       -->
<!-- The synonyms Map shape (D-04, D-09):                                                   -->
<!--   Map<from_binomial: string, { binomial_resolved: string, species_slug: string }>     -->
<!--                                                                                       -->
<!--   - from_binomial: lowercased space-separated form (matches manifest binomial_raw)    -->
<!--   - binomial_resolved: lower(genus + ' ' + species) from the matched species.csv row  -->
<!--   - species_slug: the to_species_slug value (already lowercased + hyphen-joined),     -->
<!--                   confirmed to exist in species.bySlug at load time                   -->
<!--                                                                                       -->
<!-- The species fixture shape (already in use; do not change):                            -->
<!--   { byBinomial: Map<lower-binomial, record>, bySlug: Map<lower-slug, record>, genera: Set<lower-genus> } -->
<!--                                                                                       -->
<!-- The manifest row shape (D-05; locked by scripts/lib/manifest.js COLUMNS — do not modify): -->
<!--   { content_hash, dropbox_path, size_bytes, server_modified, filename_raw,            -->
<!--     binomial_raw, specimen_id, view, binomial_resolved, species_slug,                -->
<!--     match_bucket, status, last_error }                                                -->
<!--                                                                                       -->
<!-- Phase 26 RED-then-GREEN commit messages (the contract to mirror, D-10):               -->
<!--   test(26-01): add failing tests for parse-photo-filename library                     -->
<!--   feat(26-01): port spike parser into scripts/lib/parse-photo-filename.js             -->
<!--   test(26-02): add failing tests for scripts/lib/manifest.js                          -->
<!--   feat(26-02): scripts/lib/manifest.js — D-05 COLUMNS + read/write + investigate sort -->
<!-- For Phase 27 use:                                                                     -->
<!--   test(27-02): add failing tests for synonym-aware classify cascade and loadSynonyms  -->
<!--   feat(27-02): synonyms.csv pre-pass in classify() promotes matched rows to resolved-via-synonym -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — add failing tests for the synonym-aware classify cascade and loadSynonyms helper, and register the new test file in package.json</name>
  <files>scripts/ingest-photos.test.js, package.json</files>
  <read_first>
    - scripts/lib/manifest.test.js (entire file — copy the import block at lines 1-5; the tmpdir + mkdtempSync + try/finally rmSync pattern at lines 64-67 and 128-130; the describe/it shape; the `node:test` + `node:assert/strict` discipline)
    - scripts/lib/parse-photo-filename.test.js (lines 1-80 — pure-function table-driven test shape; useful for the classify() tests which are pure-function tests with multiple branches)
    - scripts/ingest-photos.js (entire 467-line file — confirm the exported names the tests will import; in particular note that classify and loadSynonyms are NOT exported yet, so the GREEN task will add `export` keywords. The tests in this RED task import them anyway — the failing import is part of the RED signal)
    - .planning/phases/27-synonym-curation-pass/27-PATTERNS.md (the "Test(s) for the synonym-aware classify cascade" section — concrete test-case enumeration for classify and loadSynonyms; the "RED-then-GREEN commit shape" block; the "package.json test runner registration" block)
    - .planning/phases/27-synonym-curation-pass/27-CONTEXT.md (D-04, D-06, D-09, D-10 — the contracts the tests pin)
    - package.json lines 7-23 (the existing `scripts.test` line at line 22 — the glob to extend with `scripts/ingest-photos.test.js`)
  </read_first>
  <action>
    Create `scripts/ingest-photos.test.js` as a new ESM test module using `node:test`. Mirror the shape of `scripts/lib/manifest.test.js` (the imports, the `describe`/`it` discipline, the tmpdir + mkdtempSync + rmSync pattern for filesystem fixtures). DO NOT implement the helpers — this is the RED commit. The test file MUST fail when run against the current (unmodified) `scripts/ingest-photos.js` because (a) `classify` and `loadSynonyms` are not yet exported, and (b) `classify` does not yet accept a third `synonyms` argument or implement the pre-pass.

    Test imports at the top of the new file:
    - `describe`, `it` from `node:test`
    - `assert` (default import) from `node:assert/strict`
    - `tmpdir` from `node:os`
    - `join` from `node:path`
    - `mkdtempSync`, `rmSync`, `writeFileSync` from `node:fs`
    - `classify`, `loadSynonyms` from `./ingest-photos.js` (named imports — failing until Task 2 exports them)

    Test groups to write (verbatim test names locked so the SUMMARY can grep for them; assertions follow 27-PATTERNS.md "Test cases" tables):

    Group A — `describe('classify (with synonyms pre-pass)')` — build a small in-memory species fixture (byBinomial / bySlug Maps and genera Set) seeded with `apantesis nevadensis`, `abagrotis apposita`, plus genera `apantesis`, `abagrotis`, `grammia`, `monostoecha`. Build a synonyms Map seeded with two entries: `grammia nevadensis → { binomial_resolved: 'apantesis nevadensis', species_slug: 'apantesis-nevadensis' }` and `monostoecha n sp → { binomial_resolved: 'abagrotis apposita', species_slug: 'abagrotis-apposita' }`. Then five `it` blocks (locked names):
    - `it('promotes a genus-only binomial to resolved-via-synonym when synonyms.csv has a matching row')` — input `{ binomialFromParser: 'grammia nevadensis', bucketHintFromParser: null }`; expected `match_bucket === 'resolved-via-synonym'`, `binomial_resolved === 'apantesis nevadensis'`, `species_slug === 'apantesis-nevadensis'`.
    - `it('promotes a provisional-marked binomial through synonyms.csv (D-06 widening)')` — input `{ binomialFromParser: 'monostoecha n sp', bucketHintFromParser: 'provisional' }`; expected `match_bucket === 'resolved-via-synonym'`, `binomial_resolved === 'abagrotis apposita'`. This pin enforces that the synonym pre-pass runs BEFORE the provisional short-circuit.
    - `it('falls through to clean-match when synonyms.csv does not contain the binomial')` — input `{ binomialFromParser: 'abagrotis apposita', bucketHintFromParser: null }`; expected `match_bucket === 'clean-match'`.
    - `it('falls through to provisional when no synonym matches and the parser flagged provisional')` — input `{ binomialFromParser: null, bucketHintFromParser: 'provisional' }`; expected `match_bucket === 'provisional'`.
    - `it('falls through to unparseable when no synonym matches and the binomial is null')` — input `{ binomialFromParser: null, bucketHintFromParser: null }`; expected `match_bucket === 'unparseable'`.

    Group B — `describe('loadSynonyms')` — build a minimal species fixture (only `bySlug` needs `apantesis-nevadensis` for the positive case; `byBinomial` can be empty and `genera` can be empty). Four `it` blocks (locked names; use the tmpdir + mkdtempSync + try/finally rmSync pattern from manifest.test.js:64-67/128-130):
    - `it('returns an empty Map when the file does not exist (first-run safe)')` — pass a `join(tmpdir(), 'synonyms-missing-${Date.now()}.csv')` path; assert `result.size === 0`.
    - `it('returns an empty Map when the file has only the header (D-08 seed shape)')` — `mkdtempSync` a dir, write `'from_binomial,to_species_slug\n'`, load, assert `result.size === 0`, rmSync in finally.
    - `it('builds a one-row map for a single valid synonym')` — write `'from_binomial,to_species_slug\ngrammia nevadensis,apantesis-nevadensis\n'`; assert `result.size === 1`, the entry under key `'grammia nevadensis'` has `binomial_resolved === 'apantesis nevadensis'` and `species_slug === 'apantesis-nevadensis'`.
    - `it('drops a row whose to_species_slug is not in species.bySlug (orphan target → synonym-warn → drop)')` — write `'from_binomial,to_species_slug\nfoo bar,nonexistent-slug\n'`; assert `result.size === 0` (the orphan row was dropped). For the RED commit it is acceptable to only assert the drop; the log-line assertion is not load-bearing for the test signal.

    DO NOT write helpers; DO NOT export anything from this test file. Each `describe` block constructs its fixtures inline so the file is self-contained and a different test ordering does not matter.

    SECOND EDIT in this task: modify `package.json` to add `scripts/ingest-photos.test.js` to the test glob. The current line 22 reads:

    `"test": "node --test eleventy.config.test.js scripts/build-data.test.js scripts/check-page-weight.test.js scripts/migrate-species.test.js scripts/lib/*.test.js src/components/*.test.js src/_lib/*.test.js"`

    Insert `scripts/ingest-photos.test.js` after `scripts/check-page-weight.test.js` and before `scripts/migrate-species.test.js` so the resulting line reads:

    `"test": "node --test eleventy.config.test.js scripts/build-data.test.js scripts/check-page-weight.test.js scripts/ingest-photos.test.js scripts/migrate-species.test.js scripts/lib/*.test.js src/components/*.test.js src/_lib/*.test.js"`

    Do not modify any other key in package.json. Validate the file is still valid JSON.

    COMMIT discipline: stage ONLY `scripts/ingest-photos.test.js` and `package.json` for this RED commit. Do not stage `scripts/ingest-photos.js` (which is not modified in this task). Use the commit message:

    `test(27-02): add failing tests for synonym-aware classify cascade and loadSynonyms`

    Confirm `npm test` exits NON-ZERO before declaring this task done — the failure is the RED signal. Acceptable failure modes: import error (`classify`/`loadSynonyms` not exported), assertion failure (classify ignores the synonyms arg and returns the wrong bucket), or both.
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -50; node -e "const p=JSON.parse(require('node:fs').readFileSync('package.json')); const t=p.scripts.test; const inGlob=t.includes('scripts/ingest-photos.test.js'); process.exit(inGlob?0:1)" && echo "GLOB_OK" || echo "GLOB_MISSING"</automated>
  </verify>
  <acceptance_criteria>
    - File `scripts/ingest-photos.test.js` exists — verify with `test -f scripts/ingest-photos.test.js && echo OK` prints `OK`
    - Test file imports `classify` and `loadSynonyms` from `./ingest-photos.js` — verify with `grep -cE "^import\s*\{[^}]*(classify|loadSynonyms)" scripts/ingest-photos.test.js` returns at least 1
    - Test file uses `node:test` — verify with `grep -cF "from 'node:test'" scripts/ingest-photos.test.js` returns 1
    - Test file uses `node:assert/strict` — verify with `grep -cF "from 'node:assert/strict'" scripts/ingest-photos.test.js` returns 1
    - All five `classify` test names from <action> appear verbatim — verify with `grep -cF "promotes a genus-only binomial to resolved-via-synonym when synonyms.csv has a matching row" scripts/ingest-photos.test.js` returns 1, AND `grep -cF "promotes a provisional-marked binomial through synonyms.csv (D-06 widening)" scripts/ingest-photos.test.js` returns 1, AND `grep -cF "falls through to clean-match when synonyms.csv does not contain the binomial" scripts/ingest-photos.test.js` returns 1, AND `grep -cF "falls through to provisional when no synonym matches and the parser flagged provisional" scripts/ingest-photos.test.js` returns 1, AND `grep -cF "falls through to unparseable when no synonym matches and the binomial is null" scripts/ingest-photos.test.js` returns 1
    - All four `loadSynonyms` test names appear verbatim — verify with grep -cF for each: `returns an empty Map when the file does not exist`, `returns an empty Map when the file has only the header`, `builds a one-row map for a single valid synonym`, `drops a row whose to_species_slug is not in species.bySlug` each returns at least 1
    - package.json test glob includes the new test file — the `node -e ...` JSON-introspection check in <verify> exits 0 and prints `GLOB_OK`
    - package.json remains valid JSON — verify with `node -e "JSON.parse(require('node:fs').readFileSync('package.json'))"` exits 0
    - `npm test` exits NON-ZERO (the RED signal) — verify with `npm test 2>/dev/null; test $? -ne 0 && echo RED_OK` prints `RED_OK`
    - The RED commit message follows the Phase 26 contract — verify with `git log -1 --pretty=%s` returns a string starting with `test(27-02): add failing tests for synonym-aware classify cascade`
    - The RED commit modifies ONLY `scripts/ingest-photos.test.js` and `package.json` (NOT `scripts/ingest-photos.js`) — verify with `git show --name-only HEAD | tail -n +7 | sort | tr '\n' ',' | sed 's/,$//'` returns the two paths in any order, exit 0; OR equivalently `git diff-tree --no-commit-id --name-only -r HEAD | sort | tr '\n' ' '` returns `package.json scripts/ingest-photos.test.js` (or the reverse)
  </acceptance_criteria>
  <done>scripts/ingest-photos.test.js exists with the locked test names, package.json registers it in the test glob, `npm test` exits non-zero (RED), and the failing-test commit `test(27-02): add failing tests for synonym-aware classify cascade and loadSynonyms` is committed touching only the test file and package.json.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement loadSynonyms helper, SYNONYMS_CSV constant, classify() pre-pass, and synonym-aware RESORT_ONLY re-classification; export classify and loadSynonyms; confirm npm test passes</name>
  <files>scripts/ingest-photos.js</files>
  <read_first>
    - scripts/ingest-photos.js (entire 467-line file — every change in this task is a localized insertion/edit. Re-read so the executor knows the EXACT current line numbers; the modifications cluster at:
        - line 24-29 imports — add `existsSync` from node:fs
        - line 36-43 module-level constants — add SYNONYMS_CSV
        - line 117-144 loadSpecies — add a sibling loadSynonyms helper directly below
        - line 173-213 classify — add the pre-pass as step −1; change signature to accept synonyms; the body of classify() is otherwise unchanged
        - line 267-275 RESORT_ONLY block — replace with the synonym-aware re-classification loop
        - line 308 main() — load synonyms after species; pass into classify in the full-run code path (line 378-384)
        - end of file — add `export` to classify and loadSynonyms declarations (or add an explicit `export { classify, loadSynonyms }` block before the self-invocation guard)
      )
    - scripts/lib/manifest.js (lines 26-30 for the existsSync import pattern; lines 73-77 for the first-run-safe readManifest shape that loadSynonyms mirrors)
    - .planning/phases/27-synonym-curation-pass/27-PATTERNS.md (the "scripts/ingest-photos.js (modified — add synonyms pre-pass)" section — explicit "four edits" enumeration, the loadSynonyms shape suggestion, the pre-pass injection point, the RESORT_ONLY replacement, the logStage usage examples)
    - .planning/phases/27-synonym-curation-pass/27-CONTEXT.md (D-03, D-04, D-05, D-06, D-09 — the contracts the implementation satisfies)
    - scripts/lib/parse-photo-filename.js lines 162-165 (toSpeciesSlug — referenced for tests; not invoked by the implementation, but the test file already pins its shape)
  </read_first>
  <action>
    Implement the synonym-aware classification path inside `scripts/ingest-photos.js`. After this task `npm test` MUST exit 0 and `node --check scripts/ingest-photos.js` MUST exit 0. The Phase 26 behavior is preserved verbatim when `data/species-synonyms.csv` is missing or contains only the header — the synonyms Map is empty in that case and the existing cascade runs identically.

    Edit 1 — add the existsSync import. After the existing import at line 25 (`import { parse } from 'csv-parse/sync';`) insert:

    `import { existsSync } from 'node:fs';`

    (existsSync is the same symbol scripts/lib/manifest.js:29 already imports — established project pattern.)

    Edit 2 — add the SYNONYMS_CSV constant. Immediately after the existing line `const SPECIES_CSV = resolve('data/species.csv');` (line 37) insert:

    `const SYNONYMS_CSV = resolve('data/species-synonyms.csv');`

    (Module-level path constant convention; see PROJECT.md Key Decisions and 27-PATTERNS.md "Shared Patterns §3".)

    Edit 3 — add the `loadSynonyms(csvPath, species)` helper directly below `loadSpecies` (current end-of-loadSpecies is line 144). The helper:
    - Guards with `if (!existsSync(csvPath)) return new Map();` as the first statement (first-run-safe — matches scripts/lib/manifest.js:73-77 readManifest).
    - Reads the file with `await readFile(csvPath)`; parses with `parse(raw, { columns: true, skip_empty_lines: true })` (same call shape as loadSpecies).
    - Iterates rows; for each row extracts `from = (r.from_binomial || '').trim().toLowerCase()` and `to = (r.to_species_slug || '').trim().toLowerCase()`; if either is empty, `continue` (skip blank rows).
    - Looks up `target = species.bySlug.get(to)`; if absent, emits `logStage('', 'synonym-warn', 'target-not-in-species-csv', `${from} → ${to}`)` and `continue` (drops the orphan; one log line per orphan, NOT per manifest row — D-04).
    - Computes the resolved binomial from the target's record: `const resolvedBinomial = `${(target.genus || '').toLowerCase()} ${(target.species || '').toLowerCase()}`.trim();` (this matches the lower(genus + ' ' + species) form Phase 26 emits for clean-match rows; see scripts/ingest-photos.js:137).
    - Sets `synonyms.set(from, { binomial_resolved: resolvedBinomial, species_slug: to });`
    - Returns the synonyms Map.

    Add a JSDoc comment above the helper following the same docstring style as loadSpecies (purpose, the shape of the returned Map, the D-04 first-run-safety, the D-04 synonym-warn behavior).

    Edit 4 — modify `classify()`'s signature and body. The current signature is `function classify({ binomialFromParser, bucketHintFromParser }, species) {`. Add a third parameter so it becomes `function classify({ binomialFromParser, bucketHintFromParser }, species, synonyms) {`. Then INSERT the pre-pass as the very first non-comment statement inside the function body (step −1, BEFORE the existing step 0 provisional short-circuit at line 174-178 and BEFORE the existing step 5 unparseable early return at line 180-182):

    The pre-pass: if `synonyms` is truthy (defensive null-guard for unit tests that may pass undefined for backward compatibility, although in production both call sites now pass a Map), AND `binomialFromParser` is non-empty, AND `synonyms.has(binomialFromParser)` is true → destructure `binomial_resolved` and `species_slug` from `synonyms.get(binomialFromParser)` and return `{ match_bucket: 'resolved-via-synonym', binomial_resolved, species_slug }`. Otherwise fall through to the existing cascade unchanged.

    Add a leading comment block explaining: step −1, D-04 + D-06, the bucket-agnostic widening (provisional and unparseable routable through synonyms.csv when binomial_raw is non-empty), and that the lookup key is binomialFromParser (already-normalized lowercase 'genus species' from the parser, or the row's binomial_raw on the RESORT_ONLY path).

    Update the bucket-list comment block at the top of classify (currently the JSDoc at lines 156-170) to add step −1 as the first cascade step:
    - `−1. resolved-via-synonym — synonyms.has(binomialFromParser)  (Phase 27, D-04, D-06; runs BEFORE provisional/unparseable so any non-empty binomial can be re-routed)`

    Do NOT modify steps 0–4. The slug-match step (step 2) stays as-is.

    Edit 5 — replace the RESORT_ONLY block in main(). The current block is at lines 268-275:
    ```
    if (RESORT_ONLY) {
      const existing = await readManifest(MANIFEST_PATH);
      const sorted = sortForInvestigation(existing);
      await writeManifest(MANIFEST_PATH, sorted);
      console.log(`[ingest-photos] re-sorted manifest; ${sorted.length} rows`);
      return;
    }
    ```

    Replace with synonym-aware re-classification (D-05 mandate that photos:investigate also reclassifies):
    1. Load species from SPECIES_CSV (`const species = await loadSpecies(SPECIES_CSV);`).
    2. Load synonyms from SYNONYMS_CSV against the species fixture (`const synonyms = await loadSynonyms(SYNONYMS_CSV, species);`).
    3. Read the existing manifest (`const existing = await readManifest(MANIFEST_PATH);`).
    4. Initialize `let promoted = 0;`.
    5. Iterate each row in `existing`. Compute `const binomial = (row.binomial_raw || '').toLowerCase();`. If `binomial && synonyms.has(binomial)` → destructure `{ binomial_resolved, species_slug }` from `synonyms.get(binomial)`. Idempotency guard: only mutate the row if `row.match_bucket !== 'resolved-via-synonym' || row.species_slug !== species_slug`. When mutating, set `row.match_bucket = 'resolved-via-synonym'`, `row.binomial_resolved = binomial_resolved`, `row.species_slug = species_slug`; increment `promoted`; emit `logStage(row.content_hash, 'reclassify', 'resolved-via-synonym', `${binomial} → ${species_slug}`)`.
    6. Do NOT touch any other column (status, content_hash, dropbox_path, size_bytes, server_modified, filename_raw, binomial_raw, specimen_id, view, last_error remain as-they-were on disk). CURATE-02: re-classification does not redownload the source TIFFs; this loop never touches the network, never touches the filesystem outside the manifest, never spawns a child process. The status column is deliberately untouched per D-06 + Phase 28 reads `status: discovered` to gate downloads.
    7. After the loop: `const sorted = sortForInvestigation(existing); await writeManifest(MANIFEST_PATH, sorted);` (sortForInvestigation is L-03-unchanged — `resolved-via-synonym` rows trail with clean-match rows).
    8. Final log line: `console.log(`[ingest-photos] re-sorted manifest; ${sorted.length} rows; ${promoted} promoted to resolved-via-synonym`);` and `return;`.

    Edit 6 — wire synonyms into the full-run (non-RESORT_ONLY) path. After `const species = await loadSpecies(SPECIES_CSV);` at line 308, add:

    `const synonyms = await loadSynonyms(SYNONYMS_CSV, species);`
    `console.log(`[ingest-photos] loaded ${synonyms.size} synonyms from ${SYNONYMS_CSV}`);`

    Then update the existing classify() call at lines 378-384 to pass synonyms as the third argument:
    ```
    const { match_bucket, binomial_resolved, species_slug } = classify(
      {
        binomialFromParser: parsed.binomial,
        bucketHintFromParser: parsed.bucketHint,
      },
      species,
      synonyms,
    );
    ```

    Edit 7 — export `classify` and `loadSynonyms` so the test file can import them. Two equivalent approaches; pick the one that minimizes diff:
    - Option A: change `function classify(...)` to `export function classify(...)` and `async function loadSynonyms(...)` to `export async function loadSynonyms(...)`. This is the minimum-diff approach.
    - Option B: add an `export { classify, loadSynonyms };` line immediately before the self-invocation guard at line 462.
    Choose Option A — it matches Phase 26's lib-file pattern (every helper in scripts/lib/manifest.js and scripts/lib/parse-photo-filename.js uses `export function …`). Do NOT export any other identifier; in particular do NOT export the module-level constants or main() — the test file does not need them.

    DO NOT modify any other file in this task. In particular: scripts/lib/manifest.js, scripts/lib/parse-photo-filename.js, scripts/lib/dropbox-list.js are NOT touched. package.json was already updated in Task 1.

    COMMIT discipline: stage ONLY `scripts/ingest-photos.js` for the GREEN commit. Use the commit message:

    `feat(27-02): synonyms.csv pre-pass in classify() promotes matched rows to resolved-via-synonym`

    After committing, confirm `npm test` exits 0 — all the RED tests from Task 1 are now green. Also confirm `node --check scripts/ingest-photos.js` exits 0 (syntax-valid).
  </action>
  <verify>
    <automated>node --check scripts/ingest-photos.js && echo SYNTAX_OK && npm test 2>&1 | tail -30 && echo "---" && git log -2 --pretty="%s" && echo "---" && git diff-tree --no-commit-id --name-only -r HEAD</automated>
  </verify>
  <acceptance_criteria>
    - `node --check scripts/ingest-photos.js` exits 0 — verify with `node --check scripts/ingest-photos.js && echo SYNTAX_OK` prints `SYNTAX_OK`
    - `npm test` exits 0 (every test passes including the new synonym-cascade tests from Task 1) — verify with `npm test >/dev/null 2>&1 && echo TEST_OK` prints `TEST_OK`
    - The new test names are RUN and PASS — verify with `npm test 2>&1 | grep -F "promotes a genus-only binomial to resolved-via-synonym when synonyms.csv has a matching row" | wc -l` returns at least 1 (the test name appears in `node --test` output), AND `npm test 2>&1 | grep -cE "(fail|FAIL)" | head -1` reports no failures (or the output's final summary shows 0 fail)
    - `loadSynonyms` is exported — verify with `grep -cE "^export\s+(async\s+)?function\s+loadSynonyms" scripts/ingest-photos.js` returns 1
    - `classify` is exported — verify with `grep -cE "^export\s+function\s+classify" scripts/ingest-photos.js` returns 1
    - existsSync is imported from node:fs — verify with `grep -cE "^import\s*\{\s*existsSync\s*\}\s*from\s*['\"]node:fs['\"]" scripts/ingest-photos.js` returns 1
    - SYNONYMS_CSV path constant is declared — verify with `grep -cF "const SYNONYMS_CSV = resolve('data/species-synonyms.csv');" scripts/ingest-photos.js` returns 1
    - The synonym pre-pass is wired into the classify() function — verify with `grep -cF "resolved-via-synonym" scripts/ingest-photos.js` returns at least 2 (one in classify(), one in the RESORT_ONLY re-classification log line)
    - classify() signature now accepts three args — verify with `grep -cE "function classify\(\{[^}]+\},\s*species,\s*synonyms\)" scripts/ingest-photos.js` returns 1
    - The RESORT_ONLY block re-classifies before sorting — verify with `awk '/if\s*\(\s*RESORT_ONLY\s*\)/,/^\s*return;\s*$/' scripts/ingest-photos.js | grep -cF "promoted" >= 1` AND the same awk-range contains `synonyms.has(binomial)`
    - The full-run code path passes synonyms into classify() — verify with `grep -cE "classify\([^)]*species,\s*synonyms" scripts/ingest-photos.js` returns 1 (the call site in main())
    - The full-run code path loads synonyms after species — verify with `grep -n "loadSynonyms\|loadSpecies" scripts/ingest-photos.js | awk -F: '$2 ~ /await loadSpecies/ {sp=$1} $2 ~ /await loadSynonyms/ {syn=$1} END {exit (syn>sp)?0:1}'` exits 0 (loadSynonyms call comes after loadSpecies call by line number)
    - logStage emits the new 'synonym-warn' action — verify with `grep -cF "'synonym-warn'" scripts/ingest-photos.js` returns at least 1
    - logStage emits the new 'reclassify' action — verify with `grep -cF "'reclassify'" scripts/ingest-photos.js` returns at least 1
    - sortForInvestigation is still called in the RESORT_ONLY block — verify with `awk '/if\s*\(\s*RESORT_ONLY\s*\)/,/^\s*return;\s*$/' scripts/ingest-photos.js | grep -cF "sortForInvestigation" >= 1`
    - scripts/lib/manifest.js was NOT modified by this plan — verify with `git diff HEAD~2 HEAD -- scripts/lib/manifest.js | wc -l` returns 0
    - scripts/lib/parse-photo-filename.js was NOT modified by this plan — verify with `git diff HEAD~2 HEAD -- scripts/lib/parse-photo-filename.js | wc -l` returns 0
    - The GREEN commit message follows the Phase 26 contract — verify with `git log -1 --pretty=%s` returns `feat(27-02): synonyms.csv pre-pass in classify() promotes matched rows to resolved-via-synonym`
    - The GREEN commit modifies ONLY `scripts/ingest-photos.js` — verify with `git diff-tree --no-commit-id --name-only -r HEAD` returns `scripts/ingest-photos.js`
    - The previous commit (HEAD~1) is the RED commit from Task 1 — verify with `git log -1 --skip=1 --pretty=%s` returns a string starting with `test(27-02): add failing tests for synonym-aware classify cascade`
  </acceptance_criteria>
  <done>scripts/ingest-photos.js carries the loadSynonyms helper, SYNONYMS_CSV constant, the classify() pre-pass with the new third synonyms argument, synonym-aware re-classification in the RESORT_ONLY block, the existsSync import, exports on classify and loadSynonyms, and the wired-in load + classify-with-synonyms call in the full-run path. npm test exits 0, node --check passes, the RED/GREEN commit pair `test(27-02): …` → `feat(27-02): …` is committed, and no other source files were touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed CSV → JS Map | data/species-synonyms.csv values flow into a Map<string, {string, string}> used by classify() at every per-row call. |
| Map values → committed manifest | binomial_resolved and species_slug from the Map become column values in data/species-photos-manifest.csv on the RESORT_ONLY path. |
| Test fixtures → tmpdir | Test code writes synthetic CSV fixtures under os.tmpdir(); cleanup via rmSync in try/finally. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27.02-01 | Tampering | CSV-injection via curator-typed values in data/species-synonyms.csv flowing into data/species-photos-manifest.csv | mitigate | csv-stringify (already used by scripts/lib/manifest.js writeManifest) quotes any field containing comma, newline, or double-quote. Plan 02 NEVER writes to the manifest by hand — every write goes through writeManifest. The round-trip property is locked by scripts/lib/manifest.test.js (Phase 26's tests still pass). |
| T-27.02-02 | Tampering | Path traversal via to_species_slug containing `../` or `/etc/passwd` | mitigate | to_species_slug is ONLY used as a Map key into species.bySlug (string-equality lookup) and as a column value written into the manifest CSV (string). The implementation does NOT pass it to any filesystem API (no readFile, writeFile, open, fs.* call uses it as a path argument). Any malicious value simply fails the bySlug lookup and is dropped with a synonym-warn line. |
| T-27.02-03 | Information disclosure | Logging of synonym values that may contain sensitive curator input | accept | Phase 27 has no secrets — there is no DROPBOX_TOKEN, no API key, no PII in this code path. The only data is taxonomic binomials (genus + species names) which are public scientific names. logStage emits these to stdout, which is the curator's terminal; no log persistence beyond the curator's own shell. |
| T-27.02-04 | Denial of service | A very large species-synonyms.csv (e.g. 100,000 rows) consuming memory | accept | The bounded workload is ~30–80 unique decisions per CONTEXT.md "Phase Boundary"; even an order-of-magnitude overshoot to ~1,000 rows costs ~50 KB of heap. csv-parse/sync loads the whole file into memory, but for the actual data size this is trivial. If the file grows past ~10,000 rows we revisit (out of scope for v2.2). |
| T-27.02-05 | Repudiation | Manifest mutation without an audit record | accept | The audit record is git history: the per-row mutation only happens via `npm run photos:investigate`, the manifest is committed, and the commit diff shows exactly which rows were promoted. PR review at commit time provides the human checkpoint. logStage emits a `reclassify` line per actual promotion (not on idempotent re-runs) for live monitoring. |
| T-27.02-06 | Elevation of privilege | Synonym-promoted rows getting unintended `status` change (e.g. accidentally advancing to `tiled` or `uploaded`) | mitigate | The RESORT_ONLY re-classification loop mutates ONLY `match_bucket`, `binomial_resolved`, `species_slug`. The status column is explicitly left untouched. This is enforced by acceptance criteria + by the idempotency guard's narrow set of assigned fields. Phase 28 gates downloads on `status: discovered`, not on `match_bucket`, so synonym promotion does not unintentionally drive downstream effects. |
| T-27.02-SC | Tampering | npm package legitimacy (no new packages) | accept | This plan adds zero npm dependencies. csv-parse/sync (already in deps since Phase 13) and node:fs/node:path/node:os/node:test/node:assert (Node built-ins) are the only modules used. RESEARCH.md package legitimacy audit is not required since this plan introduces no installs. |
</threat_model>

<verification>
- `node --check scripts/ingest-photos.js` exits 0 after Task 2 (syntax-valid)
- `npm test` exits 0 after Task 2 — the nine new tests (5 classify + 4 loadSynonyms) PASS along with every pre-existing test
- The RED commit `test(27-02): …` lands BEFORE the GREEN commit `feat(27-02): …` — verifiable via `git log --oneline -3`
- The RED commit modifies only scripts/ingest-photos.test.js + package.json; the GREEN commit modifies only scripts/ingest-photos.js
- scripts/lib/* files are NOT modified (verified by `git diff` against the plan's parent commit)
- All thirteen 27-CONTEXT.md decisions in `decisions_covered` are reflected in code grep checks (resolved-via-synonym appears, synonym-warn appears, reclassify appears, sortForInvestigation untouched, etc.)
- `npm run photos:investigate` against the Plan 01 header-only synonyms.csv leaves the existing manifest unchanged except for a re-sort (loadSynonyms returns an empty Map; no row is mutated; the final log line reports `0 promoted to resolved-via-synonym`)
</verification>

<success_criteria>
- A curator who edits `data/species-synonyms.csv` and runs `npm run photos:investigate` sees the affected manifest rows promoted to `match_bucket: resolved-via-synonym` with `binomial_resolved` and `species_slug` populated from `data/species.csv`. No Dropbox calls. No file downloads. CURATE-02 satisfied.
- A second `npm run photos:investigate` against the same synonyms.csv is idempotent — the `promoted` counter reports 0 and no `reclassify` log lines are emitted (the idempotency guard skips already-promoted rows).
- A typo in `to_species_slug` (a slug not present in `data/species.csv`) emits one `synonym-warn target-not-in-species-csv <from> → <to>` line at load time and does NOT silently mutate any manifest row. CURATE-02 + Plan 03 runbook "When Things Go Wrong" reachable.
- A `binomial_raw` value present in the manifest but absent from synonyms.csv is unaffected — the existing Phase 26 cascade decides its bucket. CURATE-03 (investigation queue from Phase 26) continues to surface unresolved rows at the top.
- A row in the `provisional` or `unparseable` bucket whose `binomial_raw` matches a synonyms.csv row IS promoted to `resolved-via-synonym` (D-06 widening). The unit test `promotes a provisional-marked binomial through synonyms.csv (D-06 widening)` pins this.
- The Phase 26 RED-then-GREEN commit pair pattern is followed; git log shows the two commits in order.
- Plan 03's curator runbook can reference the actual commands and log shapes this plan ships (no forward references to unspecified behavior).
</success_criteria>

<output>
Create `.planning/phases/27-synonym-curation-pass/27-02-SUMMARY.md` when done. Include:
- Output of `git log --oneline -3` showing the RED commit BEFORE the GREEN commit
- Output of `npm test 2>&1 | tail -10` showing all tests pass (including the new nine)
- Line count change for `scripts/ingest-photos.js` (was 467 lines)
- Line count of the new `scripts/ingest-photos.test.js`
- Confirmation that scripts/lib/* was NOT modified (`git diff HEAD~2 HEAD -- scripts/lib/ | wc -l` returns 0)
- The list of all new exports added to scripts/ingest-photos.js (grep for `^export` and report)
- A one-line note on which option from Edit 7 was used (Option A vs Option B)
- Any deviation from 27-PATTERNS.md "scripts/ingest-photos.js (modified)" section and why
</output>
</content>
</invoke>