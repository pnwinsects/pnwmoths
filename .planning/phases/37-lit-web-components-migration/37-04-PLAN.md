---
phase: 37-lit-web-components-migration
plan: 04
type: execute
wave: 3
depends_on: ["37-01", "37-02", "37-03"]
files_modified:
  - src/components/pnwm-occurrence-map.ts
  - src/components/pnwm-phenology-chart.ts
  - src/components/pnwm-taxon-browser.ts
  - src/components/pnwm-taxon-browser.test.ts
autonomous: true
requirements: [MIG-04, SCHEMA-08]
must_haves:
  truths:
    - "pnwm-occurrence-map and pnwm-phenology-chart are typed and import loadParquet from parquet-cache.ts"
    - "their pnwm-filter-change listeners type the event detail via the HTMLElementEventMap merge (no cast)"
    - "pnwm-taxon-browser validates species-states.json by top-level-array + single-element shape (O(1)) and HARD-FAILS on a schema mismatch (re-throws), while preserving soft degradation for network errors (D-05 intent)"
    - "the species-states validator is unit-testable and proven to throw on non-array and on bad element shape"
  artifacts:
    - path: "src/components/pnwm-occurrence-map.ts"
      provides: "typed Leaflet occurrence-map component listening for pnwm-filter-change"
    - path: "src/components/pnwm-phenology-chart.ts"
      provides: "typed Chart.js phenology component listening for pnwm-filter-change"
    - path: "src/components/pnwm-taxon-browser.ts"
      provides: "typed taxon-browser with O(1) species-states.json structural validator (SCHEMA-08)"
      exports: ["buildStateMap", "taxonHasState", "collectSlugs"]
      contains: "safeParse"
  key_links:
    - from: "src/components/pnwm-taxon-browser.ts"
      to: "SpeciesStateSchema.safeParse(rows[0])"
      via: "O(1) single-element shape probe"
      pattern: "safeParse\\(rows\\[0\\]\\)|safeParse\\("
    - from: "src/components/pnwm-occurrence-map.ts"
      to: "parquet-cache.ts loadParquet"
      via: "import"
      pattern: "from './parquet-cache.ts'"
---

<objective>
Convert the two parquet-cache CONSUMER components plus the taxon-browser, and add the second SCHEMA-08 validator. These depend on Plan 02 (`parquet-cache.ts` must exist as `.ts` for these imports to typecheck) and Plan 03 (popup `.ts`), so they run in Wave 3:

1. **pnwm-occurrence-map.ts (MIG-04):** rename + annotate the `static get properties()` getter form; type the Leaflet `L.Map`/`L.FeatureGroup` fields (`@types/leaflet` already installed); update the `./parquet-cache.js` import to `.ts`; type the `pnwm-filter-change` listener via the `HTMLElementEventMap` merge (no cast needed). Map creates `<pnwm-occurrence-popup>` via `createElement` at runtime (already a `.ts` after Plan 03).
2. **pnwm-phenology-chart.ts (MIG-04):** rename + annotate the `static get properties()` form; type the Chart.js usage and the `pnwm-filter-change` listener; update the parquet-cache import to `.ts`.
3. **pnwm-taxon-browser.ts (SCHEMA-08 / SC-3 + MIG-04):** rename + annotate; inject the O(1) `species-states.json` validator (top-level `Array.isArray` + `SpeciesStateSchema.safeParse(rows[0])`) and resolve the D-05 throw discipline (RESEARCH Open Question 1) by HARD-FAILING on schema mismatch. Convert its test to `.ts` and add two SCHEMA-08 validator test cases.

Purpose: This wave closes SCHEMA-08 (both validators now live) and MIG-04 (both filter-change listener sites typed). The taxon-browser catch decision is the one open design question in this phase; this plan resolves it.
Output: three typed components, one converted test, the second load-time validator, and the resolved D-05 hard-fail behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-lit-web-components-migration/37-RESEARCH.md
@.planning/phases/37-lit-web-components-migration/37-PATTERNS.md
@.planning/phases/37-lit-web-components-migration/37-02-SUMMARY.md
@.planning/phases/37-lit-web-components-migration/37-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Convert pnwm-occurrence-map.js and pnwm-phenology-chart.js → .ts with typed filter-change listeners (MIG-04)</name>
  <files>src/components/pnwm-occurrence-map.ts, src/components/pnwm-phenology-chart.ts</files>
  <read_first>
    - src/components/pnwm-occurrence-map.js (141 lines; imports L from leaflet + loadParquet/filterRecords; createElement('pnwm-occurrence-popup'); listens for pnwm-filter-change)
    - src/components/pnwm-phenology-chart.js (155 lines; imports Chart.js + loadParquet/filterRecords/aggregateByMonth; listens for pnwm-filter-change)
    - src/components/parquet-cache.ts (the typed loadParquet/filterRecords/aggregateByMonth signatures, from Plan 02)
    - src/types/events.ts (the FilterChangeDetail + HTMLElementEventMap merge, from Plan 01)
    - 37-PATTERNS.md section "`src/components/pnwm-occurrence-map.ts`, `pnwm-phenology-chart.ts`" (imports, listener-site FilterChangeDetail usage, Leaflet L.Map/L.FeatureGroup fields, Chart.js note)
    - 37-RESEARCH.md Pattern 1 + Pattern 6 listener usage + Pitfall 1 + Pitfall 7 + D-06/D-08
  </read_first>
  <action>
    Rename both files `.js` → `.ts` (git mv). For BOTH: expand the lit import to include `type PropertyDeclarations, type CSSResult, type TemplateResult`; update the `./parquet-cache.js` import specifier to `./parquet-cache.ts`; add `import type { OccurrenceRecord, FilterChangeDetail } from '../types/index.ts';`. Type `static get properties(): PropertyDeclarations` (KEEP the getter form per D-06); declare typed instance fields above the constructor and keep constructor assignments (Pitfall 1). For the `pnwm-filter-change` listener, the HTMLElementEventMap merge (Plan 01) types the event automatically; annotate the callback parameter as `(e: CustomEvent<FilterChangeDetail>)` for clarity and destructure `e.detail` without a cast (37-PATTERNS.md listener-site pattern). Type `render(): TemplateResult`. For pnwm-occurrence-map.ts: keep `import L from 'leaflet'` (typed via @types/leaflet) and type `_map: L.Map | null`, `_markerGroup: L.FeatureGroup | null` per 37-PATTERNS.md; for any `arr[index]` access apply the Pitfall 7 `?? fallback!` guard. For pnwm-phenology-chart.ts: type the Chart.js usage following the existing import shape (read the .js import block first); type the chart instance field. KEEP `customElements.define(...)` at each tail (D-06). NO decorators (D-07). No `@ts-ignore`, no unguarded casts.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/pnwm-occurrence-map.ts` and `pnwm-phenology-chart.ts` exist; neither `.js` variant remains
    - `grep -c "from './parquet-cache.js'" src/components/pnwm-occurrence-map.ts src/components/pnwm-phenology-chart.ts` returns 0 (specifiers updated to .ts)
    - `grep -c "FilterChangeDetail" src/components/pnwm-occurrence-map.ts` returns at least 1 and same for pnwm-phenology-chart.ts (listener detail typed without cast)
    - `grep -c "@customElement\|@property\|@state" src/components/pnwm-occurrence-map.ts src/components/pnwm-phenology-chart.ts` returns 0
    - `npm run typecheck` exits 0 with zero errors
  </acceptance_criteria>
  <done>Both parquet-cache consumers are typed, import from parquet-cache.ts, and type their pnwm-filter-change listener detail via the HTMLElementEventMap merge without a cast; getter-form static properties and manual customElements.define preserved; typecheck green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Convert pnwm-taxon-browser.js → .ts with the O(1) species-states.json validator and D-05 hard-fail (SCHEMA-08)</name>
  <files>src/components/pnwm-taxon-browser.ts</files>
  <read_first>
    - src/components/pnwm-taxon-browser.js (all 327 lines; connectedCallback lines 79-93 with the existing `try { ... fetch species-states.json ... } catch (_e) { /* leave empty */ }`; buildStateMap/taxonHasState/collectSlugs at lines 17-45)
    - src/types/schemas.ts (the migrated SpeciesStateSchema + SpeciesState/TaxonFamily/TaxonSubfamily/TaxonGenus types, from Plan 01)
    - 37-PATTERNS.md section "`src/components/pnwm-taxon-browser.ts`" (imports, static get properties() form, instance fields, connectedCallback validator injection, buildStateMap/taxonHasState/collectSlugs signatures, collectSlugs union-type note)
    - 37-RESEARCH.md Pattern 5 + "Note on D-03 throw discipline" + Open Question 1 (the D-05 hard-fail decision) + Anti-Pattern "z.array(...).parse(rows)"
  </read_first>
  <behavior>
    - The species-states validator throws on a non-array top-level value (e.g. `{}` or `null`).
    - It throws on a bad element shape (e.g. `rows[0]` missing the `state` field — fails SpeciesStateSchema.safeParse).
    - It does NOT throw for a valid array of `{ species_slug, state }` objects, and an empty array `[]` is accepted (length 0 → no probe).
    - A schema-validation failure HARD-FAILS (propagates out of connectedCallback / re-thrown), surfacing the error rather than silently leaving the state filter empty; a network/fetch failure still degrades softly (state filter disabled), matching the prior behavior.
  </behavior>
  <action>
    Rename `src/components/pnwm-taxon-browser.js` → `.ts` (git mv). Expand the lit import (`import { LitElement, html, type PropertyDeclarations, type TemplateResult } from 'lit';`); add `import * as z from 'zod/mini';` and `import { SpeciesStateSchema, type SpeciesState, type TaxonFamily, type TaxonSubfamily, type TaxonGenus } from '../types/index.ts';`. Type `static get properties(): PropertyDeclarations` (KEEP getter form, D-06); declare typed instance fields above the constructor per 37-PATTERNS.md (`_families: TaxonFamily[]`, `_stateMap: Record<string, Set<string>>`, etc.) and keep constructor assignments (Pitfall 1). Type `buildStateMap(rows: SpeciesState[]): Record<string, Set<string>>`, `taxonHasState(...)`, and `collectSlugs(node: TaxonFamily | TaxonSubfamily | TaxonGenus): string[]` (union per 37-PATTERNS.md note). Inject the SCHEMA-08 validator inside the existing connectedCallback try block, after `const rows: unknown = await res.json();`: if `!Array.isArray(rows)` throw `new Error('species-states.json: expected array at top level')`; if `rows.length > 0`, `const probe = SpeciesStateSchema.safeParse(rows[0]);` and if `!probe.success` throw `new Error('species-states.json: element shape mismatch: ' + probe.error.issues.map(i => i.message).join('; '))` (Anti-Pattern: do NOT `z.array(...).parse(rows)` — that is O(rows); probe rows[0] only, D-03). RESOLVE D-05 (Open Question 1) with HARD FAIL on schema errors: define a distinguishable schema-validation error and change the existing `catch (_e) { /* leave empty */ }` so that schema-validation errors RE-THROW (surface — "showing no occurrence data beats silently-wrong data") while genuine network/fetch errors continue to degrade softly (leave _stateMap empty). Implementation approach: create a small `SchemaValidationError extends Error` class (or a tagged error), throw it from the two validation failures, and in the catch re-throw when `err instanceof SchemaValidationError`, otherwise keep the soft-degradation path. Factor the array/shape check into an EXPORTED pure function (name it `validateSpeciesStates(rows: unknown): asserts rows is SpeciesState[]`, or a boolean+throw helper) so Task 3 can unit-test it without DOM/fetch. KEEP `customElements.define('pnwm-taxon-browser', PnwmTaxonBrowser)` at the tail (D-06). NO decorators (D-07). No `@ts-ignore`.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/pnwm-taxon-browser.ts` exists; `pnwm-taxon-browser.js` no longer exists
    - `grep -c "safeParse" src/components/pnwm-taxon-browser.ts` returns at least 1 (single-element probe)
    - `grep -c "z.array(" src/components/pnwm-taxon-browser.ts` returns 0 (no O(rows) parse)
    - `grep -c "export function validateSpeciesStates\|class SchemaValidationError" src/components/pnwm-taxon-browser.ts` returns at least 1 (testable validator + hard-fail discriminant)
    - the catch block re-throws schema-validation errors (hard fail) and preserves soft degradation for network errors — confirmed by Task 3 tests + source review
    - `grep -c "@customElement\|@property\|@state" src/components/pnwm-taxon-browser.ts` returns 0
    - `npm run typecheck` exits 0 with zero errors
  </acceptance_criteria>
  <done>pnwm-taxon-browser.ts is typed; species-states.json is validated at O(1) (Array.isArray + single-element safeParse); a schema mismatch hard-fails (re-throws, surfacing the error) per the resolved D-05 decision, while network errors still degrade softly; the validator is exported for unit testing; no decorators.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Rename pnwm-taxon-browser.test.js → .ts and add species-states validator test cases (SCHEMA-08)</name>
  <files>src/components/pnwm-taxon-browser.test.ts</files>
  <read_first>
    - src/components/pnwm-taxon-browser.test.js (existing buildStateMap/taxonHasState/collectSlugs tests; import on line 3)
    - src/components/pnwm-taxon-browser.ts (the validateSpeciesStates export / SchemaValidationError, from Task 2 — the unit under test)
    - 37-PATTERNS.md section "Test files" (import-specifier update + new species-states test-case skeletons)
  </read_first>
  <behavior>
    - New test "throws when top-level is not an array": calling the exported validator with `{}` (non-array) throws.
    - New test "throws when element shape is wrong": calling it with `[{ species_slug: 's' }]` (missing `state`) throws.
    - New test "accepts a valid array": `[{ species_slug: 's', state: 'WA' }]` and `[]` do not throw.
    - All pre-existing buildStateMap/taxonHasState/collectSlugs tests still pass.
  </behavior>
  <action>
    Rename `src/components/pnwm-taxon-browser.test.js` → `.ts` (git mv). Update the import specifier `'./pnwm-taxon-browser.js'` → `'./pnwm-taxon-browser.ts'` and add the exported validator (`validateSpeciesStates` and/or `SchemaValidationError`) to the import. Keep `node:test`/`node:assert` imports unchanged. Add a new `describe('species-states.json validator', ...)` block with the cases from the behavior above using `assert.throws(...)` for the non-array and bad-shape cases and `assert.doesNotThrow(...)` for the valid-array and empty-array cases. Test the exported pure validator directly — do NOT instantiate the Lit component or mock fetch/DOM. No `@ts-ignore`.
  </action>
  <verify>
    <automated>node --test src/components/pnwm-taxon-browser.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/pnwm-taxon-browser.test.ts` exists; `pnwm-taxon-browser.test.js` no longer remains
    - `grep -c "pnwm-taxon-browser.js" src/components/pnwm-taxon-browser.test.ts` returns 0
    - `grep -c "species-states.json validator" src/components/pnwm-taxon-browser.test.ts` returns 1
    - `node --test src/components/pnwm-taxon-browser.test.ts` passes with the new non-array, bad-shape, and valid-array cases green plus all pre-existing cases
  </acceptance_criteria>
  <done>The taxon-browser test runs as .ts under node --test; new SCHEMA-08 cases prove the species-states validator throws on non-array and bad element shape and accepts valid/empty arrays; all pre-existing pure-function tests still pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CDN → browser pnwm-taxon-browser fetch | `species-states.json` is fetched from a mutable CDN URL; a stale/malformed file (version skew) is untrusted structurally |
| CDN → browser loadParquet (consumed by map/phenology) | covered by T-37-02 in Plan 02; these components consume the already-validated loadParquet output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-05 | Tampering | pnwm-taxon-browser ← species-states.json (CDN version skew, wrong structure) | mitigate | `Array.isArray()` top-level check + `SpeciesStateSchema.safeParse(rows[0])` O(1) element-shape probe; schema mismatch HARD-FAILS (re-throws) so the state filter shows no data rather than silently-wrong data (resolved D-05) |
| T-37-06 | Tampering | prototype-pollution-style keys in species-states.json elements | mitigate | SpeciesStateSchema is a closed `z.object({ species_slug, state })`; extra/`__proto__`-style keys are ignored by the schema and the consumer only reads `species_slug`/`state`; no key is used to index into a prototype-reachable object |
| T-37-07 | Denial of Service | oversized species-states.json (large row count) | accept | Validation is O(1) (single-element probe, not per-row); buildStateMap iterating the array is the same cost as the pre-migration code (no new DoS surface introduced) |
| T-37-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase (leaflet/chart.js/zod/hyparquet vetted in prior phases) |
</threat_model>

<verification>
- `npm run typecheck` exits 0
- `node --test src/components/pnwm-taxon-browser.test.ts` passes (incl. new validator cases)
- `grep -c "safeParse" src/components/pnwm-taxon-browser.ts` ≥ 1; `grep -c "z.array(" src/components/pnwm-taxon-browser.ts` returns 0
- No `.js` remains for map, phenology, taxon-browser, or the taxon-browser test
- No decorators in any converted file
</verification>

<success_criteria>
- Second SCHEMA-08 validator (species-states.json, O(1)) in place with resolved D-05 hard-fail (SC-3 / SCHEMA-08)
- Both filter-change listener sites typed via HTMLElementEventMap merge (SC-2 / MIG-04)
- map/phenology import the typed parquet-cache.ts (SC-1)
- New validator test cases green (SC-1)
- No decorators; getter-form static properties + manual customElements.define preserved (D-06/D-07)
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces (Plan 04 contribution)

Full phase manifest in `37-ARTIFACTS.md`. This plan adds:

- **`src/components/pnwm-occurrence-map.ts`** (renamed from `.js`) — typed Leaflet component (`L.Map`/`L.FeatureGroup` fields); imports `loadParquet`/`filterRecords` from `parquet-cache.ts`; `pnwm-filter-change` listener detail typed via the merge (no cast)
- **`src/components/pnwm-phenology-chart.ts`** (renamed from `.js`) — typed Chart.js component; imports from `parquet-cache.ts`; typed filter-change listener
- **`src/components/pnwm-taxon-browser.ts`** (renamed from `.js`) — typed; exports `buildStateMap`, `taxonHasState`, `collectSlugs`, and NEW `validateSpeciesStates(rows)` + `SchemaValidationError` (O(1) species-states.json validator; hard-fail on schema mismatch per resolved D-05)
- **`src/components/pnwm-taxon-browser.test.ts`** (renamed from `.test.js`) — specifier updated; gains 2+ new SCHEMA-08 species-states validator cases
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/37-lit-web-components-migration/37-04-SUMMARY.md` when done.
</output>
