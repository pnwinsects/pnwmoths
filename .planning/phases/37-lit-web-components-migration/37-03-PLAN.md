---
phase: 37-lit-web-components-migration
plan: 03
type: execute
wave: 2
depends_on: ["37-01"]
files_modified:
  - src/components/pnwm-image-slideshow.ts
  - src/components/pnwm-image-slideshow.test.ts
  - src/components/pnwm-occurrence-popup.ts
  - src/components/pnwm-plate-viewer.ts
  - src/components/glossary-tooltip.ts
autonomous: true
requirements: [MIG-04]
must_haves:
  truths:
    - "pnwm-image-slideshow, pnwm-occurrence-popup, pnwm-plate-viewer, glossary-tooltip are all .ts with full strict types"
    - "pnwm-image-slideshow.test.ts imports the PnwmImageSlideshow class directly and runs green under node --test"
    - "no decorators are used; static-properties form and manual customElements.define are preserved"
    - "noUncheckedIndexedAccess array accesses are guarded (?? fallback pattern preserved)"
  artifacts:
    - path: "src/components/pnwm-image-slideshow.ts"
      provides: "typed slideshow Lit component (static properties = {} form), typed Specimen[] + OSD viewer field"
      exports: ["PnwmImageSlideshow"]
    - path: "src/components/pnwm-occurrence-popup.ts"
      provides: "typed popup component taking OccurrenceRecord"
    - path: "src/components/pnwm-plate-viewer.ts"
      provides: "typed plate-viewer component"
    - path: "src/components/glossary-tooltip.ts"
      provides: "typed vanilla DOM tooltip script (no Lit class)"
  key_links:
    - from: "src/components/pnwm-image-slideshow.ts"
      to: "Specimen type"
      via: "import type from ../types/index.ts"
      pattern: "import type \\{ Specimen"
    - from: "src/components/pnwm-image-slideshow.test.ts"
      to: "PnwmImageSlideshow class"
      via: "direct class import (the reason decorators are banned, D-07)"
      pattern: "import \\{ PnwmImageSlideshow"
---

<objective>
Convert the four standalone display components — which have NO inter-component or parquet-cache imports (only `lit` and Plan 01 types) — to TypeScript. These run in parallel with Plan 02 (disjoint file sets):

1. **pnwm-image-slideshow.ts (MIG-04):** rename + annotate the `static properties = {}` class-field form, type the `Specimen[]` high-res field, the OSD viewer field, and apply the `noUncheckedIndexedAccess` `?? fallback!` pattern (Pitfall 7). Rename its test `.ts`, update the direct class import specifier, keep tests green.
2. **pnwm-occurrence-popup.ts (MIG-04):** rename + annotate; type the `record: OccurrenceRecord | null` input.
3. **pnwm-plate-viewer.ts (MIG-04):** rename + annotate simple typed fields (`static properties = {}` form).
4. **glossary-tooltip.ts (MIG-04):** rename + annotate DOM API calls; this is a vanilla top-level script (no LitElement class, no customElements.define) — typed `querySelectorAll<HTMLElement>` and justified non-null assertions.

Purpose: These components depend only on the types layer (Plan 01), so they convert in Wave 2 alongside parquet-cache/filter-bar without file conflicts. The slideshow test imports the component CLASS directly — the exact pattern that makes decorators illegal under `node --test` type-stripping (D-07), so the conversion is rename + annotate only.
Output: four typed component files + one converted slideshow test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-lit-web-components-migration/37-RESEARCH.md
@.planning/phases/37-lit-web-components-migration/37-PATTERNS.md
@.planning/phases/37-lit-web-components-migration/37-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Convert pnwm-image-slideshow.js → .ts (+ test) with typed Specimen/OSD fields and noUncheckedIndexedAccess guards (MIG-04)</name>
  <files>src/components/pnwm-image-slideshow.ts, src/components/pnwm-image-slideshow.test.ts</files>
  <read_first>
    - src/components/pnwm-image-slideshow.js (the file being renamed — all 402 lines; note the existing `?? this._highResSpecimens[0]` patterns in _prevSpecimen/_nextSpecimen)
    - src/components/pnwm-image-slideshow.test.js (imports PnwmImageSlideshow class directly; tests _prevSpecimen/_nextSpecimen/_formatCaption/useOsd/view-to-label)
    - src/types/schemas.ts (the migrated Specimen type, from Plan 01)
    - 37-PATTERNS.md section "`src/components/pnwm-image-slideshow.ts`" (static properties = {} form, instance fields list, _osdViewer type, noUncheckedIndexedAccess handling) and "Test files"
    - 37-RESEARCH.md Pattern 2 (static class fields) + Pitfall 1 + Pitfall 7 (noUncheckedIndexedAccess) + D-06/D-07/D-08
  </read_first>
  <behavior>
    - The pre-existing slideshow tests (_formatCaption, _prevSpecimen/_nextSpecimen index wrapping, useOsd, view-to-label) all still pass after conversion.
    - Index access into _highResSpecimens still falls back to element 0 when the current index is out of range (existing `?? [0]` behavior preserved).
  </behavior>
  <action>
    Rename `src/components/pnwm-image-slideshow.js` → `.ts` and `pnwm-image-slideshow.test.js` → `.ts` (git mv both). In the component: expand the lit import to `import { LitElement, html, css, type PropertyDeclarations, type CSSResult, type TemplateResult } from 'lit';` and add `import type { Specimen } from '../types/index.ts';`. KEEP the `static properties = {...}` class-field form (annotate as `static properties: PropertyDeclarations = {...}`) and `static styles: CSSResult = css\`...\`` — do NOT convert to the getter form (Pitfall 2: both forms coexist, do not normalize). Declare typed instance fields above the constructor per 37-PATTERNS.md (`slug: string`, `_currentIndex: number`, `_lightboxOpen: boolean`, `_images: Array<{ filename: string; photographer: string; license: string }>`, `_stripOverflows: boolean`, `highResAvailable: boolean`, `highResSpecimens: string`, `cdnBaseUrl: string`, `prefixUrl: string`, `_highResSpecimens: Specimen[]`, `_osdViewer: import('openseadragon').Viewer | null`) and KEEP existing constructor assignments (Pitfall 1). For `noUncheckedIndexedAccess` array accesses, keep/extend the existing `this._highResSpecimens[this._currentIndex] ?? this._highResSpecimens[0]!` pattern (Pitfall 7). Type `render(): TemplateResult` and method signatures. KEEP `customElements.define('pnwm-image-slideshow', PnwmImageSlideshow)` at the tail (D-06) and KEEP `export class PnwmImageSlideshow` so the test can import it. NO decorators (D-07). In the test: update the import specifier `'./pnwm-image-slideshow.js'` → `'./pnwm-image-slideshow.ts'`; keep `node:test`/`node:assert` imports unchanged. No `@ts-ignore`.
  </action>
  <verify>
    <automated>node --test src/components/pnwm-image-slideshow.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/pnwm-image-slideshow.ts` and `pnwm-image-slideshow.test.ts` exist; neither `.js` variant remains
    - `grep -c "@customElement\|@property\|@state" src/components/pnwm-image-slideshow.ts` returns 0
    - `grep -c "static properties" src/components/pnwm-image-slideshow.ts` returns 1 (class-field form preserved, NOT normalized to getter)
    - `grep -c "pnwm-image-slideshow.js" src/components/pnwm-image-slideshow.test.ts` returns 0
    - `node --test src/components/pnwm-image-slideshow.test.ts` passes (direct class import works under type-stripping — proves no decorators)
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>pnwm-image-slideshow.ts is fully typed with the class-field static-properties form preserved, typed Specimen[]/OSD fields, noUncheckedIndexedAccess guards intact; its test imports the class directly and passes under node --test (the decorator-free proof).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Convert pnwm-occurrence-popup.js and pnwm-plate-viewer.js → .ts (MIG-04)</name>
  <files>src/components/pnwm-occurrence-popup.ts, src/components/pnwm-plate-viewer.ts</files>
  <read_first>
    - src/components/pnwm-occurrence-popup.js (129 lines; takes an OccurrenceRecord, renders via Lit html — the XSS-escaping boundary)
    - src/components/pnwm-plate-viewer.js (62 lines; simple component)
    - src/types/schemas.ts (the migrated OccurrenceRecord type, from Plan 01)
    - 37-PATTERNS.md section "`src/components/pnwm-occurrence-popup.ts`, `pnwm-plate-viewer.ts`" (static properties = {} form, popup record field)
    - 37-RESEARCH.md Pattern 2 + Pitfall 1 + D-06/D-08; Security Domain note on Lit html XSS escaping (no change required)
  </read_first>
  <action>
    Rename both files `.js` → `.ts` (git mv). For pnwm-occurrence-popup.ts: expand the lit import (`import { LitElement, html, type PropertyDeclarations, type TemplateResult } from 'lit';`) and add `import type { OccurrenceRecord } from '../types/index.ts';`; annotate the `static properties = {...}` class-field form as `static properties: PropertyDeclarations`; declare `record: OccurrenceRecord | null` (and any other instance fields) above the constructor, keep constructor assignments (Pitfall 1); type `render(): TemplateResult`. PRESERVE the Lit `html\`...\`` rendering exactly — it escapes occurrence-record text content and is the existing XSS mitigation (T-03-01 from a prior phase); do NOT switch to unsafeHTML or string concatenation. For pnwm-plate-viewer.ts: same rename + annotate pattern, typing its simple fields per the static properties = {} form. KEEP `customElements.define(...)` at each tail (D-06). NO decorators (D-07). No `@ts-ignore`, no unguarded casts.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/pnwm-occurrence-popup.ts` and `pnwm-plate-viewer.ts` exist; neither `.js` variant remains
    - `grep -c "@customElement\|@property\|@state" src/components/pnwm-occurrence-popup.ts src/components/pnwm-plate-viewer.ts` returns 0
    - `grep -c "customElements.define" src/components/pnwm-occurrence-popup.ts` returns 1 and same for pnwm-plate-viewer.ts
    - pnwm-occurrence-popup.ts still renders via Lit `html` tagged template (grep for `html\`` returns ≥ 1; no `unsafeHTML`)
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>Both display components are typed with no decorators and manual customElements.define preserved; the popup retains Lit html escaping for occurrence-record content (security mitigation unchanged); typecheck green.</done>
</task>

<task type="auto">
  <name>Task 3: Convert glossary-tooltip.js → .ts (vanilla DOM script, MIG-04)</name>
  <files>src/components/glossary-tooltip.ts</files>
  <read_first>
    - src/components/glossary-tooltip.js (all 93 lines; vanilla top-level script — no class, no LitElement, no customElements.define)
    - 37-PATTERNS.md section "`src/components/glossary-tooltip.ts`" (typed DOM queries, justified non-null assertions)
    - 37-RESEARCH.md Pattern 7 (vanilla JS → TypeScript)
  </read_first>
  <action>
    Rename `src/components/glossary-tooltip.js` → `.ts` (git mv). Add DOM API type annotations only — this is a top-level script, NOT a Lit component (no class to annotate, no customElements.define). Type `document.querySelectorAll<HTMLElement>('abbr.glossary-term')`; in the forEach, type the callback params (`abbr: HTMLElement, index: number`); type `hideTimer` as `ReturnType<typeof setTimeout> | undefined`; for the popover child lookups use typed querySelector with non-null assertions justified by build-time-authored HTML: `popover.querySelector<HTMLImageElement>('.gt-img')!` and `popover.querySelector<HTMLParagraphElement>('.gt-def')!`. Preserve all runtime behavior (timers, listeners, positioning) — byte-for-byte logically unchanged (SC-5). No `enum`/`namespace` (isolatedModules). No `@ts-ignore`.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/glossary-tooltip.ts` exists; `glossary-tooltip.js` no longer exists
    - `grep -c "querySelectorAll<HTMLElement>" src/components/glossary-tooltip.ts` returns at least 1
    - `grep -c "@ts-ignore" src/components/glossary-tooltip.ts` returns 0
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>glossary-tooltip.ts is a typed vanilla DOM script with no behavior change; typecheck green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| occurrence-record data → rendered popup DOM | popup renders record fields into the DOM; if rendered unsafely this would be an XSS sink |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-04 | Tampering | pnwm-occurrence-popup rendering occurrence-record text | mitigate | Lit `html` tagged template auto-escapes interpolated text content; conversion preserves the existing template (no unsafeHTML, no string concat) — the existing T-03-01 mitigation is carried forward unchanged |
| T-37-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase (lit/openseadragon vetted in prior phases) |

The slideshow, plate-viewer, and glossary-tooltip touch no untrusted-data boundary (build-baked image lists and build-authored glossary HTML); this is a rename+annotate conversion with no new runtime behavior (SC-5).
</threat_model>

<verification>
- `npm run typecheck` exits 0
- `node --test src/components/pnwm-image-slideshow.test.ts` passes
- No `.js` remains for the four components or the slideshow test
- No decorators in any converted file
</verification>

<success_criteria>
- Four standalone components converted to .ts with strict types, no decorators, static-properties form and manual customElements.define preserved (MIG-04, D-06/D-07)
- Slideshow test imports the class directly and passes under node --test (SC-1)
- Popup XSS-escaping behavior preserved (SC-5)
- Typecheck green
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces (Plan 03 contribution)

Full phase manifest in `37-ARTIFACTS.md`. This plan adds:

- **`src/components/pnwm-image-slideshow.ts`** (renamed from `.js`) — typed Lit component, class-field static-properties form preserved; typed `Specimen[]` + OSD viewer field; exports `PnwmImageSlideshow` for direct test import
- **`src/components/pnwm-occurrence-popup.ts`** (renamed from `.js`) — typed popup; `record: OccurrenceRecord | null`; Lit html escaping preserved
- **`src/components/pnwm-plate-viewer.ts`** (renamed from `.js`) — typed simple component
- **`src/components/glossary-tooltip.ts`** (renamed from `.js`) — typed vanilla DOM script (no Lit class)
- **`src/components/pnwm-image-slideshow.test.ts`** (renamed from `.test.js`) — specifier updated to `.ts`; direct class import preserved
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/37-lit-web-components-migration/37-03-SUMMARY.md` when done.
</output>
