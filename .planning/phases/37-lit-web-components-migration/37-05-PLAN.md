---
phase: 37-lit-web-components-migration
plan: 05
type: execute
wave: 4
depends_on: ["37-01", "37-02", "37-03", "37-04"]
files_modified:
  - src/components/main.ts
  - package.json
autonomous: false
requirements: [MIG-04, SCHEMA-08]
user_setup: []
must_haves:
  truths:
    - "main.ts side-effect-imports all 8 components by .ts specifier; no .js source remains anywhere in src/components/"
    - "package.json test glob points at src/components/*.test.ts; npm test runs the full suite green via node --test"
    - "npm run typecheck exits 0 across both tsconfigs"
    - "the Vite production bundle contains no full-Zod (ZodError/ZodType absent); only zod/mini appears"
    - "the gzipped bundle delta over the pre-Phase-37 baseline is measured and recorded"
    - "build-generated data files are byte-identical to the pre-Phase-37 baseline and rendered HTML is identical modulo content-hashed asset filenames"
  artifacts:
    - path: "src/components/main.ts"
      provides: "component registry side-effect imports (all .ts)"
      contains: "import './pnwm-occurrence-map.ts'"
    - path: "package.json"
      provides: "test glob updated to src/components/*.test.ts"
      contains: "src/components/*.test.ts"
  key_links:
    - from: "src/components/main.ts"
      to: "all 8 component .ts modules"
      via: "side-effect imports"
      pattern: "import './pnwm-.*\\.ts'"
    - from: "package.json test script"
      to: "src/components/*.test.ts"
      via: "node --test glob"
      pattern: "src/components/\\*.test.ts"
---

<objective>
Finalize the migration and prove the phase success criteria. This wave depends on every component being `.ts` (Plans 02–04), so `main.ts` is converted last:

1. **main.ts (MIG-04 / SC-1):** rename `main.js` → `.ts` and update all 8 side-effect import specifiers `.js` → `.ts`.
2. **package.json test glob (SC-1):** change `src/components/*.test.js` → `src/components/*.test.ts` so `npm test` discovers the converted test files.
3. **Full-suite + SC-1 closure:** `npm run typecheck` (both tsconfigs) exits 0 with no `@ts-ignore`/`allowJs`/unguarded `as unknown as`; `npm test` green; zero `.js` source remains in `src/components/`.
4. **SC-4 bundle verification:** `npm run build`, then grep the production bundle for `ZodError`/`ZodType` (must be 0 — proves only `zod/mini` shipped) and measure the gzipped delta over the Plan 01 pre-migration baseline; record it.
5. **SC-5 byte-identical verification (checkpoint):** diff the freshly-built `_site/` against the Plan 01 `_site_baseline/` — build-generated data files (Parquet/JSON) byte-identical, rendered HTML identical except content-hashed asset filenames — and a human confirms the diff is clean (asset-hash-only differences are expected; per a prior-phase finding, Vite content-hash filenames are non-deterministic).

Purpose: This is the verification gate that proves SC-1, SC-4, and SC-5 before `/gsd-verify-work`. It is non-autonomous because SC-5's "HTML identical modulo asset hashes" judgment needs a human eye on the normalized diff.
Output: typed main.ts, updated package.json, recorded SC-4 gzip delta, accepted SC-5 byte-identical diff.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-lit-web-components-migration/37-RESEARCH.md
@.planning/phases/37-lit-web-components-migration/37-PATTERNS.md
@.planning/phases/37-lit-web-components-migration/37-01-SUMMARY.md
@.planning/phases/37-lit-web-components-migration/37-02-SUMMARY.md
@.planning/phases/37-lit-web-components-migration/37-03-SUMMARY.md
@.planning/phases/37-lit-web-components-migration/37-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert main.js → .ts, update test glob, and close SC-1 (full typecheck + full test suite green)</name>
  <files>src/components/main.ts, package.json</files>
  <read_first>
    - src/components/main.js (all 8 side-effect imports)
    - package.json (the `test` script glob on line 24 — currently `src/components/*.test.js`; the `typecheck` script on line 25)
    - 37-PATTERNS.md section "`src/components/main.ts`" (the 8 specifier updates) and "package.json test glob update"
    - 37-RESEARCH.md Pattern 8 + "package.json test glob update" code example
  </read_first>
  <action>
    Rename `src/components/main.js` → `.ts` (git mv) and change all 8 side-effect import specifiers from `.js` to `.ts` (`./pnwm-occurrence-map.ts`, `./pnwm-occurrence-popup.ts`, `./pnwm-phenology-chart.ts`, `./pnwm-filter-bar.ts`, `./pnwm-image-slideshow.ts`, `./pnwm-taxon-browser.ts`, `./pnwm-plate-viewer.ts`, `./glossary-tooltip.ts`). No other change to main. In `package.json`, change the `test` script's `src/components/*.test.js` token to `src/components/*.test.ts` (no brace expansion needed — all five test files are now `.ts`). Do NOT change the `typecheck` script (already `tsc -p tsconfig.browser.json --noEmit && tsc -p tsconfig.node.json --noEmit`). Then verify SC-1 closure: full typecheck exits 0; full `npm test` green; no `.js` source files remain in `src/components/`; no `@ts-ignore`, `allowJs`, or unguarded `as unknown as` introduced.
  </action>
  <verify>
    <automated>npm run typecheck && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/main.ts` exists; `main.js` no longer exists
    - `grep -c "\.js'" src/components/main.ts` returns 0 (all 8 specifiers are .ts)
    - `ls src/components/*.js 2>/dev/null | wc -l` returns 0 (zero .js source remains in src/components/)
    - `grep -c "src/components/\*.test.ts" package.json` returns 1; `grep -c "src/components/\*.test.js" package.json` returns 0
    - `grep -rc "@ts-ignore" src/components/*.ts | grep -v ':0' | wc -l` returns 0 (no @ts-ignore in any converted file)
    - `npm run typecheck` exits 0; `npm test` passes the full suite via node --test
  </acceptance_criteria>
  <done>main.ts imports all 8 components by .ts specifier, the test glob targets *.test.ts, the full typecheck and full test suite are green, and zero .js source files remain in src/components/ (SC-1 closed).</done>
</task>

<task type="auto">
  <name>Task 2: Build the production bundle and verify SC-4 (no full-Zod; record gzip delta)</name>
  <files>(no source files — produces _site/ build output and a recorded SC-4 measurement)</files>
  <read_first>
    - 37-RESEARCH.md Code Examples "SC-4 Bundle Grep Verification" and "SC-4 Gzip Delta Measurement" (commands + expected outputs)
    - .planning/phases/37-lit-web-components-migration/37-01-SUMMARY.md (the recorded pre-migration gzipped bundle baseline byte count)
  </read_first>
  <action>
    Run `npm run build` to produce the post-migration `_site/`. Verify SC-4: run `grep -c "ZodError\|ZodType" _site/assets/main-*.js` and `grep -c "ZodError\|ZodType" _site/assets/chunk-*.js` (any chunk files) — both must return 0, proving only `zod/mini` (which exports neither symbol) reached the bundle, not full classic Zod. Then measure the post-migration gzipped bundle size (`gzip -c _site/assets/main-*.js | wc -c`) and compute the delta versus the Plan 01 pre-migration baseline; record the delta in the SUMMARY (D-04 — committed note encouraged but not required; if a committed note is chosen, a short BUNDLE-DELTA.md is acceptable). If the grep returns non-zero, the migration leaked full Zod (likely a stray `import { z } from 'zod'` on the browser path) — fail and fix before proceeding.
  </action>
  <verify>
    <automated>npm run build && test "$(grep -c 'ZodError\|ZodType' _site/assets/main-*.js)" = "0" && echo SC4_BUNDLE_CLEAN</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` completes successfully
    - `grep -c "ZodError\|ZodType" _site/assets/main-*.js` returns 0 (no full-Zod in the main bundle)
    - `grep -c "ZodError\|ZodType" _site/assets/chunk-*.js` returns 0 for every chunk file (or no chunk files exist)
    - the post-migration gzipped bundle byte count and the delta over the Plan 01 baseline are recorded in the SUMMARY
  </acceptance_criteria>
  <done>The production bundle contains no full-Zod runtime (ZodError/ZodType absent), confirming only zod/mini shipped, and the measured gzipped delta over the pre-migration baseline is recorded (SC-4).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: SC-5 byte-identical verification — human confirms data identical + HTML identical modulo asset hashes</name>
  <files>(no source files — runs the SC-5 diff against _site_baseline/ and records the outcome)</files>
  <what-built>
    The full Phase 37 migration is complete and the production bundle is built. This checkpoint verifies SC-5: build-generated data files (Parquet/JSON) are byte-identical to the pre-Phase-37 baseline, and rendered HTML is identical except for content-hashed asset filenames. The automated diff is run first; the human confirms that the ONLY differences are the expected non-deterministic Vite content-hash asset filenames (a documented prior-phase finding) and not any prose/data change.
  </what-built>
  <read_first>
    - 37-RESEARCH.md SC-5 rows of "Phase Requirements → Test Map" + Open Question 2
    - .planning/STATE.md "Phase 34 Plan 03" decision (Vite content-hash filenames are non-deterministic between builds — asset-hash-only differences are expected, not a regression)
    - .planning/phases/37-lit-web-components-migration/37-01-SUMMARY.md (confirms _site_baseline/ is the fresh pre-Phase-37 snapshot)
  </read_first>
  <action>
    Run the SC-5 byte-identical diff against the Plan 01 `_site_baseline/`: first `diff -rq _site_baseline/ _site/ | grep -E '\.(parquet|json)$'` (must be empty — data byte-identical); then `diff -rq _site_baseline/ _site/ | grep -v '/assets/'` (HTML differences must be confined to content-hashed asset-filename references, no prose/markup change); then present the full `diff -rq _site_baseline/ _site/` summary to the human, noting which differences are the expected re-hashed `assets/` bundles. Do NOT auto-approve — pause for human confirmation per the resume-signal. If any `.parquet`/`.json` data file differs, STOP and surface it as a regression rather than approving.
  </action>
  <verify>
    <human-check>Human confirms data files are byte-identical and HTML differs only in content-hashed asset filenames (see how-to-verify).</human-check>
  </verify>
  <how-to-verify>
    1. Confirm the post-migration build exists: `_site/assets/main-*.js` is present (from Task 2's `npm run build`).
    2. Diff the build-generated DATA files against the baseline — these must be byte-identical:
       `diff -rq _site_baseline/ _site/ | grep -E '\.(parquet|json)$'`
       Expected: no output (every .parquet and .json identical). Any line here is a real regression — STOP and investigate.
    3. Diff the rendered HTML, ignoring the assets/ directory (where content-hashed JS/CSS live):
       `diff -rq _site_baseline/ _site/ | grep -v '/assets/'`
       Expected: differences ONLY in HTML lines that reference a hashed asset filename (e.g. `<script src="/assets/main-XXXX.js">`), nothing in prose/markup content. Spot-check one species page and the taxon-browser index page side by side.
    4. Confirm the only remaining differences (`diff -rq _site_baseline/ _site/`) are under `assets/` (the re-hashed JS/CSS bundles) — these are expected per the Phase 34 finding and behavior-equivalence is covered by the green full test suite.
  </how-to-verify>
  <acceptance_criteria>
    - `diff -rq _site_baseline/ _site/ | grep -E '\.(parquet|json)$'` produces NO output (data byte-identical)
    - HTML differences are confined to content-hashed asset-filename references; no prose/markup/data content differs
    - the human confirms the diff is clean (asset-hash-only) and approves
  </acceptance_criteria>
  <done>Data files are byte-identical to the pre-Phase-37 baseline and rendered HTML is identical modulo content-hashed asset filenames; the human has approved the SC-5 diff.</done>
  <resume-signal>Type "approved" if the only differences are content-hashed asset filenames (data byte-identical, HTML prose identical), or describe the unexpected diff if any data/prose changed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan introduces no new runtime/network boundary. It wires the component registry, updates a test glob, and verifies build output. The runtime trust boundaries (the two CDN-fetch validators) are implemented and threat-modeled in Plans 02 and 04.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-08 | Tampering | full-Zod accidentally leaking into the production bundle | mitigate | SC-4 grep gate (`grep -c "ZodError\|ZodType" _site/assets/main-*.js` must be 0) fails the task if any browser-path file imported classic `zod`; only zod/mini (which exports neither symbol) is permitted |
| T-37-09 | Tampering | unintended build-output (data/HTML) drift from the migration | mitigate | SC-5 byte-identical diff against the fresh pre-Phase-37 baseline (data files byte-identical; HTML identical modulo asset hashes), gated by a blocking human-verify checkpoint |
| T-37-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase (RESEARCH Package Legitimacy Audit: no new packages in Phase 37) |
</threat_model>

<verification>
- `npm run typecheck` exits 0; `npm test` passes the full suite
- `ls src/components/*.js` returns nothing (zero .js source remains)
- `grep -c "ZodError\|ZodType" _site/assets/main-*.js` returns 0
- `diff -rq _site_baseline/ _site/ | grep -E '\.(parquet|json)$'` produces no output
- gzip delta recorded; SC-5 diff approved by human
</verification>

<success_criteria>
- All .js → .ts complete; full typecheck + full test suite green; zero @ts-ignore/allowJs (SC-1)
- No full-Zod in production bundle; gzip delta recorded (SC-4)
- Data byte-identical; HTML identical modulo asset hashes (SC-5)
- Test glob updated to *.test.ts (SC-1)
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces (Plan 05 contribution)

Full phase manifest in `37-ARTIFACTS.md`. This plan adds:

- **`src/components/main.ts`** (renamed from `.js`) — component registry; all 8 side-effect imports use `.ts` specifiers
- **`package.json`** (MODIFIED) — `test` script glob changed `src/components/*.test.js` → `src/components/*.test.ts`
- Recorded SC-4 outcome: post-migration gzipped bundle byte count + delta over the pre-Phase-37 baseline; confirmation that `ZodError`/`ZodType` are absent from the bundle
- Recorded SC-5 outcome: data byte-identical to baseline; HTML identical modulo content-hashed asset filenames (human-approved)
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/37-lit-web-components-migration/37-05-SUMMARY.md` when done. Record the final SC-4 gzip delta and the SC-5 diff outcome.
</output>
