---
phase: 37-lit-web-components-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/schemas.ts
  - src/types/events.ts
  - src/types/index.ts
autonomous: true
requirements: [MIG-04, SCHEMA-08]
must_haves:
  truths:
    - "schemas.ts imports only zod/mini; full classic zod is no longer imported anywhere on the browser path"
    - "OccurrenceRecordSchema and SpeciesStateSchema validate identically to before (same accepted/rejected shapes)"
    - "FilterChangeDetail interface with 8 fields exists in src/types/events.ts"
    - "addEventListener('pnwm-filter-change', cb) types cb's event as CustomEvent<FilterChangeDetail> via global merge"
    - "a fresh pre-Phase-37 _site_baseline/ snapshot exists for SC-5 comparison"
  artifacts:
    - path: "src/types/schemas.ts"
      provides: "zod/mini-authored schemas (single source of truth) importable by browser without full Zod"
      contains: "import * as z from 'zod/mini'"
    - path: "src/types/events.ts"
      provides: "FilterChangeDetail interface + HTMLElementEventMap declaration merge"
      contains: "interface HTMLElementEventMap"
    - path: "src/types/index.ts"
      provides: "re-export barrel including events.ts"
      contains: "export * from './events.ts'"
  key_links:
    - from: "src/types/index.ts"
      to: "src/types/events.ts"
      via: "export *"
      pattern: "export \\* from './events.ts'"
    - from: "src/types/events.ts"
      to: "global HTMLElementEventMap"
      via: "declare global augmentation"
      pattern: "declare global"
---

<objective>
Build the shared types foundation that every Phase 37 component conversion depends on, and capture the SC-5 byte-identical baseline before any source changes. Three pieces:

1. **D-02 schema migration:** convert ALL schemas in `src/types/schemas.ts` from the classic chained `zod` API to the `zod/mini` functional API, so a browser component importing `OccurrenceRecordSchema`/`SpeciesStateSchema` pulls only `zod/mini` (never `ZodError`/`ZodType`) into the Vite bundle (SC-4 precondition).
2. **D-09 typed event:** create `src/types/events.ts` with the `FilterChangeDetail` interface (8 fields) and a global `HTMLElementEventMap` declaration merge; re-export it from `src/types/index.ts`.
3. **SC-5 baseline:** capture a fresh `_site_baseline/` snapshot from the current (pre-Phase-37) build, because the existing baseline is stale from Phase 34 (RESEARCH Open Question 2).

Purpose: This is the producer wave. The schema migration touches a file imported by both browser components (Wave 2–3) AND seven Node build-side consumers (`verify-parquet.ts`, `build-data.ts`, `profile-data.ts`, `generate-species-photos.ts`, `src/_data/{glossary,speciesPhotos,taxon}.ts`), so the full typecheck and full test suite must stay green here before any component work begins.
Output: zod/mini-authored `schemas.ts`, new `events.ts`, updated `index.ts`, fresh `_site_baseline/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-lit-web-components-migration/37-CONTEXT.md
@.planning/phases/37-lit-web-components-migration/37-RESEARCH.md
@.planning/phases/37-lit-web-components-migration/37-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate src/types/schemas.ts to the zod/mini functional API (D-02)</name>
  <files>src/types/schemas.ts</files>
  <read_first>
    - src/types/schemas.ts (the file being modified — read all 153 lines; note every `.nullable()`, `.int()`, and `NavImageSchema.nullable()` chain)
    - 37-PATTERNS.md section "`src/types/schemas.ts` (model, transform) — D-02 migration" (the exact before/after conversion table)
    - 37-RESEARCH.md Pattern 3 + Pitfall 3 + Pitfall 5 (zod/mini functional API, no `.int()`, no mixed imports)
  </read_first>
  <behavior>
    - OccurrenceRecordSchema still accepts a record with all string/number fields populated and county/locality/elevation_ft/year/month/day = null.
    - OccurrenceRecordSchema still rejects a record missing `species_slug`.
    - SpeciesStateSchema accepts `{ species_slug: 's', state: 'WA' }` and rejects `{ species_slug: 's' }`.
    - `z.infer<typeof OccurrenceRecordSchema>` still resolves to the same field/null-union shape (compile-time check via typecheck).
    - safeParse().error.issues[].message / .path remain readable strings (consumed by verify-parquet.ts).
  </behavior>
  <action>
    Replace the single import on line 5 (`import { z } from 'zod';`) with `import * as z from 'zod/mini';`. Convert EVERY chained method call in the file to the functional form per the 37-PATTERNS.md conversion table: `X.nullable()` → `z.nullable(X)` (applies to all `.nullable()` chains AND `NavImageSchema.nullable()` on line 127 → `z.nullable(NavImageSchema)`); `z.number().int()` → `z.number()` (drop `.int()` — absent in zod/mini; int is enforced at DuckDB INT32 write time per Pitfall 3). There are 6 `.int()` occurrences to drop: `elevation_ft`, `year`, `month`, `day` in OccurrenceRecordSchema, `id` in SpeciesSchema, `weight` in NavImageSchema. Keep `z.object()`, `z.string()`, `z.number()`, `z.boolean()`, `z.array(X)` unchanged (all exist identically in zod/mini). Keep all `export const ...Schema` and `export type ... = z.infer<typeof ...>` lines unchanged (z.infer works identically in mini). Do NOT mix `import { z } from 'zod'` anywhere in the file (Pitfall 5). Do NOT add `@ts-ignore` or `as unknown as` casts. Preserve the header comments (lines 1-4).
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "import \* as z from 'zod/mini'" src/types/schemas.ts` returns 1
    - `grep -c "from 'zod'" src/types/schemas.ts` returns 0 (no classic import remains)
    - `grep -c "\.int()" src/types/schemas.ts` returns 0
    - `grep -c "\.nullable()" src/types/schemas.ts` returns 0 (all converted to functional z.nullable())
    - `npm run typecheck` exits 0 with zero errors (both tsconfig.browser.json and tsconfig.node.json — proves all 7 Node-side consumers still compile)
    - `npm test` passes via node --test (proves verify-parquet.ts safeParse error-reporting still works against the migrated schemas)
  </acceptance_criteria>
  <done>schemas.ts is authored entirely in the zod/mini functional API; full typecheck (browser + node) and full test suite remain green; all 7 build-side consumers and the browser path import the same single-source-of-truth module without dragging full Zod.</done>
</task>

<task type="auto">
  <name>Task 2: Create src/types/events.ts (FilterChangeDetail + HTMLElementEventMap merge) and re-export it (D-09)</name>
  <files>src/types/events.ts, src/types/index.ts</files>
  <read_first>
    - src/types/index.ts (the 4-line barrel being modified)
    - 37-PATTERNS.md section "`src/types/events.ts` (NEW file — no analog)" (full file content to author) and "`src/types/index.ts` (config, transform)" (the one line to add)
    - 37-RESEARCH.md Pattern 6 + Pitfall 6 + Anti-Pattern "export default in src/types/events.ts" (module-boundary requirement, named export only)
  </read_first>
  <action>
    Create `src/types/events.ts` as a NEW module file. Add a named `export interface FilterChangeDetail` with exactly these 8 fields and types: `state: string`, `recordType: string`, `yearMin: number`, `yearMax: number`, `county: string`, `collection: string`, `elevationMin: number`, `elevationMax: number` (these mirror the existing `pnwm-filter-bar.js` dispatch detail). Below the interface, add a `declare global { interface HTMLElementEventMap { 'pnwm-filter-change': CustomEvent<FilterChangeDetail>; } }` block. The `export interface` line is what makes this file a module so `declare global` is treated as a module augmentation under `verbatimModuleSyntax: true` (Pitfall 6) — do NOT use `export default` (Anti-Pattern) and do NOT add `enum`/`namespace`. Then add exactly one line to `src/types/index.ts` after the existing `export * from './schemas.ts';`: `export * from './events.ts';`. No tsconfig change is needed (`tsconfig.browser.json` already includes `src/types/**/*.ts`); do NOT add events.ts to `tsconfig.node.json` include.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/types/events.ts` exists
    - `grep -c "interface HTMLElementEventMap" src/types/events.ts` returns 1
    - `grep -c "export interface FilterChangeDetail" src/types/events.ts` returns 1
    - `grep -c "export default" src/types/events.ts` returns 0
    - events.ts FilterChangeDetail declares exactly 8 fields (state, recordType, yearMin, yearMax, county, collection, elevationMin, elevationMax)
    - `grep -c "export \* from './events.ts'" src/types/index.ts` returns 1
    - `npm run typecheck` exits 0 with zero errors
  </acceptance_criteria>
  <done>FilterChangeDetail is defined with all 8 fields and exported via the index barrel; the HTMLElementEventMap global merge is in place so any TypeScript addEventListener('pnwm-filter-change', ...) callback is typed without a cast; typecheck stays green.</done>
</task>

<task type="auto">
  <name>Task 3: Capture a fresh pre-Phase-37 _site_baseline/ snapshot for SC-5</name>
  <files>(no source files — produces gitignored _site_baseline/ working-tree snapshot)</files>
  <read_first>
    - 37-RESEARCH.md Open Question 2 ("_site_baseline/ currency for SC-5 gate") and the SC-5 row of the "Phase Requirements → Test Map"
    - .gitignore (confirm _site/ and _site_baseline/ are both ignored — they are, lines 1-2)
    - package.json (the `build` script)
  </read_first>
  <action>
    Run the production build (`npm run build`) on the current source (BEFORE any component conversion), then snapshot the resulting `_site/` into `_site_baseline/` (remove any stale `_site_baseline/` first, then copy `_site/` → `_site_baseline/`). This captures the true pre-Phase-37 output (the existing `_site_baseline/` is stale from Phase 34 and reflects Phase 36 config changes already). Record the pre-migration bundle gzip baseline for the SC-4 delta in the SUMMARY: the bundle is `_site/assets/main-*.js`; capture its gzipped byte count via `gzip -c _site/assets/main-*.js | wc -c` (RESEARCH baseline reference: 121,833 bytes gzipped for main-mhZWKs7f.js). Do NOT commit `_site_baseline/` (it is gitignored). This is a working-tree snapshot consumed by the Plan 05 SC-5 diff.
  </action>
  <verify>
    <automated>test -d _site_baseline && ls _site_baseline/assets/main-*.js >/dev/null 2>&1 && echo BASELINE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `_site_baseline/` directory exists and contains `assets/main-*.js`
    - `_site_baseline/` contains species page output (e.g. `find _site_baseline -name '*.parquet' | head -1` returns a path)
    - the pre-migration gzipped bundle byte count is recorded in the plan SUMMARY (for the SC-4 delta in Plan 05)
    - `_site_baseline/` is NOT staged in git (it is gitignored)
  </acceptance_criteria>
  <done>A fresh pre-Phase-37 _site_baseline/ snapshot exists from a clean build of the unmodified source, and the pre-migration gzipped bundle size is recorded; Plan 05 can diff post-Phase-37 _site/ against it for SC-5.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Node build scripts ↔ schemas.ts | Build-side consumers parse data with these schemas; a behavior change in the schema would alter build output (SC-5 risk, not a security risk) |

This plan touches no runtime/network security surface — it authors type definitions and captures a build snapshot. The two SCHEMA-08 load-time validators that consume these schemas at the CDN trust boundary are implemented in Plans 02 and 04; their threat models live there. The only risk here is a behavior-altering schema change, mitigated by the full typecheck + full test suite gates and the byte-identical SC-5 baseline.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-01 | Tampering | schemas.ts zod/mini conversion altering accepted shapes | mitigate | Conversion is mechanical (chained→functional, drop build-enforced `.int()`); full typecheck (browser+node) + full test suite + byte-identical SC-5 baseline confirm no behavior change |
| T-37-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase; all of lit/zod/hyparquet already vetted in Phases 33–36 (RESEARCH Package Legitimacy Audit: no new packages) |
</threat_model>

<verification>
- `npm run typecheck` exits 0 (browser + node tsconfigs)
- `npm test` passes (no build-side regression from the schema migration)
- `grep -c "from 'zod'" src/types/schemas.ts` returns 0; `grep -c "zod/mini" src/types/schemas.ts` returns 1
- `src/types/events.ts` contains `interface HTMLElementEventMap` and `export interface FilterChangeDetail`
- `_site_baseline/` exists with a fresh pre-Phase-37 build
</verification>

<success_criteria>
- schemas.ts authored entirely in zod/mini (SC-4 precondition — no full-Zod import on the browser path)
- FilterChangeDetail + HTMLElementEventMap merge in place (SC-2)
- Fresh _site_baseline/ captured (SC-5 baseline)
- Full typecheck and full test suite green (SC-1 / SC-5 no regression)
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces (Plan 01 contribution)

This plan establishes the shared-types foundation consumed by Plans 02–04. Full phase manifest in `37-ARTIFACTS.md`.

- **`src/types/events.ts`** (NEW file):
  - `export interface FilterChangeDetail` — 8 fields: `state, recordType, yearMin, yearMax, county, collection, elevationMin, elevationMax`
  - global `declare global { interface HTMLElementEventMap { 'pnwm-filter-change': CustomEvent<FilterChangeDetail> } }` augmentation
- **`src/types/schemas.ts`** (REFACTORED) — all schemas re-authored in the `zod/mini` functional API (`import * as z from 'zod/mini'`); export names unchanged; `OccurrenceRecordSchema`/`OccurrenceRecord` and `SpeciesStateSchema`/`SpeciesState` now browser-importable without full Zod; 6 `.int()` constraints dropped (DuckDB-enforced)
- **`src/types/index.ts`** (MODIFIED) — adds `export * from './events.ts'`
- **`_site_baseline/`** (gitignored snapshot) — fresh pre-Phase-37 build output for the SC-5 diff
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/37-lit-web-components-migration/37-01-SUMMARY.md` when done. Record the pre-migration gzipped bundle byte count for the Plan 05 SC-4 delta.
</output>
