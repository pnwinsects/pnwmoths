# Project Retrospective

*A living document updated after each milestone. Lessons feed forward into future planning.*

## Milestone: v1.0 — MVP

**Shipped:** 2026-04-12
**Phases:** 5 | **Plans:** 12 | **Commits:** 109

### What Was Built

- CSV → DuckDB → per-species Parquet build pipeline with UTF-8 validation and integrity checks
- ~700 static species factsheets generated by Eleventy from flat-file data
- Interactive factsheets: Leaflet map, phenology chart, state/type/year filters, image slideshow — all as Lit web components loading Parquet asynchronously via hyparquet
- Pagefind static search, alphabetized glossary, post-build link checker, page weight validator, data integrity validator
- GitHub Actions CI/CD, Docker build environment, four LLM-actionable `_instructions/` files for non-technical maintainers

### What Worked

- **DuckDB as the build-time query layer** — handled all the data joins (species → occurrences → images) cleanly; no ORM needed
- **Parquet + hyparquet for client-side data** — async loading from columnar files avoids the large inline JSON antipattern; Snappy compression worked well
- **Lit web components** — light DOM requirement for Leaflet was a minor wrinkle but overall the component model was clean; no framework churn
- **Eleventy pagination** — the data file → template model mapped well to the species factsheet use case
- **Phase-per-subsystem structure** — keeping data pipeline, static templates, interactive JS, and tooling as separate phases prevented scope bleed

### What Was Inefficient

- **@duckdb/node-api API shape** — research assumed `.rows` property; actual API uses `.getRowObjectsJS()` and `conn.closeSync()`. Caught in Phase 1 but cost time. Better: verify API shape in a one-file spike before building around it.
- **Vite + Eleventy output directory interaction** — Vite wipes `_site/` during its build step; required a `copy-parquet.js` workaround. The `emptyOutDir: false` comment in `vite.config.js` is misleading. Should be documented or resolved upstream.
- **Nyquist validation left as draft** — all 5 VALIDATION.md files ended the milestone in `draft` status. Running `/gsd-validate-phase N` per phase would surface gaps earlier; deferred too long.
- **Accomplishment extraction by gsd-tools** — the CLI pulled from deviation logs in SUMMARY.md files instead of deliverable one-liners; MILESTONES.md needed manual cleanup. The SUMMARY.md format for Plans 01–01 and 01–02 lacked a clear `one_liner` field.

### Patterns Established

- `data/parquet/{slug}/records.parquet` as export target (not `_site/`); Eleventy passthrough copy handles deployment
- Slug generated as `(genus+'-'+species).toLowerCase()` — alphanumeric-only validated before use in file paths
- Glossary template keyed by first letter (object, not array) for `for letter, terms in glossary` Nunjucks pattern
- Pagefind UI styled via Pico CSS custom property mappings (no custom CSS file)
- Docker anonymous volume protects image-baked `node_modules` from workspace bind mount

### Key Lessons

1. **Verify API shapes in a spike before building.** Research flagged `@duckdb/node-api` as low-confidence; the spike should have run before writing the full build script.
2. **Vite + Eleventy output directory ordering is fragile.** Any build step that wipes `_site/` must be explicitly sequenced; document it and add a guard.
3. **Run Nyquist validation per phase, not at milestone end.** Draft VALIDATION.md files accumulated without being filled; validation gaps are cheapest to fix immediately after a phase ships.
4. **SUMMARY.md one-liner field matters for tooling.** Plans 01-01 and 01-02 used YAML frontmatter without a `one_liner` field; the milestone complete CLI fell back to deviation log text. Add `one_liner` to all SUMMARY.md files.

### Cost Observations

- Model mix: primarily Sonnet (execution); Opus used for planning and research phases
- Sessions: 2 days of work
- Notable: 5-phase milestone completed in ~2 days with no blocking integration failures

---

## Cross-Milestone Trends

### Process Evolution

| Milestone | Commits | Phases | Key Change |
|-----------|---------|--------|------------|
| v1.0 | 109 | 5 | Initial GSD workflow established |

### Cumulative Quality

| Milestone | Test Files | Automated Coverage | Nyquist Compliant |
|-----------|------------|-------------------|-------------------|
| v1.0 | 2 | Build pipeline unit/integration tests only | 0/5 phases |

### Top Lessons (Verified Across Milestones)

1. Verify external API shapes before building on them — research assumptions are often stale
2. Run validation passes per phase, not just at milestone close
